node_modules/
.planning/
.claude/

# CLAUDE.md contains GSD auto-injected blocks (marked with <!-- GSD:*-start -->)
# that assemble STACK/CONVENTIONS/ARCHITECTURE from .planning/codebase/*.md.
# The injected sections carry MD022/MD032/MD036 violations because the upstream
# templates pack headings+lists without the required blank lines. Treating this
# as injection-template tech debt — file stays in repo for Claude sessions but
# is excluded from lint until the injection templates are fixed.
CLAUDE.md

# AUDIT-REPORT.md is a transient deep-audit artifact (subagent output) kept
# in the repo as a fix-tracking reference. Not part of the published toolkit.
AUDIT-REPORT.md

# templates/skills-marketplace/ contains upstream skill content mirrored verbatim
# from external authors. The upstream files may carry MD031/MD032/MD036/MD040
# violations. Lint is excluded to avoid noise from third-party content; the
# toolkit's own authored markdown (templates/base/, commands/, components/, etc.)
# remains fully linted. Exclusion approved as part of Phase 26 Plan 03.
templates/skills-marketplace/

# plugins/ sub-plugin trees use symlinks to canonical repo content. Exclude
# the symlinked paths from lint to avoid double-processing content already
# covered above (plugins/ are distribution artifacts, not authored markdown).
plugins/

# docs/research/ contains exploratory research documents (gsd-vs-alternatives,
# toolkit-v6-redesign) authored quickly during analysis sessions. They use
# emphasis-as-heading and bare URLs by design (research log style, not user
# docs). Excluded from lint; they're not part of the published toolkit.
docs/research/

# _external/ is a gitignored vendor-source dump for read-only analysis
# of upstream plugins (superpowers, get-shit-done, etc.). Third-party
# markdown is not lintable on our terms — excluded from local lint.
_external/
