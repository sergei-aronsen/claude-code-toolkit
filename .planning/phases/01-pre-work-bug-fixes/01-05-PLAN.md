---
phase: 01-pre-work-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/init-local.sh
  - CHANGELOG.md
  - Makefile
autonomous: true
requirements:
  - BUG-06
branch: fix/phase-1-bugs
commit_message: "fix: align version references — init-local.sh reads manifest.json, CHANGELOG unreleased entry added"
commit_body: |
  Refs: BUG-06
  Files: scripts/init-local.sh:11,38; CHANGELOG.md:8-10; Makefile validate target
must_haves:
  truths:
    - "scripts/init-local.sh --version reads the version from manifest.json at runtime (no hardcoded constant)"
    - "CHANGELOG.md has an [Unreleased] section listing the seven BUG fixes under ### Fixed"
    - "make validate fails if manifest.json version and the latest non-[Unreleased] CHANGELOG version differ"
    - "Phase 1 does NOT bump the version number to 4.0.0 — the bump happens in Phase 7 (D-19); Phase 1 only aligns sources structurally"
  artifacts:
    - path: scripts/init-local.sh
      provides: "runtime version read from manifest.json with sed fallback"
      contains: "jq -r '.version' \"$MANIFEST_FILE\""
    - path: CHANGELOG.md
      provides: "[Unreleased] section with ### Fixed bullets"
      contains: "### Fixed"
    - path: Makefile
      provides: "version-alignment validate check"
      contains: "MANIFEST_VER"
  key_links:
    - from: manifest.json .version
      to: CHANGELOG.md latest `## [X.Y.Z]` entry
      via: "make validate assertion"
      pattern: "MANIFEST_VER=.*CHANGELOG_VER=.*\\[ \"\\$\\$MANIFEST_VER\" != \"\\$\\$CHANGELOG_VER\" \\]"
    - from: scripts/init-local.sh --version output
      to: manifest.json .version
      via: "jq/sed runtime read"
      pattern: "VERSION=\\$\\(jq -r '\\.version'"
---

<objective>
Align the three version-reference sites so `manifest.json` is the single source of truth. Remove the hardcoded `VERSION="2.0.0"` from `scripts/init-local.sh` and replace with runtime read. Add a `[Unreleased]` section to `CHANGELOG.md` listing the seven BUG fixes. Extend `make validate` to assert manifest-changelog version alignment.

Purpose: BUG-06 is structural, not a runtime bug today (the 2.0.0 string is never compared to anything). But Phase 7's 4.0.0 release requires a single-file bump, and the validate check catches future drift. Without it, every release risks mismatched versions across three places.

Output: Three files patched. The actual version bump to `4.0.0` is explicitly deferred to Phase 7 (D-19) — this plan leaves `manifest.json .version` at `3.0.0` and `CHANGELOG.md` top entry at `[Unreleased]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pre-work-bug-fixes/01-CONTEXT.md
@.planning/phases/01-pre-work-bug-fixes/01-PATTERNS.md
@scripts/init-local.sh
@scripts/install-statusline.sh
@manifest.json
@CHANGELOG.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded VERSION in init-local.sh with manifest.json runtime read</name>
  <files>scripts/init-local.sh</files>
  <read_first>
    - scripts/init-local.sh (lines 1-50 — header, VERSION constant at line 11, --version handler at line 38)
    - scripts/install-statusline.sh (lines 31-40 — jq read analog)
    - manifest.json (line 2 — `"version": "3.0.0"` source of truth)
    - .planning/phases/01-pre-work-bug-fixes/01-PATTERNS.md (§"scripts/init-local.sh — BUG-06")
  </read_first>
  <action>
    Two edits in `scripts/init-local.sh`:

    **Edit 1** — Remove the hardcoded `VERSION="2.0.0"` at line 11. Simply delete that line (including the line itself, not leaving a blank placeholder — the surrounding structure is clean).

    Current:
    ```bash
    set -euo pipefail

    VERSION="2.0.0"

    # Get the directory where this script is located
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    GUIDES_DIR="$(dirname "$SCRIPT_DIR")"
    ```

    After:
    ```bash
    set -euo pipefail

    # Get the directory where this script is located
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    GUIDES_DIR="$(dirname "$SCRIPT_DIR")"
    ```

    **Edit 2** — Insert a manifest-reading block AFTER `GUIDES_DIR="..."` (line 15 post-Edit-1) and BEFORE the `# Colors` block at line ~17. The block reads the version from `manifest.json` with a sed fallback for environments without `jq`:

    ```bash
    # BUG-06: single source of truth — manifest.json
    MANIFEST_FILE="$GUIDES_DIR/manifest.json"
    if command -v jq &>/dev/null && [[ -f "$MANIFEST_FILE" ]]; then
        VERSION=$(jq -r '.version' "$MANIFEST_FILE")
    elif [[ -f "$MANIFEST_FILE" ]]; then
        VERSION=$(grep -m1 '"version"' "$MANIFEST_FILE" | sed 's/.*"version": *"\([^"]*\)".*/\1/')
    else
        VERSION="unknown"
    fi
    ```

    **Edit 3** — The `--version` / `-v` handler at line ~38 already prints `"claude-code-toolkit v$VERSION (local)"` — no change needed there, the new `$VERSION` now comes from manifest.json.

    **Important:** leave the `# Colors` block, `CLAUDE_DIR=".claude"`, and all subsequent logic untouched. The only changes are: remove one line (hardcoded VERSION), add one block (runtime read).
  </action>
  <verify>
    <automated>bash -c '! grep -q "^VERSION=\"2" scripts/init-local.sh && grep -q "jq -r .\.version. \"\$MANIFEST_FILE\"" scripts/init-local.sh && grep -q "MANIFEST_FILE=\"\$GUIDES_DIR/manifest.json\"" scripts/init-local.sh && VER=$(bash scripts/init-local.sh --version) && echo "$VER" | grep -q "v3\.0\.0" && echo PASS' | grep -q PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^VERSION="2' scripts/init-local.sh` prints `0` (hardcoded constant removed)
    - `grep -c 'jq -r .\.version. "\$MANIFEST_FILE"' scripts/init-local.sh` prints `1`
    - `grep -c 'MANIFEST_FILE="\$GUIDES_DIR/manifest.json"' scripts/init-local.sh` prints `1`
    - `bash scripts/init-local.sh --version` prints `claude-code-toolkit v3.0.0 (local)` (matching manifest.json current value)
    - `shellcheck scripts/init-local.sh` exits 0
    - `bash -n scripts/init-local.sh` exits 0
  </acceptance_criteria>
  <done>
    `init-local.sh` no longer hardcodes a version. `--version` output tracks `manifest.json` automatically. Phase 7 bump to 4.0.0 becomes a one-file edit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add [Unreleased] ### Fixed block to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-15 — current `[Unreleased]` + `[3.0.0]` structure)
    - .planning/phases/01-pre-work-bug-fixes/01-PATTERNS.md (§"CHANGELOG.md — BUG-06")
  </read_first>
  <action>
    Replace the empty `[Unreleased]` section at `CHANGELOG.md:8-9` with a populated `### Fixed` block listing all seven BUG fixes.

    Current:
    ```markdown
    ## [Unreleased]

    ## [3.0.0] - 2026-02-16
    ```

    Replace with:
    ```markdown
    ## [Unreleased]

    ### Fixed

    - BUG-01: Replace GNU-only `head -n -1` with POSIX `sed '$d'` in `scripts/update-claude.sh` smart-merge (preserves user CLAUDE.md sections on macOS/BSD)
    - BUG-02: Add `< /dev/tty` guards to all interactive `read` calls in `scripts/setup-council.sh` + early non-interactive guard; switch API-key prompts to silent `read -rs`
    - BUG-03: JSON-escape API key values via `python3 json.dumps` before heredoc write in `scripts/setup-council.sh` and `scripts/init-claude.sh` (keys containing `"`, `\`, newline now produce valid config.json)
    - BUG-04: Remove silent `sudo apt-get` for `tree` install in `scripts/setup-council.sh`; prompt user interactively and handle skip gracefully
    - BUG-05: Timestamped backup of `~/.claude/settings.json` before every mutation in `scripts/setup-security.sh`; restore from backup on python3 merge failure and exit non-zero
    - BUG-06: Read toolkit version from `manifest.json` at runtime in `scripts/init-local.sh`; remove hardcoded `VERSION="2.0.0"`; add `make validate` manifest ↔ CHANGELOG alignment check
    - BUG-07: Add `design.md` (and any other manifest-drifted commands) to `scripts/update-claude.sh` install loop; add `make validate` check that loop filenames match `manifest.json` commands

    ## [3.0.0] - 2026-02-16
    ```

    Key formatting constraints:
    - Blank line before and after `### Fixed` (MD032 — lists)
    - Blank line before `## [3.0.0]` (MD022 — headings)
    - Each bullet starts with `BUG-NN:` for grep-ability
    - No trailing punctuation on the `### Fixed` heading (MD026)
    - Use backticks around file paths, commands, and code symbols
    - Do NOT modify the `[3.0.0]` section or anything below it
    - Do NOT change the top-of-file intro (lines 1-7)
    - Do NOT bump version numbers — `[Unreleased]` stays `[Unreleased]`, `manifest.json` stays `3.0.0` (D-19)

    After saving, run `npx markdownlint-cli CHANGELOG.md` (or `make mdlint` if feasible) and fix any violations in the edited region.
  </action>
  <verify>
    <automated>bash -c 'grep -qE "^## \[Unreleased\]" CHANGELOG.md && grep -qE "^### Fixed" CHANGELOG.md && for B in BUG-01 BUG-02 BUG-03 BUG-04 BUG-05 BUG-06 BUG-07; do grep -q "^- $B:" CHANGELOG.md || { echo "MISSING $B"; exit 1; }; done && npx --yes markdownlint-cli CHANGELOG.md --ignore node_modules && echo PASS' | grep -q PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## \[Unreleased\]" CHANGELOG.md` prints `1`
    - `grep -cE "^### Fixed" CHANGELOG.md` prints at least `1`
    - Each of `BUG-01` through `BUG-07` appears on its own `- BUG-NN: ...` bullet line
    - `npx markdownlint-cli CHANGELOG.md` exits 0 (no MD040/MD031/MD032/MD026 violations)
    - `grep '^## \[3.0.0\]' CHANGELOG.md` still prints the line (existing section preserved)
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a populated `[Unreleased]` ### Fixed block documenting all seven Phase 1 fixes. Ready for Phase 7 to rename the header to `## [4.0.0] - <date>`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend make validate with manifest ↔ CHANGELOG version-alignment check (D-18)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (entire file — 96 lines; focus on the existing `validate:` target at lines 66-86)
    - manifest.json (line 2 — version source)
    - CHANGELOG.md (top — first `## [X.Y.Z]` line)
    - .planning/phases/01-pre-work-bug-fixes/01-PATTERNS.md (§"Makefile — BUG-06 (D-18) + BUG-07 (D-23)")
  </read_first>
  <action>
    Append a version-alignment assertion to the existing `validate:` target in `Makefile`. Insert BEFORE the final `@echo "✅ All templates valid"` line at Makefile:86.

    Current end of `validate:` target (Makefile:82-86):
    ```makefile
    		if [ $$ERRORS -gt 0 ]; then \
    			echo "Found $$ERRORS errors"; \
    			exit 1; \
    		fi
    	@echo "✅ All templates valid"
    ```

    Replace with:
    ```makefile
    		if [ $$ERRORS -gt 0 ]; then \
    			echo "Found $$ERRORS errors"; \
    			exit 1; \
    		fi
    	@MANIFEST_VER=$$(grep -m1 '"version"' manifest.json | sed 's/.*"version": *"\([^"]*\)".*/\1/'); \
    		CHANGELOG_VER=$$(grep -m1 '^## \[[0-9]' CHANGELOG.md | sed 's/.*\[\([^]]*\)\].*/\1/'); \
    		if [ "$$MANIFEST_VER" != "$$CHANGELOG_VER" ]; then \
    			echo "❌ Version mismatch: manifest.json=$$MANIFEST_VER, CHANGELOG.md=$$CHANGELOG_VER"; \
    			exit 1; \
    		fi; \
    		echo "✅ Version aligned: $$MANIFEST_VER"
    	@echo "✅ All templates valid"
    ```

    Critical makefile formatting:

    1. Use literal TAB characters for the recipe indentation (not spaces). This is a HARD Makefile requirement.
    2. Use `$$` (double dollar) to escape shell `$` from Make's own variable expansion.
    3. Line continuation: trailing `\` on every line except the final one of a logical shell command.
    4. New recipe line starts with `@` to suppress Make's echo.
    5. Only the FIRST `## [X.Y.Z]` header in CHANGELOG.md is compared — `grep -m1 '^## \[[0-9]'` matches `## [3.0.0]` and skips `## [Unreleased]` (because regex requires a digit after `[`).

    **Verify on current repo state before committing:** at this point, `manifest.json .version = "3.0.0"` and the first numeric-versioned CHANGELOG header is `## [3.0.0] - 2026-02-16`. The check must PASS on the current tree. Run `make validate` locally after the edit and confirm the `✅ Version aligned: 3.0.0` line prints.
  </action>
  <verify>
    <automated>bash -c 'grep -q "MANIFEST_VER=" Makefile && grep -q "Version aligned" Makefile && make validate 2>&1 | grep -q "Version aligned: 3.0.0" && echo PASS' | grep -q PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MANIFEST_VER=" Makefile` prints at least `1`
    - `grep -c "Version aligned" Makefile` prints at least `1`
    - `make validate` prints `✅ Version aligned: 3.0.0` and exits 0
    - `make validate` prints `✅ All templates valid` (existing checks still pass)
    - Recipe indentation uses literal TAB (verify with `grep -P "^\t" Makefile | wc -l` — count unchanged from baseline + new lines)
  </acceptance_criteria>
  <done>
    `make validate` enforces manifest ↔ CHANGELOG version alignment. Any future drift (e.g., bumping manifest without updating CHANGELOG or vice versa) fails CI.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json on disk → init-local.sh runtime | Local file read, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Integrity (drift) | manifest.json / CHANGELOG.md / init-local.sh version fields | mitigate | Three independent sources can drift. Fix: `init-local.sh` now reads manifest at runtime (eliminates drift between it and manifest); `make validate` enforces manifest ↔ CHANGELOG alignment at CI time. |
| T-05-02 | Availability | `jq` dependency in init-local.sh | accept | Fallback to `grep + sed` when jq absent; third fallback returns `"unknown"` rather than erroring. Script continues to function. |
| T-05-03 | n/a (no external input path) | n/a | n/a | BUG-06 does not introduce new trust boundaries. |

Security severity: **low** — correctness/maintenance invariant; no direct security exposure.
</threat_model>

<verification>
- `make check` passes cleanly (shellcheck + mdlint + validate all exit 0)
- `make validate` prints both `✅ Version aligned: 3.0.0` and `✅ All templates valid`
- `bash scripts/init-local.sh --version` prints `claude-code-toolkit v3.0.0 (local)`
- `npx markdownlint-cli CHANGELOG.md` exits 0
- Manual smoke (D-26): temporarily edit `manifest.json .version` to `"99.0.0"` and confirm `make validate` fails with `❌ Version mismatch`; revert
</verification>

<success_criteria>
Phase success criterion 5 from ROADMAP.md: "`scripts/init-local.sh --version` and `manifest.json` and `CHANGELOG.md` all report the same version string."

Satisfied by:
- `init-local.sh` reads from `manifest.json` directly → trivially matches manifest
- `make validate` asserts `manifest.json .version == CHANGELOG.md first numeric header` → forces CHANGELOG alignment
- All three report `3.0.0` today; Phase 7 single-file bump (manifest + changelog header rename) will align all three to `4.0.0`
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-work-bug-fixes/01-05-SUMMARY.md` recording:

- Lines changed in init-local.sh (removed line 11, added block after GUIDES_DIR)
- CHANGELOG.md diff (before/after [Unreleased] block)
- Makefile diff (added version-alignment check)
- `make validate` output showing both ✅ lines
- Commit SHA
</output>
