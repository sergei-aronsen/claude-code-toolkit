# Phase 07-validation — Continue Here

**Session paused:** 2026-04-20 — Waves 1+2 shipped, Wave 3 blocked on Phase 06.1
**Current HEAD:** `63d8c52` (pushed to origin/main)
**Active milestone:** v4.0

## Wave Status

| Wave | Plans | Status |
|------|-------|--------|
| 1 | 07-01, 07-02 | ✅ Complete, merged, pushed |
| 2 | 07-03 | ✅ Complete, merged, pushed (4 commits: `40f0c24`, `0e20f6d`, `4b0ba5c`, `27930fe`) |
| 3 | 07-04 | ⏸ **Blocked on Phase 06.1** |

## `make check` Current State

| Target | Status |
|--------|--------|
| shellcheck | ✅ PASS (after `66e4063` fix — SC2155 + Makefile severity alignment) |
| mdlint | ✅ PASS |
| validate | ✅ PASS (manifest schema, template sections, base plugins, version alignment) |
| agent-collision-static | ✅ PASS (7 files annotated `conflicts_with: ["superpowers"]`) |
| version-align | ✅ PASS (manifest.json == CHANGELOG.md == init-local.sh → 4.0.0) |
| **translation-drift** | ❌ **FAIL** (expected — Phase 06.1 blocker: 8 × 148 lines vs tolerance 161–242) |

## Next Step: Phase 06.1 (README translations sync)

**Directory:** `.planning/phases/06.1-readme-translations/` (only `.gitkeep` — no CONTEXT.md)

**Goal:** bring 8 translations (`docs/readme/{de,es,fr,ja,ko,pt,ru,zh}.md`) into sync with README.md's Phase 6 complement-first rewrite. Each file must:

- Translate the complete README.md structure (quickstart, 4 install modes, Required Base Plugins, Components, optional plugins, etc.)
- Reflect the "complement to `superpowers` + `get-shit-done`" positioning in the target language
- Land in 161–242 line tolerance band (±20% of README.md's 202 lines)
- Pass `make mdlint`

**Current state per file (all 8 at 148 lines, 54 lines short of floor):**

```bash
wc -l docs/readme/*.md README.md
```

**Recommended planning path:**

```bash
# Option A — autonomous chain (fastest)
/gsd-discuss-phase 06.1 --auto --chain

# Option B — manual plan review
/gsd-plan-phase 06.1 --skip-research
# Then /gsd-execute-phase 06.1
```

Planner should create **8 parallel plans** (one per language — `docs/readme/{lang}.md` files are disjoint, parallel-safe) OR **1 plan with 8 tasks** (same execution parallelism via wave structure). The per-language-plan shape makes wave-based parallel execution natural: 8 agents run concurrently in Wave 1, translating their own file.

**Requirement IDs to add to REQUIREMENTS.md at plan time:** TRANS-01 (all 8 in tolerance band), TRANS-02 (`make translation-drift` exits 0), TRANS-03 (positioning reflected), TRANS-04 (mdlint clean).

## After Phase 06.1: Resume Phase 7 Wave 3 (Plan 07-04)

**Plan 07-04** — release gate, `autonomous: false`:

1. Task 1 (checkpoint:human-verify) — pre-flight: `make check` fully green including `translation-drift`
2. Task 2 (auto) — flip CHANGELOG.md `[4.0.0] - TBD` → `[4.0.0] - 2026-MM-DD` using the actual completion date
3. Task 3 (checkpoint:human-verify) — ready-to-tag sign-off (human does `git tag -a v4.0.0 -m "Release 4.0.0"` + `git push --tags` outside the phase per CLAUDE.md invariant)

**Command:** `/gsd-execute-phase 7 --wave 3`

## Locked Decisions (do not re-litigate)

From `07-CONTEXT.md` D-01..D-12 and the mid-session decision to insert Phase 06.1:

- Phase 06.1 inserted (INSERTED marker in ROADMAP.md) — reverses Phase 6 CONTEXT.md v4.1 deferral on translations
- `make translation-drift` uses ±20% line-count tolerance (approach b)
- No `git tag` / `git push --tags` inside any phase — manual human action
- `v3.0.0-preflight` tag NOT created; v3.x upgrade cells use commit SHA `e9411201` directly (no `v3.0.0` git tag exists in the repo)

## CI State

- Last push: `63d8c52` — CI fix #2: wires `markdownlint-cli2-action` to read `.markdownlint-cli2.jsonc` (rule overrides + ignores list). Earlier `.markdownlintignore` file (shipped in `5805821`) unblocked local `make mdlint` but CI still failed because the workflow was calling cli2 with a cli1-shaped config that has no `ignores` key. Verify next session:
  ```bash
  gh run list --branch main --limit 3
  ```
- Run `24692519651` pending at pause time — should be the first green CI run of this session if the cli2 config wiring is correct.
- Prior failures: `24692447678`, `24692398958`, `24692326479` — all mdlint/shellcheck caused by ordering (wave 2 shellcheck issues were fixed in `66e4063`; mdlint cli2 config in `63d8c52`).

## Full Commit Log (this session)

```text
63d8c52 fix(ci): wire markdownlint-cli2 config so CI honors ignores list
4c77f09 docs(07): update continue-here handoff — Wave 2 complete, Phase 06.1 next
66e4063 fix(07-03): resolve SC2155 + align Makefile shellcheck with CI
a85c126 docs(phase-07): mark 07-03 complete in roadmap after Wave 2
27930fe docs(07-03): complete plan — 13-cell matrix + RELEASE-CHECKLIST + Test 16
4b0ba5c feat(07-03): add test-matrix.sh wrapper and wire Test 16
0e20f6d docs(07-03): create RELEASE-CHECKLIST.md
40f0c24 feat(07-03): wire 13 cell bodies into validate-release.sh
b888fe4 docs(07): add continue-here handoff before context /clear
34bdd45 docs(06.1): insert README translations sync as urgent decimal phase
5805821 fix(ci): add .markdownlintignore to unblock CI markdownlint job
1678fa6 docs(phase-07): wave 1 tracking + add missing PATTERNS.md
5154cd7 docs(07-01): complete plan 07-01 — validate-release.sh skeleton
3fbb625 feat(07-01): add validate-release.sh skeleton with invariant helpers
34a0981 docs(07-02): complete plan 07-02 — version-align + translation-drift + agent-collision
49189a6 feat(07-02): add version-align, translation-drift, agent-collision-static targets
7fe1f29 docs(state): record phase 7 planning complete
```

All on `main`, pushed.

## Critical Anti-Patterns

None recorded. `make translation-drift` failure is **designed** (blocker flag for Phase 06.1), not an anti-pattern.
