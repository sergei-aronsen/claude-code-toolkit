# Phase 06.1: README translations sync - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 06.1-readme-translations
**Mode:** `--auto --chain` (non-interactive, Claude auto-selected recommended defaults for every decision)
**Areas discussed:** Translation scope, Execution strategy, Translation source, Technical term handling, Howto link targets, Language switcher, Line-count targeting, Required Base Plugins rendering, Quality validation, Commit granularity, Lint config

---

## Translation scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full rewrite | Re-translate each file top-to-bottom from current `README.md` | ✓ |
| Targeted delta | Patch only sections that changed between v3.x and v4.0 |  |
| Hybrid (section-by-section diff) | Translate only sections where the English diff is non-trivial |  |

**User's choice:** Full rewrite (auto-default)
**Notes:** Phase 6 reshuffled sections enough that partial patches would leave v3.x and v4.0 prose interleaved in the same file — impossible to audit. Full rewrite is the only safe path.

---

## Execution strategy

| Option | Description | Selected |
|--------|-------------|----------|
| 1 plan per language (8 plans) | Maximum parallelism, granular commits | |
| Grouped family waves (3 plans) | Europe / CJK / Cyrillic clustering | ✓ |
| Single mega-plan | All 8 languages in one atomic plan | |

**User's choice:** Grouped family waves (auto-default)
**Notes:** Isolates `ru` for native-speaker scrutiny, caps blast radius per plan to 3–4 files, enables 3-way parallel execution.

---

## Translation source

| Option | Description | Selected |
|--------|-------------|----------|
| Claude inline during execute | Translations produced by executor agent during plan run | ✓ |
| External translator service | DeepL / Google Translate API call from a script | |
| Per-language user-review gate | Pause chain after each language for manual review | |

**User's choice:** Claude inline (auto-default)
**Notes:** Per-language gate would break `--chain` autonomous flow. External services add dependency. Claude quality is adequate for a tech README.

---

## Technical term handling

| Option | Description | Selected |
|--------|-------------|----------|
| Translate everything including code blocks | Maximum localization | |
| Code English + prose localized + proper nouns preserved | Selective localization per content type | ✓ |
| Keep everything English except prose paragraphs | Minimal localization | |

**User's choice:** Selective (auto-default)
**Notes:** Standard professional-localization pattern. Code must be copy-pasteable; proper nouns must stay searchable; prose and headings localized for readability.

---

## Howto link target

| Option | Description | Selected |
|--------|-------------|----------|
| Translated howto per language | Link `docs/howto/{lang}.md` | ✓ |
| English howto always | Link `docs/howto/en.md` from every translation | |
| Mixed with fallback | Translated if exists, en otherwise | (subsumed by ✓) |

**User's choice:** Translated howto (auto-default)
**Notes:** All 9 howto translations already exist — zero fallback needed. Matches existing `ru.md` pattern.

---

## Language switcher

| Option | Description | Selected |
|--------|-------------|----------|
| Same order, current as plain text | Mirror `README.md:8` with current-lang unlinked | ✓ |
| Per-language alphabetization | Reorder by current-language collation | |
| Localized language names | Translate "English" → "Englisch" etc. | |

**User's choice:** Same order (auto-default)
**Notes:** Matches existing `ru.md:8`. Consistency across files helps user orientation.

---

## Line-count targeting

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror English 1:1 | Same section + paragraph count, natural expansion | ✓ |
| Target middle of band (~202 lines) | Pad or trim to hit center | |
| Aggressively compress to minimum 161 | Meet floor with shortest viable prose | |

**User's choice:** Mirror 1:1 (auto-default)
**Notes:** Content fidelity beats length gaming. Natural language expansion (de +15–20%, CJK ±5%, etc.) lands inside 161–242 band organically.

---

## Required Base Plugins rendering

| Option | Description | Selected |
|--------|-------------|----------|
| Localize prose, keep commands verbatim | Translate intent, keep install lines untouched | ✓ |
| Fully localize including command examples | Localize everything including commands | |
| Keep entire block in English | Copy-paste English block unchanged | |

**User's choice:** Localize prose (auto-default)
**Notes:** Commands are copy-paste surface — they must match English exactly. Prose around them localized for comprehension.

---

## Quality validation

| Option | Description | Selected |
|--------|-------------|----------|
| mdlint + drift + spot-checks | Machine gates + 3 manual spot-checks per language | ✓ |
| Native-speaker QA per language | Hire or request native reviewers for all 8 | |
| LLM-back-translation check | Round-trip translate back to English, diff against source | |

**User's choice:** Machine + spot-check (auto-default)
**Notes:** Native QA deferred to v4.1. Back-translation adds compute for marginal signal. mdlint + drift are CI-enforceable and catch the real failure modes.

---

## Commit granularity

| Option | Description | Selected |
|--------|-------------|----------|
| One commit per language inside plan wave | 8 commits total, 2–4 per plan | ✓ |
| One commit per plan (3 commits) | Atomic per wave | |
| One mega-commit for all 8 files | Single atomic bump | |

**User's choice:** Per language (auto-default)
**Notes:** Git blame surfaces translation history per file; per-language revert is possible without affecting siblings in the same wave.

---

## Markdownlint config verification

| Option | Description | Selected |
|--------|-------------|----------|
| Rely on existing `MD024.siblings_only=true` | No config change; sibling heading dedup already handled | ✓ |
| Add per-file MD024 disable | Belt-and-suspenders | |
| Re-enable MD024 strictly | Force unique headings across all docs | |

**User's choice:** Rely on existing config (auto-default)
**Notes:** `.markdownlint.json` already sets siblings-only. Translations producing identical headings to each other is the expected (and allowed) case.

---

## Claude's Discretion

- Exact idiomatic phrasing per language (translator picks).
- Punctuation conventions per language (em-dash, quotation marks, apostrophes).
- Formal vs. informal register — defaulted to **formal** across all 8 languages (professional README audience).

## Deferred Ideas

- Native-speaker QA pass across all 8 languages → v4.1 backlog.
- Italian translation (`it.md`) → separate phase if desired later.
- Translation-freshness hash check in CI → v4.1 DX improvement.
- `docs/features.md` multi-language translations → separate phase.
- `CHANGELOG.md` translations → not required for v4.0.
</content>
</invoke>