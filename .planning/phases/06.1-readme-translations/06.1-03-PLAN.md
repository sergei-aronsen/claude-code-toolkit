---
phase: 06.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/readme/ru.md
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
must_haves:
  truths:
    - "docs/readme/ru.md is a full v4.0 rewrite mirroring README.md in formal Вы register"
    - "Russian translation reuses terminology baseline from existing v3.x ru.md (Solo-разработчики, Быстрый старт, etc.)"
    - "Russian translation lands in the 161-242 line band (projected 212-232)"
    - "make mdlint passes after Plan 03 completes"
    - "Russian translation includes an ### Install Modes section in Russian with 3 subsections matching English subsections"
  artifacts:
    - path: "docs/readme/ru.md"
      provides: "Russian README mirror of v4.0 complement-first English (maintainer-native language)"
      min_lines: 161
  key_links:
    - from: "docs/readme/ru.md"
      to: "docs/howto/ru.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/ru\\.md"
    - from: "docs/readme/ru.md"
      to: "../../README.md"
      via: "language switcher row 8"
      pattern: "\\.\\./\\.\\./README\\.md"
---

# Plan 06.1-03: Sync Russian README translation (maintainer-native language)

## Objective

Rewrite the Russian README translation — `docs/readme/ru.md` — from the current English
`README.md` (202 lines, v4.0 complement-first). Russian is maintainer-native per
06.1-CONTEXT.md D-02: this plan gets the tightest scrutiny and the final manual
read-through before the phase wraps. The translation reuses the terminology baseline
already established by the existing v3.x `ru.md` (verified natural choices:
`Solo-разработчики`, `Быстрый старт`, `Killer Features` kept English as table header,
`плагин`, `хук`/`hook` — whichever the existing file favors) while adding all the new
v4.0 content (Install Modes section, updated MCP table with `sentry` and `dbhub`,
Components section, etc.).

**Purpose:** Unblock the Phase 7 release gate for Russian. Isolated in its own plan
because the maintainer is a native speaker and this translation gets the final human
review pass.

**Output:** 1 rewritten translation file, passing per-file `markdownlint-cli2` and
contributing to `make mdlint` green. One commit.

## Tasks

<task id="06.1-03-01" type="auto">
  <name>Task 1: Rewrite docs/readme/ru.md (Russian, formal Вы, reuse v3.x lexical baseline)</name>
  <files>docs/readme/ru.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH — 202 lines, v4.0 complement-first)
    - docs/readme/ru.md (EXISTING v3.x translation — 148 lines; treat as TERMINOLOGY REFERENCE: reuse verified lexical choices like `Solo-разработчики`, `Быстрый старт`, `Killer Features` kept English, `slash-команды`, `плагин`, admonition label `Примечание`, file-tree comments already in Russian. DO NOT copy body paragraphs unchanged — the v3.x body is stale; mirror v4.0 English structure)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions (D-01..D-15)
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Russian (ru) conventions (lines 480–532), § Length Expansion Profile (ru: +5–15%, 212–232 lines — safe midband), § Localized Heading Glossary ru column, § Language-Switcher Pattern § Verified example (ru.md line 8 pattern already correct), § Markdownlint Pitfalls, § Open Questions
    - .markdownlint.json
    - docs/howto/ru.md (confirm exists; already linked from current ru.md line 10)
  </read_first>

  <action>
    Overwrite `docs/readme/ru.md` with a complete Russian translation of `README.md`.
    Full rewrite per D-01. Formal `Вы` (capital V for formal singular address) throughout
    prose.

    **Special scrutiny:** The project maintainer is a native Russian speaker. Grammatical
    correctness is the floor, not the ceiling — aim for idiomatic Russian tech-doc prose
    that reads naturally. When in doubt between two options:

    1. Reuse the exact phrasing from the existing v3.x `ru.md` if the phrase still
       applies in the v4.0 context (e.g., `Solo-разработчики`, `Быстрый старт`,
       `Поддерживаемые стеки`, admonition `Примечание`).
    2. Reuse the lexical conventions the existing file established (e.g., `slash-команды`
       hyphenated, `плагин`, `хук` or keep `hook` — whichever exists in v3.x ru.md).
    3. Prefer native Russian constructions over calque translations from English.

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH ru column
    (most already matching v3.x ru.md):

    - `## Для кого это`
    - `## Быстрый старт` with `### 1. Глобальная настройка (один раз)`, `#### a) Security Pack` (keep English per glossary — consistent with v3.x ru.md), `#### b) RTK — Оптимизатор токенов (рекомендуется)`, `#### c) Rate Limit Statusline (Claude Max / Pro, опционально)` (kept English per glossary)
    - `## Режимы установки` with `### Самостоятельная установка`, `### Дополняющая установка`, `### Обновление с v3.x`
    - `## Killer Features` (English per glossary, matches v3.x) with columns `Функция | Описание`
    - `## MCP-серверы (рекомендуем!)` with `### Глобально (все проекты)` and `### Для каждого проекта (учётные данные)`
    - `## Структура после установки`
    - `## Поддерживаемые фреймворки` with columns `Фреймворк | Шаблон | Skills | Автоопределение` (keep `Skills` English per glossary, matches v3.x precedent)
    - `## Компоненты`

    **Exact line 8 — Russian unlinked (this already matches the v3.x ru.md line 8 pattern — keep it verbatim):**

    ```markdown
    **[English](../../README.md)** | **Русский** | **[Español](es.md)** | **[Deutsch](de.md)** | **[Français](fr.md)** | **[中文](zh.md)** | **[日本語](ja.md)** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10 (carry the existing pattern):**

    ```markdown
    > Сначала прочитайте полный [пошаговый мануал по установке](../howto/ru.md).
    ```

    **Code blocks VERBATIM from README.md.** Same list as Plan 01 Task 2. IMPORTANT: the
    existing v3.x ru.md has a 3-line MCP block missing `sentry`; the v4.0 rewrite MUST
    have the 4-line global MCP block (context7, playwright, sequential-thinking, sentry)
    and the separate `### Для каждого проекта (учётные данные)` dbhub block. Also the
    `init-claude.sh` install command and the `--mode complement-full` override command
    are NEW in v4.0 and must be present.

    **Structure tree inline comments (reuse existing Russian translations where they
    already exist in v3.x ru.md, add new ones for new inline comments):**

    - `# Главные инструкции (адаптируйте под свой проект)` (existing)
    - `# Hooks, permissions` (existing — kept English)
    - `# Slash-команды` (existing — hyphenated)
    - `# † пропущено в complement-sp/full` (NEW — add for the † footnote markers)
    - `# Аудиты` (existing)
    - `# Субагенты` (existing)
    - `# Экспертиза по фреймворкам` (existing)
    - `# Автозагружаемые факты проекта` (existing)
    - `# Рабочие заметки` (existing)

    **Proper nouns preserved EXACTLY (D-06 list):** `Claude Code`, `superpowers`,
    `get-shit-done`, `RTK`, `MCP`, `Supreme Council`, `Gemini`, `ChatGPT`, `Laravel`,
    `Rails`, `Next.js`, `Node.js`, `Python`, `Go`, `Ruby on Rails`, `context7`,
    `playwright`, `sequential-thinking`, `sentry`, `dbhub`. Also: `Security Pack`
    (English per glossary), `Rate Limit Statusline` (English per glossary), `Skills`
    (English per glossary), `Killer Features` (English table header per glossary).

    **Russian-specific terminology (reuse v3.x lexical baseline):**
    - `Solo-разработчики` (compound preserving English "Solo") — reuse
    - `плагин` / `плагины` for plugin (standard Russian tech term)
    - `хук` for hook (or keep `hook` if v3.x file favors English — verify in existing ru.md)
    - `skill` / `скилл` — v3.x uses English `Skills` in headers; keep `Skills` English in
      column header but `скиллы` in prose if needed
    - `stack` → `стеки` (plural, matches v3.x)
    - `agent` → `агент`, `subagent` → `субагент` (matches v3.x)
    - `workflow` → no strong convention; `рабочий процесс` is safe, `воркфлоу` is
      informal — use `workflow` or `рабочий процесс` in prose

    **Register:** formal `Вы` (capital В). Verbs conjugate accordingly
    (`запустите`, `установите`, `прочитайте`, `используйте`).

    **Admonition labels:** `> **Примечание:**` (matches v3.x), `> **Важно:**`,
    `> **Безопасность:**`.

    **Punctuation:** ` — ` (spaced em-dash, standard Russian typography, matches v3.x).
    Guillemets `«...»` are natural in Russian prose but ASCII `"..."` is also safe;
    the existing ru.md mixes both — use ASCII in code/links, guillemets in prose if
    preferred (discretion). Russian typographic conventions: no space before `:`;
    comma before subordinate clauses that lack it in English.

    **What NOT to translate (D-04):** code blocks, CLI commands, CLI flags, file paths,
    package names, URL anchors, badge alt text, URL shield images, proper nouns above.

    **Line count target:** 202 source → 212–232 projected. Stay within 161–242 band.

    **Trailing newline required.**

    Commit message: `docs(06.1): sync ru README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/ru.md && [ "$(wc -l < docs/readme/ru.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/ru.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/ru.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/ru.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/ru.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/ru.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/ru.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/ru.md` returns ≥ 1
    - Current language unlinked: `grep -F '**Русский**' docs/readme/ru.md` returns ≥ 1 AND `grep -F '[Русский]' docs/readme/ru.md` returns 0
    - Howto link: `grep -F '../howto/ru.md' docs/readme/ru.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/ru.md`
    - `grep -F 'superpowers' docs/readme/ru.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/ru.md` ≥ 1
    - `grep -E 'дополня|complement' docs/readme/ru.md` ≥ 1 (Russian or English complement word)
    - `grep -c 'bash <(curl' docs/readme/ru.md` ≥ 3
    - Install Modes section: `grep -F '## Режимы установки' docs/readme/ru.md` returns 1
    - Install Modes subheadings: `grep -F '### Самостоятельная установка' docs/readme/ru.md` returns 1 AND `grep -F '### Дополняющая установка' docs/readme/ru.md` returns 1 AND `grep -F '### Обновление с v3.x' docs/readme/ru.md` returns 1
    - New v4.0 MCP entries present: `grep -F 'sentry' docs/readme/ru.md` returns ≥ 1 AND `grep -F 'dbhub' docs/readme/ru.md` returns ≥ 1
    - `--mode complement-full` override present: `grep -F -- '--mode complement-full' docs/readme/ru.md` returns ≥ 1
    - Components section with orchestration-pattern link: `grep -F 'orchestration-pattern.md' docs/readme/ru.md` returns ≥ 1
    - Terminology baseline preserved: `grep -F 'Solo-разработчики' docs/readme/ru.md` returns ≥ 1 (maintainer's preferred compound from v3.x)
    - `grep -F 'Быстрый старт' docs/readme/ru.md` returns ≥ 1 (reuse of v3.x heading)
    - Formal Вы register: `grep -c '\bВы\b' docs/readme/ru.md` ≥ 1 (at least one formal-you capitalized)
    - No heading ends with period/colon/question mark: `grep -E '^#+.*[.:?!]$' docs/readme/ru.md` returns 0
    - File ends with newline: `tail -c 1 docs/readme/ru.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/ru.md` matches `docs\(06\.1\): sync ru README`
  </acceptance_criteria>
</task>

<task id="06.1-03-02" type="auto">
  <name>Task 2: Plan 03 machine verification — per-file lint + band + make mdlint + maintainer read cue</name>
  <files>(no files modified — verification-only task)</files>

  <read_first>
    - Makefile (§ mdlint target at line 37)
    - .planning/phases/06.1-readme-translations/06.1-VALIDATION.md § Sampling Rate § Per-plan merge
    - docs/readme/ru.md (the file just written)
  </read_first>

  <action>
    Run the per-plan machine-verify gate for Plan 03. Like Plans 01 and 02, avoid running
    `make translation-drift` at this stage because the other 7 translations may still be
    stale if their plans haven't completed. The phase-level wrap-up in
    `/gsd-verify-work` runs `make translation-drift` after all 3 plans are done.

    Execute:

    ```bash
    set -e
    LINES=$(wc -l < "docs/readme/ru.md" | tr -d ' ')
    if [ "$LINES" -lt 161 ] || [ "$LINES" -gt 242 ]; then
      echo "FAIL: docs/readme/ru.md has $LINES lines (outside 161-242)"
      exit 1
    fi
    echo "PASS: docs/readme/ru.md has $LINES lines"

    npx markdownlint-cli2 docs/readme/ru.md || exit 1
    echo "PASS: docs/readme/ru.md markdownlint clean"

    make mdlint
    ```

    All must exit 0.

    **Maintainer-native read cue:** Print a visible notice in the task output that the
    Russian translation is now eligible for the maintainer's manual read-through. The
    maintainer (native Russian speaker) may spot idiomatic awkwardness that machine
    checks cannot catch. This is NOT a blocking automated gate — it is an informational
    prompt that the `/gsd-verify-work` phase wrap can reflect to the user. Example
    output line:

    ```text
    NOTE: docs/readme/ru.md written and machine-clean.
          Maintainer (native Russian speaker) should skim for idiomatic quality
          before the phase is closed. No blocking gate here.
    ```

    No commit required for this task; verification-only.
  </action>

  <verify>
    <automated>LINES=$(wc -l < docs/readme/ru.md | tr -d ' '); [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] && npx markdownlint-cli2 docs/readme/ru.md && make mdlint</automated>
  </verify>

  <acceptance_criteria>
    - ru.md within 161–242 line band
    - `npx markdownlint-cli2 docs/readme/ru.md` exits 0
    - `make mdlint` exits 0 (repo-wide, no regressions)
    - Maintainer read-through cue printed in task output (informational, not blocking)
    - No commit required (verification-only)
  </acceptance_criteria>
</task>

## Verification

Run at end of Plan 03 (executor-side):

```bash
# 1. Line count
LINES=$(wc -l < docs/readme/ru.md | tr -d ' ')
echo "docs/readme/ru.md: ${LINES} lines"
[ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] || { echo "OUT OF BAND"; exit 1; }

# 2. Structural parity
H2_TR=$(grep -c '^## ' docs/readme/ru.md)
H2_EN=$(grep -c '^## ' README.md)
[ "$H2_TR" = "$H2_EN" ] || { echo "H2 mismatch"; exit 1; }
H3_TR=$(grep -c '^### ' docs/readme/ru.md)
H3_EN=$(grep -c '^### ' README.md)
[ "$H3_TR" = "$H3_EN" ] || { echo "H3 mismatch"; exit 1; }

# 3. Per-file lint
npx markdownlint-cli2 docs/readme/ru.md

# 4. Repo-wide mdlint
make mdlint

# 5. Terminology baseline preserved from v3.x
grep -F 'Solo-разработчики' docs/readme/ru.md
grep -F 'Быстрый старт' docs/readme/ru.md

# 6. New v4.0 content present
grep -F 'sentry' docs/readme/ru.md
grep -F 'dbhub' docs/readme/ru.md
grep -F '## Режимы установки' docs/readme/ru.md
```

## Must-Haves (plan-level goal-backward checklist)

1. **TRUTH:** `docs/readme/ru.md` mirrors README.md's 12 sections in Russian (formal Вы), 161–242 lines.
2. **TRUTH:** Terminology baseline from v3.x ru.md preserved where still applicable (`Solo-разработчики`, `Быстрый старт`, `Примечание`, `Skills` as English column header).
3. **TRUTH:** New v4.0 content present in Russian form: Install Modes section with 3 subheadings, `sentry` and `dbhub` in MCP tables, Components section linking to `orchestration-pattern.md`, `--mode complement-full` override command.
4. **TRUTH:** File passes `npx markdownlint-cli2` individually.
5. **TRUTH:** `make mdlint` passes repo-wide after Plan 03 commits.
6. **TRUTH:** All code fences byte-identical to README.md.
7. **TRUTH:** Language switcher has `**Русский**` unlinked and `**[English](../../README.md)**` linked (already correct in v3.x line 8 — preserve).
8. **TRUTH:** Howto link `../howto/ru.md` preserved.

## Output

After Plan 03 completes, all 8 non-English READMEs have been rewritten to v4.0. At this
point `/gsd-verify-work` (phase-level wrap-up) runs:

1. `make translation-drift` — must exit 0 (all 8 files in band)
2. `make mdlint` — must exit 0
3. `make check` — full repo gate — must exit 0

That phase-level gate closes Phase 06.1 and unblocks Phase 7 Plan 07-04 (release gate).
