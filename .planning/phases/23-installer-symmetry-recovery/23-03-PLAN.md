---
phase: 23-installer-symmetry-recovery
plan: 03
type: execute
wave: 2
depends_on:
  - "23-02"
files_modified:
  - scripts/tests/test-uninstall-keep-state.sh
  - Makefile
  - .github/workflows/quality.yml
  - CHANGELOG.md
  - docs/INSTALL.md
autonomous: true
requirements:
  - KEEP-02
requirements_addressed:
  - KEEP-02
user_setup: []

must_haves:
  truths:
    - "`bash scripts/tests/test-uninstall-keep-state.sh` exits 0 against HEAD with all S1+S2+S3 scenario assertions passing."
    - "S1-A1: After `init-local.sh` install + `uninstall.sh --keep-state` (N to every modified prompt), `~/.claude/toolkit-install.json` exists on disk."
    - "S1-A2: A second `uninstall.sh` invocation (without --keep-state) is NOT a no-op — proven by `Backup created:` marker in stdout."
    - "S1-A3: The MODIFIED-files list on the second invocation is non-empty — proven by `MODIFIED` marker in stdout."
    - "S1-A4: Base-plugin `diff -q` invariant (UN-05 D-10) still passes after the second invocation — proven by exit code 0."
    - "S2-A1: `uninstall.sh --keep-state` with y-choice on every modified prompt also leaves the state file on disk (preservation independent of [y/N/d] branch)."
    - "S3-A1: `TK_UNINSTALL_KEEP_STATE=1` env-only path (no flag) preserves the state file (D-09 env-var precedence proven end-to-end)."
    - "Makefile Test 30 entry runs `bash scripts/tests/test-uninstall-keep-state.sh`; `make test` exits 0 with `All tests passed!`"
    - "CI step name updated from `Tests 21-29` to `Tests 21-30`; new test invocation appended."
    - "CHANGELOG.md `[4.4.0]` Added section gains 3 bullets (BANNER-01, KEEP-01, KEEP-02) — single consolidated v4.4 release entry per D-18."
    - "docs/INSTALL.md Installer Flags table gains `--no-banner` and `--keep-state` rows; extended sections describe both flags."
    - "`make check` exits 0 (markdownlint + shellcheck + validate)."
  artifacts:
    - path: "scripts/tests/test-uninstall-keep-state.sh"
      provides: "S1+S2+S3 scenario functions, sandbox setup, stdin-injection, A1-A4 assertions"
      min_lines: 150
      contains: "TK_UNINSTALL_KEEP_STATE"
    - path: "Makefile"
      provides: ".PHONY append, Test 30 block, test-uninstall-keep-state standalone target"
      contains: "test-uninstall-keep-state"
    - path: ".github/workflows/quality.yml"
      provides: "step rename Tests 21-29 → Tests 21-30, appended bash invocation"
      contains: "test-uninstall-keep-state.sh"
    - path: "CHANGELOG.md"
      provides: "Three bullets in [4.4.0] Added — BANNER-01, KEEP-01, KEEP-02"
      contains: "--no-banner"
    - path: "docs/INSTALL.md"
      provides: "Installer Flags table rows + extended sections for --no-banner and --keep-state"
      contains: "--keep-state"
  key_links:
    - from: "scripts/tests/test-uninstall-keep-state.sh"
      to: "scripts/init-local.sh + scripts/uninstall.sh"
      via: "real (cd $SANDBOX && bash init-local.sh) + (printf 'N\\n' | bash uninstall.sh --keep-state)"
      pattern: "init-local.sh.*uninstall.sh --keep-state"
    - from: "Makefile Test 30 line"
      to: "scripts/tests/test-uninstall-keep-state.sh"
      via: "@bash scripts/tests/test-uninstall-keep-state.sh"
      pattern: "test-uninstall-keep-state\\.sh"
    - from: ".github/workflows/quality.yml step name"
      to: "Test 30 invocation"
      via: "Tests 21-30 step appends bash test-uninstall-keep-state.sh"
      pattern: "Tests 21-30"
    - from: "CHANGELOG.md [4.4.0] Added section"
      to: "BANNER-01 + KEEP-01 + KEEP-02 bullets"
      via: "consolidated v4.4 release entry"
      pattern: "(--no-banner|--keep-state)"
    - from: "docs/INSTALL.md Installer Flags table"
      to: "--no-banner + --keep-state rows"
      via: "table append + extended sections"
      pattern: "(--no-banner|--keep-state)"
---

<objective>
KEEP-02 — write the hermetic test that proves the KEEP-01 contract end-to-end against a real `init-local.sh` install + `uninstall.sh --keep-state` round-trip in a /tmp sandbox. Mirror the shape of `scripts/tests/test-uninstall.sh` and `scripts/tests/test-uninstall-idempotency.sh` (sandbox HOME, `TK_UNINSTALL_*` test seams, scenario sub-functions, PASS/FAIL counter, exit non-zero on any assertion failure). Also wire the new test into `Makefile` (Test 30 + standalone target), update `.github/workflows/quality.yml` (rename `Tests 21-29` → `Tests 21-30`), append three bullets to `CHANGELOG.md` `[4.4.0]` Added (BANNER-01 + KEEP-01 + KEEP-02 — consolidated v4.4 release entry per D-18), and add `--no-banner` / `--keep-state` rows to `docs/INSTALL.md` Installer Flags table + extended sections.

Purpose: Prove KEEP-01's behaviour against real install + uninstall paths (Plan 23-02 only ships the wiring; this plan ships the proof). Document v4.4's three new flags in the user-visible CHANGELOG + INSTALL.md. Mirror the v4.3 test architecture so the test composes naturally with the existing 29 tests.

Output: One new test file (~180 lines, S1+S2+S3 scenarios, A1-A4 assertions); 4 distribution-surface files updated (Makefile, CI workflow, CHANGELOG, docs). No code changes to `scripts/uninstall.sh` — Plan 23-02 is the implementation; this plan is the proof + distribution layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-installer-symmetry-recovery/23-CONTEXT.md
@.planning/phases/23-installer-symmetry-recovery/23-RESEARCH.md
@.planning/phases/23-installer-symmetry-recovery/23-PATTERNS.md
@.planning/phases/23-installer-symmetry-recovery/23-VALIDATION.md
@scripts/tests/test-uninstall.sh
@scripts/tests/test-uninstall-idempotency.sh
@scripts/uninstall.sh
@Makefile
@.github/workflows/quality.yml
@CHANGELOG.md
@docs/INSTALL.md

<interfaces>
<!-- KEEP-02 test scaffold — extracted from test-uninstall.sh + test-uninstall-idempotency.sh (HEAD VERIFIED). -->

From scripts/tests/test-uninstall.sh (closest analog — provides scenario function + stdin injection):
```bash
# Header pattern
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"

# Helpers (REUSE — copy verbatim from test-uninstall-idempotency.sh:27-60)
assert_pass() { PASS=$((PASS + 1)); printf "  ${GREEN}OK${NC} %s\n" "$1"; }
assert_fail() { FAIL=$((FAIL + 1)); printf "  ${RED}FAIL${NC} %s\n" "$1"; printf "      %s\n" "$2"; }
assert_eq() { ... }
assert_contains() { ... }

# Sandbox + scenario function pattern (test-uninstall.sh:73-111)
run_s1() {
    local SANDBOX RC OUTPUT FILE_COUNT CANARY
    SANDBOX="$(mktemp -d /tmp/test-uninstall-keep-state.XXXXXX)"
    # shellcheck disable=SC2064  # we want $SANDBOX expanded NOW for the trap
    trap "rm -rf '${SANDBOX:?}'" RETURN

    # Real install via init-local.sh
    if ! (cd "$SANDBOX" && bash "$REPO_ROOT/scripts/init-local.sh" >/dev/null 2>&1); then
        assert_fail "S1 setup" "init-local.sh must succeed before test"
        return
    fi

    # Pick canary for MODIFIED classification (test-uninstall.sh:131-133)
    CANARY=$(jq -r '.installed_files[] | .path' "$SANDBOX/.claude/toolkit-install.json" \
             | grep -E '\.(md|json)$' | grep -v 'toolkit-install' | head -1)
    printf '\n# S1 keep-state modification\n' >> "$SANDBOX/$CANARY"

    # Real uninstall.sh --keep-state with stdin injection (test-uninstall.sh:142-148 pattern)
    OUTPUT=$(printf 'N\n' | \
        HOME="$SANDBOX" \
        TK_UNINSTALL_HOME="$SANDBOX" \
        TK_UNINSTALL_LIB_DIR="$REPO_ROOT/scripts/lib" \
        TK_UNINSTALL_TTY_FROM_STDIN=1 \
        bash "$REPO_ROOT/scripts/uninstall.sh" --keep-state 2>&1) || RC=$?
}
```

From scripts/uninstall.sh (output markers used by KEEP-02 assertions):
- Line 509: `log_info "Creating backup at $BACKUP_DIR (this may take a moment)…"` — pre-backup marker
- Line 520: `log_success "Backup created: $BACKUP_DIR"` — post-backup success marker (PRIMARY for "not a no-op" assertion)
- Line 209: `printf 'MODIFIED'` — classification output marker (assertable via grep -F 'MODIFIED')
- Line 440: `dro_print_header "?" "MODIFIED" "$n_modified" _DRO_Y` — dry-run output header (also produces 'MODIFIED' substring)

From Makefile (HEAD lines 1, 144-154):
- Line 1: `.PHONY: ... test-update-libs` — APPEND `test-uninstall-keep-state` to the end of this list
- Lines 147-150: existing Test 29 block + "All tests passed!" — INSERT new Test 30 block before `@echo "All tests passed!"`
- Lines 152-154: `test-update-libs:` standalone target — APPEND new `test-uninstall-keep-state:` standalone target after this block

From .github/workflows/quality.yml (HEAD lines 109-119):
- Line 109: `- name: Tests 21-29 — ...` step header — RENAME to `Tests 21-30 — ...` and add `, BANNER-01, KEEP-01..02` to coverage list
- Line 119: `bash scripts/tests/test-update-libs.sh` — APPEND new line `bash scripts/tests/test-uninstall-keep-state.sh` after it

From CHANGELOG.md (HEAD lines 8-29):
- Lines 8-10: `## [4.4.0] - 2026-04-27` + `### Added` heading
- Line 29: end of LIB-01/02 bullet (last line of existing v4.4 Added section)
- Line 30: blank line before `## [4.3.0]`
- INSERT 3 new bullets between line 29 and line 30 (or after line 30 within the same Added block — see action below for exact placement)

From docs/INSTALL.md (HEAD lines 29-53):
- Lines 34-41: Installer Flags table (currently 6 rows)
- Line 41: last row (`--no-bootstrap`)
- INSERT new rows for `--no-banner` and `--keep-state` after line 41 (and before the `### --no-bootstrap (v4.4+)` extended section starting at line 43)
- Lines 43-53: existing `### --no-bootstrap (v4.4+)` extended section
- INSERT 2 new extended sections (`### --no-banner (v4.4+)` and `### --keep-state for uninstall.sh (v4.4+)`) AFTER line 53 (and before the `---` separator at line 55)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `scripts/tests/test-uninstall-keep-state.sh` with S1+S2+S3 scenarios</name>
  <files>scripts/tests/test-uninstall-keep-state.sh</files>
  <read_first>
    - scripts/tests/test-uninstall.sh (full file, especially lines 1-180 — header, helpers, run_s1, run_s2 patterns; provides scenario function shape + stdin injection seam)
    - scripts/tests/test-uninstall-idempotency.sh (full file — provides assertion helper definitions to copy verbatim)
    - scripts/uninstall.sh (lines 380-395 idempotency guard, 509-520 backup creation markers, 200-215 classify_file MODIFIED marker, 649-680 state-delete gate from Plan 23-02)
    - .planning/phases/23-installer-symmetry-recovery/23-PATTERNS.md (test-uninstall-keep-state.sh section — full S1/S2/S3 scaffolding)
    - .planning/phases/23-installer-symmetry-recovery/23-RESEARCH.md (sections 5, 6, 8)
    - .planning/phases/23-installer-symmetry-recovery/23-CONTEXT.md (D-13 through D-17)
  </read_first>
  <action>
Create a new file `scripts/tests/test-uninstall-keep-state.sh` (executable bash, shebang `#!/usr/bin/env bash`) with three scenario functions S1+S2+S3 (D-16 — recommended scope per Claude's Discretion). Mirror the shape of `scripts/tests/test-uninstall.sh` (closest analog for scenario functions + stdin injection) and copy helper functions verbatim from `scripts/tests/test-uninstall-idempotency.sh:27-60`.

**Step 1 — Create file with header + helpers + sandbox setup pattern.**

Write the full file contents:

```bash
#!/usr/bin/env bash
# test-uninstall-keep-state.sh — KEEP-01/KEEP-02 partial-uninstall recovery contract.
#
# Verifies that --keep-state preserves toolkit-install.json and that a subsequent
# uninstall.sh invocation re-classifies still-present modified files (i.e. is NOT
# a no-op). Mirrors the shape of test-uninstall.sh (S1/S2/S3 scenarios) and reuses
# the assertion helpers from test-uninstall-idempotency.sh.
#
# Three scenarios (each a self-contained sandbox):
#   S1. --keep-state + N-choice: state file survives, second run re-classifies
#       Asserts A1 (state present), A2 (second run not no-op via 'Backup created:'),
#       A3 (MODIFIED list non-empty), A4 (base-plugin diff-q invariant via exit 0)
#   S2. --keep-state + y-choice: state file survives even on full-y branch
#       Asserts A1 only (state present)
#   S3. TK_UNINSTALL_KEEP_STATE=1 env-only (no flag): D-09 env-precedence path
#       Asserts A1 only (state present)
#
# Test seams (Phase 18-22 lineage):
#   TK_UNINSTALL_HOME           — redirects CLAUDE_DIR / STATE_FILE / LOCK_DIR to sandbox
#   TK_UNINSTALL_LIB_DIR        — sources lib files from repo root
#   TK_UNINSTALL_TTY_FROM_STDIN — reads [y/N/d] prompts from /dev/stdin instead of /dev/tty
#   TK_UNINSTALL_KEEP_STATE     — KEEP-01 D-09 env-var precedence path (S3)
#
# Usage: bash scripts/tests/test-uninstall-keep-state.sh
# Exit:  0 = all assertions passed, 1 = any failed

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"

RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m'

PASS=0
FAIL=0

# ─────────────────────────────────────────────────
# Assertion helpers (copy verbatim from test-uninstall-idempotency.sh:27-60)
# ─────────────────────────────────────────────────
assert_pass() {
    PASS=$((PASS + 1))
    printf "  ${GREEN}OK${NC} %s\n" "$1"
}

assert_fail() {
    FAIL=$((FAIL + 1))
    printf "  ${RED}FAIL${NC} %s\n" "$1"
    printf "      %s\n" "$2"
}

assert_eq() {
    local expected="$1" actual="$2" label="$3"
    if [ "$expected" = "$actual" ]; then
        assert_pass "$label"
    else
        assert_fail "$label" "expected='$expected' actual='$actual'"
    fi
}

assert_contains() {
    local pattern="$1" haystack="$2" label="$3"
    if printf '%s\n' "$haystack" | grep -q -- "$pattern"; then
        assert_pass "$label"
    else
        assert_fail "$label" "pattern not found: $pattern"
        printf '      output excerpt:\n'
        printf '%s\n' "$haystack" | head -10 | sed 's/^/        /'
    fi
}

# ─────────────────────────────────────────────────
# S1 — --keep-state + N-choice: state survives, second run re-classifies
# ─────────────────────────────────────────────────
run_s1() {
    local SANDBOX RC OUTPUT CANARY
    SANDBOX="$(mktemp -d /tmp/test-uninstall-keep-state.XXXXXX)"
    # shellcheck disable=SC2064  # expand $SANDBOX NOW for the trap
    trap "rm -rf '${SANDBOX:?}'" RETURN

    echo "  -- Scenario S1: --keep-state + N-choice, state survives, second run re-classifies --"

    # Real install via init-local.sh
    if ! (cd "$SANDBOX" && bash "$REPO_ROOT/scripts/init-local.sh" >/dev/null 2>&1); then
        assert_fail "S1 setup" "init-local.sh must succeed before test"
        return
    fi
    if [ ! -f "$SANDBOX/.claude/toolkit-install.json" ]; then
        assert_fail "S1 setup" "toolkit-install.json missing after init-local.sh"
        return
    fi

    # Modify a canary so the uninstaller classifies at least one MODIFIED file
    CANARY=$(jq -r '.installed_files[] | .path' "$SANDBOX/.claude/toolkit-install.json" \
             | grep -E '\.(md|json)$' | grep -v 'toolkit-install' | head -1)
    if [ -z "$CANARY" ]; then
        assert_fail "S1 setup: pick canary" "no .md/.json file in installed_files[]"
        return
    fi
    printf '\n# S1 keep-state modification\n' >> "$SANDBOX/$CANARY"

    # First uninstall WITH --keep-state, answer N to every modified prompt
    RC=0
    OUTPUT=$(printf 'N\n' | \
        HOME="$SANDBOX" \
        TK_UNINSTALL_HOME="$SANDBOX" \
        TK_UNINSTALL_LIB_DIR="$REPO_ROOT/scripts/lib" \
        TK_UNINSTALL_TTY_FROM_STDIN=1 \
        bash "$REPO_ROOT/scripts/uninstall.sh" --keep-state 2>&1) || RC=$?

    assert_eq "0" "$RC" "S1: first uninstall (--keep-state, N) exits 0"

    # A1 — state file preserved by --keep-state
    if [ -f "$SANDBOX/.claude/toolkit-install.json" ]; then
        assert_pass "S1-A1: state file preserved after --keep-state run (KEEP-01)"
    else
        assert_fail "S1-A1: state file preserved" "toolkit-install.json absent — gate failed"
    fi

    # Second uninstall WITHOUT --keep-state — must NOT be a no-op
    RC=0
    OUTPUT=$(printf 'y\n' | \
        HOME="$SANDBOX" \
        TK_UNINSTALL_HOME="$SANDBOX" \
        TK_UNINSTALL_LIB_DIR="$REPO_ROOT/scripts/lib" \
        TK_UNINSTALL_TTY_FROM_STDIN=1 \
        bash "$REPO_ROOT/scripts/uninstall.sh" 2>&1) || RC=$?

    # A2 — second run proceeded past idempotency guard (line 389)
    # 'Backup created:' is logged at scripts/uninstall.sh:520 — proves backup completed
    assert_contains 'Backup created:' "$OUTPUT" \
        "S1-A2: second run not a no-op (Backup created: marker present)"

    # A3 — MODIFIED list re-classified the still-present modified canary
    # 'MODIFIED' literal is in classify_file output (line 209) and dry-run header (line 440)
    assert_contains 'MODIFIED' "$OUTPUT" \
        "S1-A3: MODIFIED files re-classified on second run (non-empty list)"

    # A4 — base-plugin diff-q invariant (UN-05 D-10) still passes
    # exit 0 proves the diff-q check at uninstall.sh:638-647 succeeded
    assert_eq "0" "$RC" \
        "S1-A4: second run exits 0 (base-plugin diff-q invariant holds)"

    # Bonus: confirm default-branch deletes state file as before (no regression)
    if [ ! -f "$SANDBOX/.claude/toolkit-install.json" ]; then
        assert_pass "S1: control — second run (no --keep-state) deletes state file (UN-05 default unchanged)"
    else
        assert_fail "S1: control — second run deletes state" \
            "toolkit-install.json still present after non-keep-state run — default-branch regression"
    fi
}

# ─────────────────────────────────────────────────
# S2 — --keep-state + y-choice: state file survives full-y branch
# ─────────────────────────────────────────────────
run_s2() {
    local SANDBOX RC OUTPUT CANARY
    SANDBOX="$(mktemp -d /tmp/test-uninstall-keep-state.XXXXXX)"
    # shellcheck disable=SC2064
    trap "rm -rf '${SANDBOX:?}'" RETURN

    echo "  -- Scenario S2: --keep-state + y-choice, state file survives full-y branch --"

    if ! (cd "$SANDBOX" && bash "$REPO_ROOT/scripts/init-local.sh" >/dev/null 2>&1); then
        assert_fail "S2 setup" "init-local.sh must succeed before test"
        return
    fi

    CANARY=$(jq -r '.installed_files[] | .path' "$SANDBOX/.claude/toolkit-install.json" \
             | grep -E '\.(md|json)$' | grep -v 'toolkit-install' | head -1)
    [ -n "$CANARY" ] || { assert_fail "S2 setup: pick canary" "no canary"; return; }
    printf '\n# S2 keep-state y-modification\n' >> "$SANDBOX/$CANARY"

    RC=0
    OUTPUT=$(printf 'y\n' | \
        HOME="$SANDBOX" \
        TK_UNINSTALL_HOME="$SANDBOX" \
        TK_UNINSTALL_LIB_DIR="$REPO_ROOT/scripts/lib" \
        TK_UNINSTALL_TTY_FROM_STDIN=1 \
        bash "$REPO_ROOT/scripts/uninstall.sh" --keep-state 2>&1) || RC=$?

    assert_eq "0" "$RC" "S2: --keep-state + y-choice exits 0"

    # A1 — state file preserved even after y-branch (KEEP-01 is independent of [y/N/d])
    if [ -f "$SANDBOX/.claude/toolkit-install.json" ]; then
        assert_pass "S2-A1: state file preserved on y-branch --keep-state run"
    else
        assert_fail "S2-A1: state file preserved on y-branch" \
            "toolkit-install.json absent after y-branch --keep-state — gate misplaced inside [y/N/d] loop?"
    fi

    # Sanity: y-branch should have removed the modified canary
    if [ ! -f "$SANDBOX/$CANARY" ]; then
        assert_pass "S2: y-branch removed the modified canary (UN-03 default unchanged)"
    else
        assert_fail "S2: y-branch removed canary" "canary still present — y-choice regression"
    fi
}

# ─────────────────────────────────────────────────
# S3 — TK_UNINSTALL_KEEP_STATE=1 env-only path (no flag): D-09 env-precedence
# ─────────────────────────────────────────────────
run_s3() {
    local SANDBOX RC OUTPUT CANARY
    SANDBOX="$(mktemp -d /tmp/test-uninstall-keep-state.XXXXXX)"
    # shellcheck disable=SC2064
    trap "rm -rf '${SANDBOX:?}'" RETURN

    echo "  -- Scenario S3: TK_UNINSTALL_KEEP_STATE=1 env-only (no flag), D-09 env-precedence --"

    if ! (cd "$SANDBOX" && bash "$REPO_ROOT/scripts/init-local.sh" >/dev/null 2>&1); then
        assert_fail "S3 setup" "init-local.sh must succeed before test"
        return
    fi

    CANARY=$(jq -r '.installed_files[] | .path' "$SANDBOX/.claude/toolkit-install.json" \
             | grep -E '\.(md|json)$' | grep -v 'toolkit-install' | head -1)
    [ -n "$CANARY" ] || { assert_fail "S3 setup: pick canary" "no canary"; return; }
    printf '\n# S3 env-only modification\n' >> "$SANDBOX/$CANARY"

    # Invoke uninstall.sh with env var set, NO --keep-state flag
    RC=0
    OUTPUT=$(printf 'N\n' | \
        HOME="$SANDBOX" \
        TK_UNINSTALL_HOME="$SANDBOX" \
        TK_UNINSTALL_LIB_DIR="$REPO_ROOT/scripts/lib" \
        TK_UNINSTALL_KEEP_STATE=1 \
        TK_UNINSTALL_TTY_FROM_STDIN=1 \
        bash "$REPO_ROOT/scripts/uninstall.sh" 2>&1) || RC=$?

    assert_eq "0" "$RC" "S3: TK_UNINSTALL_KEEP_STATE=1 env-only exits 0"

    # A1 — state file preserved via env-var path (no --keep-state flag)
    if [ -f "$SANDBOX/.claude/toolkit-install.json" ]; then
        assert_pass "S3-A1: TK_UNINSTALL_KEEP_STATE=1 preserves state file (D-09 env-precedence)"
    else
        assert_fail "S3-A1: TK_UNINSTALL_KEEP_STATE=1 preserves state" \
            "toolkit-install.json absent after env-only run — env-var path broken (check KEEP_STATE=\${TK_UNINSTALL_KEEP_STATE:-0} init at line ~24)"
    fi
}

# ─────────────────────────────────────────────────
# Main run block
# ─────────────────────────────────────────────────
echo "Running test-uninstall-keep-state..."
echo ""
run_s1
run_s2
run_s3

echo ""
if [ "$FAIL" -eq 0 ]; then
    printf "${GREEN}✓ test-uninstall-keep-state: all %d assertions passed${NC}\n" "$PASS"
    exit 0
else
    printf "${RED}✗ test-uninstall-keep-state: %d of %d assertions FAILED${NC}\n" \
        "$FAIL" "$((PASS + FAIL))"
    exit 1
fi
```

**Step 2 — Make file executable.**

After writing, run `chmod +x scripts/tests/test-uninstall-keep-state.sh` so it can be invoked directly without `bash` prefix (consistent with sibling tests — `ls -l scripts/tests/test-uninstall*.sh` shows all are executable; if `chmod` is not effective in the worktree, the test still runs via `bash scripts/tests/test-uninstall-keep-state.sh` per Makefile/CI invocation pattern, so this is a soft requirement).

**Step 3 — Verify against current HEAD (Plan 23-02 must already be merged into the same branch).**

Run the test to confirm S1+S2+S3 all pass:

```bash
bash scripts/tests/test-uninstall-keep-state.sh
```

Expected output: 8 assertions pass (S1: 5 incl. control, S2: 2, S3: 1). Exit 0.

**Constraints:**
- Use `set -euo pipefail` at the top (project convention — all sibling tests use this).
- Reuse `assert_pass`/`assert_fail`/`assert_eq`/`assert_contains` helpers verbatim from `test-uninstall-idempotency.sh:27-60`. Do NOT introduce new helpers.
- Stdin injection MUST use `printf 'N\n'` or `printf 'y\n'` patterns (not heredoc; not echo with backslashes — same shape as `test-uninstall.sh:142-148`).
- Test seams MUST include `TK_UNINSTALL_HOME`, `TK_UNINSTALL_LIB_DIR`, and `TK_UNINSTALL_TTY_FROM_STDIN=1` for ALL three scenarios. Without `TK_UNINSTALL_TTY_FROM_STDIN=1`, the prompt loop reads from `/dev/tty` and hangs in CI (RESEARCH.md §5).
- Canary selection MUST use `jq -r '.installed_files[] | .path' ... | grep -E '\.(md|json)$' | grep -v 'toolkit-install' | head -1` exactly — proven resilient to install-set changes (Phase 20 D-canary precedent).
- The "Backup created:" string at line 520 is the PRIMARY "not a no-op" marker — proves backup completed AND idempotency guard at line 389 did NOT fire. The "Creating backup at" string at line 509 is also assertable but appears earlier and could fire even if the backup later failed.
- `MODIFIED` literal appears in `classify_file` (line 209) and `dro_print_header "?" "MODIFIED"` (line 440) — both produce the substring. Plain `grep -q -- 'MODIFIED'` matches either.
- Trap cleanup MUST use `${SANDBOX:?}` parameter-length guard (Phase 19 T-19-01-01 threat-model precedent — protects against `rm -rf` of root if SANDBOX is empty).
- shellcheck disable comments MUST be present where needed: `# shellcheck disable=SC2064` for the trap (we want $SANDBOX expanded NOW, not at trap-time — same reason as test-uninstall.sh:76).
- Header comment block lists the 3 scenarios + 4 seam variables explicitly so the test is self-documenting.
  </action>
  <verify>
    <automated>shellcheck scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>bash scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>bash scripts/tests/test-uninstall-keep-state.sh 2>&1 | grep -q 'all [0-9]* assertions passed'</automated>
    <automated>grep -q 'TK_UNINSTALL_KEEP_STATE=1' scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>grep -q 'TK_UNINSTALL_TTY_FROM_STDIN=1' scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>grep -q 'run_s1' scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>grep -q 'run_s2' scripts/tests/test-uninstall-keep-state.sh</automated>
    <automated>grep -q 'run_s3' scripts/tests/test-uninstall-keep-state.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/tests/test-uninstall-keep-state.sh` exists and is at least 150 lines
    - `bash scripts/tests/test-uninstall-keep-state.sh` exits 0 against HEAD with Plan 23-02 merged
    - Output contains `all N assertions passed` summary line (where N >= 8)
    - All 3 scenario functions present: `grep -q 'run_s[123]'` matches S1, S2, S3
    - All 4 test seams used: `grep -q TK_UNINSTALL_HOME`, `TK_UNINSTALL_LIB_DIR`, `TK_UNINSTALL_TTY_FROM_STDIN=1`, `TK_UNINSTALL_KEEP_STATE=1`
    - `shellcheck scripts/tests/test-uninstall-keep-state.sh` exits 0
    - Helpers (`assert_pass`, `assert_fail`, `assert_eq`, `assert_contains`) defined verbatim from `test-uninstall-idempotency.sh`
    - Trap uses parameter-length guard `${SANDBOX:?}` (T-19-01-01 precedent)
  </acceptance_criteria>
  <done>
    `scripts/tests/test-uninstall-keep-state.sh` exists with S1+S2+S3 scenarios, exits 0 on every assertion, and is shellcheck-clean. The test exercises `init-local.sh` install + `uninstall.sh --keep-state` (S1+S2) and `TK_UNINSTALL_KEEP_STATE=1 uninstall.sh` (S3) against /tmp sandboxes. KEEP-02 contract proven end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `test-uninstall-keep-state.sh` into Makefile (Test 30) and `.github/workflows/quality.yml`</name>
  <files>Makefile, .github/workflows/quality.yml</files>
  <read_first>
    - Makefile (full file — note line 1 .PHONY list, lines 144-154 Test 29 block + standalone target)
    - .github/workflows/quality.yml (lines 105-125 — Tests 21-29 step)
    - .planning/phases/23-installer-symmetry-recovery/23-PATTERNS.md (Makefile + quality.yml sections)
    - .planning/phases/23-installer-symmetry-recovery/23-RESEARCH.md (section 6)
  </read_first>
  <action>
Wire the new test into both the Makefile and the GitHub Actions workflow so `make test` and CI both run it.

**Step 1 — Makefile: Append PHONY target.**

Modify Makefile line 1 from:

```makefile
.PHONY: help check check-full lint shellcheck mdlint test validate validate-base-plugins version-align translation-drift agent-collision-static validate-commands test-matrix-bats cell-parity clean install test-update-libs
```

to:

```makefile
.PHONY: help check check-full lint shellcheck mdlint test validate validate-base-plugins version-align translation-drift agent-collision-static validate-commands test-matrix-bats cell-parity clean install test-update-libs test-uninstall-keep-state
```

(Append `test-uninstall-keep-state` to the end of the list.)

**Step 2 — Makefile: Insert Test 30 block.**

Modify Makefile lines 147-150 from:

```makefile
	@echo "Test 29: smart-update coverage for scripts/lib/*.sh (LIB-01..02)"
	@bash scripts/tests/test-update-libs.sh
	@echo ""
	@echo "All tests passed!"
```

to:

```makefile
	@echo "Test 29: smart-update coverage for scripts/lib/*.sh (LIB-01..02)"
	@bash scripts/tests/test-update-libs.sh
	@echo ""
	@echo "Test 30: --keep-state partial-uninstall recovery (KEEP-01..02)"
	@bash scripts/tests/test-uninstall-keep-state.sh
	@echo ""
	@echo "All tests passed!"
```

(Insert 3 lines — `@echo "Test 30..."`, `@bash ... .sh`, blank `@echo ""` — before the existing `@echo "All tests passed!"` final line.)

CRITICAL: Use TAB indentation (not spaces) — Makefile recipe lines REQUIRE TABs. Same rule applies to lines 148, 149.

**Step 3 — Makefile: Append standalone target.**

Modify Makefile lines 152-154 from:

```makefile
# Test 29 — smart-update coverage for scripts/lib/*.sh (LIB-01..02), invokable standalone
test-update-libs:
	@bash scripts/tests/test-update-libs.sh
```

to (append the new target after line 154):

```makefile
# Test 29 — smart-update coverage for scripts/lib/*.sh (LIB-01..02), invokable standalone
test-update-libs:
	@bash scripts/tests/test-update-libs.sh

# Test 30 — --keep-state partial-uninstall recovery (KEEP-01..02), invokable standalone
test-uninstall-keep-state:
	@bash scripts/tests/test-uninstall-keep-state.sh
```

(Add 4 lines — blank, comment, target line, recipe line — after the existing test-update-libs recipe at line 154. TAB indentation on the recipe line.)

**Step 4 — quality.yml: Rename step header + append test invocation.**

Modify `.github/workflows/quality.yml` lines 109-119 from:

```yaml
      - name: Tests 21-29 — uninstall + banner suite + bootstrap + lib coverage (UN-01..UN-08, BOOTSTRAP-01..04, LIB-01..02)
        run: |
          bash scripts/tests/test-uninstall-dry-run.sh
          bash scripts/tests/test-uninstall-backup.sh
          bash scripts/tests/test-uninstall-prompt.sh
          bash scripts/tests/test-uninstall.sh
          bash scripts/tests/test-install-banner.sh
          bash scripts/tests/test-uninstall-idempotency.sh
          bash scripts/tests/test-uninstall-state-cleanup.sh
          bash scripts/tests/test-bootstrap.sh
          bash scripts/tests/test-update-libs.sh
```

to:

```yaml
      - name: Tests 21-30 — uninstall + banner suite + bootstrap + lib coverage (UN-01..UN-08, BOOTSTRAP-01..04, LIB-01..02, BANNER-01, KEEP-01..02)
        run: |
          bash scripts/tests/test-uninstall-dry-run.sh
          bash scripts/tests/test-uninstall-backup.sh
          bash scripts/tests/test-uninstall-prompt.sh
          bash scripts/tests/test-uninstall.sh
          bash scripts/tests/test-install-banner.sh
          bash scripts/tests/test-uninstall-idempotency.sh
          bash scripts/tests/test-uninstall-state-cleanup.sh
          bash scripts/tests/test-bootstrap.sh
          bash scripts/tests/test-update-libs.sh
          bash scripts/tests/test-uninstall-keep-state.sh
```

Two edits:
- Line 109: rename `Tests 21-29` → `Tests 21-30` AND append `, BANNER-01, KEEP-01..02` to the coverage list (note: BANNER-01 was already covered by `test-install-banner.sh` extension in Plan 23-01; this is the formal coverage-list update for the step header).
- Line 119 (or after): append new line `          bash scripts/tests/test-uninstall-keep-state.sh` (10 leading spaces to match YAML run-block indentation, no trailing whitespace).

**Constraints:**
- Makefile recipe lines MUST use TAB indentation (not spaces). Default macOS / GNU Make requirement; mixing breaks the recipe.
- quality.yml YAML indentation MUST match the existing `run: |` block (10 leading spaces for the bash commands).
- Step header rename MUST be byte-accurate — coverage-list parser scripts (none currently, but future REL-* checks may) grep step names for REQ-IDs.
- Standalone Makefile target `test-uninstall-keep-state` allows local invocation: `make test-uninstall-keep-state` runs only Test 30 without the full suite — useful during plan execution to iterate quickly.
- `make test` MUST exit 0 with all 30 tests green after this task.
  </action>
  <verify>
    <automated>make test</automated>
    <automated>make test-uninstall-keep-state</automated>
    <automated>grep -qF 'test-uninstall-keep-state' Makefile</automated>
    <automated>grep -q '^test-uninstall-keep-state:' Makefile</automated>
    <automated>grep -q 'Test 30:' Makefile</automated>
    <automated>grep -q 'Tests 21-30' .github/workflows/quality.yml</automated>
    <automated>grep -q 'test-uninstall-keep-state.sh' .github/workflows/quality.yml</automated>
    <automated>grep -q 'BANNER-01, KEEP-01..02' .github/workflows/quality.yml</automated>
    <automated>shellcheck Makefile 2>/dev/null || true  # makefiles are not shellcheck targets</automated>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with output line `All tests passed!` after 30 test entries (Test 1..Test 30)
    - `make test-uninstall-keep-state` exits 0 (standalone target works)
    - `grep -q '^test-uninstall-keep-state:' Makefile` exits 0 (PHONY target declared)
    - `grep -q 'Test 30:' Makefile` exits 0 (test entry in main `test:` target)
    - `grep -q 'Tests 21-30' .github/workflows/quality.yml` exits 0 (CI step renamed)
    - `grep -q 'KEEP-01..02' .github/workflows/quality.yml` exits 0 (CI coverage list updated)
    - `grep -q 'bash scripts/tests/test-uninstall-keep-state.sh' .github/workflows/quality.yml` exits 0 (CI invocation appended)
    - `make check` exits 0 (no markdownlint, shellcheck, validate regressions from Makefile or YAML edits)
  </acceptance_criteria>
  <done>
    Makefile gains `test-uninstall-keep-state` PHONY entry, Test 30 block in the main `test:` target, and a standalone target. `.github/workflows/quality.yml` step renamed `Tests 21-29` → `Tests 21-30` with `BANNER-01, KEEP-01..02` in coverage list and the new test appended to the run block. `make test` runs all 30 tests green; `make check` clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append CHANGELOG.md `[4.4.0]` Added bullets (BANNER-01 + KEEP-01 + KEEP-02) and update docs/INSTALL.md Installer Flags + extended sections</name>
  <files>CHANGELOG.md, docs/INSTALL.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-35 — header + [4.4.0] Added section)
    - docs/INSTALL.md (lines 25-60 — Installer Flags table + --no-bootstrap extended section)
    - .planning/phases/23-installer-symmetry-recovery/23-PATTERNS.md (CHANGELOG.md + docs/INSTALL.md sections)
    - .planning/phases/23-installer-symmetry-recovery/23-CONTEXT.md (D-18 distribution decision)
  </read_first>
  <action>
Update both user-facing docs in a single task: CHANGELOG (release notes) and INSTALL.md (flag reference). Per D-18, all 3 v4.4 phases (21+22+23) consolidate into a single `[4.4.0]` Added section — Plan 22 already added BOOTSTRAP-01..04 + LIB-01..02 bullets; this task appends BANNER-01 + KEEP-01 + KEEP-02 bullets to the SAME section.

**Step 1 — CHANGELOG.md: Append three bullets to existing `[4.4.0]` Added section.**

Find the existing v4.4 Added section. Lines 8-29 contain:
- Line 8: `## [4.4.0] - 2026-04-27`
- Line 10: `### Added`
- Lines 12-19: BOOTSTRAP-01..04 bullet (Phase 21)
- Lines 21-29: LIB-01/02 bullet (Phase 22)
- Line 30: blank line
- Line 31: `## [4.3.0] - 2026-04-26` (next release block)

INSERT three new bullets between line 29 (end of LIB-01/02 bullet) and line 30 (blank line before [4.3.0]). Each bullet uses the same structural shape as the existing two (markdown unordered list with `-`, two-space hanging indent on continuation lines, blank line separator between bullets).

Insert the following content after line 29 (before line 30 blank):

```markdown

- **`--no-banner` flag for `init-claude.sh` and `init-local.sh`** — BANNER-01: both
  installers now accept `--no-banner` (and the `NO_BANNER=1` env var) to suppress the
  closing `To remove: bash <(curl …)` line. Default behaviour (flag absent) is byte-identical
  to v4.3. Symmetric with `update-claude.sh`, which already honoured this flag. Hermetic test:
  `scripts/tests/test-install-banner.sh` extended from 3 to 7 source-grep assertions
  covering the new defaults, argparse clauses, and gates in both init scripts.

- **`--keep-state` flag for `scripts/uninstall.sh`** — KEEP-01: passing `--keep-state`
  (or setting `TK_UNINSTALL_KEEP_STATE=1`) preserves `~/.claude/toolkit-install.json`
  after the run instead of deleting it as the LAST step. All other UN-01..UN-08 invariants
  (backup, sentinel-strip, base-plugin diff-q) are unchanged. A subsequent `uninstall.sh`
  invocation sees the state file, re-classifies still-present modified files, and re-presents
  the `[y/N/d]` prompt — enabling recovery after a partial-N uninstall session.

- **Hermetic test for `--keep-state`** — KEEP-02: `scripts/tests/test-uninstall-keep-state.sh`
  (Test 30) proves three scenarios end-to-end: N-choice preserves state and second run
  re-classifies modified files (S1: A1+A2+A3+A4); y-choice preserves state on full-y branch
  (S2: A1); `TK_UNINSTALL_KEEP_STATE=1` env-only path preserves state with no `--keep-state`
  flag (S3: A1, D-09 env-precedence).
```

(The leading blank line creates the separator between the LIB-01/02 bullet and the new BANNER-01 bullet, matching the existing inter-bullet spacing.)

**Step 2 — docs/INSTALL.md: Append two table rows to Installer Flags table.**

Find the existing Installer Flags table at lines 34-41. Append two new rows AFTER the `--no-bootstrap` row (line 40) and BEFORE the `--no-council` row (line 41).

Modify lines 40-41 from:

```markdown
| `--no-bootstrap` | `init-claude.sh`, `init-local.sh` | Skip the SP / GSD pre-install prompts. Equivalent env var: `TK_NO_BOOTSTRAP=1`. Use this in CI or scripted installs to keep behaviour identical to v4.3. |
| `--no-council` | `init-claude.sh` | Skip Supreme Council setup |
```

to:

```markdown
| `--no-bootstrap` | `init-claude.sh`, `init-local.sh` | Skip the SP / GSD pre-install prompts. Equivalent env var: `TK_NO_BOOTSTRAP=1`. Use this in CI or scripted installs to keep behaviour identical to v4.3. |
| `--no-banner` | `init-claude.sh`, `init-local.sh` | Suppress the closing `To remove: bash <(curl …)` banner line. Equivalent env: `NO_BANNER=1`. Symmetric with `update-claude.sh` which already honoured this flag. |
| `--keep-state` | `scripts/uninstall.sh` | Preserve `toolkit-install.json` after uninstall, enabling re-run recovery after a partial-N session. Equivalent env: `TK_UNINSTALL_KEEP_STATE=1`. |
| `--no-council` | `init-claude.sh` | Skip Supreme Council setup |
```

(Inserting two rows between `--no-bootstrap` and `--no-council`, preserving the alphabetical-ish grouping by flag prefix.)

**Step 3 — docs/INSTALL.md: Append two extended sections after `### --no-bootstrap (v4.4+)`.**

Find the existing `### --no-bootstrap (v4.4+)` section at lines 43-53. Append two new H3 sections AFTER line 53 (which currently reads `the flag explicitly when you want to make the intent visible in CI logs.`) and BEFORE line 55 (`---`):

After line 53, INSERT:

```markdown

### `--no-banner` (v4.4+)

Pass `--no-banner` (or set `NO_BANNER=1`) to suppress the closing
`To remove: bash <(curl …)` line that both installers print on success. Default behaviour
(flag absent, env unset) is byte-identical to v4.3. Use in CI pipelines or scripted
installs where the banner is redundant noise. Symmetric with `update-claude.sh`, which
has honoured this flag since v4.1 — Phase 23 closes the asymmetry gap.

### `--keep-state` for `uninstall.sh` (v4.4+)

Pass `--keep-state` (or set `TK_UNINSTALL_KEEP_STATE=1`) to preserve
`~/.claude/toolkit-install.json` after the uninstall run. This is a recovery flag
for partial-uninstall sessions: if you answered `N` to every modified-file prompt and
want to re-run the uninstaller to finish the job, the state file must still exist for
re-classification to work. A subsequent `uninstall.sh` run (with or without the flag)
proceeds normally — it is NOT a no-op, because the state file is present.

All other UN-01..UN-08 invariants stand: backup is still written to
`~/.claude-backup-pre-uninstall-<unix-ts>/`, the sentinel block is still stripped from
`~/.claude/CLAUDE.md`, and the base-plugin `diff -q` invariant still fires. The ONLY
behavioural delta is the LAST step (`rm -f $STATE_FILE`) — replaced with a `log_info`
message when `--keep-state` is set.
```

(Two new `### ...` sections, separated by blank lines, each with prose body. The leading blank line creates the separator between `--no-bootstrap` extended section and the new `--no-banner` section.)

**Step 4 — Lint pass.**

Run `npx markdownlint-cli "CHANGELOG.md" "docs/INSTALL.md"` (or `make mdlint`) and fix any markdownlint violations:

- MD040: ensure no fenced code blocks lack language tag (the bullets above don't introduce new fences; safe).
- MD031/MD032: ensure blank lines around lists and code blocks (the bullet structure above already includes blank lines between bullets and around the table edits).
- MD026: no trailing punctuation in headings (`### --no-banner (v4.4+)` and `### --keep-state for uninstall.sh (v4.4+)` both end with `(v4.4+)`, no trailing `.` or `:` — compliant).
- MD024: H3 headings (`### --no-banner` etc.) are siblings of `### --no-bootstrap` — `siblings_only: true` is configured in `.markdownlint.json`, so no duplicate-heading conflict.

**Constraints:**
- D-18: SINGLE consolidated `[4.4.0]` release entry — do NOT create a new `[4.4.1]` block. Append to the existing Added section.
- Bullet shape MUST match the existing BOOTSTRAP and LIB bullets (markdown unordered list, `-`, hanging indent on continuation lines, blank line between bullets).
- Banner string in CHANGELOG bullets MUST match the production string `To remove: bash <(curl …)` (Unicode ellipsis `…` — the existing v4.4 bullet for BOOTSTRAP uses ellipsis too; matches docs/INSTALL.md style).
- Table column widths: keep alignment loose (markdownlint MD060 disabled per `.markdownlint.json`); just keep `|` separators consistent.
- Per CONTEXT.md "Claude's Discretion": this task documents `--no-banner` and `--keep-state` in `docs/INSTALL.md` (recommended). Skipping this would close BANNER-01/KEEP-01 functionally but leave docs out of sync — D-18 expects single-release docs parity.
- markdownlint MUST pass after edits; the project's `make check` gate enforces this.
  </action>
  <verify>
    <automated>make check</automated>
    <automated>npx markdownlint-cli CHANGELOG.md docs/INSTALL.md</automated>
    <automated>grep -qF -- '--no-banner' CHANGELOG.md</automated>
    <automated>grep -qF -- '--keep-state' CHANGELOG.md</automated>
    <automated>grep -qF 'BANNER-01' CHANGELOG.md</automated>
    <automated>grep -qF 'KEEP-01' CHANGELOG.md</automated>
    <automated>grep -qF 'KEEP-02' CHANGELOG.md</automated>
    <automated>grep -qF -- '--no-banner' docs/INSTALL.md</automated>
    <automated>grep -qF -- '--keep-state' docs/INSTALL.md</automated>
    <automated>grep -q '### --no-banner (v4.4+)' docs/INSTALL.md</automated>
    <automated>grep -q '### --keep-state' docs/INSTALL.md</automated>
    <automated>grep -c '^## \[4\.4\.0\]' CHANGELOG.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[4\.4\.0\]' CHANGELOG.md` returns exactly `1` (single consolidated release entry per D-18)
    - All three new bullets present in CHANGELOG: `grep -qF 'BANNER-01'`, `grep -qF 'KEEP-01'`, `grep -qF 'KEEP-02'` all exit 0
    - `grep -q -- '--no-banner' CHANGELOG.md` and `grep -q -- '--keep-state' CHANGELOG.md` both exit 0
    - `grep -q -- '--no-banner' docs/INSTALL.md` and `grep -q -- '--keep-state' docs/INSTALL.md` both exit 0
    - `grep -q '### --no-banner (v4.4+)' docs/INSTALL.md` exits 0 (extended section present)
    - `grep -q '### --keep-state for' docs/INSTALL.md` exits 0 (extended section present)
    - `make check` exits 0 (markdownlint clean + shellcheck clean + validate clean)
    - `npx markdownlint-cli CHANGELOG.md docs/INSTALL.md` exits 0 (no MD040/MD031/MD032/MD026 violations)
    - Installer Flags table in docs/INSTALL.md gains exactly 2 new rows (`--no-banner`, `--keep-state`); existing rows unchanged
  </acceptance_criteria>
  <done>
    `CHANGELOG.md` `[4.4.0]` Added section now lists 5 bullets (BOOTSTRAP-01..04, LIB-01..02 from prior phases, and new BANNER-01 + KEEP-01 + KEEP-02 from this phase). `docs/INSTALL.md` Installer Flags table includes `--no-banner` and `--keep-state` rows; two new H3 extended sections describe each flag in detail. Single consolidated v4.4 release entry per D-18 — no new release-block heading created. `make check` exits 0 across markdownlint + shellcheck + validate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test sandbox `/tmp/test-uninstall-keep-state.XXXXXX` → real install / uninstall scripts | mktemp creates restrictive-perm dir; tests mutate only inside SANDBOX; trap RETURN cleans up |
| stdin pipe `printf 'N\n'` → `prompt_modified_for_uninstall()` | Stdin is opened by the test, fed deterministic content, closed before the next assertion. No external input crosses this seam. |
| Test seam env vars (`TK_UNINSTALL_*`) → uninstall.sh | Already-existing seams (Phase 18-22 lineage). The new `TK_UNINSTALL_KEEP_STATE` joins this set; test-only consumption. |
| CI workflow → bash scripts | GitHub Actions runs `bash scripts/tests/...` in `ubuntu-latest`; the workflow is the only entry point that runs without local user oversight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | mktemp sandbox `/tmp/test-uninstall-keep-state.XXXXXX` | accept | mktemp -d uses random 6-char suffix and creates dir with mode 0700 (POSIX default). Symlink races are prevented by mktemp's atomic creation. Same pattern used by 5+ existing v4.3 uninstall tests with no incidents. Trap uses `${SANDBOX:?}` parameter-length guard (T-19-01-01 precedent) — if SANDBOX is somehow empty, `rm -rf` exits with error, not deleting `/`. Threat surface: ZERO. |
| T-23-03-02 | DoS | Test sandbox cleanup on failure | mitigate | `trap "rm -rf '${SANDBOX:?}'" RETURN` cleans up even on test failure. Without trap, accumulated /tmp dirs would consume disk over CI runs. Trap is RETURN-scoped (per-function) so each scenario's sandbox is cleaned independently. Threat surface: LOW; mitigated. |
| T-23-03-03 | Tampering (CI) | quality.yml workflow consumes test invocation | accept | Workflow is pinned to `ubuntu-latest` with `permissions: contents: read` (workflow-level constraint per project conventions). The new `bash scripts/tests/test-uninstall-keep-state.sh` line cannot escalate privileges; runs under default GITHUB_TOKEN scope. Same threat profile as existing 9 uninstall test invocations. Threat surface: ZERO additional. |
| T-23-03-04 | Tampering | Bullet content in CHANGELOG.md / docs/INSTALL.md | accept | Pure documentation; no executable content. markdownlint enforces structural correctness; no XSS / injection vector since markdown is rendered server-side by GitHub or other Markdown viewers with their own escaping. Threat surface: ZERO. |
| T-23-03-05 | Disclosure | Sandbox temp files contain installed file SHA256 hashes (state file copy) | accept | SHA256 hashes are not secrets — they're public per the `manifest.json` entries. The test trap removes the sandbox before any /tmp cron job could read it (lifecycle is sub-second on CI). Same threat profile as existing 5 uninstall integration tests. Threat surface: ZERO. |

**Summary:** No HIGH-severity threats. Test sandboxes use existing mktemp + trap RETURN patterns proven safe across Phase 18-22 (10+ tests). Documentation edits are pure content, no executable injection. Approved per ASVS L1 / `block_on_high` policy.
</threat_model>

<verification>
After all 3 tasks complete, run the full Phase 23 suite plus distribution-surface verification:

```bash
# 1. The new test passes standalone
bash scripts/tests/test-uninstall-keep-state.sh

# 2. The full Make test target runs all 30 tests including the new one
make test

# 3. Standalone Make target works
make test-uninstall-keep-state

# 4. CI workflow consistency
grep -q 'Tests 21-30' .github/workflows/quality.yml
grep -q 'test-uninstall-keep-state.sh' .github/workflows/quality.yml
grep -q 'BANNER-01, KEEP-01..02' .github/workflows/quality.yml

# 5. CHANGELOG consistency — single [4.4.0] release entry, all 3 v4.4 phases consolidated
[ "$(grep -c '^## \[4.4.0\]' CHANGELOG.md)" -eq 1 ]
grep -qF 'BANNER-01' CHANGELOG.md
grep -qF 'KEEP-01' CHANGELOG.md
grep -qF 'KEEP-02' CHANGELOG.md
grep -qF 'BOOTSTRAP-01' CHANGELOG.md   # Phase 21 bullet still present
grep -qF 'LIB-01' CHANGELOG.md          # Phase 22 bullet still present

# 6. docs/INSTALL.md consistency — table + extended sections
grep -q '| `--no-banner` |' docs/INSTALL.md
grep -q '| `--keep-state` |' docs/INSTALL.md
grep -q '### --no-banner (v4.4+)' docs/INSTALL.md
grep -q '### --keep-state' docs/INSTALL.md

# 7. Quality gates
make check                    # markdownlint + shellcheck + validate
shellcheck scripts/tests/test-uninstall-keep-state.sh
npx markdownlint-cli CHANGELOG.md docs/INSTALL.md

# 8. Sanity — Plan 23-01 + 23-02 still green (no regression)
bash scripts/tests/test-install-banner.sh                       # 7 assertions from Plan 23-01
bash scripts/tests/test-uninstall.sh                            # v4.3 round-trip
bash scripts/tests/test-uninstall-idempotency.sh                # v4.3 idempotency
bash scripts/tests/test-uninstall-state-cleanup.sh              # v4.3 state cleanup

# 9. Smoke — manual --keep-state run via the same seams the test uses
SANDBOX=$(mktemp -d /tmp/keep-state-manual.XXXXXX)
(cd "$SANDBOX" && bash "$PWD/../scripts/init-local.sh" >/dev/null 2>&1) || true
[ -f "$SANDBOX/.claude/toolkit-install.json" ] || echo "install failed"
HOME="$SANDBOX" TK_UNINSTALL_HOME="$SANDBOX" TK_UNINSTALL_LIB_DIR="$PWD/scripts/lib" \
    TK_UNINSTALL_TTY_FROM_STDIN=1 \
    bash scripts/uninstall.sh --keep-state < /dev/null
[ -f "$SANDBOX/.claude/toolkit-install.json" ] && echo "state preserved (manual)" || echo "state preservation broken"
rm -rf "$SANDBOX"
```
</verification>

<success_criteria>
- All 3 tasks pass their `<verify>` automated commands.
- `bash scripts/tests/test-uninstall-keep-state.sh` exits 0 with `all N assertions passed` (N >= 8).
- `make test` exits 0 with `All tests passed!` after running 30 test entries.
- `make test-uninstall-keep-state` exits 0 standalone.
- `.github/workflows/quality.yml` step renamed `Tests 21-30`; coverage list includes `BANNER-01, KEEP-01..02`; new test invocation present.
- `CHANGELOG.md` has exactly one `[4.4.0]` release block; section contains 5 bullets total (BOOTSTRAP-01..04, LIB-01..02 from prior phases + new BANNER-01, KEEP-01, KEEP-02).
- `docs/INSTALL.md` Installer Flags table contains `--no-banner` and `--keep-state` rows; extended sections present.
- `make check` exits 0 (markdownlint + shellcheck + validate all clean).
- All KEEP-02 must-have truths verified through real `init-local.sh` + `uninstall.sh` round-trip in `/tmp` sandboxes.
- No regression in Plan 23-01 (test-install-banner.sh still 7 assertions green) or Plan 23-02 (uninstall.sh tests still green).
</success_criteria>

<output>
After completion, create `.planning/phases/23-installer-symmetry-recovery/23-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` with:
- 3 tasks complete
- Files created: scripts/tests/test-uninstall-keep-state.sh (new, ~180 lines, 8+ assertions across S1+S2+S3)
- Files modified: Makefile (PHONY + Test 30 block + standalone target), .github/workflows/quality.yml (step rename + invocation append), CHANGELOG.md (3 new bullets), docs/INSTALL.md (table rows + extended sections)
- Net diff: ~180 lines new test file, ~10 lines Makefile, ~2 lines quality.yml, ~25 lines CHANGELOG, ~25 lines docs/INSTALL.md
- KEEP-02 closed (hermetic test passes; CI mirror updated; docs synced)
- Phase 23 complete: BANNER-01 (Plan 23-01) + KEEP-01 (Plan 23-02) + KEEP-02 (Plan 23-03) all shipped
- v4.4 milestone status: 3 phases (21+22+23) consolidated into single CHANGELOG `[4.4.0]` release entry per D-18 — ready for `git tag v4.4.0` after milestone verification gate passes
- No new manifest.json entries (D-19 — uninstall.sh stays at its single Phase-20-registered entry; init scripts are entry points, not toolkit-installed files)
- Verification commands to re-run on regression
</output>
