## Claims 6-10

| Claim # | Claim Summary | Status | Evidence | Action |
|---------|---------------|--------|----------|--------|
| AUDIT-06 | No template version pinning — installer pulls main branch, no `template.lock.json` / `template_version` field | PARTIAL | `scripts/init-claude.sh:18` sets `REPO_URL="https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main"` — hardcoded to `main` branch, no version param accepted. `manifest.json:3` has `"version": "4.0.0"` field. `find . -name "template.lock.json"` → not found; `find . -name ".toolkit-version"` → not found. `scripts/init-claude.sh:117` checks for `~/.claude/toolkit-install.json` to detect reinstall. Gap: user cannot request a specific toolkit version at install time (no `--version` flag, no tag-based URL). Version is recorded post-install in `toolkit-install.json` but not enforced at fetch time. | → HARDEN-C-03 (deferred v4.2+) |
| AUDIT-07 | No template feature-flags — workflow-v2 / memory-v3 / agents-v1 versions not declared | FALSE | `grep -n "feature_flags\|features\|workflow-v\|memory-v\|agents-v" manifest.json scripts/init-claude.sh` → no matches. These version identifiers (`workflow-v2`, `memory-v3`, `agents-v1`) are not a concept in this repo; the toolkit does not version individual feature subsystems. The manifest tracks file-level inventory and `conflicts_with` vocabulary, not feature flags. No gap exists relative to this repo's design; the claim describes a feature system that was never part of this toolkit. | → No action |
| AUDIT-08 | Stack autodetection fragile — confidence scoring, override, dry-run preview missing | FALSE | `scripts/detect.sh` uses filesystem heuristics (file presence checks). `scripts/init-claude.sh:26-27` parses `--dry-run` flag; `scripts/init-claude.sh:42` parses positional `FRAMEWORK` override arg; `scripts/init-claude.sh:191-198` shows interactive menu allowing user to pick any of 8 frameworks including overriding the detected one. `scripts/init-local.sh:81-82` also supports `--dry-run`. `scripts/tests/test-dry-run.sh` exists. No confidence scoring, but CLI override + interactive menu + dry-run preview collectively address the usability gap. Claim's "no override, no dry-run" is contradicted by code. | → No action |
| AUDIT-09 | No dry-run installer mode — no preview of install plan | FALSE | `scripts/tests/test-dry-run.sh` exists (`ls scripts/tests/` confirms). `scripts/init-claude.sh:20,26-27` declares and parses `DRY_RUN=false` / `--dry-run`. `scripts/init-local.sh:72,81-82` same. `scripts/migrate-to-complement.sh:22,27` same. `scripts/init-claude.sh:385` documents "When --dry-run, prints grouped [INSTALL]/[SKIP] output and exits before any write." Three of three installer scripts implement `--dry-run`. Claim is fully contradicted by evidence. | → No action |
| AUDIT-10 | No collision detection with existing `.claude/` — overwrite/merge/fail behavior undeclared | PARTIAL | `scripts/init-claude.sh:118` detects existing install and prints "Install already present at ~/.claude/. Use 'update-claude.sh' to refresh or 'init-claude.sh --force' to reinstall." — top-level guard exists. Per-file idempotent guards: `scripts/init-local.sh:303,318,330,347` create files only if missing. `scripts/migrate-to-complement.sh:6` does 3-way diff for migration path. Gap: installer guard is a single yes/no check; no per-file merge policy is declared in documentation; behavior depends on which script runs (init vs update vs migrate) without a unified declared strategy. | → HARDEN-B-01 (deferred v4.2+) |
