## Claims 1-5

| Claim # | Claim Summary | Status | Evidence | Action |
|---------|---------------|--------|----------|--------|
| AUDIT-01 | Plugin manifest schema missing — no `plugin.schema.json` for `.claude-plugin/plugin.json` validation | FALSE | `scripts/validate-manifest.py:2` validates `manifest.json` v2 schema (6 checks: version, path keys, conflicts_with vocab, duplicate paths, file existence, drift). No `.claude-plugin/plugin.json` exists in this repo (`find . -name "plugin.json"` → not found). The claim targets a non-existent file type; `validate-manifest.py` covers what this repo actually ships. | → No action |
| AUDIT-02 | No template compatibility matrix — no `compatibility.json` to block incompatible stack combos | PARTIAL | `compatibility.json` not found (`find . -name "compatibility.json"` → not found). Inline compat metadata exists: `manifest.json:12` `conflicts_with: ["superpowers"]`, `manifest.json:35-36` `sp_equivalent` fields cover TK vs SP conflict vocabulary. No `conflicts_with` between framework templates (laravel vs nextjs incompatibility not tracked). Gap: inter-template incompatibility undeclared, though only one framework template installs per run. | → HARDEN-C-01 (deferred v4.2+) |
| AUDIT-03 | Namespace collision between templates — two framework templates ship same-named commands, overwrite each other at install | FALSE | `ls templates/*/commands/` → no `commands/` subdirectory exists under any framework template (`find templates -path "*/commands/*"` → not found). All 30 commands live exclusively in repo-root `commands/`; framework templates ship only `CLAUDE.md`, `settings.json`, `agents/`, `prompts/`, `skills/`, `rules/`. Single-copy install means zero collision risk at the commands layer. | → No action |
| AUDIT-04 | No template merge-strategy declaration — base+python+rag overlay semantics undefined | PARTIAL | No top-level merge-strategy declaration found in `scripts/init-claude.sh` or `scripts/init-local.sh`. Documented per-file behavior exists: `scripts/init-local.sh:303,318,330,347` create lessons-learned, current-task.md, CLAUDE.md, settings.json only-if-missing. No "rag" template exists (templates: base, global, go, laravel, nextjs, nodejs, python, rails). Multi-framework overlay not supported by design; installer picks exactly one framework. Gap: strategy is emergent from code, not declared as a policy document. | → HARDEN-C-02 (deferred v4.2+) |
| AUDIT-05 | Relative path assumptions in templates — `../skills/rag.md` inside template markdown breaks post-install | FALSE | `grep -rn "\.\./" templates/ --include="*.md"` returns only code examples inside audit prompts (e.g., `templates/laravel/prompts/SECURITY_AUDIT.md:472` shows `../../../config/app.php` as a path-traversal exploit example) and import statements in test-writer agents (user's project code, not installer paths). No `@../` Claude Code file-reference syntax found (`grep -rn "@\.\./" templates/ --include="*.md"` → none). No skill file named `rag.md` exists. All actual installer paths use absolute or framework-relative references handled by the fallback chain. | → No action |
