---
phase: 12-audit-verification-template-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
  - AUDIT-08
  - AUDIT-09
  - AUDIT-10
  - AUDIT-11
  - AUDIT-12
  - AUDIT-13
  - AUDIT-14
  - AUDIT-15

must_haves:
  truths:
    - "12-AUDIT.md exists with a 15-row verdict table; every row has Status (REAL|PARTIAL|FALSE), Evidence (file:line or 'not found'), and Action"
    - "REQUIREMENTS.md contains AUDIT-01..AUDIT-15 rows in the traceability table, each mapped to Phase 12 with a status reflecting the verdict"
    - "REQUIREMENTS.md contains proposed HARDEN-A-NN rows for every REAL or PARTIAL Wave-A verdict, marked 'Proposed - awaiting user gate'"
    - "ROADMAP.md Phase 12 block replaces '[To be planned]' with a real Goal, Requirements list, and Success Criteria; the Plans line declares 2 plans"
    - "User is presented with an explicit approval gate listing every proposed HARDEN-A-NN REQ before Plan 12.2 starts"
    - "No code under scripts/, Makefile, or .github/workflows/ is modified in Plan 12.1 (audit is read-only against the codebase)"
  artifacts:
    - path: ".planning/phases/12-audit-verification-template-hardening/12-AUDIT.md"
      provides: "15-row verdict table with Claim | Status | Evidence | Action"
      contains: "## Claim Verdicts"
      min_rows: 15
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUDIT-01..AUDIT-15 traceability rows + proposed HARDEN-A-NN rows"
      contains: "AUDIT-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 goal block filled in"
      contains: "Audit Verification + Template Hardening"
  key_links:
    - from: "12-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "each AUDIT-NN row in the verdict table has a matching row in the REQUIREMENTS traceability table with the same status"
      pattern: "AUDIT-0[1-9]|AUDIT-1[0-5]"
    - from: "12-AUDIT.md REAL/PARTIAL Wave-A rows"
      to: "proposed HARDEN-A-NN rows in REQUIREMENTS.md"
      via: "Action column of the AUDIT row names the HARDEN-A-NN it proposes"
      pattern: "HARDEN-A-0[0-9]"
    - from: "ROADMAP.md Phase 12 block"
      to: "12-AUDIT.md + REQUIREMENTS.md"
      via: "Requirements line references AUDIT-01..AUDIT-15 and the HARDEN-A-NN placeholder"
      pattern: "AUDIT-01..AUDIT-15"
---

<objective>
Produce a verified, evidence-backed audit of all 15 ChatGPT pass-3 template-level
claims against this repo's actual code. Record every verdict in a single
`12-AUDIT.md` file. Mirror every claim as an AUDIT-NN row in REQUIREMENTS.md.
For each REAL or PARTIAL Wave-A verdict, propose a corresponding HARDEN-A-NN
requirement. Stop at a user review gate; do not implement anything.

Purpose: Without ground-truth verification, Wave A implementation risks fixing
non-existent bugs (ChatGPT pass-1/2 already had false positives). Phase 12's
value is the audit itself — every claim confirmed or retracted with `file:line`
evidence, producing a paper trail that prevents re-opening the same claims
in v4.2+.

Output: `12-AUDIT.md` (15 verdicts), REQUIREMENTS.md AUDIT-01..15 + proposed
HARDEN-A-NN rows, ROADMAP.md Phase 12 block filled in. Plan 12.2 gated on user
approval of the HARDEN-A-NN list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-audit-verification-template-hardening/12-CONTEXT.md
@.planning/phases/12-audit-verification-template-hardening/12-PATTERNS.md

# Pass-3 audit source (the 15 claims to verify)
@$HOME/.claude/projects/-Users-sergeiarutiunian-Projects-claude-code-toolkit/memory/project_next_session_audit.md

# Existing validation infrastructure (verification targets)
@scripts/validate-manifest.py
@Makefile
@manifest.json

# Install scripts (verification targets for claims about install behavior)
@scripts/init-claude.sh
@scripts/init-local.sh
@scripts/update-claude.sh
@scripts/detect.sh

<interfaces>
<!-- The 15 claims from project_next_session_audit.md §"Template-level risks (pass 3 claims)". -->
<!-- AUDIT-NN numbering matches the source-document ordering 1..15. -->

AUDIT-01: Plugin manifest schema missing — no `plugin.schema.json` for `.claude-plugin/plugin.json` validation
AUDIT-02: No template compatibility matrix — no `compatibility.json` to block incompatible stack combos
AUDIT-03: Namespace collision between templates — `templates/python/commands/` + `templates/node/commands/` → `.claude/commands/` overwrites
AUDIT-04: No template merge-strategy declaration — base+python+rag activation: overwrite/merge/skip/fail undefined
AUDIT-05: Relative path assumptions in templates — `../skills/rag.md` inside template markdown breaks post-install
AUDIT-06: No template version pinning — installer pulls main branch, no `template.lock.json` / `template_version` field
AUDIT-07: No template feature-flags — workflow-v2 / memory-v3 / agents-v1 versions not declared
AUDIT-08: Stack autodetection fragile — `package.json` exists but project is python+node tooling → wrong template; need confidence scoring, override, dry-run preview
AUDIT-09: No dry-run installer mode — no preview of install plan
AUDIT-10: No collision detection with existing `.claude/` — overwrite/merge/fail behavior undeclared
AUDIT-11: No template integrity checksum — no `manifest.hash`
AUDIT-12: Markdown commands as templates without linting — required sections / frontmatter / step markers not enforced
AUDIT-13: No dependency graph between templates — rag requires memory, installer doesn't enforce
AUDIT-14: No uninstall semantics — can't remove template safely
AUDIT-15: No template provenance metadata — no `installed_templates.json` post-install

<!-- Wave-A theme (per D-05): schema/validation flavored claims. Initial partition (not a lock): -->
<!-- AUDIT-01 (plugin manifest schema), AUDIT-06 (version pinning), AUDIT-11 (integrity checksum), -->
<!-- AUDIT-12 (markdown commands linting). AUDIT.md may reclassify based on verdicts. -->

<!-- Wave-B theme (install safety) — deferred v4.2+: AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-08, AUDIT-10. -->
<!-- Wave-C theme (provenance/metadata) — deferred v4.2+: AUDIT-02, AUDIT-07, AUDIT-13, AUDIT-14, AUDIT-15. -->

<!-- Evidence format (D-02): -->
<!--   scripts/init-claude.sh:289-294     (line range) -->
<!--   manifest.json:67-74                (single-section reference) -->
<!--   "not found"                        (grep/glob returned empty, supports FALSE verdict) -->

<!-- Status vocabulary (D-04): REAL | PARTIAL | FALSE -->
<!-- Action vocabulary (D-04 + D-07): -->
<!--   → HARDEN-A-NN (Wave A schema/validation) -->
<!--   → HARDEN-B-NN (Wave B install safety — defer v4.2+) -->
<!--   → HARDEN-C-NN (Wave C provenance/metadata — defer v4.2+) -->
<!--   → No action (FALSE verdict, paper trail only) -->
<!--   → Document only (FALSE but worth noting in CONTRIBUTING.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify claims 1–5 via parallel Explore subagent</name>
  <files>.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md</files>
  <read_first>
    - .planning/phases/12-audit-verification-template-hardening/12-CONTEXT.md (D-02, D-09 methodology)
    - .planning/phases/12-audit-verification-template-hardening/12-PATTERNS.md (Evidence format, Status vocabulary)
    - $HOME/.claude/projects/-Users-sergeiarutiunian-Projects-claude-code-toolkit/memory/project_next_session_audit.md (source claims 1–5)
    - scripts/validate-manifest.py (AUDIT-01 verification target)
    - manifest.json (AUDIT-01, AUDIT-02, AUDIT-04 targets)
    - scripts/init-claude.sh (AUDIT-03, AUDIT-05 targets)
    - scripts/init-local.sh (AUDIT-03, AUDIT-05 targets)
  </read_first>
  <action>
    Spawn a subagent via the Agent tool with `model: "haiku"` and
    `subagent_type: "general-purpose"`. Pass a self-contained prompt that
    includes the 5 claims (AUDIT-01..AUDIT-05) and the verification rubric.

    The subagent MUST verify these 5 claims exactly:

    AUDIT-01: Plugin manifest schema missing — no `plugin.schema.json` for
      `.claude-plugin/plugin.json` validation.
      Verification steps:
        1. `grep -n "schema" scripts/validate-manifest.py` — list all checks
        2. `grep -rn "plugin.schema.json" .` — does this exact path exist?
        3. `find . -name "*.claude-plugin*" -o -name "plugin.json"` — does the
           claimed target file even exist in this repo?
        4. Read `scripts/validate-manifest.py` end-to-end and list WHAT it
           validates (manifest.json structure? .claude-plugin/plugin.json? both?)
        5. Verdict rule: if `validate-manifest.py` already validates what the
           claim calls "plugin.schema.json" → FALSE. If it validates something
           different → PARTIAL. If no validation at all → REAL.

    AUDIT-02: No template compatibility matrix — no `compatibility.json`.
      Verification steps:
        1. `find . -name "compatibility.json"` — does the file exist?
        2. `grep -n "conflicts_with" manifest.json | head -20` — is compat
           tracked inline instead?
        3. `grep -n "sp_equivalent" manifest.json | head -20` — alternative
           metadata?
        4. Verdict rule: file missing AND no equivalent inline metadata → REAL.
           File missing but inline `conflicts_with` + `sp_equivalent` cover the
           use case → PARTIAL. If `compatibility.json` is ever referenced in
           code or docs → FALSE.

    AUDIT-03: Namespace collision between templates — two framework templates
      both ship a `plan.md` command, they overwrite each other at install.
      Verification steps:
        1. `find templates -name "plan.md" -o -name "ship.md" -o -name "next.md"`
           — do multiple templates actually ship same-named commands?
        2. `ls templates/*/commands/ 2>/dev/null` — do framework templates even
           have a `commands/` subdir? (CLAUDE.md says commands live in
           repo-root `commands/`, not per-template.)
        3. Read `scripts/init-claude.sh` fallback chain at lines 289-294 (or
           current equivalent — re-grep `fallback` and `framework` to find it).
        4. Verdict rule: if framework templates have no `commands/` subdir
           (commands are repo-root only, installed once) → FALSE. If they do
           and fallback picks ONE → PARTIAL (override semantics exist but
           undeclared). If they do and installer truly concatenates → REAL.

    AUDIT-04: No template merge-strategy declaration — base+python+rag overlay
      semantics undefined.
      Verification steps:
        1. `grep -rn "merge" scripts/init-claude.sh scripts/init-local.sh scripts/update-claude.sh`
        2. `grep -rn "overwrite\|skip\|fail" scripts/init-*.sh` — is strategy
           declared anywhere?
        3. Check `manifest.json` for merge-strategy metadata per file.
        4. Verdict rule: if init scripts document per-file behavior (e.g., "only
           create if missing" for lessons-learned) but lack a top-level strategy
           declaration → PARTIAL. If nothing at all → REAL. If a declared
           strategy exists → FALSE.

    AUDIT-05: Relative path assumptions in templates — `../skills/rag.md`
      inside template markdown breaks post-install.
      Verification steps:
        1. `grep -rn "\.\./" templates/ --include="*.md" | head -50` — list all
           relative paths that traverse upward
        2. For each hit: does the target resolve at install destination? (i.e.,
           does the installer preserve sibling layout?)
        3. `grep -rn "@\.\./" templates/ --include="*.md"` — @-references
           (Claude Code syntax) to parents?
        4. Verdict rule: hits count = 0 → FALSE. Hits exist but all resolve to
           valid post-install paths (tested by installer layout) → PARTIAL.
           Hits exist and clearly break post-install → REAL.

    The subagent MUST return a markdown table body (5 rows, no header) with
    columns: `Claim # | Claim Summary | Status | Evidence | Action`. Evidence
    must use repo-relative `file:line` or `file:start-end` or the literal
    string `"not found"`. Action must use the D-04 vocabulary:
    `→ HARDEN-A-NN (proposed)`, `→ HARDEN-B-NN (deferred v4.2+)`,
    `→ HARDEN-C-NN (deferred v4.2+)`, `→ No action`, `→ Document only`.

    After the subagent returns, write its 5-row table to
    `.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md`
    under a single H2 heading `## Claims 1-5`. No other content in the file —
    this is a staging artifact consumed by Task 4.

    Per D-09 (Haiku tier for deterministic grep/glob): if the subagent reports
    a claim requires cross-file reasoning beyond grep evidence, note that in
    the Action column with suffix `(escalate to Sonnet for follow-up)` and
    continue. The main thread (Task 4) will synthesize escalations.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md &amp;&amp; [ "$(grep -c '^|' .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `12-AUDIT-partial-1.md` exists in the phase dir
    - File contains exactly one H2 heading `## Claims 1-5`
    - File contains ≥5 markdown table data rows (lines starting with `|`)
    - Every row's Status column contains one of: `REAL`, `PARTIAL`, `FALSE`
    - Every row's Evidence column contains either `file:` (repo-relative path +
      colon) or the exact string `"not found"`
    - Every row's Action column starts with `→ ` and uses one of the D-04
      action strings
    - No row is blank or prose-only (no `TBD`, no `—` alone)
  </acceptance_criteria>
  <done>
    5-row staging table written; ready for Task 4 synthesis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify claims 6–10 via parallel Explore subagent</name>
  <files>.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md</files>
  <read_first>
    - .planning/phases/12-audit-verification-template-hardening/12-CONTEXT.md (D-02, D-09 methodology)
    - .planning/phases/12-audit-verification-template-hardening/12-PATTERNS.md (Evidence format, Status vocabulary)
    - $HOME/.claude/projects/-Users-sergeiarutiunian-Projects-claude-code-toolkit/memory/project_next_session_audit.md (source claims 6–10)
    - manifest.json (AUDIT-06, AUDIT-07 targets)
    - scripts/detect.sh (AUDIT-08 target)
    - scripts/tests/test-dry-run.sh (AUDIT-09 target — existence is the verdict)
    - scripts/init-claude.sh (AUDIT-09, AUDIT-10 targets)
    - scripts/update-claude.sh (AUDIT-09, AUDIT-10 targets)
  </read_first>
  <action>
    Spawn a subagent via the Agent tool with `model: "haiku"` and
    `subagent_type: "general-purpose"`. Pass a self-contained prompt with
    claims AUDIT-06..AUDIT-10 and the verification rubric below.

    AUDIT-06: No template version pinning — installer pulls main branch, no
      `template.lock.json` / `template_version` field.
      Verification steps:
        1. `grep -n "raw.githubusercontent.com" scripts/init-claude.sh scripts/update-claude.sh`
           — confirm branch (main?) in fetch URLs
        2. `grep -n "version" manifest.json | head -20` — is there a `.version`
           field? (yes per `version-align` Makefile target)
        3. `find . -name "template.lock.json" -o -name ".toolkit-version"` —
           any equivalent?
        4. `grep -n "toolkit-version\|toolkit-install\.json" scripts/*.sh`
        5. Verdict rule: pinning absent → REAL. Pinning via manifest `.version`
           + `.toolkit-version` file written at install → PARTIAL (exists but
           user can't request a specific version at install time). Fully
           versioned installer → FALSE.

    AUDIT-07: No template feature-flags — workflow-v2 / memory-v3 / agents-v1
      versions not declared.
      Verification steps:
        1. `grep -rn "feature_flags\|features\|workflow-v\|memory-v\|agents-v" manifest.json scripts/init-claude.sh`
        2. `grep -n "flags" manifest.json`
        3. Verdict rule: no such concept in repo → REAL. Manifest carries
           feature metadata but not under a dedicated `features` key → PARTIAL.

    AUDIT-08: Stack autodetection fragile — confidence scoring, override,
      dry-run preview.
      Verification steps:
        1. Read `scripts/detect.sh` end-to-end (it exists — see ls output).
        2. `grep -n "confidence\|score\|override\|--framework\|--stack" scripts/detect.sh scripts/init-claude.sh scripts/init-local.sh`
        3. `grep -n "dry-run\|--dry-run\|DRY_RUN" scripts/init-claude.sh scripts/init-local.sh`
        4. Verdict rule: detect.sh uses file-presence heuristic + CLI override
           flag + `--dry-run` preview → FALSE or PARTIAL (no confidence
           scoring but mitigated by override + preview). No override and no
           preview → REAL.

    AUDIT-09: No dry-run installer mode — no preview of install plan.
      Verification steps:
        1. `test -f scripts/tests/test-dry-run.sh && echo exists` — if it
           exists, dry-run is implemented.
        2. `grep -n "DRY_RUN\|--dry-run\|dry_run" scripts/init-claude.sh scripts/update-claude.sh scripts/migrate-to-complement.sh`
        3. `bash scripts/init-claude.sh --dry-run --help 2>&1 | head -20` (read
           only — do NOT execute install) — does the flag parse?
        4. Verdict rule: `--dry-run` works in all 3 scripts → FALSE. Works in
           some but not all → PARTIAL. Not implemented anywhere → REAL.

    AUDIT-10: No collision detection with existing `.claude/` — overwrite /
      merge / fail behavior undeclared.
      Verification steps:
        1. `grep -n "existing\|overwrite\|already\|-f\|if \[ -f" scripts/init-claude.sh scripts/init-local.sh`
        2. Read `scripts/migrate-to-complement.sh` — does it do 3-way diff? If
           yes, collision detection exists at migration time.
        3. Check CONTEXT.md code_context section — "Idempotent creation:
           lessons-learned/project-context/skill-rules/current-task/CLAUDE.md
           only if missing" — confirm via grep in scripts.
        4. Verdict rule: zero existing-file checks → REAL. Per-file idempotent
           guards (create-if-missing) + migrate-to-complement.sh 3-way diff →
           PARTIAL (declared for some files, not a top-level policy). Full
           declared policy → FALSE.

    Same output contract as Task 1: 5-row markdown table body, columns
    `Claim # | Claim Summary | Status | Evidence | Action`, Evidence uses
    repo-relative `file:line` or `"not found"`, Action uses D-04 vocabulary.

    Write the result to
    `.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md`
    under H2 heading `## Claims 6-10`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md &amp;&amp; [ "$(grep -c '^|' .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `12-AUDIT-partial-2.md` exists in the phase dir
    - File contains exactly one H2 heading `## Claims 6-10`
    - File contains ≥5 markdown table data rows
    - Every row's Status is `REAL`, `PARTIAL`, or `FALSE`
    - Every row's Evidence contains `file:` or the exact string `"not found"`
    - Every row's Action starts with `→ ` and uses the D-04 vocabulary
  </acceptance_criteria>
  <done>
    5-row staging table written; ready for Task 4 synthesis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify claims 11–15 via parallel Explore subagent</name>
  <files>.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md</files>
  <read_first>
    - .planning/phases/12-audit-verification-template-hardening/12-CONTEXT.md (D-02, D-09 methodology)
    - .planning/phases/12-audit-verification-template-hardening/12-PATTERNS.md (Evidence format, Status vocabulary)
    - $HOME/.claude/projects/-Users-sergeiarutiunian-Projects-claude-code-toolkit/memory/project_next_session_audit.md (source claims 11–15)
    - manifest.json (AUDIT-11, AUDIT-13, AUDIT-15 targets)
    - Makefile (AUDIT-12 target — existing validate rules)
    - .github/workflows/quality.yml (AUDIT-12 target — CI template validation)
    - scripts/update-claude.sh (AUDIT-14, AUDIT-15 targets)
  </read_first>
  <action>
    Spawn a subagent via the Agent tool with `model: "haiku"` and
    `subagent_type: "general-purpose"`. Pass a self-contained prompt with
    claims AUDIT-11..AUDIT-15 and the verification rubric below.

    AUDIT-11: No template integrity checksum — no `manifest.hash`.
      Verification steps:
        1. `grep -n "hash\|sha256\|checksum" manifest.json scripts/init-claude.sh scripts/update-claude.sh scripts/validate-manifest.py`
        2. `find . -name "*.sha256" -o -name "manifest.hash"`
        3. Verdict rule: no hashing anywhere → REAL. Hash computed but not
           distributed with installer → PARTIAL. Full integrity pipeline →
           FALSE.

    AUDIT-12: Markdown commands as templates without linting — required
      sections / frontmatter / step markers not enforced.
      Verification steps:
        1. Read Makefile `validate` target (lines ~104-137) — what does it
           require? (`QUICK CHECK`, `САМОПРОВЕРКА|SELF-CHECK` in
           `templates/*/prompts/*.md` audit files).
        2. `grep -n "commands/" Makefile` — are `commands/*.md` linted? (No —
           only `templates/*/prompts/*.md`.)
        3. Read `.github/workflows/quality.yml` `validate-templates` job —
           same scope?
        4. `ls commands/` — list commands that lack a Purpose / Usage check
        5. Verdict rule: audit-prompt validation exists but `commands/*.md`
           are unlinted → PARTIAL (linting exists for ONE surface, missing
           for another). Nothing at all → REAL. Full linting for both → FALSE.

    AUDIT-13: No dependency graph between templates — rag requires memory,
      installer doesn't enforce.
      Verification steps:
        1. `grep -rn "requires\|depends\|dependency" manifest.json`
        2. Read `manifest.json` top-level structure — any `dependencies` /
           `requires` keys?
        3. `ls templates/` — does a `rag` template even exist? If the claim's
           example doesn't apply, note that in Evidence.
        4. Verdict rule: no dependency metadata and templates do have
           implicit inter-dependencies → REAL. Metadata missing but templates
           are self-contained (no rag-requires-memory situation) → FALSE.

    AUDIT-14: No uninstall semantics — can't remove template safely.
      Verification steps:
        1. `grep -rn "uninstall\|remove\|--uninstall" scripts/`
        2. Is there an `uninstall-claude.sh`? `ls scripts/ | grep -i uninstall`
        3. Does `migrate-to-complement.sh` serve as partial uninstall? Read it.
        4. Verdict rule: no uninstall script, no remove flag → REAL. Migration
           script can partially un-do installs → PARTIAL.

    AUDIT-15: No template provenance metadata — no `installed_templates.json`
      post-install.
      Verification steps:
        1. `find . -name "installed_templates.json" -o -name "toolkit-install.json"`
        2. `grep -rn "toolkit-install\|installed_templates\|toolkit-version" scripts/`
        3. What DOES the installer write post-install? (`.toolkit-version`
           file? a JSON? nothing?)
        4. Verdict rule: nothing recorded → REAL. `.toolkit-version` exists
           but not structured JSON → PARTIAL. Structured install manifest
           written → FALSE.

    Same output contract as Tasks 1–2: 5-row markdown table body, same
    columns, same Evidence and Action formats. Write to
    `.planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md`
    under H2 heading `## Claims 11-15`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md &amp;&amp; [ "$(grep -c '^|' .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `12-AUDIT-partial-3.md` exists in the phase dir
    - File contains exactly one H2 heading `## Claims 11-15`
    - File contains ≥5 markdown table data rows
    - Every row's Status is `REAL`, `PARTIAL`, or `FALSE`
    - Every row's Evidence contains `file:` or the exact string `"not found"`
    - Every row's Action starts with `→ ` and uses the D-04 vocabulary
  </acceptance_criteria>
  <done>
    5-row staging table written; ready for Task 4 synthesis.
  </done>
</task>

<task type="auto">
  <name>Task 4: Synthesize 12-AUDIT.md + update REQUIREMENTS.md + ROADMAP.md</name>
  <files>
    .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md (Task 1 output)
    - .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md (Task 2 output)
    - .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md (Task 3 output)
    - .planning/phases/12-audit-verification-template-hardening/12-PATTERNS.md (table header pattern, document header pattern, traceability table rows)
    - .planning/REQUIREMENTS.md (exact traceability block lines 63-79 to append to)
    - .planning/ROADMAP.md (Phase 8 block lines 44-55 as template, Phase 12 block lines 104-113 to replace)
    - .markdownlint.json (lint rules MD040/MD031/MD032/MD026)
  </read_first>
  <action>
    This task synthesizes the 3 partial tables into the final AUDIT.md,
    updates REQUIREMENTS.md with AUDIT rows + proposed HARDEN-A-NN rows,
    and fills in the ROADMAP.md Phase 12 block.

    STEP 1 — Create `12-AUDIT.md` with this exact header:

    ```markdown
    # Phase 12: Audit Verification — Verdict Table

    **Verified:** 2026-04-24
    **Source:** ChatGPT pass-3 audit (15 template-level claims)
    **Method:** grep/glob proof + code read per claim; 3 parallel Explore agents (claims 1-5, 6-10, 11-15)

    ## Verdict Summary

    | Status | Count |
    |--------|-------|
    | REAL | N |
    | PARTIAL | N |
    | FALSE | N |

    ## Claim Verdicts

    | Claim # | Claim Summary | Status | Evidence | Action |
    |---------|---------------|--------|----------|--------|
    ```

    Append the 15 rows from the 3 partial files in order (AUDIT-01..AUDIT-15).
    Update the `N` counts in the Verdict Summary to the actual counts.

    STEP 2 — After the 15-row table, add this exact section:

    ```markdown
    ## Wave Assignment

    Per D-05, findings split into 3 waves by theme:

    - **Wave A (schema/validation):** AUDIT-NN rows with REAL/PARTIAL status
      AND Action naming `HARDEN-A-NN`. These go to the user gate for approval
      into Plan 12.2.
    - **Wave B (install safety):** AUDIT-NN rows with Action naming
      `HARDEN-B-NN`. Deferred v4.2+; REQ definitions here only.
    - **Wave C (provenance/metadata):** AUDIT-NN rows with Action naming
      `HARDEN-C-NN`. Deferred v4.2+; REQ definitions here only.

    ## Proposed HARDEN-A-NN Requirements (awaiting user gate)

    | HARDEN ID | Derived From | Proposed Work |
    |-----------|--------------|----------------|
    | HARDEN-A-01 | AUDIT-NN | [concrete proposal derived from Evidence] |
    ```

    For every REAL or PARTIAL row whose Action names `HARDEN-A-NN (proposed)`,
    add a row to the proposals table with:
      - HARDEN ID: sequential `HARDEN-A-01`, `HARDEN-A-02`, ...
      - Derived From: the AUDIT-NN row number
      - Proposed Work: a one-sentence concrete description (e.g., "Add
        `validate-commands` Makefile target that greps `commands/*.md` for
        `## Purpose` and `## Usage` headings; wire into `check` target and
        `.github/workflows/quality.yml`")

    STEP 3 — Update `.planning/REQUIREMENTS.md`:

    3a. After the `## UX Polish` section (before `## Future Requirements`
    heading at ~line 46), INSERT this new section:

    ```markdown
    ## Audit Verification (Phase 12)

    Traceability records for all 15 ChatGPT pass-3 template-level claims.
    Every claim gets a REQ-ID regardless of verdict. FALSE rows are closed
    immediately; REAL/PARTIAL rows that pass the user gate become HARDEN-A-NN
    requirements.

    - [ ] **AUDIT-01**: [claim summary from 12-AUDIT.md row 1] — Status: [REAL|PARTIAL|FALSE]
    - [ ] **AUDIT-02**: [claim summary from row 2] — Status: ...
    ... (AUDIT-03 through AUDIT-15)

    ## Wave A Hardening (Phase 12 — awaiting user gate)

    HARDEN-A-NN requirements are populated only after the user gate in
    Plan 12.1 approves the corresponding REAL/PARTIAL Wave-A findings.

    - [ ] **HARDEN-A-01**: [proposed work from 12-AUDIT.md proposals table]
    - [ ] **HARDEN-A-02**: ...
    ```

    3b. Extend the `## Traceability` table (at ~line 65-79). Append after
    the existing `| UX-01 | Phase 11 | Planned |` row:

    ```markdown
    | AUDIT-01 | Phase 12 | Closed - FALSE |
    | AUDIT-02 | Phase 12 | REAL |
    | ... (15 rows, one per AUDIT-NN, Status column = 'Closed - FALSE' | 'Closed - PARTIAL' | 'REAL') |
    | HARDEN-A-01 | Phase 12 | Proposed - awaiting user gate |
    | HARDEN-A-02 | Phase 12 | Proposed - awaiting user gate |
    ```

    Use these exact status strings based on verdict:
      - FALSE verdict → `Closed - FALSE`
      - PARTIAL verdict + no Wave-A Action → `Closed - PARTIAL`
      - PARTIAL verdict + Wave-A Action (proposed HARDEN-A) → `REAL` (promoted
        proposal, same as REAL semantically)
      - REAL verdict + Wave-A Action → `REAL`
      - REAL verdict + Wave-B/C Action → `REAL` (Deferred v4.2+) — use
        exactly that string including parentheses
      - HARDEN-A-NN proposed row → `Proposed - awaiting user gate`

    3c. Update the Coverage line (currently line 79 reads
    `**Coverage:** 11 / 11 requirements mapped to phases ✓`):

    New value: `**Coverage:** (11 + 15 + N) / (11 + 15 + N) requirements mapped to phases ✓`
    where N is the count of proposed HARDEN-A-NN rows. Compute the number and
    substitute: e.g., if 3 HARDEN-A proposed, line becomes
    `**Coverage:** 29 / 29 requirements mapped to phases ✓`.

    STEP 4 — Update `.planning/ROADMAP.md`:

    4a. Replace the current Phase 12 block (lines ~104-113) with this exact
    structure (copying Phase 8 shape from lines 44-55):

    ```markdown
    ### Phase 12: Audit Verification + Template Hardening

    **Goal**: Verify all 15 ChatGPT pass-3 template-level audit claims against
    actual code; implement Wave A (schema/validation) REAL findings approved
    at user gate; create full AUDIT-NN + HARDEN-A-NN REQ traceability
    **Depends on**: Phase 11
    **Requirements**: AUDIT-01..AUDIT-15, HARDEN-A-01..HARDEN-A-NN (NN TBD after gate)
    **Success Criteria** (what must be TRUE):

    1. `12-AUDIT.md` exists with 15-row verdict table; every row has Status
       + Evidence (file:line or "not found") + Action; no row is blank or
       prose-only
    2. REQUIREMENTS.md carries AUDIT-01..AUDIT-15 rows with correct statuses;
       FALSE rows are closed; REAL/PARTIAL rows have HARDEN wave assignment
    3. HARDEN-A-NN REQs (user-approved subset) are implemented and wired into
       `make check`; CI passes
    4. Wave B and Wave C REQs are defined in AUDIT.md but NOT entered in
       REQUIREMENTS.md until promoted in v4.2+

    **Plans:** 2 plans

    Plans:
    - [ ] 12-01-PLAN.md — AUDIT: verify 15 claims, produce verdict table, propose HARDEN-A-NN
    - [ ] 12-02-PLAN.md — WAVE-A: implement user-approved HARDEN-A-NN REQs (gated on 12-01 approval)
    ```

    4b. Update the Progress table at line ~97-102 — REPLACE the existing
    progress table with one that includes Phase 12. Add this row after
    Phase 11's row:

    ```markdown
    | 12. Audit Verification + Template Hardening | v4.1 | 0/2 | Not started | - |
    ```

    STEP 5 — Delete the 3 staging files (they are consumed):

    ```bash
    rm .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-1.md
    rm .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-2.md
    rm .planning/phases/12-audit-verification-template-hardening/12-AUDIT-partial-3.md
    ```

    STEP 6 — Verify markdownlint on the new / modified files:

    ```bash
    markdownlint .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
    ```

    Fix MD040/MD031/MD032/MD026 violations before completing the task.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md &amp;&amp; [ "$(grep -c '^| AUDIT-' .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md)" -ge 15 ] &amp;&amp; grep -q 'AUDIT-01' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'AUDIT-15' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Audit Verification + Template Hardening' .planning/ROADMAP.md &amp;&amp; ! grep -q 'To be planned' .planning/ROADMAP.md &amp;&amp; markdownlint .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `12-AUDIT.md` exists at the phase dir path
    - `12-AUDIT.md` contains the exact H1 `# Phase 12: Audit Verification — Verdict Table`
    - `12-AUDIT.md` contains the exact H2 `## Verdict Summary`
    - `12-AUDIT.md` contains the exact H2 `## Claim Verdicts`
    - `12-AUDIT.md` contains the exact H2 `## Wave Assignment`
    - `12-AUDIT.md` contains the exact H2 `## Proposed HARDEN-A-NN Requirements (awaiting user gate)`
    - `12-AUDIT.md` has ≥15 rows starting with `| AUDIT-` (one per claim)
    - REQUIREMENTS.md contains `AUDIT-01` AND `AUDIT-15` strings
    - REQUIREMENTS.md contains heading `## Audit Verification (Phase 12)`
    - REQUIREMENTS.md contains heading `## Wave A Hardening (Phase 12 — awaiting user gate)`
    - REQUIREMENTS.md Traceability table contains ≥15 rows with `| AUDIT-` prefix
    - ROADMAP.md Phase 12 block no longer contains the string `[To be planned]`
    - ROADMAP.md Phase 12 block contains `**Plans:** 2 plans`
    - ROADMAP.md Progress table contains a row for Phase 12 with `0/2` plan count
    - No staging file `12-AUDIT-partial-*.md` remains in the phase dir
    - `markdownlint .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` exits 0
  </acceptance_criteria>
  <done>
    Single-source-of-truth AUDIT.md written; REQUIREMENTS.md carries full
    paper trail; ROADMAP.md Phase 12 goal filled in; staging files cleaned.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: USER GATE — approve HARDEN-A-NN REQs for Plan 12.2</name>
  <what-built>
    - `12-AUDIT.md` with 15 verdicts (REAL/PARTIAL/FALSE per claim, file:line evidence)
    - REQUIREMENTS.md carries AUDIT-01..15 + proposed HARDEN-A-NN rows
    - ROADMAP.md Phase 12 block replaces `[To be planned]` with real goal
  </what-built>
  <decision>
    Which proposed HARDEN-A-NN REQs get promoted into Plan 12.2 implementation?
  </decision>
  <context>
    Per D-11, auto-promotion is rejected. Some REAL findings may be
    real-but-not-worth-fixing in v4.1 (e.g., a gap already mitigated by
    existing checks from a different angle). User reads each proposal and
    approves, rejects, or defers each one individually.

    Rejected or deferred proposals stay recorded in REQUIREMENTS.md with a
    `Closed - accepted risk` or `Deferred v4.2+` status — they are not
    deleted. Approved proposals flip to `Planned` and drop into Plan 12.2.
  </context>
  <how-to-verify>
    1. Read `.planning/phases/12-audit-verification-template-hardening/12-AUDIT.md`
       — scan the Verdict Summary, then the `## Proposed HARDEN-A-NN
       Requirements (awaiting user gate)` table
    2. For each proposed HARDEN-A-NN row, decide: approve / reject / defer.
    3. For each DECISION, respond with the HARDEN ID and the disposition, e.g.:
       - `HARDEN-A-01: approve`
       - `HARDEN-A-02: reject - already covered by validate-manifest.py`
       - `HARDEN-A-03: defer v4.2+`
    4. Claude will apply your decisions by updating REQUIREMENTS.md statuses
       before Plan 12.2 starts. Rejected/deferred rows get the correct status
       written; approved rows become the concrete scope of Plan 12.2.
  </how-to-verify>
  <options>
    <option id="approve-all">
      <name>Approve all proposed HARDEN-A-NN REQs</name>
      <pros>Maximum Wave A coverage; closes every REAL finding in v4.1.</pros>
      <cons>Some proposals may duplicate existing Makefile checks; risks
      spurious fix work per D-11 rationale.</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a subset; reject / defer the rest</name>
      <pros>Honors D-11 rationale: only fix what is actually worth fixing in
      v4.1; preserves paper trail for rejections.</pros>
      <cons>Requires per-REQ decision — more work at the gate.</cons>
    </option>
    <option id="reject-all">
      <name>Reject all; Phase 12 ships AUDIT.md only, no Wave A work</name>
      <pros>Clean boundary — verify now, implement v4.2+. Matches "full
      pass-3 paper trail in REQUIREMENTS" framing if all proposals are
      reviewed-but-rejected.</pros>
      <cons>Plan 12.2 becomes no-op. Consider making this a 1-plan phase in
      that case.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with a list of per-REQ decisions in the format:
    `HARDEN-A-NN: approve | reject - <reason> | defer v4.2+`

    Or pick one option: `approve-all`, `reject-all`, `approve-subset + list`.

    Plan 12.2 runs only after this reply is processed.
  </resume-signal>
  <files>.planning/REQUIREMENTS.md (status updates applied AFTER user reply)</files>
  <action>
    This task is a blocking user gate. Do NOT execute any file writes in this
    task. Present the `## Proposed HARDEN-A-NN Requirements (awaiting user
    gate)` table from `12-AUDIT.md` to the user and wait for their reply.

    After the user reply arrives (format: per-REQ decisions `HARDEN-A-NN:
    approve | reject - <reason> | defer v4.2+`, OR one of the preset
    options `approve-all` / `reject-all` / `approve-subset + list`), the
    main thread applies the decisions to REQUIREMENTS.md and
    `12-01-SUMMARY.md` as follows:

    - approved → Traceability Status = `Planned` (ready for Plan 12.2)
    - rejected → Traceability Status = `Closed - accepted risk`, with the
      reason appended inline
    - deferred → Traceability Status = `Deferred v4.2+`

    Resume signal: user replies in the resume-signal format. The executor
    MUST halt until that reply is received; auto-promotion is rejected
    per D-11.
  </action>
  <verify>
    <automated>grep -q 'HARDEN-A-' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    User has responded with per-REQ decisions; REQUIREMENTS.md reflects the
    approve / reject / defer status for every proposed HARDEN-A-NN row;
    `12-01-SUMMARY.md` records the decision list verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Read-only codebase access | Subagents and main thread grep/read files under the repo root; no user input is processed, no credentials are handled, no code is executed beyond `markdownlint` (which is already CI-trusted). |
| Planning docs write path | Three files are modified (AUDIT.md, REQUIREMENTS.md, ROADMAP.md) — all inside `.planning/`; no source code, no secrets, no configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | 12-AUDIT.md table rows | mitigate | Each row requires `file:line` evidence or literal `"not found"`; Task 4 acceptance criteria grep-verifies row count and column content; markdownlint enforces structure. |
| T-12-02 | Information Disclosure | subagent prompts | accept | Prompts contain only the 15 claims + repo-relative paths; no credentials, no PII, no secrets. Subagents run on Haiku tier per project CLAUDE.md routing; no external network calls beyond grep/read. |
| T-12-03 | Repudiation | REQUIREMENTS.md edits | mitigate | All edits land in a single git commit per Plan 12.1 completion, with conventional-commit message and co-author trailer. REQUIREMENTS.md traceability table carries explicit status per REQ; paper trail is the explicit goal of D-08. |
| T-12-04 | Elevation of Privilege | none | accept | No privileged operations performed. Phase 12.1 is strictly read-only against scripts/, Makefile, CI workflows. Write scope is limited to `.planning/` markdown files. |
</threat_model>

<verification>
Phase-level checks after Task 4 completes:

- `make mdlint` exits 0 (no new MD040/MD031/MD032/MD026 violations)
- `grep -c '^| AUDIT-' .planning/phases/12-audit-verification-template-hardening/12-AUDIT.md` returns ≥ 15
- `grep -c '| AUDIT-' .planning/REQUIREMENTS.md` returns ≥ 15 (traceability rows)
- `grep -q 'To be planned' .planning/ROADMAP.md` returns non-zero (goal was replaced)
- No code files (under `scripts/`, `Makefile`, `.github/workflows/`, `manifest.json`, `templates/`, `commands/`) were modified — `git diff --name-only HEAD` shows only files under `.planning/`
</verification>

<success_criteria>
Plan 12.1 completes when:

1. `12-AUDIT.md` exists with 15 verdict rows (Status + Evidence + Action
   columns all populated per acceptance criteria)
2. REQUIREMENTS.md carries AUDIT-01..AUDIT-15 rows + proposed HARDEN-A-NN rows
3. ROADMAP.md Phase 12 block has a real Goal + Requirements + Success Criteria
   (no `[To be planned]` or `TBD`)
4. User has reviewed the HARDEN-A-NN proposals and responded with per-REQ
   decisions
5. No scripts, Makefile, CI, manifest, templates, or commands were modified
</success_criteria>

<output>
After completion, create
`.planning/phases/12-audit-verification-template-hardening/12-01-SUMMARY.md`
with:

- Total claims verified: 15
- Verdict counts: REAL / PARTIAL / FALSE
- Proposed HARDEN-A-NN count
- User gate decisions (list of approve/reject/defer per REQ)
- Next step: Plan 12.2 runs with the approved subset (or is a no-op if all rejected)
</output>
