---
phase: 17-distribution-manifest-installers-changelog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - CHANGELOG.md
autonomous: true
requirements:
  - DIST-01
  - DIST-03
threat_refs:
  - T-17-01
user_setup: []

must_haves:
  truths:
    - "manifest.json `version` field reads `4.2.0` (was `4.1.1`)"
    - "manifest.json `files.rules` array contains an entry for `rules/audit-exceptions.md`"
    - "CHANGELOG.md top entry is `## [4.2.0] - YYYY-MM-DD` (literal placeholder, real date set in 17-03)"
    - "The [4.2.0] CHANGELOG body documents every Phase 13–16 user-visible feature (FP allowlist seed, /audit-skip, /audit-restore, 6-phase /audit, structured-report schema, Council audit-review mode, 49-file template propagation, manifest 4.2.0 bump)"
    - "manifest.json `updated:` field carries placeholder `YYYY-MM-DD` (real date set in 17-03)"
  artifacts:
    - path: "manifest.json"
      provides: "Toolkit version bump + new audit-exceptions.md rule registration"
      contains: '"version": "4.2.0"'
    - path: "CHANGELOG.md"
      provides: "v4.2.0 release notes covering all Phase 13–16 user-visible features"
      contains: "## [4.2.0]"
  key_links:
    - from: "manifest.json `version`"
      to: "CHANGELOG.md `## [4.2.0]` heading"
      via: "make version-align gate (validates equality after 17-03)"
      pattern: "version-align"
    - from: "manifest.json `files.rules`"
      to: "templates/base/rules/audit-exceptions.md"
      via: "manifest path entry"
      pattern: '"path": "rules/audit-exceptions.md"'
---

<objective>
Bump `manifest.json` to v4.2.0, register the missing `templates/base/rules/audit-exceptions.md` entry under `files.rules`, and create a complete `## [4.2.0] - YYYY-MM-DD` placeholder entry in `CHANGELOG.md` that covers every Phase 13–16 user-visible feature.

Purpose: DIST-01 requires `manifest.json` to register all v4.2 artifacts (audit-exceptions.md is the one that's missing — `commands/audit-skip.md` and `commands/audit-restore.md` already shipped in Phase 13 manifest entries). DIST-03 requires a CHANGELOG entry covering the milestone. Per D-08, the ship date is a literal `YYYY-MM-DD` placeholder until the milestone-close commit replaces it in 17-03.

Output: manifest.json (version + new rule entry), CHANGELOG.md (new [4.2.0] entry above [4.1.1]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-distribution-manifest-installers-changelog/17-CONTEXT.md

# Current artifacts to mutate
@manifest.json
@CHANGELOG.md

# Coverage source — features must be reflected in [4.2.0] CHANGELOG body
@.planning/phases/13-foundation-fp-allowlist/13-01-PLAN.md
@.planning/phases/13-foundation-fp-allowlist/13-02-PLAN.md
@.planning/phases/13-foundation-fp-allowlist/13-03-PLAN.md
@.planning/phases/13-foundation-fp-allowlist/13-04-PLAN.md
@.planning/phases/14-audit-pipeline-fp-recheck/14-03-PLAN.md
@.planning/phases/15-council-audit-review-integration/15-04-PLAN.md
@.planning/phases/16-template-propagation-49-prompt-files/16-03-PLAN.md

<interfaces>
<!-- Existing manifest.json schema (relevant fragment, from current state). Executor should match path style exactly. -->

```json
{
  "files": {
    "rules": [
      { "path": "rules/README.md" },
      { "path": "rules/project-context.md" }
    ]
  }
}
```

After this plan the rules array MUST contain a third entry: `{ "path": "rules/audit-exceptions.md" }`.

<!-- Existing CHANGELOG.md headings (relevant fragment). New [4.2.0] entry goes above [4.1.1]. -->

```markdown
## [4.1.1] - 2026-04-25

### Fixed
...

## [4.1.0] - 2026-04-25
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump manifest.json version + register audit-exceptions.md rule entry</name>
  <files>manifest.json</files>
  <action>Edit `manifest.json` with two changes (per D-01 and D-02):

1. `version`: change `"4.1.1"` → `"4.2.0"`.
2. `updated`: leave value as `"YYYY-MM-DD"` literal placeholder (D-08 — real date set in 17-03's milestone-close commit). Replace the current `"2026-04-25"` with `"YYYY-MM-DD"`. The `make version-align` gate will FAIL while the placeholder is in place — that's expected; 17-03 closes the gate.
3. `files.rules` array: append a third entry `{ "path": "rules/audit-exceptions.md" }` after the existing `rules/project-context.md` entry. Match the existing object style exactly (same indentation, no `conflicts_with` annotation — it's a base-only rule with no SP/GSD equivalent). Path is relative to `templates/base/` per the manifest's path convention (consistent with other `rules/*` entries).

Keep all other fields unchanged: `manifest_version`, `description`, `sp_equivalent_note`, `inventory`, `claude_md_sections`, `templates`. Do NOT add a `components` section, do NOT touch the `inventory` block (per `<constraints>` in 17-CONTEXT.md — "out of scope: Adding a manifest scripts/ or components/ section"). Do NOT modify any other `files.*` array.

Run `python3 scripts/validate-manifest.py` after edits — it must exit 0 (the audit-exceptions.md file already exists at `templates/base/rules/audit-exceptions.md` per the Phase 13 ship state, so the path-existence check passes).</action>
  <verify>
    <automated>jq -e '.version == "4.2.0" and .updated == "YYYY-MM-DD" and (.files.rules | map(.path) | contains(["rules/audit-exceptions.md"]))' manifest.json &amp;&amp; python3 scripts/validate-manifest.py</automated>
  </verify>
  <done>jq query returns `true`, `validate-manifest.py` exits 0, no other fields touched (verify with `git diff manifest.json` — diff scope strictly limited to version + updated + the new rules entry).</done>
</task>

<task type="auto">
  <name>Task 2: Create [4.2.0] CHANGELOG entry covering all Phase 13–16 features</name>
  <files>CHANGELOG.md</files>
  <action>Insert a new `## [4.2.0] - YYYY-MM-DD` entry in `CHANGELOG.md` directly above the existing `## [4.1.1] - 2026-04-25` heading (per D-05 + D-08).

Section ordering per D-05: **Added** → **Changed** → **Fixed** → **Documentation**. Use Keep-a-Changelog phrasing; user-focused, no internal phase/plan mechanics like "Phase 14 plan 3 task 2".

**Coverage requirement (D-05, DIST-03):** the entry body MUST mention every Phase 13–16 user-visible feature. Use grep-friendly terminology so the 17-03 verifier can confirm coverage. Mandatory feature terms (every term below must appear at least once in the [4.2.0] section, case-insensitive grep):

1. `audit-exceptions.md` (FP allowlist seed file, Phase 13 — EXC-03)
2. `/audit-skip` (Phase 13 — EXC-01)
3. `/audit-restore` (Phase 13 — EXC-02)
4. `6-phase` or `6-step FP recheck` (Phase 14 — AUDIT-02; the structured 6-phase /audit workflow)
5. `structured` (Phase 14 — AUDIT-04/05; structured report schema with verbatim ±10 lines code block)
6. `Council` AND `audit-review` (Phase 15 — COUNCIL-01..06; mandatory Council audit-review pass, severity reclassification forbidden)
7. `49` or `template propagation` (Phase 16 — TEMPLATE-01..03; the 7×7 prompt-file fan-out)
8. `4.2.0` (the version bump itself, in the heading)

Suggested structure (executor adapts to stay under ~25 bullets total — keep it scannable):

```markdown
## [4.2.0] - YYYY-MM-DD

### Added

- **Persistent FP allowlist** — `.claude/rules/audit-exceptions.md` auto-loads via `globs: ["**/*"]` and is consulted by `/audit` Phase 0 to drop known false positives before reporting (EXC-01..05).
- **`/audit-skip <file:line> <rule> <reason>`** — appends a structured exception block to `audit-exceptions.md` after validating the file:line exists in the working tree and isn't already allowlisted.
- **`/audit-restore <file:line> <rule>`** — comment-aware removal of an allowlist entry with a `[y/N]` confirmation prompt.
- **6-phase `/audit` workflow** — load context → quick check → deep analysis → 6-step FP recheck → structured report → mandatory Council pass. Every reported finding survives the FP-recheck and ships with verbatim ±10 lines of source code so the Council reasons from the code, not the rule label.
- **Structured audit reports** — `/audit` writes to `.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md` with a fixed schema: Summary table → Findings (ID, severity, rule, location, claim, verbatim code, data flow, "why it's real", suggested fix) → Skipped (allowlist) → Skipped (FP recheck) → Council verdict slot.
- **Mandatory Supreme Council `audit-review` mode** — every `/audit` run terminates in `/council audit-review --report <path>`. Council emits per-finding `REAL | FALSE_POSITIVE | NEEDS_MORE_CONTEXT` verdicts with confidence scores in `[0.0, 1.0]`, plus a "Missed findings" section. Severity reclassification is explicitly forbidden (COUNCIL-02).
- **`brain.py --mode audit-review`** — runs Gemini and ChatGPT in parallel for audit-review, flags per-finding disagreements as `disputed` without auto-resolution.
- **Template propagation across all 49 prompt files** — every `templates/{base,laravel,rails,nextjs,nodejs,python,go}/prompts/{SECURITY_AUDIT,CODE_REVIEW,PERFORMANCE_AUDIT,MYSQL_PERFORMANCE_AUDIT,POSTGRES_PERFORMANCE_AUDIT,DEPLOY_CHECKLIST,DESIGN_REVIEW}.md` carries the audit-exceptions callout, 6-step FP-recheck SELF-CHECK, structured OUTPUT FORMAT, and Council Handoff footer.

### Changed

- **`manifest.json`** — bumped to `4.2.0` and registered `templates/base/rules/audit-exceptions.md` under `files.rules`.
- **`commands/audit.md`** — rewritten around the 6-phase workflow; documents the Council Handoff UX (FALSE_POSITIVE nudge → user runs `/audit-skip`; disputed verdict prompt).
- **`commands/council.md`** — added `## Modes` section with `audit-review` subsection documenting input format (path to structured audit report), expected Council prompt, and verdict-table output schema.

### Fixed

- _None — this is an additive feature release. See [4.1.1] for the prior patch._

### Documentation

- **CI gates** — `make validate` now asserts every audit prompt carries the `Council Handoff` marker plus all six numbered FP-recheck steps; missing markers fail the build (TEMPLATE-03).
- **`make test`** — adds Test 18 (audit pipeline fixture), Test 19 (Council audit-review verdict-slot rewrite + parallel dispatch), Test 20 (template propagation idempotency).
```

Do NOT modify the existing `[4.1.1]`, `[4.1.0]`, or earlier entries. The diff scope is strictly the new section above `[4.1.1]`.</action>
  <verify>
    <automated>grep -F '## [4.2.0] - YYYY-MM-DD' CHANGELOG.md &amp;&amp; for term in 'audit-exceptions.md' '/audit-skip' '/audit-restore' '6-phase' 'structured' 'Council' 'audit-review' '49' '4.2.0'; do awk '/^## \[4.2.0\]/,/^## \[4.1.1\]/' CHANGELOG.md | grep -qF "$term" || { echo "MISSING: $term"; exit 1; }; done &amp;&amp; echo OK</automated>
  </verify>
  <done>`## [4.2.0] - YYYY-MM-DD` heading exists above `## [4.1.1]`; the body of the [4.2.0] section contains all 9 mandatory coverage terms; existing entries are untouched (`git diff CHANGELOG.md` shows only insertions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → end users | manifest.json + CHANGELOG.md are the contract users read to know what's installed and what changed. Drift here ships the wrong story. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering / Information disclosure | manifest.json `version` + CHANGELOG.md `[4.2.0]` heading + scripts/init-local.sh `--version` output | mitigate | `make version-align` gate (already authored; reused per D-06) compares all three on every CI run. After 17-03 replaces the `YYYY-MM-DD` placeholder with the real ship date, the gate exits 0; until then it fails — that is by design and is closed in 17-03. |
</threat_model>

<verification>
- `jq -e '.version == "4.2.0"' manifest.json` exits 0
- `jq -e '.files.rules | map(.path) | contains(["rules/audit-exceptions.md"])' manifest.json` exits 0
- `python3 scripts/validate-manifest.py` exits 0 (manifest schema + path existence check)
- `grep -F '## [4.2.0] - YYYY-MM-DD' CHANGELOG.md` matches
- All 9 mandatory coverage terms (audit-exceptions.md, /audit-skip, /audit-restore, 6-phase, structured, Council, audit-review, 49, 4.2.0) appear inside the `[4.2.0]` section body
- `git diff CHANGELOG.md` scope is strictly inserts above `## [4.1.1]`; no prior entries modified
- `git diff manifest.json` scope is strictly `version` + `updated` + the new `rules/audit-exceptions.md` array entry
- Note: `make version-align` will FAIL until 17-03 replaces the `YYYY-MM-DD` placeholder; that is intentional per D-08
</verification>

<success_criteria>
- manifest.json reads `version: "4.2.0"`, `updated: "YYYY-MM-DD"`, and lists `rules/audit-exceptions.md` under `files.rules`
- CHANGELOG.md has a complete `[4.2.0]` placeholder entry above `[4.1.1]` covering every Phase 13–16 user-visible feature
- `python3 scripts/validate-manifest.py` exits 0
- 9-term grep coverage check exits 0
- No collateral damage to other manifest fields or earlier CHANGELOG entries
</success_criteria>

<output>
After completion, create `.planning/phases/17-distribution-manifest-installers-changelog/17-01-SUMMARY.md` per the standard summary template.
</output>
