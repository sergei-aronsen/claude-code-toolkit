---
phase: 15-council-audit-review-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/audit.md
autonomous: true
requirements:
  - COUNCIL-01
  - COUNCIL-05
must_haves:
  truths:
    - "commands/audit.md '## Council Handoff (Phase 15)' section contains the FALSE_POSITIVE-to-/audit-skip nudge text per D-12 (COUNCIL-05) — explicitly states /audit NEVER auto-writes audit-exceptions.md"
    - "commands/audit.md '## Council Handoff (Phase 15)' section contains the disputed-resolution prompt per D-13 — '(R)eal | (F)alse positive | (N)eeds more context' with no default"
    - "commands/audit.md Phase 5 description states the audit run is incomplete until /council audit-review returns (COUNCIL-01)"
    - "commands/audit.md retains the existing 6-phase workflow structure unchanged (no Phase 0/1/2/3/4 edits)"
    - "commands/audit.md retains all 5 AUDIT-XX traceability comments (Test 18 regression guards still pass)"
    - "commands/audit.md still passes the existing test-audit-pipeline.sh assertions"
    - "commands/audit.md passes markdownlint after the edit"
    - "Net line addition is ≤ 25 lines (this is a focused UX text expansion, not a rewrite)"
  artifacts:
    - path: "commands/audit.md"
      provides: "User-facing /audit command with explicit Council Handoff UX (FP nudge + disputed prompt)"
      contains: "/audit-skip"
      min_lines: 200
  key_links:
    - from: "commands/audit.md"
      to: "commands/audit-skip.md"
      via: "FALSE_POSITIVE-to-/audit-skip nudge text per D-12"
      pattern: "/audit-skip"
    - from: "commands/audit.md"
      to: "scripts/council/brain.py (Plan 15-04 consumer)"
      via: "Council Handoff section documents the runtime behavior brain.py implements"
      pattern: "/council audit-review"
---

<objective>
Extend `commands/audit.md`'s `## Council Handoff (Phase 15)` section (currently a 1-paragraph stub at lines 192-194) with the FALSE_POSITIVE-to-`/audit-skip` nudge text (D-12 / COUNCIL-05) and the disputed-resolution prompt (D-13). These two UX behaviors are operational outputs of Plan 15-04's `brain.py` audit-review mode; this plan documents them in the user-facing command spec so the contract is explicit.

Purpose: When Council marks a finding `FALSE_POSITIVE`, `/audit` MUST display a structured nudge prompting the user to run `/audit-skip` — never auto-writing `.claude/rules/audit-exceptions.md` (COUNCIL-05). When Council returns a `disputed` row (Gemini/ChatGPT disagreement per D-09), `/audit` MUST present a single-character prompt with no default (D-13). This plan locks both UX contracts into the command spec.

Output: One file modified — `commands/audit.md`. The edit is scoped to the existing `## Council Handoff (Phase 15)` H2 section (~10-25 net lines added). No other sections touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-council-audit-review-integration/15-CONTEXT.md
@.planning/phases/15-council-audit-review-integration/15-RESEARCH.md
@.planning/phases/15-council-audit-review-integration/15-PATTERNS.md
@commands/audit.md
@commands/audit-skip.md
@commands/audit-restore.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing structure of commands/audit.md (read-confirmed):
     - Lines 1-41:    Purpose / Usage / Types / Aliases / Scope / Examples
     - Lines 42-83:   Quick Checks (Security / Performance / Code Quality)
     - Lines 84-170:  6-Phase Workflow (Phase 0 through Phase 5)
     - Lines 171-176: Output Format
     - Lines 177-189: Framework Detection
     - Lines 190-194: ## Council Handoff (Phase 15)  <-- TARGET SECTION
     - Lines 195-206: Related Commands
-->

<!-- Current Council Handoff stub (line 192-194 in commands/audit.md):
     ## Council Handoff (Phase 15)

     Phase 5 of the workflow invokes `/council audit-review --report <path>`. Council is mandatory:
     the audit run is reported as incomplete until the Council pass returns. Council confirms or
     rejects each finding using the embedded verbatim code block in the report — see
     `components/audit-output-format.md` for the report schema Council reads. Council MUST NOT
     reclassify severity (COUNCIL-02); it confirms REAL vs FALSE_POSITIVE only. When Council marks
     a finding `FALSE_POSITIVE`, this command prints the verdict and prompts the user to run
     `/audit-skip` to persist the exception (`/audit` never auto-writes the allowlist).
-->

<!-- Existing test guards in scripts/tests/test-audit-pipeline.sh that MUST still pass:
     - Phase 0..5 headings present (`### Phase 0` ... `### Phase 5`)
     - All 5 AUDIT-XX traceability comments present (AUDIT-01..AUDIT-05)
     - Allowlist file path present (`.claude/rules/audit-exceptions.md`)
     - Comment-stripping pattern present (`sed '/^<!--/,/^-->/d'`)
     - All 7 canonical slugs present
     - Backward-compat aliases present (`code`, `deploy`)
     - Council handoff present (`/council audit-review`)
     - Related Commands includes `/audit-skip` and `/audit-restore`
     - No orphan `text` close fence in 6-Phase Workflow section
-->

<!-- D-12 nudge text (per 15-CONTEXT.md and 15-PATTERNS.md):
     Council confirmed F-NNN as FALSE_POSITIVE.
     To persist: /audit-skip <path>:<line> <rule> "<reason>"

     /audit NEVER writes to audit-exceptions.md directly.
     /audit-skip is the only writer (Phase 13 EXC-01 contract).
-->

<!-- D-13 disputed prompt (per 15-CONTEXT.md and 15-PATTERNS.md):
     F-NNN is disputed:
       Gemini: <g_verdict> (confidence <g_conf>) — <g_justification>
       ChatGPT: <c_verdict> (confidence <c_conf>) — <c_justification>

     Choose: (R)eal — keep as a finding
             (F)alse positive — run /audit-skip
             (N)eeds more context — leave open in next audit

     No default. The user must choose before the audit run is considered complete.
-->

<!-- markdownlint rules to honor (.markdownlint.json):
     MD040 (every fenced code block declares a language — use `text` for plain text)
     MD031 (blank line before/after fenced code blocks)
     MD032 (blank line before/after lists)
     MD026 (no trailing punctuation in headings)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend commands/audit.md ## Council Handoff section with FP nudge and disputed prompt UX</name>
  <files>commands/audit.md</files>
  <read_first>
    - commands/audit.md (the current state — confirm the `## Council Handoff (Phase 15)` section at lines 192-194 and the `## Related Commands` section that follows; do not edit anything outside the `## Council Handoff` section except confirm Related Commands stays intact)
    - commands/audit-skip.md (the command this section nudges users to invoke; understand the argument shape `/audit-skip <file:line> <rule> <reason>` so the nudge syntax matches)
    - commands/audit-restore.md (sibling command — reference style, especially the `[y/N]` confirmation pattern; D-13 mirrors this single-character convention)
    - .planning/phases/15-council-audit-review-integration/15-CONTEXT.md D-11..D-13 (the verbatim UX text contracts)
    - .planning/phases/15-council-audit-review-integration/15-PATTERNS.md `commands/audit.md` analog block (the exact insertion patterns and example code blocks)
    - scripts/tests/test-audit-pipeline.sh (the regression test guards that must continue to pass)
    - .markdownlint.json (lint rules)
  </read_first>
  <action>
Modify ONLY the `## Council Handoff (Phase 15)` section in `commands/audit.md`. Do NOT change Phase 0/1/2/3/4/5 sections, do NOT change the Related Commands section, do NOT change the Framework Detection table, do NOT renumber AUDIT-XX traceability comments. The edit is purely additive UX text.

**Find this section** (currently 1 paragraph, lines 192-194):

```markdown
## Council Handoff (Phase 15)

Phase 5 of the workflow invokes `/council audit-review --report <path>`. Council is mandatory: the audit run is reported as incomplete until the Council pass returns. Council confirms or rejects each finding using the embedded verbatim code block in the report — see `components/audit-output-format.md` for the report schema Council reads. Council MUST NOT reclassify severity (COUNCIL-02); it confirms REAL vs FALSE_POSITIVE only. When Council marks a finding `FALSE_POSITIVE`, this command prints the verdict and prompts the user to run `/audit-skip` to persist the exception (`/audit` never auto-writes the allowlist).
```

**Replace with this expanded section** (preserves the existing intro paragraph; adds two H3 subsections — FP nudge and disputed resolution; adds a closing reference to brain.py mode):

```markdown
## Council Handoff (Phase 15)

Phase 5 of the workflow invokes `/council audit-review --report <path>`. Council is mandatory: the audit run is reported as incomplete until the Council pass returns. Council confirms or rejects each finding using the embedded verbatim code block in the report — see `components/audit-output-format.md` for the report schema Council reads. Council MUST NOT reclassify severity (COUNCIL-02); it confirms REAL vs FALSE_POSITIVE only.

### FALSE_POSITIVE Nudge (COUNCIL-05)

For each verdict where `verdict == FALSE_POSITIVE` (and the auditor agrees), `/audit` prints a structured nudge:

```text
Council confirmed F-NNN as FALSE_POSITIVE.
To persist: /audit-skip <path>:<line> <rule> "<reason>"
```

`/audit` NEVER writes to `.claude/rules/audit-exceptions.md` directly. The `/audit-skip` command (Phase 13 EXC-01 contract) is the only writer. The user MUST run `/audit-skip` themselves — `/audit` only nudges. This preserves the EXC-04 validation guard chain (file existence, line count, duplicate check) that `/audit-skip` enforces before appending an entry.

### Disputed Resolution (D-13)

For each `disputed` row (Gemini and ChatGPT disagree on REAL vs FALSE_POSITIVE per COUNCIL-06), `/audit` prints the disagreement and prompts the user to choose:

```text
F-NNN is disputed:
  Gemini: <g_verdict> (confidence <g_conf>) — <g_justification>
  ChatGPT: <c_verdict> (confidence <c_conf>) — <c_justification>

Choose: (R)eal — keep as a finding
        (F)alse positive — run /audit-skip
        (N)eeds more context — leave open in next audit
```

No default. The user MUST choose before the audit run is considered complete. The single-character prompt mirrors the `[y/N]` style of `/audit-restore` (Phase 13 EXC-02 contract) — explicit, fail-loud, no implicit acceptance.

### Orchestrator Reference

The Council orchestrator that implements both behaviors above is `scripts/council/brain.py --mode audit-review --report <path>`. See `commands/council.md` `## Modes` section (added in Phase 15) for invocation syntax and the prompt template at `scripts/council/prompts/audit-review.md` for the contract Council enforces against backends.
```

**Editing constraints:**

- The edit MUST preserve the existing intro paragraph verbatim (the first sentence about Phase 5 invoking `/council audit-review`).
- The em-dash in `Council confirmed F-NNN as FALSE_POSITIVE.\nTo persist: /audit-skip <path>:<line> <rule> "<reason>"` is U+2014 (where shown).
- Em-dashes in `Gemini: <g_verdict> (confidence <g_conf>) — <g_justification>` are U+2014.
- The em-dashes in the choose-line `(R)eal — keep as a finding`, `(F)alse positive — run /audit-skip`, `(N)eeds more context — leave open in next audit` are U+2014.
- All fenced code blocks MUST have the `text` language tag (MD040).
- Blank lines around code blocks (MD031) and around lists (MD032) — verify after editing.
- The `## Related Commands` H2 must remain immediately after this section, unchanged.

**Existing references to NOT break:**

- `/audit-skip` and `/audit-restore` mentions in the existing `## Related Commands` section must remain
- All AUDIT-XX traceability comments (`Implements **AUDIT-01**`, etc.) inside Phases 0..5 must remain unchanged
- The 6-phase headings `### Phase 0` through `### Phase 5` must remain unchanged

After editing:

1. Run `npx markdownlint-cli commands/audit.md` — must exit 0
2. Run `bash scripts/tests/test-audit-pipeline.sh` — Test Group 4 (commands/audit.md regression guards) must still pass

The edit produces ~25 net new lines (3 new H3 subsections each ~5-8 lines + 2 fenced code blocks each ~5-8 lines).
  </action>
  <verify>
    <automated>npx markdownlint-cli commands/audit.md && bash scripts/tests/test-audit-pipeline.sh && grep -qF '/audit-skip <path>:<line> <rule>' commands/audit.md && grep -qF 'Council confirmed F-NNN as FALSE_POSITIVE' commands/audit.md && grep -qF 'is disputed:' commands/audit.md && grep -qF '(R)eal' commands/audit.md && grep -qF '(F)alse positive' commands/audit.md && grep -qF '(N)eeds more context' commands/audit.md && grep -qF 'NEVER writes' commands/audit.md && grep -qF 'No default' commands/audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx markdownlint-cli commands/audit.md` exits 0 (lint clean post-edit)
    - `bash scripts/tests/test-audit-pipeline.sh` exits 0 (no regression of Test 18's commands/audit.md guards)
    - `grep -qF '## Council Handoff (Phase 15)' commands/audit.md` matches (heading preserved)
    - `grep -qF '/audit-skip <path>:<line> <rule>' commands/audit.md` matches (D-12 nudge syntax present)
    - `grep -qF 'Council confirmed F-NNN as FALSE_POSITIVE' commands/audit.md` matches (D-12 nudge phrase verbatim)
    - `grep -qF 'is disputed:' commands/audit.md` matches (D-13 disputed prompt body present)
    - `grep -qF '(R)eal' commands/audit.md && grep -qF '(F)alse positive' commands/audit.md && grep -qF '(N)eeds more context' commands/audit.md` matches (all 3 disputed-prompt options present)
    - `grep -qF 'No default' commands/audit.md` matches (D-13 explicit no-default rule)
    - `grep -qiF 'NEVER writes' commands/audit.md || grep -qiF 'never auto-writes' commands/audit.md` matches (COUNCIL-05 enforcement explicit)
    - All 5 AUDIT-XX traceability comments preserved: `for r in AUDIT-01 AUDIT-02 AUDIT-03 AUDIT-04 AUDIT-05; do grep -qF "$r" commands/audit.md || exit 1; done`
    - All 6 phase headings preserved: `for n in 0 1 2 3 4 5; do grep -qE "^### Phase $n" commands/audit.md || exit 1; done`
    - `## Related Commands` heading still present and after `## Council Handoff`: `awk '/^## Council Handoff/,/^## Related Commands/' commands/audit.md | tail -1 | grep -qF '## Related Commands'` matches
    - `grep -qF '/audit-skip' commands/audit.md && grep -qF '/audit-restore' commands/audit.md` matches (Related Commands still references both)
    - Net line addition reasonable: `wc -l commands/audit.md | awk '{print $1}'` returns N where 215 ≤ N ≤ 240 (was 206; +9 to +34 lines is the acceptable band; target ~+25)
    - Fenced code blocks in the new section have `text` language: `awk '/^## Council Handoff/,/^## Related Commands/' commands/audit.md | grep -cE '^\`\`\`text$'` returns ≥ 2 (FP nudge code block + disputed prompt code block)
    - Em-dash byte integrity preserved: `python3 -c "data = open('commands/audit.md').read(); assert chr(0x2014) in data, 'em-dash U+2014 missing'"` exits 0
    - References to brain.py orchestrator and prompt file present: `grep -qF 'scripts/council/brain.py --mode audit-review' commands/audit.md && grep -qF 'scripts/council/prompts/audit-review.md' commands/audit.md` matches
  </acceptance_criteria>
  <done>
    `commands/audit.md` `## Council Handoff (Phase 15)` section now contains three H3 subsections: FALSE_POSITIVE Nudge (D-12 / COUNCIL-05) with the byte-exact nudge text and the explicit "/audit NEVER writes" rule; Disputed Resolution (D-13) with the byte-exact disputed prompt and the no-default rule; Orchestrator Reference pointing to `scripts/council/brain.py --mode audit-review` and the new prompt file at `scripts/council/prompts/audit-review.md`. The existing 6-phase workflow, AUDIT-XX traceability comments, Framework Detection, and Related Commands sections are unchanged. File passes markdownlint and test-audit-pipeline.sh.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commands/audit.md -> markdownlint | Read-only by lint. No code execution. |
| commands/audit.md -> /audit invocation | Documentation file consumed by Claude at runtime when the user types `/audit ...`. No untrusted input flows through the documentation itself; the documentation describes behavior Claude implements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | Documentation drift weakens COUNCIL-05 enforcement | mitigate | The acceptance criteria assert byte-exact presence of "Council confirmed F-NNN as FALSE_POSITIVE", "/audit-skip <path>:<line> <rule>", "NEVER writes" (or "never auto-writes"), "No default", and the three disputed options "(R)eal", "(F)alse positive", "(N)eeds more context". Plan 15-06's regression test will additionally assert these strings stay present in commands/audit.md, so future PRs that drift the UX text fail before merge. |
| T-15-09 | Spoofing | Phantom auto-skip behavior introduced by drift | mitigate | The "## Related Commands" section's preservation is asserted (must still reference `/audit-skip`). The new H3 "FALSE_POSITIVE Nudge (COUNCIL-05)" explicitly states `/audit NEVER writes to .claude/rules/audit-exceptions.md directly` — the mandatory phrase is grep-asserted. If a future contributor edits the section to introduce auto-skip behavior, the regression test fires. |
</threat_model>

<verification>
- `npx markdownlint-cli commands/audit.md` exits 0
- `bash scripts/tests/test-audit-pipeline.sh` exits 0 (Test 18 still green)
- All acceptance_criteria pass
- The `## Council Handoff (Phase 15)` section now contains the FALSE_POSITIVE nudge + disputed resolution UX text byte-exactly
- File line count grows by ~25 lines (within the 9-34 line acceptable band)
</verification>

<success_criteria>
- COUNCIL-01 reinforced: the existing "audit run is incomplete until the Council pass returns" language is preserved
- COUNCIL-05 enforced: the new H3 "FALSE_POSITIVE Nudge" subsection contains the byte-exact nudge text and the explicit "/audit NEVER writes" rule
- D-12 satisfied: the nudge syntax `/audit-skip <path>:<line> <rule> "<reason>"` is documented verbatim
- D-13 satisfied: the disputed prompt with `(R)eal | (F)alse positive | (N)eeds more context` and "No default" rule is documented verbatim
- No regression of Phase 14 work: all 5 AUDIT-XX traceability comments, all 6 phase headings, Framework Detection, and Related Commands sections are unchanged
- File passes markdownlint and the existing test-audit-pipeline.sh (Test 18) regression guards
- Forward references to Plan 15-04's `brain.py --mode audit-review` and Plan 15-01's `scripts/council/prompts/audit-review.md` are present in the new "Orchestrator Reference" subsection — this gives users a navigation breadcrumb without requiring those files to exist yet (the references are documentation, not runtime imports)
</success_criteria>

<output>
After completion, create `.planning/phases/15-council-audit-review-integration/15-03-SUMMARY.md`
</output>
