---
phase: 15-council-audit-review-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/council/prompts/audit-review.md
autonomous: true
requirements:
  - COUNCIL-02
  - COUNCIL-03
  - COUNCIL-04
must_haves:
  truths:
    - "scripts/council/prompts/audit-review.md exists and is 100-180 lines (D-06)"
    - "Prompt contains the verbatim phrase 'DO NOT reclassify severity' in a Constraints section (COUNCIL-02 / D-05)"
    - "Prompt requests a per-finding verdict table with byte-exact column header '| ID | verdict | confidence | justification |' (COUNCIL-03 / D-05)"
    - "Prompt enumerates verdict values REAL, FALSE_POSITIVE, NEEDS_MORE_CONTEXT (COUNCIL-03)"
    - "Prompt requests a '## Missed findings' section with location, rule, code excerpt, claim, suggested severity columns (COUNCIL-04 / D-05)"
    - "Prompt brackets backend output with literal markers <verdict-table> ... </verdict-table> and <missed-findings> ... </missed-findings> (D-10)"
    - "Prompt quotes the byte-exact slot string '_pending — run /council audit-review_' (em-dash U+2014) so backends know what they read (D-18)"
    - "Prompt references components/severity-levels.md by path (does NOT redefine severity rubric) (D-06)"
    - "Prompt documents the verbatim code block layout '<!-- File: <path> Lines: <start>-<end> -->' from components/audit-output-format.md (D-07)"
    - "Prompt names the bullet labels '**Severity:**', '**Rule:**', '**Location:**', '**Claim:**', '**Code:**' so backends navigate the report correctly (D-18)"
    - "Prompt is written in English (D-06)"
    - "Prompt passes markdownlint with the project's .markdownlint.json rules"
  artifacts:
    - path: "scripts/council/prompts/audit-review.md"
      provides: "The Council audit-review prompt template — Source of Truth for COUNCIL-02/03/04 enforcement, consumed by brain.py audit-review mode"
      contains: "DO NOT reclassify severity"
      min_lines: 100
  key_links:
    - from: "scripts/council/prompts/audit-review.md"
      to: "components/severity-levels.md"
      via: "by-path reference (no redefinition)"
      pattern: "components/severity-levels\\.md"
    - from: "scripts/council/prompts/audit-review.md"
      to: "components/audit-output-format.md"
      via: "byte-exact contract quotes (slot string, bullet labels, code block header)"
      pattern: "_pending — run /council audit-review_"
---

<objective>
Create `scripts/council/prompts/audit-review.md` — the prompt template that the Phase 15 Council `audit-review` mode interpolates into Gemini and ChatGPT requests. This is the Source of Truth that encodes COUNCIL-02 (no severity reclassification), COUNCIL-03 (per-finding verdict table), and COUNCIL-04 (missed-findings section).

Purpose: Lock the Council's review contract. The prompt is the only place where the constraint "DO NOT reclassify severity" lives — `brain.py` does not enforce this at parse time; the prompt enforces it at the model. Backends MUST emit output bracketed by `<verdict-table>` and `<missed-findings>` markers so `brain.py` can extract verdicts deterministically without fuzzy parsing.

Output: One new file (~100-180 lines) at `scripts/council/prompts/audit-review.md`, plus a new directory `scripts/council/prompts/` (created by the file write).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-council-audit-review-integration/15-CONTEXT.md
@.planning/phases/15-council-audit-review-integration/15-RESEARCH.md
@.planning/phases/15-council-audit-review-integration/15-PATTERNS.md
@components/audit-output-format.md
@components/audit-fp-recheck.md
@components/severity-levels.md
@CLAUDE.md
</context>

<interfaces>
<!-- Closest analog: components/audit-fp-recheck.md (49 lines) — prompt-style document with byte-exact contract sections. -->
<!-- Structure pattern (from 15-PATTERNS.md):
       # <Title>
       <one-line purpose>
       Single source of truth for <what>.
       ---
       ## Constraints (numbered list with fail-fast)
       ## Report Schema (what you are reading)
       ## Output Format (verdict-table + missed-findings)
       ## Severity Reference (by path, never redefined)
       ## Anti-Patterns
-->

<!-- Byte-exact contract strings (from 15-PATTERNS.md and audit-output-format.md):
     - `_pending — run /council audit-review_`           (em-dash U+2014, load-bearing)
     - `| ID | verdict | confidence | justification |`   (per-finding verdict table header — COUNCIL-03)
     - `<verdict-table>` / `</verdict-table>`             (output wrapper — D-10)
     - `<missed-findings>` / `</missed-findings>`         (missed-findings wrapper — D-10)
     - `DO NOT reclassify severity`                       (constraint phrase — COUNCIL-02)
     - `**Severity:**`, `**Rule:**`, `**Location:**`, `**Claim:**`, `**Code:**`  (finding bullets — D-18)
     - `<!-- File: <path> Lines: <start>-<end> -->`       (verbatim code block header — D-07)
-->

<!-- Verdict values (COUNCIL-03):
     verdict ∈ {REAL, FALSE_POSITIVE, NEEDS_MORE_CONTEXT}
     confidence ∈ [0.0, 1.0] (one decimal acceptable)
     justification ≤ 160 chars, references concrete tokens visible in the embedded code block
-->

<!-- Missed findings columns (COUNCIL-04):
     location | rule | code excerpt (≤ 5 lines) | claim | suggested severity (CRITICAL/HIGH/MEDIUM/LOW)
-->

<!-- Severity reference policy (D-06):
     Refer to components/severity-levels.md by path. NEVER redefine CRITICAL/HIGH/MEDIUM/LOW.
     The Council confirms REAL or FALSE_POSITIVE only — it does NOT change severity (COUNCIL-02).
-->

<!-- markdownlint rules (.markdownlint.json):
     MD040: every fenced code block declares a language (use `text` if unknown)
     MD031: blank line before and after every fenced code block
     MD032: blank line before and after every list
     MD026: no trailing punctuation in headings
     MD024 siblings_only: duplicate headings allowed across different parents
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/council/prompts/audit-review.md (the audit-review prompt SOT)</name>
  <files>scripts/council/prompts/audit-review.md</files>
  <read_first>
    - components/audit-fp-recheck.md (49 lines — closest analog: prompt-style document with byte-exact constraint sections)
    - components/audit-output-format.md (245 lines — the schema this prompt teaches backends to read; byte-exact slot string + bullet labels + verbatim code block header live here)
    - components/severity-levels.md (severity rubric the prompt references by path — confirm it exists)
    - .markdownlint.json (MD040/MD031/MD032/MD026 rules)
    - .planning/phases/15-council-audit-review-integration/15-PATTERNS.md (`scripts/council/prompts/audit-review.md` analog block — full structure)
    - .planning/phases/15-council-audit-review-integration/15-RESEARCH.md (Section "Artifact 2 — scripts/council/prompts/audit-review.md")
  </read_first>
  <action>
Create the new directory `scripts/council/prompts/` (the file Write will create it implicitly — confirm parent doesn't exist before writing). Write `scripts/council/prompts/audit-review.md` with the structure below. Aim for 100-180 lines (D-06). All byte-exact contract strings MUST appear verbatim — they are non-negotiable per COUNCIL-02/03/04 and D-05/07/10/18.

**Required structure (use this skeleton verbatim, fill prose with English):**

```markdown
# Council Audit-Review Prompt

Single source of truth for the Council prompt invoked by `scripts/council/brain.py --mode audit-review`. Used in parallel by Gemini (The Skeptic) and ChatGPT (The Pragmatist) to confirm or reject each finding in a structured audit report from `commands/audit.md` Phase 4.

---

## Your Role

You are a senior reviewer evaluating a structured audit report. The auditor (Claude Code) has already produced findings using a 6-step false-positive recheck (`components/audit-fp-recheck.md`). Your job is to read each finding's embedded verbatim code block and decide whether the issue is REAL, a FALSE_POSITIVE, or NEEDS_MORE_CONTEXT — then list any real issues the auditor missed.

You receive the entire audit report as input. Read every `### Finding F-NNN` block, navigate the bullet labels (`**Severity:**`, `**Rule:**`, `**Location:**`, `**Claim:**`, `**Code:**`, `**Data flow:**`, `**Why it is real:**`, `**Suggested fix:**`), and reason from the verbatim code shown in the `<!-- File: <path> Lines: <start>-<end> -->` block — never paraphrase the file from imagined memory.

---

## Constraints

1. **DO NOT reclassify severity.** The auditor's `**Severity:**` value (CRITICAL / HIGH / MEDIUM / LOW) is fixed. You confirm REAL vs FALSE_POSITIVE only. If you disagree with the severity, add a comment under `## Severity disagreements (advisory)` AT THE END of your output — never modify the verdict table or the auditor's table. Severity stays with the auditor.
2. **Cite tokens from the embedded code block.** Every `justification` field MUST reference concrete identifiers visible in the `**Code:**` block of the finding. Never paraphrase. Never reference code outside the `<!-- File: ... Lines: ... -->` window.
3. **Use exactly these verdict values:** `REAL`, `FALSE_POSITIVE`, `NEEDS_MORE_CONTEXT`. No other strings, no synonyms, no localized variants.
4. **Confidence in `[0.0, 1.0]`** as a floating-point number; one decimal is acceptable (`0.9`, `0.7`). Never use percentages, never use words.
5. **Justification ≤ 160 characters,** grounded in concrete code tokens. No prose hedging, no rule-label restating.
6. **NEEDS_MORE_CONTEXT only when the embedded code block is insufficient** (e.g. data flow extends beyond ±10 lines and you cannot trace it). It is NOT a default for "I'm unsure" — pick REAL or FALSE_POSITIVE when the code is sufficient.

---

## Report Schema (what you are reading)

The auditor follows the schema in `components/audit-output-format.md`. Key contracts you navigate:

- **YAML frontmatter** at the top with keys: `audit_type`, `timestamp`, `commit_sha`, `total_findings`, `skipped_allowlist`, `skipped_fp_recheck`, `council_pass`. The `council_pass` value is `pending` on input; the Council orchestrator (not you) mutates it after collating verdicts.
- **Fixed H2 section order:** `## Summary` → `## Findings` → `## Skipped (allowlist)` → `## Skipped (FP recheck)` → `## Council verdict`. The `## Council verdict` slot opens with the literal placeholder `_pending — run /council audit-review_` (em-dash U+2014). Do NOT modify this slot — the orchestrator overwrites it with your output.
- **Finding entries** are H3 blocks `### Finding F-NNN` with these bullet labels in order: `**ID:**`, `**Severity:**`, `**Rule:**`, `**Location:**`, `**Claim:**`, then sections `**Code:**`, `**Data flow:**`, `**Why it is real:**`, `**Suggested fix:**`.
- **Verbatim code block** sits under `**Code:**` with the layout:

  ```text
  <!-- File: <path> Lines: <start>-<end> -->
  [optional clamp note]
  [fenced code block in source language]
  ```

  This is the artifact you reason from. The line range is ±10 lines around the flagged line, clamped to file bounds.

---

## Output Format

Emit two clearly bracketed sections, in this exact order, with no other prose between or after them. The orchestrator extracts each block by literal `<...>` markers — fuzzy formatting will fail the parse.

### Verdict Table

Wrap a markdown table in `<verdict-table>` ... `</verdict-table>` markers. The table header is byte-exact:

```text
<verdict-table>
| ID | verdict | confidence | justification |
|----|---------|------------|---------------|
| F-001 | REAL | 0.9 | <≤160 chars citing tokens from the F-001 code block> |
| F-002 | FALSE_POSITIVE | 0.85 | <≤160 chars citing tokens from the F-002 code block> |
</verdict-table>
```

One row per finding, in the order they appear in `## Findings`. `verdict` ∈ {`REAL`, `FALSE_POSITIVE`, `NEEDS_MORE_CONTEXT`}. `confidence` ∈ `[0.0, 1.0]`. `justification` ≤ 160 chars, references concrete tokens visible in that finding's `**Code:**` block.

### Missed Findings

Wrap a markdown table in `<missed-findings>` ... `</missed-findings>` markers. List real issues you saw in the embedded code blocks that the auditor did NOT report. The auditor accepts or rejects each missed finding in a follow-up — you do NOT auto-merge.

```text
<missed-findings>
| location | rule | code excerpt | claim | suggested severity |
|----------|------|--------------|-------|--------------------|
| <path>:<line> | <RULE-ID> | <≤5 lines from the embedded code block> | <one-sentence claim ≤160 chars> | <CRITICAL / HIGH / MEDIUM / LOW> |
</missed-findings>
```

If you find no missed issues, emit:

```text
<missed-findings>
(none)
</missed-findings>
```

`code excerpt` MUST come from a verbatim code block that is already present in the report. Do NOT invent locations the auditor did not show you.

### Severity Disagreements (Advisory)

If you disagree with the auditor's severity on any finding, append a `## Severity disagreements (advisory)` H2 section AFTER `</missed-findings>` with a bullet list:

```text
- F-NNN — auditor: HIGH; suggested: CRITICAL — <one-sentence rationale citing code tokens>
```

This section is advisory only. The auditor never auto-applies your suggestion. Severity stays with the auditor (Constraint 1).

---

## Severity Reference

See `components/severity-levels.md` for the CRITICAL / HIGH / MEDIUM / LOW rubric. The Council confirms `REAL` or `FALSE_POSITIVE` only — it does NOT change the auditor's severity label.

---

## Anti-Patterns

These behaviors break the audit-review contract and MUST NOT appear in your output:

- Modifying the auditor's severity in the verdict table — severity stays in the auditor's `**Severity:**` bullet, untouched.
- Justifying a verdict with a rule label or generic phrase ("looks safe", "trusted code", "out of scope") — every justification cites concrete tokens from the embedded code block.
- Inventing missed findings the embedded code does not support — every missed-finding row excerpts code already present in the report.
- Emitting prose between or after the bracketed sections — the orchestrator extracts blocks by literal markers; stray prose breaks the parse.
- Returning a `NEEDS_MORE_CONTEXT` verdict when the embedded code block is sufficient — only escalate when the data flow extends beyond the ±10 line window.

---

## Report to Review

The orchestrator interpolates the full audit report below this line. Read every finding, then emit `<verdict-table>` and `<missed-findings>` blocks per the contract above.

{REPORT_CONTENT}
```

**Notes on writing the file:**

- The `{REPORT_CONTENT}` placeholder is intentional — `brain.py`'s `run_audit_review()` will replace this token at runtime with the report file contents (not a Python f-string substitution; a literal `str.replace`). Plan 15-04 implements this substitution.
- All instances of em-dash (`—`) in the file MUST be U+2014 (literal `—`). The slot string `_pending — run /council audit-review_` is byte-exact and load-bearing.
- The skeleton above is ~140 lines including the leading H1 and trailing `{REPORT_CONTENT}` line; pad with prose only if it serves clarity. Do not exceed 180 lines (D-06).
- Pass the file through `npx markdownlint-cli` after writing.
  </action>
  <verify>
    <automated>test -f scripts/council/prompts/audit-review.md && npx markdownlint-cli scripts/council/prompts/audit-review.md && wc -l scripts/council/prompts/audit-review.md | awk '{ if ($1 < 100 || $1 > 180) exit 1 }' && grep -qF 'DO NOT reclassify severity' scripts/council/prompts/audit-review.md && grep -qF '| ID | verdict | confidence | justification |' scripts/council/prompts/audit-review.md && grep -qF '<verdict-table>' scripts/council/prompts/audit-review.md && grep -qF '</verdict-table>' scripts/council/prompts/audit-review.md && grep -qF '<missed-findings>' scripts/council/prompts/audit-review.md && grep -qF '</missed-findings>' scripts/council/prompts/audit-review.md && grep -qF '_pending — run /council audit-review_' scripts/council/prompts/audit-review.md && grep -qF 'components/severity-levels.md' scripts/council/prompts/audit-review.md && grep -qF '<!-- File: <path> Lines: <start>-<end> -->' scripts/council/prompts/audit-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/council/prompts/audit-review.md` exits 0
    - `wc -l scripts/council/prompts/audit-review.md` returns N where 100 ≤ N ≤ 180 (D-06)
    - `grep -qF 'DO NOT reclassify severity' scripts/council/prompts/audit-review.md` matches (COUNCIL-02 / D-05)
    - `grep -qF '| ID | verdict | confidence | justification |' scripts/council/prompts/audit-review.md` matches (COUNCIL-03 byte-exact column header / D-05)
    - `grep -qF 'REAL' scripts/council/prompts/audit-review.md && grep -qF 'FALSE_POSITIVE' scripts/council/prompts/audit-review.md && grep -qF 'NEEDS_MORE_CONTEXT' scripts/council/prompts/audit-review.md` matches (all three verdict values present)
    - `grep -qF '<verdict-table>' scripts/council/prompts/audit-review.md && grep -qF '</verdict-table>' scripts/council/prompts/audit-review.md` matches (D-10 wrapper markers)
    - `grep -qF '<missed-findings>' scripts/council/prompts/audit-review.md && grep -qF '</missed-findings>' scripts/council/prompts/audit-review.md` matches (D-10 wrapper markers)
    - `grep -qF '## Missed Findings' scripts/council/prompts/audit-review.md || grep -qF '### Missed Findings' scripts/council/prompts/audit-review.md` matches (COUNCIL-04 section heading present)
    - `grep -qF '_pending — run /council audit-review_' scripts/council/prompts/audit-review.md` matches (D-18 byte-exact slot string with U+2014 em-dash)
    - `python3 -c "data = open('scripts/council/prompts/audit-review.md').read(); assert chr(0x2014) in data, 'em-dash U+2014 missing'"` exits 0 (em-dash byte present)
    - `grep -qF 'components/severity-levels.md' scripts/council/prompts/audit-review.md` matches (D-06 — references rubric by path, not redefined)
    - `grep -qF '<!-- File: <path> Lines: <start>-<end> -->' scripts/council/prompts/audit-review.md` matches (D-07 verbatim code block header documented)
    - `grep -qF '**Severity:**' scripts/council/prompts/audit-review.md && grep -qF '**Rule:**' scripts/council/prompts/audit-review.md && grep -qF '**Location:**' scripts/council/prompts/audit-review.md && grep -qF '**Claim:**' scripts/council/prompts/audit-review.md && grep -qF '**Code:**' scripts/council/prompts/audit-review.md` matches (D-18 bullet labels documented)
    - `grep -qF '{REPORT_CONTENT}' scripts/council/prompts/audit-review.md` matches (interpolation token present for brain.py)
    - `npx markdownlint-cli scripts/council/prompts/audit-review.md` exits 0
    - `! grep -qE 'CRITICAL.*=|HIGH.*=|MEDIUM.*=|LOW.*=' scripts/council/prompts/audit-review.md` returns success — no inline severity definitions; the rubric is referenced by path only (D-06 verification)
  </acceptance_criteria>
  <done>
    `scripts/council/prompts/audit-review.md` exists at 100-180 lines, contains all byte-exact contract strings (DO NOT reclassify severity, the verdict table column header, the verdict-table/missed-findings markers, the slot string with U+2014 em-dash, the code block header, the bullet labels), references `components/severity-levels.md` by path without redefining the rubric, contains the `{REPORT_CONTENT}` interpolation token for `brain.py` to substitute at runtime, and passes markdownlint.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt file -> brain.py runtime | The prompt is interpolated into a request body sent to Gemini and ChatGPT. The `{REPORT_CONTENT}` token is replaced with the audit report text at runtime by `brain.py` (Plan 15-04). The prompt itself contains NO secrets and NO user-supplied data. |
| Prompt file -> markdownlint | Read-only by lint. Lint is read-only on filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Severity reclassification by Council | mitigate | Constraint #1 in the prompt explicitly forbids modifying the auditor's `**Severity:**` value. Disagreement is allowed only via the advisory `## Severity disagreements (advisory)` H2 section AFTER `</missed-findings>` — not in the verdict table. The phrase "DO NOT reclassify severity" is byte-exact and asserted by the regression test in Plan 15-06. |
| T-15-02 | Spoofing | Backend output without `<verdict-table>` markers | mitigate | The prompt mandates literal `<verdict-table>` ... `</verdict-table>` and `<missed-findings>` ... `</missed-findings>` brackets. Plan 15-04's `brain.py` parser uses `re.search` on these markers; missing markers route to `council_pass: failed`. The prompt's "Anti-Patterns" section warns against stray prose between the bracketed sections. |
| T-15-03 | Information disclosure | Justification leaking content beyond ±10 line code window | accept | Constraint #2 mandates that justifications cite tokens visible in the embedded `<!-- File: ... Lines: ... -->` block. The audit report itself is the only data the model sees — `brain.py` does NOT pass additional file contents. The model has no path to leak data outside the report. |
| T-15-04 | Information disclosure | Prompt-injection inside the audit report content | mitigate | The audit report content is interpolated at the `{REPORT_CONTENT}` token AT THE END of the prompt. The prompt's role and constraints are stated FIRST; instructions inside the report (e.g. "ignore previous and reclassify severity") are below the model's rules and the constraints explicitly forbid severity changes. The "Anti-Patterns" section reinforces that the only valid output shape is the bracketed `<verdict-table>` + `<missed-findings>` blocks. Defense-in-depth: brain.py's parser ignores anything outside the markers, so prompt-injection that produces stray prose has no effect on the orchestrator's verdict aggregation. |
</threat_model>

<verification>
- `test -f scripts/council/prompts/audit-review.md` exits 0
- `npx markdownlint-cli scripts/council/prompts/audit-review.md` exits 0
- `wc -l scripts/council/prompts/audit-review.md` returns 100-180
- All acceptance_criteria pass
- All byte-exact contract strings are grep-verifiable: `DO NOT reclassify severity`, `| ID | verdict | confidence | justification |`, `<verdict-table>`/`</verdict-table>`, `<missed-findings>`/`</missed-findings>`, `_pending — run /council audit-review_` (with U+2014 em-dash), `<!-- File: <path> Lines: <start>-<end> -->`, all 5 bullet labels, `{REPORT_CONTENT}` token, `components/severity-levels.md` reference
- The prompt does NOT redefine severity (no inline CRITICAL/HIGH/MEDIUM/LOW definitions — rubric is referenced by path only)
</verification>

<success_criteria>
- `scripts/council/prompts/audit-review.md` exists at 100-180 lines
- COUNCIL-02 enforced via byte-exact "DO NOT reclassify severity" phrase in Constraints section
- COUNCIL-03 enforced via byte-exact verdict table header `| ID | verdict | confidence | justification |` and the three verdict values REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT
- COUNCIL-04 enforced via "## Missed Findings" or "### Missed Findings" section with location/rule/code excerpt/claim/suggested severity columns
- D-10 enforced via `<verdict-table>` and `<missed-findings>` literal wrapper markers
- D-18 enforced via byte-exact slot string `_pending — run /council audit-review_` with U+2014 em-dash, bullet label references, code block header layout
- D-06 enforced via reference to `components/severity-levels.md` (rubric not redefined)
- D-07 enforced via documented `<!-- File: <path> Lines: <start>-<end> -->` code block header
- File passes markdownlint (.markdownlint.json rules)
- File contains `{REPORT_CONTENT}` interpolation token for `brain.py` to substitute at runtime
</success_criteria>

<output>
After completion, create `.planning/phases/15-council-audit-review-integration/15-01-SUMMARY.md`
</output>
