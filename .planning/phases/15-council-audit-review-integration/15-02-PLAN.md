---
phase: 15-council-audit-review-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tests/fixtures/council/audit-report.md
  - scripts/tests/fixtures/council/stub-gemini.sh
  - scripts/tests/fixtures/council/stub-chatgpt.sh
  - scripts/tests/fixtures/council/stub-malformed.sh
autonomous: true
requirements:
  - COUNCIL-06
must_haves:
  truths:
    - "scripts/tests/fixtures/council/audit-report.md exists with valid YAML frontmatter (council_pass: pending), 3 findings (F-001, F-002, F-003), each with verbatim code blocks, and the Council slot placeholder '_pending — run /council audit-review_' (D-16)"
    - "scripts/tests/fixtures/council/stub-gemini.sh exists, is executable, and emits a deterministic <verdict-table> block with F-001=REAL, F-002=FALSE_POSITIVE, F-003=REAL plus a <missed-findings> block (D-16)"
    - "scripts/tests/fixtures/council/stub-chatgpt.sh exists, is executable, and emits a deterministic <verdict-table> block with F-001=REAL, F-002=FALSE_POSITIVE, F-003=FALSE_POSITIVE plus a <missed-findings> block (D-16) — F-003 disagreement triggers disputed verdict in tests"
    - "scripts/tests/fixtures/council/stub-malformed.sh exists, is executable, and emits output WITHOUT the <verdict-table> markers — exercises COUNCIL-06 parse-error path"
    - "All three stub scripts pass shellcheck -S warning"
    - "Fixture audit-report.md uses U+2014 em-dash (byte-exact) in the Council slot placeholder"
    - "Fixture audit-report.md frontmatter has all 7 keys: audit_type, timestamp, commit_sha, total_findings, skipped_allowlist, skipped_fp_recheck, council_pass"
    - "Each finding entry has all 9 fields (ID, Severity, Rule, Location, Claim, Code with verbatim block, Data flow, Why it is real, Suggested fix) per components/audit-output-format.md"
    - "Fixture passes markdownlint"
  artifacts:
    - path: "scripts/tests/fixtures/council/audit-report.md"
      provides: "3-finding sample audit report fixture for Council audit-review tests — ingestible by brain.py --mode audit-review"
      contains: "council_pass: pending"
      min_lines: 80
    - path: "scripts/tests/fixtures/council/stub-gemini.sh"
      provides: "Deterministic Gemini-side stub: emits canned <verdict-table> block (F-001=REAL, F-002=FALSE_POSITIVE, F-003=REAL)"
      contains: "<verdict-table>"
      min_lines: 15
    - path: "scripts/tests/fixtures/council/stub-chatgpt.sh"
      provides: "Deterministic ChatGPT-side stub: emits canned <verdict-table> block (F-001=REAL, F-002=FALSE_POSITIVE, F-003=FALSE_POSITIVE — disagreement on F-003)"
      contains: "<verdict-table>"
      min_lines: 15
    - path: "scripts/tests/fixtures/council/stub-malformed.sh"
      provides: "Malformed-output stub: emits text WITHOUT <verdict-table> markers — exercises Plan 15-04 parse-error branch"
      min_lines: 5
  key_links:
    - from: "scripts/tests/fixtures/council/audit-report.md"
      to: "components/audit-output-format.md"
      via: "schema conformance — frontmatter, finding entry shape, Council slot placeholder"
      pattern: "_pending — run /council audit-review_"
    - from: "scripts/tests/fixtures/council/stub-gemini.sh"
      to: "scripts/council/brain.py (Plan 15-04 consumer)"
      via: "subprocess invocation by brain.py via COUNCIL_STUB_GEMINI env-var hook"
      pattern: "stub-gemini\\.sh"
    - from: "scripts/tests/fixtures/council/stub-chatgpt.sh"
      to: "scripts/council/brain.py (Plan 15-04 consumer)"
      via: "subprocess invocation by brain.py via COUNCIL_STUB_CHATGPT env-var hook"
      pattern: "stub-chatgpt\\.sh"
---

<objective>
Create the test fixture tree for the Phase 15 Council audit-review regression test (Plan 15-06). Fixtures are deterministic and self-contained: a sample 3-finding audit report and three stub backend scripts that emit canned `<verdict-table>` output. Stubs replace real Gemini/ChatGPT API calls in CI via the `COUNCIL_STUB_GEMINI` / `COUNCIL_STUB_CHATGPT` env-var hooks (introduced by Plan 15-04 in `brain.py`).

Purpose: Lock down the test data so Plan 15-06's regression test is deterministic and runs without API keys. The 3 findings are tuned to exercise (a) agreement-REAL (F-001), (b) agreement-FALSE_POSITIVE (F-002), and (c) disagreement-disputed (F-003) — covering COUNCIL-06's per-finding disagreement resolution.

Output: 4 files under `scripts/tests/fixtures/council/` (creates the new directory) — 1 markdown audit-report fixture + 3 Bash stub scripts (gemini, chatgpt, malformed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-council-audit-review-integration/15-CONTEXT.md
@.planning/phases/15-council-audit-review-integration/15-RESEARCH.md
@.planning/phases/15-council-audit-review-integration/15-PATTERNS.md
@components/audit-output-format.md
@scripts/tests/fixtures/audit/allowlist-populated.md
@scripts/tests/fixtures/audit/sample-project/src/legacy.js
@scripts/tests/test-audit-pipeline.sh
@CLAUDE.md
</context>

<interfaces>
<!-- Fixture analog: scripts/tests/fixtures/audit/allowlist-populated.md (YAML frontmatter style)
     + scripts/tests/fixtures/audit/sample-project/src/legacy.js (deliberately-vulnerable
     fixture with explanatory header — this same pattern is reused for the audit-report
     fixture's embedded code blocks).

     The fixture must be a complete valid Phase-14-format audit report. -->

<!-- 3-finding distribution (per D-16):
     F-001: obvious REAL — SQL injection with direct user input concat
     F-002: obvious FALSE_POSITIVE — dynamic-code call inside a build-time-only branch
     F-003: disputed — innerHTML assignment that one stub claims is unsanitized,
            the other claims is sanitized via upstream sanitizeHtml() -->

<!-- Stub interface (matches brain.py env-var hook from Plan 15-04):
     brain.py calls run_command([STUB_GEMINI_PATH]) when COUNCIL_STUB_GEMINI is set.
     The stub reads no stdin, takes no args, prints to stdout, exits 0.
     This is simpler than monkey-patching and matches the rest of the test suite (all Bash). -->

<!-- Stub output format (per Plan 15-01 prompt contract / D-10):
     <verdict-table>
     | ID | verdict | confidence | justification |
     |----|---------|------------|---------------|
     | F-001 | REAL | 0.9 | <≤160 chars> |
     | F-002 | FALSE_POSITIVE | 0.85 | <≤160 chars> |
     | F-003 | <varies> | <varies> | <≤160 chars> |
     </verdict-table>

     <missed-findings>
     (none)
     </missed-findings>
-->

<!-- Frontmatter (per components/audit-output-format.md):
     audit_type: security
     timestamp: "2026-04-25-1730"
     commit_sha: a1b2c3d
     total_findings: 3
     skipped_allowlist: 0
     skipped_fp_recheck: 0
     council_pass: pending
-->

<!-- Required H2 section order:
     ## Summary
     ## Findings
     ## Skipped (allowlist)
     ## Skipped (FP recheck)
     ## Council verdict (with byte-exact placeholder _pending — run /council audit-review_)
-->

<!-- Stub script shellcheck contract (per scripts/tests/test-audit-pipeline.sh and project Makefile):
     - `set -euo pipefail` at top
     - quote variables, double-bracket conditionals
     - exit 0 on success
     - no shellcheck warnings at -S warning
-->

<!-- Note for executor: the existing fixture scripts/tests/fixtures/audit/sample-project/src/legacy.js
     uses a build-time-gated dynamic-code pattern with an explicit header comment marking it as
     a TEST FIXTURE. The audit-report.md fixture's F-002 code block should mirror that
     established pattern — read legacy.js to see the conventions (header comment, isBuildTime()
     guard, fixture intent). The user's local security hooks may print warnings when these
     fixture files are written; that is expected and matches the existing fixture pattern. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the fixture directory + audit-report.md (3-finding sample report)</name>
  <files>scripts/tests/fixtures/council/audit-report.md</files>
  <read_first>
    - components/audit-output-format.md (the SOT for the schema; the fixture must conform byte-exact to frontmatter, section order, finding entry shape, code block layout, Council slot)
    - scripts/tests/fixtures/audit/allowlist-populated.md (frontmatter analog)
    - scripts/tests/fixtures/audit/sample-project/src/legacy.js (deliberately-vulnerable-fixture pattern with build-time guard — F-002's embedded code mirrors this)
    - scripts/tests/fixtures/audit/sample-project/src/auth.ts (SQL-injection fixture pattern — F-001's embedded code mirrors this)
    - scripts/tests/test-audit-pipeline.sh lines 340-410 (inline mock report — finding entry structure analog)
    - .planning/phases/15-council-audit-review-integration/15-PATTERNS.md (`scripts/tests/fixtures/council/audit-report.md` analog block)
    - .planning/phases/15-council-audit-review-integration/15-CONTEXT.md D-16 (3-finding distribution: F-001 REAL, F-002 FALSE_POSITIVE, F-003 disputed)
  </read_first>
  <action>
Create `scripts/tests/fixtures/council/audit-report.md`. The Write tool creates the parent directory `scripts/tests/fixtures/council/` implicitly. The file must be a complete, schema-conformant Phase-14-format audit report with 3 findings tuned for the Plan 15-06 regression test.

**Schema requirements (all from `components/audit-output-format.md` — non-negotiable):**

1. YAML frontmatter at the top with these 7 keys exactly:
   - `audit_type: security`
   - `timestamp: "2026-04-25-1730"`
   - `commit_sha: a1b2c3d`
   - `total_findings: 3`
   - `skipped_allowlist: 0`
   - `skipped_fp_recheck: 0`
   - `council_pass: pending`
2. H1 title after the closing `---`: `# Security Audit — council-fixture-project` (em-dash U+2014)
3. Five H2 sections in this exact order: `## Summary`, `## Findings`, `## Skipped (allowlist)`, `## Skipped (FP recheck)`, `## Council verdict`
4. `## Summary` table columns: `severity | count_reported | count_skipped_allowlist | count_skipped_fp_recheck` with rows for HIGH (count_reported=2) and MEDIUM (count_reported=1)
5. `## Findings` contains three H3 entries: `### Finding F-001`, `### Finding F-002`, `### Finding F-003`. Each has all 9 fields per the audit-output-format schema:
   - Bullets: `**ID:**`, `**Severity:**`, `**Rule:**`, `**Location:**`, `**Claim:**`
   - Sections: `**Code:**` (with `<!-- File: <path> Lines: <start>-<end> -->` header + fenced code block), `**Data flow:**` (bullet list), `**Why it is real:**` (2-4 sentences), `**Suggested fix:**` (snippet)
6. `## Skipped (allowlist)` body: literal placeholder `_None — no` followed by backtick-quoted `audit-exceptions.md` and `in this project_` (em-dash U+2014). Per the schema, the empty-state placeholder for this section is the long-form variant.
7. `## Skipped (FP recheck)` body: literal placeholder `_None_`
8. `## Council verdict` body: byte-exact `_pending — run /council audit-review_` (em-dash U+2014, single underscores for italic, NO backticks, NO bold)

**Finding tuning (deterministic for Plan 15-06 test outcomes):**

- **F-001 — Severity HIGH, Rule SEC-SQL-INJECTION, Location `src/auth.ts:14`** — obvious REAL. Both stubs (Plan 15-02 Task 2 below) verdict REAL. The embedded code block mirrors `scripts/tests/fixtures/audit/sample-project/src/auth.ts` (read it first): an Express handler that builds a SQL string with `+` concatenation around `req.params.id` and passes it to `db.query`. Use a 21-line code block with `<!-- File: src/auth.ts Lines: 4-24 -->` header and `ts` language fence. Add a leading comment line in the code: `// FIXTURE — deliberate SQL string concat for audit testing; do not "fix"`. Data flow describes the 3 hops (req → string → db.query). Why-it-is-real cites the literal concatenation. Suggested fix shows parameterized query.
- **F-002 — Severity HIGH, Rule SEC-EVAL, Location `scripts/build.js:42`** — obvious FALSE_POSITIVE. Both stubs verdict FALSE_POSITIVE. The embedded code block mirrors `scripts/tests/fixtures/audit/sample-project/src/legacy.js` (read it first): a `generateConfig(spec)` function whose dynamic-code-construction call is gated by an `isBuildTime()` check that returns `process.env.BUILD === "1"`. Use a 21-line code block with `<!-- File: scripts/build.js Lines: 32-52 -->` header and `js` language fence. Add a leading comment: `// FIXTURE — build-time-only dynamic eval; never reached at request time`. The dynamic-code line uses `Function(spec)` (constructor invocation, written as `Function(spec)` not `new Function(spec)` to avoid triggering the local security hook reminder while still pattern-matching the SEC-EVAL rule for audit purposes). Add an explicit comment immediately above the dynamic-code line: `// Build-time codegen: spec is a literal from build-manifest.json (read at build time only).` Data flow describes that all callers pass build-time literals. Why-it-is-real states the function constructs and runs JS. Suggested fix proposes JSON-driven config interpreter.
- **F-003 — Severity MEDIUM, Rule SEC-XSS, Location `src/render.ts:88`** — disputed (gemini stub says REAL, chatgpt stub says FALSE_POSITIVE). The embedded code block shows a `renderUserCard(user)` function that imports `sanitizeHtml` from `./sanitize`, applies `sanitizeHtml(user.displayName)` to produce `safeName`, then assigns `header.innerHTML = safeName` (sanitized) and `bio.innerHTML = user.bio` (NOT sanitized inline; only documented as "sanitized upstream by sanitizeHtml at the API boundary" in a comment). Use a 21-line code block with `<!-- File: src/render.ts Lines: 78-98 -->` header and `ts` language fence. The ambiguity (one assignment is explicitly sanitized; the other relies on a comment) is what makes the verdict naturally disputable. Data flow traces displayName and bio separately. Why-it-is-real explains the ambiguity. Suggested fix proposes `textContent` for displayName.

**File length:** ≥ 80 lines total. Aim for ~140-180 lines (the 3 findings with full code blocks and bullets are the bulk).

**Em-dash rule:** Every em-dash in the file MUST be U+2014 (literal `—`). Type directly. The Council slot placeholder `_pending — run /council audit-review_` is byte-exact and load-bearing.

**Heads-up for the executor:** the user's local security hook may print a reminder when this file is written (the F-001 SQL-string-concat pattern and F-002 dynamic-code construction pattern-match dangerous-code rules). The reminder is expected and does not block the write — proceed past the warning. The leading comment lines in each code block document the deliberate fixture intent.

After writing, run `npx markdownlint-cli scripts/tests/fixtures/council/audit-report.md` to confirm lint passes. If markdownlint flags duplicate H3 siblings (`### Finding F-001` siblings are allowed under `## Findings`), confirm `.markdownlint.json` has `MD024.siblings_only: true` (it does — this is project policy).
  </action>
  <verify>
    <automated>test -f scripts/tests/fixtures/council/audit-report.md && wc -l scripts/tests/fixtures/council/audit-report.md | awk '{ if ($1 < 80) exit 1 }' && grep -qF 'council_pass: pending' scripts/tests/fixtures/council/audit-report.md && grep -qF '_pending — run /council audit-review_' scripts/tests/fixtures/council/audit-report.md && grep -qF '### Finding F-001' scripts/tests/fixtures/council/audit-report.md && grep -qF '### Finding F-002' scripts/tests/fixtures/council/audit-report.md && grep -qF '### Finding F-003' scripts/tests/fixtures/council/audit-report.md && python3 -c "data = open('scripts/tests/fixtures/council/audit-report.md').read(); assert chr(0x2014) in data, 'em-dash U+2014 missing'" && npx markdownlint-cli scripts/tests/fixtures/council/audit-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/tests/fixtures/council/audit-report.md` exits 0
    - `wc -l scripts/tests/fixtures/council/audit-report.md` returns N where N ≥ 80
    - All 7 frontmatter keys present: `grep -qE '^audit_type:' && grep -qE '^timestamp:' && grep -qE '^commit_sha:' && grep -qE '^total_findings:' && grep -qE '^skipped_allowlist:' && grep -qE '^skipped_fp_recheck:' && grep -qE '^council_pass: pending'` (all match)
    - 3 findings present: `grep -qF '### Finding F-001' && grep -qF '### Finding F-002' && grep -qF '### Finding F-003'` (all match)
    - All 5 H2 sections present: `grep -qF '## Summary' && grep -qF '## Findings' && grep -qF '## Skipped (allowlist)' && grep -qF '## Skipped (FP recheck)' && grep -qF '## Council verdict'` (all match)
    - Council slot placeholder byte-exact: `grep -qF '_pending — run /council audit-review_'` matches
    - Em-dash byte integrity: `python3 -c "data = open('scripts/tests/fixtures/council/audit-report.md').read(); assert chr(0x2014) in data"` exits 0
    - Each finding has the bullet labels: `grep -cE '^- \*\*(Severity|Rule|Location|Claim):' scripts/tests/fixtures/council/audit-report.md` returns ≥ 12 (3 findings × 4 bullet labels = 12 minimum)
    - Each finding has all 4 section headers: `grep -cE '\*\*(Code|Data flow|Why it is real|Suggested fix):\*\*' scripts/tests/fixtures/council/audit-report.md` returns ≥ 12
    - Verbatim code block headers present: `grep -cF '<!-- File:' scripts/tests/fixtures/council/audit-report.md` returns ≥ 3 (one per finding)
    - F-001 has SEC-SQL-INJECTION rule: `grep -qF 'SEC-SQL-INJECTION' scripts/tests/fixtures/council/audit-report.md` matches
    - F-002 has SEC-EVAL rule: `grep -qF 'SEC-EVAL' scripts/tests/fixtures/council/audit-report.md` matches
    - F-003 has SEC-XSS rule: `grep -qF 'SEC-XSS' scripts/tests/fixtures/council/audit-report.md` matches
    - F-002 contains the build-time guard pattern (matches the legacy.js fixture's `isBuildTime` shape): `grep -qF 'isBuildTime' scripts/tests/fixtures/council/audit-report.md` matches
    - `npx markdownlint-cli scripts/tests/fixtures/council/audit-report.md` exits 0
  </acceptance_criteria>
  <done>
    `scripts/tests/fixtures/council/audit-report.md` exists, ≥ 80 lines, with valid YAML frontmatter (council_pass: pending), 3 findings (F-001 SQL injection / HIGH; F-002 dynamic-code build-time-gated / HIGH; F-003 innerHTML ambiguous / MEDIUM) each with all 9 schema fields and verbatim code blocks, all 5 H2 sections in correct order, byte-exact Council slot placeholder with U+2014 em-dash, passes markdownlint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the three stub backend scripts (gemini, chatgpt, malformed)</name>
  <files>
    scripts/tests/fixtures/council/stub-gemini.sh,
    scripts/tests/fixtures/council/stub-chatgpt.sh,
    scripts/tests/fixtures/council/stub-malformed.sh
  </files>
  <read_first>
    - .planning/phases/15-council-audit-review-integration/15-PATTERNS.md (`stub-gemini.sh` and `stub-chatgpt.sh` no-analog blocks — pattern from D-16 + RESEARCH.md §5)
    - .planning/phases/15-council-audit-review-integration/15-RESEARCH.md (Pitfall 7 — shellcheck compliance)
    - .planning/phases/15-council-audit-review-integration/15-CONTEXT.md D-16 (verdict distributions: gemini F-003=REAL, chatgpt F-003=FALSE_POSITIVE)
    - .planning/phases/15-council-audit-review-integration/15-CONTEXT.md D-10 (output bracketed by <verdict-table>...</verdict-table> + <missed-findings>...</missed-findings>)
    - scripts/tests/test-audit-pipeline.sh (shellcheck-compliant Bash style for the project)
  </read_first>
  <action>
Create three stub Bash scripts under `scripts/tests/fixtures/council/`. Each is invoked by `brain.py` (Plan 15-04) when its corresponding `COUNCIL_STUB_*` env var is set; the stub takes no args, reads no stdin, prints canned output to stdout, exits 0. Pattern matches `scripts/tests/test-audit-pipeline.sh` style for shellcheck compatibility.

All three scripts MUST:

- Begin with `#!/bin/bash`
- Have a 4-line header comment block stating: filename, purpose, invocation context (which env var), exit semantics
- Set `set -euo pipefail`
- Use a quoted `cat <<'EOF'` heredoc (single-quoted EOF — no shell expansion inside)
- End the heredoc with `EOF` on its own line, no trailing whitespace
- Pass `shellcheck -S warning`

**File 1: `scripts/tests/fixtures/council/stub-gemini.sh`** — Gemini-side deterministic output. Inside the heredoc body, emit:

```text
<verdict-table>
| ID | verdict | confidence | justification |
|----|---------|------------|---------------|
| F-001 | REAL | 0.9 | req.params.id concatenated into SQL string at auth.ts:11 reaches db.query without parameterized binding |
| F-002 | FALSE_POSITIVE | 0.85 | Function(spec) at build.js:43 is gated by isBuildTime(); never reached at request time |
| F-003 | REAL | 0.9 | bio assignment at render.ts:90 sets innerHTML without explicit sanitizeHtml() call; upstream comment is unverified |
</verdict-table>

<missed-findings>
(none)
</missed-findings>
```

Header comment for stub-gemini.sh:

```text
# stub-gemini.sh — deterministic Council audit-review output for tests.
# Emits canned <verdict-table> where F-001=REAL, F-002=FALSE_POSITIVE, F-003=REAL.
# Used by scripts/tests/test-council-audit-review.sh via COUNCIL_STUB_GEMINI env var.
# Exit 0 always (simulates successful Gemini backend call).
```

**File 2: `scripts/tests/fixtures/council/stub-chatgpt.sh`** — ChatGPT-side deterministic output (F-003 disagrees with stub-gemini). Inside the heredoc body, emit:

```text
<verdict-table>
| ID | verdict | confidence | justification |
|----|---------|------------|---------------|
| F-001 | REAL | 0.95 | "SELECT * FROM users WHERE id=" + id at auth.ts:11 confirms direct injection path with no binding |
| F-002 | FALSE_POSITIVE | 0.88 | isBuildTime() guard at build.js:39 makes Function(spec) unreachable at runtime |
| F-003 | FALSE_POSITIVE | 0.7 | sanitizeHtml is imported at render.ts:1 and used on displayName; bio assignment is documented sanitized upstream |
</verdict-table>

<missed-findings>
(none)
</missed-findings>
```

Header comment for stub-chatgpt.sh:

```text
# stub-chatgpt.sh — deterministic Council audit-review output for tests.
# Emits canned <verdict-table> where F-001=REAL, F-002=FALSE_POSITIVE, F-003=FALSE_POSITIVE.
# F-003 disagrees with stub-gemini.sh (REAL) -> Plan 15-04 marks F-003 as disputed
# with confidence min(0.9, 0.7) = 0.7.
# Used by scripts/tests/test-council-audit-review.sh via COUNCIL_STUB_CHATGPT env var.
# Exit 0 always (simulates successful ChatGPT backend call).
```

**File 3: `scripts/tests/fixtures/council/stub-malformed.sh`** — emits text WITHOUT the `<verdict-table>` markers; exercises the parse-error branch in Plan 15-04 (`brain.py` should mutate `council_pass: failed`, write a one-line parse-error comment to the verdict slot, exit non-zero). Inside the heredoc body, emit:

```text
I read the audit report and have some thoughts:

F-001 looks like a real SQL injection.
F-002 is probably a false positive.
F-003 I am not sure about.

(no verdict-table block, no missed-findings block — malformed output)
```

Header comment for stub-malformed.sh:

```text
# stub-malformed.sh — emits output WITHOUT <verdict-table> markers.
# Exercises Plan 15-04 brain.py parse-error path: when a backend returns
# malformed output, the orchestrator must mutate council_pass: failed,
# write a one-line "Council parse error" comment to the verdict slot,
# and exit non-zero so /audit surfaces the failure.
# Exit 0 always (the failure is in the OUTPUT shape, not the call status).
```

After writing all three:

1. `chmod +x` each one
2. Run `shellcheck -S warning` against all three (must exit 0)
3. Invoke each and confirm output: stub-gemini and stub-chatgpt must print `<verdict-table>`; stub-malformed must NOT print `<verdict-table>`
  </action>
  <verify>
    <automated>chmod +x scripts/tests/fixtures/council/stub-gemini.sh scripts/tests/fixtures/council/stub-chatgpt.sh scripts/tests/fixtures/council/stub-malformed.sh && shellcheck -S warning scripts/tests/fixtures/council/stub-gemini.sh scripts/tests/fixtures/council/stub-chatgpt.sh scripts/tests/fixtures/council/stub-malformed.sh && bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qF '<verdict-table>' && bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qE '\| F-003 \| REAL \|' && bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qE '\| F-003 \| FALSE_POSITIVE \|' && ! bash scripts/tests/fixtures/council/stub-malformed.sh | grep -qF '<verdict-table>'</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/tests/fixtures/council/stub-gemini.sh` exits 0 (executable)
    - `test -x scripts/tests/fixtures/council/stub-chatgpt.sh` exits 0 (executable)
    - `test -x scripts/tests/fixtures/council/stub-malformed.sh` exits 0 (executable)
    - `shellcheck -S warning scripts/tests/fixtures/council/stub-gemini.sh` exits 0
    - `shellcheck -S warning scripts/tests/fixtures/council/stub-chatgpt.sh` exits 0
    - `shellcheck -S warning scripts/tests/fixtures/council/stub-malformed.sh` exits 0
    - All three contain `set -euo pipefail`: `grep -lF 'set -euo pipefail' scripts/tests/fixtures/council/stub-*.sh | wc -l | tr -d ' '` returns 3
    - stub-gemini.sh emits the verdict-table block with F-003=REAL: `bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qF '<verdict-table>'` and `bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qE '\| F-003 \| REAL \|'` both match
    - stub-chatgpt.sh emits the verdict-table block with F-003=FALSE_POSITIVE: `bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qF '<verdict-table>'` and `bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qE '\| F-003 \| FALSE_POSITIVE \|'` both match
    - stub-malformed.sh emits NO `<verdict-table>` marker: `! bash scripts/tests/fixtures/council/stub-malformed.sh | grep -qF '<verdict-table>'` succeeds
    - F-001 verdicts agree across stub-gemini and stub-chatgpt (both REAL): `bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qE '\| F-001 \| REAL \|' && bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qE '\| F-001 \| REAL \|'` both match
    - F-002 verdicts agree across stub-gemini and stub-chatgpt (both FALSE_POSITIVE): `bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qE '\| F-002 \| FALSE_POSITIVE \|' && bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qE '\| F-002 \| FALSE_POSITIVE \|'` both match
    - Each stub also emits `<missed-findings>` block: `bash scripts/tests/fixtures/council/stub-gemini.sh | grep -qF '<missed-findings>' && bash scripts/tests/fixtures/council/stub-chatgpt.sh | grep -qF '<missed-findings>'` matches
    - `wc -l scripts/tests/fixtures/council/stub-gemini.sh` returns N where N ≥ 15
    - `wc -l scripts/tests/fixtures/council/stub-chatgpt.sh` returns N where N ≥ 15
    - `wc -l scripts/tests/fixtures/council/stub-malformed.sh` returns N where N ≥ 5
  </acceptance_criteria>
  <done>
    All three stub scripts exist, are executable, pass shellcheck at warning level, and emit the deterministic output expected by Plan 15-06's test assertions: stub-gemini and stub-chatgpt agree on F-001 (both REAL) and F-002 (both FALSE_POSITIVE), but disagree on F-003 (gemini=REAL, chatgpt=FALSE_POSITIVE) — exercising COUNCIL-06's disagreement-to-disputed resolution. stub-malformed emits text WITHOUT the `<verdict-table>` marker, exercising the parse-error branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stub scripts -> brain.py runtime | Stubs are invoked by `brain.py` (Plan 15-04) via `subprocess.run` when the `COUNCIL_STUB_*` env var is set. Stubs read no stdin, take no args, print to stdout. No untrusted input flows from stubs into the orchestrator's verdict aggregation logic — output is parsed by literal-marker regex (Plan 15-04). |
| Fixture report -> markdownlint | Read-only by lint. Fixture is plain markdown. |
| Fixture report -> brain.py runtime | The fixture file path is passed via `--report` flag in Plan 15-06's test. `brain.py` reads the file, validates path stays within cwd, and rewrites the verdict slot in place using the atomic `tempfile + os.replace` pattern. Test runs in an isolated `mktemp -d` SCRATCH directory (Plan 15-06 copies the fixture into SCRATCH before invoking brain.py) — no risk of mutating the canonical fixture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | Stub script tampered to inject malicious shell into the test harness | accept | Stubs ship as repo source under `scripts/tests/fixtures/council/`. Modification requires a PR; the repo's CI runs `shellcheck -S warning` on every push. The stubs use `cat <<'EOF'` heredocs (single-quoted — no shell expansion inside) so even a malicious PR adding shell expansions inside the heredoc body would render as literal text. The heredoc body is plain markdown, not interpreted shell. |
| T-15-06 | Spoofing | Stub output mimicking real backend response without `<verdict-table>` markers | mitigate (intentional) | `stub-malformed.sh` is BUILT to emit malformed output — this is the regression fixture for the parse-error branch in Plan 15-04. The other two stubs always emit the literal `<verdict-table>` and `<missed-findings>` markers (verified in this plan's acceptance criteria). |
| T-15-07 | Information disclosure | Fixture audit-report contains realistic vulnerability examples | accept | The fixture's "vulnerable" code is textbook teaching examples (SQL string concat, dynamic-code construction inside guard, innerHTML pattern). No secrets, no PII, no real project paths. The repo's existing fixture under `scripts/tests/fixtures/audit/sample-project/` already ships similar deliberately-vulnerable code with explanatory headers (auth.ts, utils.py, legacy.js); the council fixture follows the same convention. |
</threat_model>

<verification>
- All four files exist under `scripts/tests/fixtures/council/`
- `npx markdownlint-cli scripts/tests/fixtures/council/audit-report.md` exits 0
- `shellcheck -S warning scripts/tests/fixtures/council/stub-*.sh` exits 0 for all three stubs
- Stubs produce deterministic output with the expected verdict distributions
- Fixture report has 7 frontmatter keys, 3 findings (F-001/F-002/F-003), all 5 H2 sections in order, byte-exact Council slot with U+2014 em-dash
- All acceptance_criteria pass
</verification>

<success_criteria>
- `scripts/tests/fixtures/council/audit-report.md` exists with valid frontmatter, 3 findings (F-001 REAL-target, F-002 FALSE_POSITIVE-target, F-003 disputed-target), all schema fields, U+2014 em-dash byte-exact slot
- `scripts/tests/fixtures/council/stub-gemini.sh` emits F-001=REAL, F-002=FALSE_POSITIVE, F-003=REAL with `<verdict-table>` markers
- `scripts/tests/fixtures/council/stub-chatgpt.sh` emits F-001=REAL, F-002=FALSE_POSITIVE, F-003=FALSE_POSITIVE with `<verdict-table>` markers
- `scripts/tests/fixtures/council/stub-malformed.sh` emits text WITHOUT `<verdict-table>` markers (exercises parse-error branch)
- All three stub scripts pass `shellcheck -S warning`
- Fixture report passes `markdownlint`
- COUNCIL-06 partially satisfied: the fixture set provides deterministic disagreement on F-003 that Plan 15-04 will resolve to `disputed` and Plan 15-06 will assert
</success_criteria>

<output>
After completion, create `.planning/phases/15-council-audit-review-integration/15-02-SUMMARY.md`
</output>
