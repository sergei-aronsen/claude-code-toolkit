---
phase: 14-audit-pipeline-fp-recheck-structured-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/audit-output-format.md
autonomous: true
requirements:
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
must_haves:
  truths:
    - "components/audit-output-format.md exists and documents the structured audit report schema"
    - "Report path pattern .claude/audits/<type>-<YYYY-MM-DD-HHMM>.md is documented verbatim per D-12"
    - "YAML frontmatter schema documented with all 7 required fields (audit_type, timestamp, commit_sha, total_findings, skipped_allowlist, skipped_fp_recheck, council_pass)"
    - "Fixed section order documented: Frontmatter -> Summary -> Findings -> Skipped (allowlist) -> Skipped (FP recheck) -> Council verdict"
    - "Finding entry schema lists all 9 required fields in D-14 order: ID, Severity, Rule, Location, Claim, Code, Data flow, Why it is real, Suggested fix"
    - "Verbatim 10-lines-each-side code block format documented with HTML range comment header per D-11"
    - "Council verdict slot literal `_pending — run /council audit-review_` (U+2014 em-dash) documented as a byte-exact handoff string"
    - "Type slug -> prompt file map documents all 7 canonical slugs (security, code-review, performance, deploy-checklist, mysql-performance, postgres-performance, design-review)"
    - "File passes markdownlint (MD040, MD031, MD032, MD026, MD029)"
  artifacts:
    - path: "components/audit-output-format.md"
      provides: "Single source of truth for the structured audit report schema"
      contains: "## YAML Frontmatter"
      min_lines: 100
  key_links:
    - from: "components/audit-output-format.md"
      to: "Phase 15 Council parser"
      via: "byte-exact `_pending — run /council audit-review_` slot string"
      pattern: "_pending — run /council audit-review_"
    - from: "components/audit-output-format.md"
      to: "components/severity-levels.md"
      via: "Severity field references existing rubric (do not redefine)"
      pattern: "severity-levels"
---

<objective>
Create `components/audit-output-format.md` as the canonical, splice-friendly source of truth for the structured audit report schema. Phase 16 splices this file's body into the OUTPUT FORMAT section of 49 framework prompt files; Phase 15 Council parser reads the byte-exact slot strings documented here.

Purpose: Lock the report contract before any consumer (Phase 15 Council, Phase 16 prompt fan-out, Plan 14-03 audit dispatcher) is built. Schema drift between this component and downstream consumers breaks the whole pipeline silently — the byte-exact strings (Council slot, frontmatter keys, section headings) are what Phase 15 greps for.

Output: `components/audit-output-format.md` (~150 lines) — single new component, not added to `manifest.json` per D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-VALIDATION.md
@components/report-format.md
@components/severity-levels.md
@CLAUDE.md

<interfaces>
<!-- Splice contract: Phase 16 copies the body of this component into the OUTPUT FORMAT
     section of each prompt. Phase 15 Council parser greps for literal strings:
       - `_pending — run /council audit-review_` (U+2014, italic-wrapped)
       - `## Council verdict` (H2 heading)
       - YAML frontmatter keys: audit_type, timestamp, commit_sha, total_findings,
         skipped_allowlist, skipped_fp_recheck, council_pass
     CHANGING ANY OF THESE BYTE SEQUENCES BREAKS PHASE 15. -->

<!-- Locked decisions for this plan (per 14-CONTEXT.md): -->
<!--   D-03 — components/audit-output-format.md is the SOT for report schema. -->
<!--   D-11 — verbatim 10-lines-each-side code block: language fence, range comment header,
                file-bound clamp. -->
<!--   D-12 — report path .claude/audits/<type>-<YYYY-MM-DD-HHMM>.md, mkdir -p, 7 type slugs. -->
<!--   D-13 — fixed section order: Frontmatter -> Summary -> Findings -> Skipped (allowlist) ->
                Skipped (FP recheck) -> Council verdict. -->
<!--   D-14 — finding entry schema: ID, Severity, Rule, Location, Claim, Code, Data flow,
                Why it is real, Suggested fix (in this order). -->
<!--   D-15 — Council slot text `_pending — run /council audit-review_` is the literal
                byte sequence Phase 15 greps for; do not reformat. -->
<!--   D-16 — markdownlint clean. -->

<!-- Discretion (per 14-CONTEXT.md): XML wrapper preferred for the full skeleton example
     (Phase 15 parser stability). Use `<output_format>` per `commands/audit-skip.md` style. -->

<!-- Analog patterns (per 14-PATTERNS.md): -->
<!--   `components/report-format.md` for top-level structure (H1 + purpose + ---). -->
<!--   `commands/audit-skip.md` style for prose + fenced bash/text blocks. -->
<!--   ` ```text ` for the report skeleton, ` ```yaml ` for frontmatter, ` ```markdown ` for the entry template. -->

<!-- 7 canonical type slugs (per 14-RESEARCH.md "Type Slug Mapping"): -->
<!--   security             -> SECURITY_AUDIT.md -->
<!--   code-review          -> CODE_REVIEW.md -->
<!--   performance          -> PERFORMANCE_AUDIT.md -->
<!--   deploy-checklist     -> DEPLOY_CHECKLIST.md -->
<!--   mysql-performance    -> MYSQL_PERFORMANCE_AUDIT.md -->
<!--   postgres-performance -> POSTGRES_PERFORMANCE_AUDIT.md -->
<!--   design-review        -> DESIGN_REVIEW.md -->

<!-- Extension -> fence map (per 14-RESEARCH.md "Verbatim Code Block Extraction"): -->
<!--   .ts/.tsx -> ts/tsx ;  .js/.jsx/.mjs/.cjs -> js ;  .py -> python ;
       .sh/.bash/.zsh -> bash ;  .rb -> ruby ;  .go -> go ;  .php -> php ;
       .md -> markdown ;  .yml/.yaml -> yaml ;  .json -> json ;  .toml -> toml ;
       .html/.htm -> html ;  .css/.scss/.sass -> css ;  .sql -> sql ;
       .rs -> rust ;  .java -> java ;  .kt/.kts -> kotlin ;  .swift -> swift ;
       unknown -> text -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/audit-output-format.md with the full report schema</name>
  <files>components/audit-output-format.md</files>
  <read_first>
    - components/audit-output-format.md (will not exist yet — confirm absence)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md (D-11 through D-16)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md (Sections "Verbatim Code Block Extraction", "Report Path Schema", "YAML Frontmatter Schema", "Council Verdict Slot", "Markdownlint Compliance for New Files")
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md (`components/audit-output-format.md` analog block + Em-Dash Literal section)
    - components/report-format.md (analog: top-level structure, type-specific differences pattern)
    - components/severity-levels.md (referenced from Severity field — do NOT redefine)
    - .markdownlint.json (active rules)
  </read_first>
  <action>
Create `components/audit-output-format.md` using the structure documented below. Strict requirements:

- Every fenced block has a language tag (` ```text `, ` ```yaml `, ` ```markdown `, ` ```bash `).
- Blank line before AND after every list and every fenced block (MD031/MD032).
- No trailing punctuation on H1/H2/H3 (MD026).
- The Council slot string `_pending — run /council audit-review_` is U+2014 em-dash, italic-wrapped, no backticks. Type the em-dash directly (not `&mdash;`, not `&#8212;`).
- The example heading in the Skipped table also uses U+2014 em-dash.
- Wrap the full report skeleton in an `<output_format>` XML block (Phase 15 parser-stability discretion per 14-CONTEXT.md).

Required sections in this exact order, with these exact H2/H3 headings:

1. `# Audit Output Format — Structured Report Schema` (H1, top of file)
2. One-paragraph intro stating: this is the SOT for `commands/audit.md` (Phase 14); Phase 16 splices this into 49 prompt files; Phase 15 Council reads the schema verbatim; every byte string is a contract.
3. `---` separator
4. `## Report Path` — fenced ` ```text ` block with the literal pattern `.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md`. Bullet list explaining `<type>`, timestamp generation (`date '+%Y-%m-%d-%H%M'`), `mkdir -p .claude/audits`, and the .gitignore note (NOT auto-added).
5. `## Type Slug to Prompt File Map` — markdown table with 3 columns (`/audit` argument, Report filename slug, Prompt loaded). One row per canonical slug — exactly 7 data rows. Below the table, prose noting backward-compat aliases (`code` -> `code-review`, `deploy` -> `deploy-checklist`); aliases resolve at dispatch time but the report filename ALWAYS uses the canonical slug.
6. `---` separator
7. `## YAML Frontmatter` — prose intro + ` ```yaml ` example block containing all 7 keys: `audit_type`, `timestamp`, `commit_sha`, `total_findings`, `skipped_allowlist`, `skipped_fp_recheck`, `council_pass`. Below the block, a bullet list documenting each field's semantics (audit_type = one of 7 slugs; timestamp = quoted YYYY-MM-DD-HHMM; commit_sha = `git rev-parse --short HEAD` or `none`; the three count fields are integers; council_pass starts at `pending` and Phase 15 mutates to `passed`/`failed`/`disputed`).
8. `---` separator
9. `## Section Order (Fixed)` — ordered list of the 6 sections in D-13 order: 1. YAML frontmatter, 2. `## Summary`, 3. `## Findings`, 4. `## Skipped (allowlist)`, 5. `## Skipped (FP recheck)`, 6. `## Council verdict`. Below the list, prose: do NOT reorder; do NOT add intermediate H2 sections; render empty sections with `_None_` placeholder; Phase 15 navigates by literal H2 headings.
10. `---` separator
11. `## Summary Section` — ` ```markdown ` example block showing the Summary table (columns: severity, count_reported, count_skipped_allowlist, count_skipped_fp_recheck) with a row per severity (CRITICAL/HIGH/MEDIUM/LOW). Below the block, a sentence pointing readers to `components/severity-levels.md` for the rubric and noting INFO is NOT a reportable finding severity (D-14).
12. `---` separator
13. `## Finding Entry Schema (### Finding F-NNN)` — prose: each surviving finding becomes an H3 block; F-NNN is zero-padded 3 digits, sequential per report. Then an ordered list (1-9) of the 9 fields in D-14 order, each formatted as `**FieldName** — short description`. The 9 field names MUST be exactly: ID, Severity, Rule, Location, Claim, Code, Data flow, Why it is real, Suggested fix. Below the list, an entry template inside ` ```markdown ` showing a fictional finding `F-001` with all 9 fields populated. The example finding's vulnerability category MUST be `SQL-INJECTION` and the example MUST trace user input flowing into a parameterized-query violation (e.g. `db.query("SELECT * FROM users WHERE id=" + req.params.id)`). Do NOT use any DOM-write API in the example. Use ` ```text ` fences for the inner code blocks within the markdown example to avoid nested-fence parsing issues. After the entry template, add a sentence noting the bullet labels (`**Severity:**`, `**Rule:**`, `**Location:**`, `**Claim:**`) and section labels (`**Code:**`, `**Data flow:**`, `**Why it is real:**`, `**Suggested fix:**`) are byte-exact and Phase 15 navigates the entry by them.
14. `---` separator
15. `## Verbatim Code Block (AUDIT-03)` — H3 sub-section `### Layout` showing in a ` ```text ` block:

    ```text
    <!-- File: <path> Lines: <start>-<end> -->
    [optional clamp note]
    \`\`\`<lang>
    [verbatim lines, byte-for-byte]
    \`\`\`
    ```

    Then prose explaining: `<lang>` per the Extension Map below; `start = max(1, L - 10)`; `end = min(T, L + 10)`; HTML range comment is FIRST line above the fence; clamp note is SECOND line above the fence (only when range was clipped).

    H3 `### Clamp Behaviour` with the clamp comment format and an example for L=5, T=8 (1-8 range, clamp note present).

    H3 `### Extension to Language Fence Map` — a 2-column table (Extension(s), Fence) listing every entry from the planning context. Closing row `_unknown_` -> `text`.

    Final paragraph: code block MUST be verbatim. No ellipses, no redaction, no `// ... rest of function` cuts.
16. `---` separator
17. `## Skipped (allowlist) Section` — ` ```markdown ` example block showing the table (columns: ID, path:line, rule, council_status) with one example row using slug `F-A001` and rule `SEC-EVAL`. Then a second ` ```markdown ` block for the empty-state placeholder `_None — no `audit-exceptions.md` in this project_`. Prose explaining: `council_status` is parsed from the matching entry's `**Council:**` bullet in `audit-exceptions.md`; allowed values are `unreviewed`, `council_confirmed_fp`, `disputed`; use `sed '/^<!--/,/^-->/d'` (per `commands/audit-restore.md` post-13-05 fix) to strip HTML comment blocks before walking the allowlist; `F-A001`..`F-ANNN` numbering is independent of `F-NNN` for surviving findings.
18. `---` separator
19. `## Skipped (FP recheck) Section` — ` ```markdown ` example block showing the table (columns: path:line, rule, dropped_at_step, one_line_reason). `dropped_at_step` MUST be 1-6. `one_line_reason` MUST be ≤ 100 chars and reference concrete tokens. Empty-state placeholder `_None_`.
20. `---` separator
21. `## Council Verdict Slot (handoff to Phase 15)` — prose: the audit writes this section as a literal placeholder; Phase 15's `/council audit-review` mutates it in place. Then a ` ```markdown ` block containing the `## Council verdict` H2 followed by a blank line and the placeholder `_pending — run /council audit-review_`. Below, a bullet list of byte-exact constraints: U+2014 em-dash, single-underscore italic, no backticks, no bold, no code fence, no trailing whitespace. Final sentence: DO NOT REFORMAT — Phase 15 greps for this exact byte sequence.
22. `---` separator
23. `## Full Report Skeleton` — `<output_format>` XML block (literal `<output_format>` open tag, blank line, ` ```text ` fence, the full report skeleton with placeholder values, ` ``` ` close, blank line, `</output_format>` close). The skeleton must include: YAML frontmatter (all 7 keys), `# <Type Title> Audit — <project name>` H1, all 6 H2 sections in fixed order, an example `### Finding F-001` block with all 9 D-14 fields (using SQL-INJECTION as the vulnerability category for the example claim — explicitly NOT any DOM-write API), the example Skipped (allowlist) row, the example Skipped (FP recheck) row, and the literal Council verdict placeholder.

Author notes:

- Inside the `<output_format>` skeleton's outer ` ```text ` fence, inner code blocks must be illustrated with placeholder text like `[fenced code block here]` rather than literal nested fences — nested ` ``` ` fences inside a ` ```text ` outer fence trip the markdown parser and may break MD040 detection.
- The em-dash in `_pending — run /council audit-review_`, in the example slug heading inside the allowlist table (`SEC-EVAL`), and in the H1 `Audit Output Format — Structured Report Schema` are all U+2014 (literal `—`).
- The `Suggested fix` example MUST be a SQL-INJECTION fix (e.g. switching from string concatenation to a parameterized query / prepared statement / Eloquent binding). Do NOT use any DOM-write API anywhere in the file.
- File should land at ~140-200 lines. If you go significantly over 220 lines, trim prose — the schema is the load-bearing content.
  </action>
  <verify>
    <automated>npx markdownlint-cli components/audit-output-format.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/audit-output-format.md` exits 0
    - `wc -l components/audit-output-format.md` returns N where 100 ≤ N ≤ 250
    - `grep -F '.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md' components/audit-output-format.md` matches (D-12 path verbatim)
    - `grep -F '_pending — run /council audit-review_' components/audit-output-format.md` matches (em-dash U+2014, byte-exact slot)
    - `grep -cE '^\| \`?(security|code-review|performance|deploy-checklist|mysql-performance|postgres-performance|design-review)' components/audit-output-format.md` returns ≥ 7 (one row per canonical slug in the type map)
    - `grep -F 'audit_type:' components/audit-output-format.md` matches (frontmatter key)
    - `grep -F 'timestamp:' components/audit-output-format.md` matches
    - `grep -F 'commit_sha:' components/audit-output-format.md` matches
    - `grep -F 'total_findings:' components/audit-output-format.md` matches
    - `grep -F 'skipped_allowlist:' components/audit-output-format.md` matches
    - `grep -F 'skipped_fp_recheck:' components/audit-output-format.md` matches
    - `grep -F 'council_pass:' components/audit-output-format.md` matches
    - `grep -F '## Summary' components/audit-output-format.md` matches
    - `grep -F '## Findings' components/audit-output-format.md` matches
    - `grep -F '## Skipped (allowlist)' components/audit-output-format.md` matches
    - `grep -F '## Skipped (FP recheck)' components/audit-output-format.md` matches
    - `grep -F '## Council verdict' components/audit-output-format.md` matches
    - `grep -cE '^[0-9]+\. \*\*(ID|Severity|Rule|Location|Claim|Code|Data flow|Why it is real|Suggested fix)\*\*' components/audit-output-format.md` returns 9 (the 9 D-14 fields in order, as a numbered list)
    - `grep -F '<!-- File:' components/audit-output-format.md` matches at least once (D-11 range comment header)
    - `grep -F 'Range clamped to file bounds' components/audit-output-format.md` matches (D-11 clamp note)
    - `grep -F '<output_format>' components/audit-output-format.md` matches (XML wrapper present)
    - `grep -F 'severity-levels' components/audit-output-format.md` matches (reference to existing rubric — do not redefine)
    - `grep -F "sed '/^<!--/,/^-->/d'" components/audit-output-format.md` matches (HTML comment stripping pattern reference)
    - `npx markdownlint-cli components/audit-output-format.md` exit code 0
  </acceptance_criteria>
  <done>
    `components/audit-output-format.md` exists with: report path pattern, 7-slug type map, YAML frontmatter schema with all 7 fields, fixed section order (Summary -> Findings -> Skipped (allowlist) -> Skipped (FP recheck) -> Council verdict), 9-field finding entry schema in D-14 order, verbatim code block layout with extension-to-language map, byte-exact Council slot string, and full report skeleton wrapped in `<output_format>`. Passes `npx markdownlint-cli`. All grep-based acceptance criteria pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit prompt -> audit report | Claude writes the report into `.claude/audits/`. The `<type>` slug is from a fixed enum in `commands/audit.md`'s dispatcher, never user-supplied free text. |
| Audit report -> Phase 15 Council | Council parser reads the structured report. The byte-exact `_pending — run /council audit-review_` slot is the only mutation point Phase 15 owns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Report path slug | mitigate | `<type>` is a 7-element fixed enum documented in this component AND enforced in `commands/audit.md` (Plan 14-03) dispatch. Path traversal via `<type>` is impossible because the slug never carries `/`, `..`, or unsanitized user input. |
| T-14-02 | Spoofing | Council verdict slot | mitigate | Phase 15 parser reads ONLY the `## Council verdict` H2 section and rewrites the placeholder. Markdown injection in earlier sections (e.g. attacker controls a finding's `Claim` field with markdown) is harmless because the verdict-replacement logic anchors on `## Council verdict` heading, not free text. This contract is documented here so Phase 15 implementer cannot accidentally widen the parser. |
| T-14-05 | Tampering | timestamp field | accept | Local time per D-12. A stale system clock produces a stale filename; the report content is correct. Documented as local (not UTC) so users running CI under containers know to set TZ if they want UTC. Plan 14-04 fixture asserts the regex pattern, not a specific timestamp. |
</threat_model>

<verification>
- `npx markdownlint-cli components/audit-output-format.md` exits 0
- `make check` (full lint suite) still passes — no regression
- All acceptance_criteria grep patterns match (byte-exact strings for Phase 15 contract)
</verification>

<success_criteria>
- `components/audit-output-format.md` exists, ~100-250 lines
- All 7 YAML frontmatter fields documented (D-13)
- All 6 sections in fixed order documented (D-13)
- All 9 finding-entry fields in D-14 order present as a numbered list
- Verbatim code block format with extension-to-language map (D-11, AUDIT-03)
- 7 canonical type slugs in the slug map (D-12)
- Council slot string `_pending — run /council audit-review_` byte-exact (D-15)
- Full skeleton wrapped in `<output_format>` (planner discretion + 14-PATTERNS analog)
- Reference to `components/severity-levels.md` (no rubric duplication)
- Reference to comment-stripping pattern (`sed '/^<!--/,/^-->/d'`) for allowlist walks
- File passes markdownlint
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-02-SUMMARY.md`
</output>
