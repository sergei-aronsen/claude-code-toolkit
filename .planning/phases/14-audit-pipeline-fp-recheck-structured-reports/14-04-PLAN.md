---
phase: 14-audit-pipeline-fp-recheck-structured-reports
plan: 04
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
  - "14-03"
files_modified:
  - scripts/tests/test-audit-pipeline.sh
  - scripts/tests/fixtures/audit/allowlist-populated.md
  - scripts/tests/fixtures/audit/allowlist-empty.md
  - scripts/tests/fixtures/audit/sample-project/src/auth.ts
  - scripts/tests/fixtures/audit/sample-project/lib/utils.py
  - Makefile
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
must_haves:
  truths:
    - "scripts/tests/test-audit-pipeline.sh exists and runs end-to-end (exit 0 on green, exit 1 on any failure)"
    - "scripts/tests/fixtures/audit/ contains allowlist-populated.md, allowlist-empty.md, and sample-project/ with at least 2 source files"
    - "Test asserts the allowlist parser extracts the correct path:line:rule triple from the populated fixture"
    - "Test asserts the HTML comment-stripping pattern prevents the seeded example heading from satisfying the match — Pitfall 3 regression guard"
    - "Test asserts the em-dash separator in the fixture heading is U+2014 (byte-exact)"
    - "Test asserts the report filename matches the regex `^[a-z-]+-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{4}\\.md$` (D-12 schema)"
    - "Test asserts a mock report contains all 7 YAML frontmatter keys (audit_type, timestamp, commit_sha, total_findings, skipped_allowlist, skipped_fp_recheck, council_pass)"
    - "Test asserts the byte-exact Council slot string `_pending — run /council audit-review_` is present in a mock report"
    - "Test asserts dropped_at_step values are integers in range 1-6"
    - "Test asserts components/audit-fp-recheck.md and components/audit-output-format.md exist (so the SOT components shipped by Plans 14-01 and 14-02 are reachable from the test environment)"
    - "Test asserts commands/audit.md contains the 6-phase headings, the canonical slug list, and the comment-stripping pattern (regression guard for Plan 14-03)"
    - "Makefile Test 17 added immediately before the final 'All tests passed!' line"
    - "make test runs the new test script and includes 'Test 17: audit pipeline' in its output"
  artifacts:
    - path: "scripts/tests/test-audit-pipeline.sh"
      provides: "Schema regression test for the audit pipeline (allowlist parser + report schema)"
      contains: "PASS=0"
      min_lines: 120
    - path: "scripts/tests/fixtures/audit/allowlist-populated.md"
      provides: "Fixture: audit-exceptions.md with 1 real entry + the seeded HTML-commented example"
      contains: "## Entries"
      min_lines: 30
    - path: "scripts/tests/fixtures/audit/allowlist-empty.md"
      provides: "Fixture: audit-exceptions.md with frontmatter + ## Entries heading and no real entries"
      contains: "## Entries"
      min_lines: 10
    - path: "scripts/tests/fixtures/audit/sample-project/src/auth.ts"
      provides: "Canned TypeScript source with a realistic SQL-injection finding around line 8"
      min_lines: 20
    - path: "scripts/tests/fixtures/audit/sample-project/lib/utils.py"
      provides: "Canned Python source with an allowlist-suppressed finding around line 5"
      min_lines: 15
    - path: "Makefile"
      provides: "Test 17 wiring"
      contains: "Test 17:"
  key_links:
    - from: "scripts/tests/test-audit-pipeline.sh"
      to: "scripts/tests/fixtures/audit/allowlist-populated.md"
      via: "FIXTURE_DIR/allowlist-populated.md"
      pattern: "allowlist-populated\\.md"
    - from: "scripts/tests/test-audit-pipeline.sh"
      to: "components/audit-fp-recheck.md"
      via: "presence assertion"
      pattern: "audit-fp-recheck\\.md"
    - from: "scripts/tests/test-audit-pipeline.sh"
      to: "components/audit-output-format.md"
      via: "presence assertion"
      pattern: "audit-output-format\\.md"
    - from: "scripts/tests/test-audit-pipeline.sh"
      to: "commands/audit.md"
      via: "regression guards (6-phase headings, slug list, parser pattern)"
      pattern: "commands/audit\\.md"
    - from: "Makefile"
      to: "scripts/tests/test-audit-pipeline.sh"
      via: "Test 17 invocation"
      pattern: "test-audit-pipeline\\.sh"
---

<objective>
Create a Bash regression test fixture and runner that asserts the schema contracts shipped by Plans 14-01, 14-02, and 14-03 — and wire it as Test 17 in the Makefile. This is the regression gate for the audit pipeline. Because the pipeline is prompt-driven (Claude executes it at runtime; no installer-side compilation), the test cannot run a full audit; instead it asserts every deterministic schema invariant that Phase 15 and Phase 16 will depend on.

Purpose: Lock the byte-exact contracts. Any future PR that drifts the Council slot string, the YAML frontmatter keys, the allowlist comment-stripping pattern, the em-dash separator, the 6-phase headings, or the canonical slug list will fail this test before merging.

Output: 6 files (5 created, 1 modified) — test runner + 4 fixture files + Makefile edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-VALIDATION.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-01-PLAN.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-02-PLAN.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-03-PLAN.md
@scripts/tests/test-setup-security-rtk.sh
@scripts/tests/test-backup-lib.sh
@templates/base/rules/audit-exceptions.md
@commands/audit-skip.md
@Makefile
@CLAUDE.md

<interfaces>
<!-- Test analog: scripts/tests/test-setup-security-rtk.sh (closest match per 14-PATTERNS.md): -->
<!--   - shebang + header (lines 1-7) -->
<!--   - set -euo pipefail (line 8) -->
<!--   - REPO_ROOT=$(cd "$(dirname "$0")/../.." && pwd) (line 10) -->
<!--   - SCRATCH=$(mktemp -d ...) ; trap 'rm -rf "$SCRATCH"' EXIT (lines 17-18) -->
<!--   - PASS=0 / FAIL=0 + report_pass / report_fail helpers (lines 20-23) -->
<!--   - assert_contains style from test-backup-lib.sh:33-42 is also acceptable -->
<!--   - Results + exit (lines 103-108) -->

<!-- Fixture layout (per 14-PATTERNS.md `scripts/tests/fixtures/audit/` analog block): -->
<!--   scripts/tests/fixtures/audit/ -->
<!--     allowlist-populated.md      # audit-exceptions.md with 1 real entry + seeded HTML-commented example -->
<!--     allowlist-empty.md          # audit-exceptions.md with frontmatter + ## Entries + no real entries -->
<!--     sample-project/ -->
<!--       src/auth.ts              # >=20-line TypeScript file (deliberately-vulnerable SQL pattern) -->
<!--       lib/utils.py             # >=15-line Python file (deliberately-vulnerable dynamic-code pattern) -->

<!-- Coverage Dimensions (per 14-VALIDATION.md): -->
<!--   1. Allowlist match           - findings in audit-exceptions.md land in ## Skipped (allowlist) -->
<!--   2. FP recheck drop           - findings dropped at any of 6 steps land in ## Skipped (FP recheck) -->
<!--   3. Surviving finding         - findings that survive render full schema (D-14) -->
<!--   4. Code block fence          - verbatim 10-lines-each-side with language fence + range comment -->
<!--   5. Report path schema        - .claude/audits/<type>-<YYYY-MM-DD-HHMM>.md -->
<!--   6. Council verdict slot      - literal `_pending — run /council audit-review_` -->
<!--   7. Frontmatter YAML          - 7 required keys present -->

<!-- Em-dash separator: U+2014 (literal `—`). Test must verify byte integrity (od -c or
     `python3 -c` codepoint check on the fixture's `### path:line — rule` heading). -->

<!-- Heading format from commands/audit-skip.md Step 4: -->
<!--   HEADING="### ${PATH_PART}:${LINE_PART} — ${RULE}" -->

<!-- Existing audit-exceptions.md template anatomy (per templates/base/rules/audit-exceptions.md):
       frontmatter open (---), description, globs, frontmatter close (---), H1, intro paragraph,
       ## Entries H2, then the seeded HTML-commented example block enclosed in <!- - ... - ->. -->

<!-- Makefile Test 17 placement: insert before the final `@echo "All tests passed!"` (line 102 in
     current Makefile). The pattern is two lines (`@echo "Test 17: ..."` + `@bash scripts/tests/test-audit-pipeline.sh`)
     followed by `@echo ""` separator. Use literal TAB (not spaces) — Makefile recipe rules require it. -->

<!-- IMPORTANT — fixture file content notes for the executor: -->
<!--   The Python fixture (lib/utils.py) intentionally contains a dynamic-code-execution pattern -->
<!--   that pattern-matches the SEC-DYNAMIC-EXEC rule, used to demonstrate allowlist suppression. -->
<!--   The user's environment may surface a security-hook reminder when committing this file — -->
<!--   that is expected and acceptable; the file is a TEST FIXTURE deliberately built to be -->
<!--   flagged by the audit pipeline. Document this in a header comment so reviewers do not -->
<!--   "fix" the deliberately-vulnerable code. The TypeScript fixture (src/auth.ts) similarly -->
<!--   contains a SQL-string-concatenation pattern that pattern-matches SEC-SQL-INJECTION. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the audit fixture tree (4 fixture files)</name>
  <files>
    scripts/tests/fixtures/audit/allowlist-populated.md,
    scripts/tests/fixtures/audit/allowlist-empty.md,
    scripts/tests/fixtures/audit/sample-project/src/auth.ts,
    scripts/tests/fixtures/audit/sample-project/lib/utils.py
  </files>
  <read_first>
    - templates/base/rules/audit-exceptions.md (the seed file the fixtures imitate — note frontmatter, intro, ## Entries H2, HTML-commented example block)
    - commands/audit-skip.md Step 4 (HEADING format and U+2014 em-dash)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md (`scripts/tests/fixtures/audit/` analog block)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md (Section "Test Fixture Pattern")
  </read_first>
  <action>
Create the fixture tree. Use absolute paths from the repo root.

**File 1: `scripts/tests/fixtures/audit/allowlist-populated.md`** — a populated `audit-exceptions.md` clone. MUST include:

- YAML frontmatter (`---`, `description: ...`, `globs: ...` with `"**/*"`, `---`)
- `# Audit Exceptions — False-Positive Allowlist` H1
- One-paragraph intro paragraph (any short prose)
- `## Entries` H2
- A real entry block with this exact heading and bullet list:

  - Heading line: `### lib/utils.py:5 — SEC-DYNAMIC-EXEC` (em-dash is U+2014)
  - Followed by a blank line, then three bullets: `- **Date:** 2026-04-25`, `- **Council:** unreviewed`, `- **Reason:** dynamic-exec pattern is build-time codegen; never reached at request time, sandbox-safe by construction`

- An HTML-commented example block AFTER the real entry. The block contents MUST mirror `templates/base/rules/audit-exceptions.md` lines 15-25 byte-for-byte: an opening `<!--` on its own line, then example prose, an example heading `### scripts/setup-security.sh:142 — SEC-RAW-EXEC` (em-dash U+2014), example bullets (Date / Council / Reason), an "Allowed Council values: unreviewed | council_confirmed_fp | disputed" sentence, and a closing `-->` on its own line.

The em-dash in BOTH the real entry's heading and the example block heading MUST be U+2014 (literal `—`). Type it directly. Total lines ~30-50.

**File 2: `scripts/tests/fixtures/audit/allowlist-empty.md`** — same shape as File 1 but with NO real entries. Includes:

- Frontmatter
- H1 + intro paragraph
- `## Entries` H2
- The HTML-commented example block ONLY (no real entry above it)

This fixture exercises the empty-state path (D-06: when no real entries are present, the audit proceeds with an empty allowlist).

**File 3: `scripts/tests/fixtures/audit/sample-project/src/auth.ts`** — a ≥20-line TypeScript file containing a plausible-looking finding around line 8. Use a SQL-string-concatenation pattern. The file MUST start with a comment block stating: this is an audit-pipeline regression-test fixture; the SQL string concatenation on line ~8 is INTENTIONAL — do not "fix" it. Then implement an Express handler that takes `req.params.id`, builds a SQL string with `+` concatenation, and passes it to a `db.query` call. Add a second harmless route (e.g. `/healthz`) so the file has body beyond the vulnerable line. Add a `module.exports` or `export { app }` at the bottom. File size: 20-30 lines.

**File 4: `scripts/tests/fixtures/audit/sample-project/lib/utils.py`** — a ≥15-line Python file containing the allowlist-suppressed finding at line 5 (`SEC-DYNAMIC-EXEC` — matches the entry in `allowlist-populated.md`). The file MUST start with a docstring stating: this is an audit-pipeline regression-test fixture; the dynamic-execution call on line ~5 is INTENTIONAL — do not "fix" it. Then implement a tiny build-time codegen helper that pattern-matches the SEC-DYNAMIC-EXEC rule (any small dynamic-execution pattern recognised by audit prompts; the deliberate non-safe call is what makes this a useful regression fixture for the allowlist path). Wrap with an `if __name__ == "__main__"` guard at the bottom. File size: 15-25 lines.

Both source fixtures MUST carry leading comments explaining the deliberate vulnerability so reviewers and security hooks understand the intent. After creating all 4 files, run `npx markdownlint-cli scripts/tests/fixtures/audit/allowlist-*.md` to confirm both markdown fixtures pass lint. The two source fixtures (`auth.ts`, `utils.py`) are not markdown and are not subject to markdownlint.

**Heads-up for the executor:** the user's local security hook may print a reminder when these fixture files are created (because they pattern-match dangerous-code patterns). The reminder is expected and does not block the write — proceed past the warning. Document the deliberate intent in the header comments so the next reviewer understands the file's purpose.
  </action>
  <verify>
    <automated>test -f scripts/tests/fixtures/audit/allowlist-populated.md && test -f scripts/tests/fixtures/audit/allowlist-empty.md && test -f scripts/tests/fixtures/audit/sample-project/src/auth.ts && test -f scripts/tests/fixtures/audit/sample-project/lib/utils.py && npx markdownlint-cli scripts/tests/fixtures/audit/allowlist-populated.md scripts/tests/fixtures/audit/allowlist-empty.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/tests/fixtures/audit/allowlist-populated.md` exits 0
    - `test -f scripts/tests/fixtures/audit/allowlist-empty.md` exits 0
    - `test -f scripts/tests/fixtures/audit/sample-project/src/auth.ts` exits 0
    - `test -f scripts/tests/fixtures/audit/sample-project/lib/utils.py` exits 0
    - `grep -F '### lib/utils.py:5 — SEC-DYNAMIC-EXEC' scripts/tests/fixtures/audit/allowlist-populated.md` matches (real entry heading, byte-exact em-dash)
    - `grep -F '### scripts/setup-security.sh:142 — SEC-RAW-EXEC' scripts/tests/fixtures/audit/allowlist-populated.md` matches (HTML-commented example heading, byte-exact em-dash)
    - `grep -F '## Entries' scripts/tests/fixtures/audit/allowlist-populated.md` matches
    - `grep -F '## Entries' scripts/tests/fixtures/audit/allowlist-empty.md` matches
    - `! grep -E '^### [^<]' scripts/tests/fixtures/audit/allowlist-empty.md` returns success (no real H3 entries outside HTML comments)
    - `wc -l scripts/tests/fixtures/audit/sample-project/src/auth.ts` returns N where N ≥ 20
    - `wc -l scripts/tests/fixtures/audit/sample-project/lib/utils.py` returns N where N ≥ 15
    - `npx markdownlint-cli scripts/tests/fixtures/audit/allowlist-populated.md scripts/tests/fixtures/audit/allowlist-empty.md` exit 0
    - `python3 -c "data = open('scripts/tests/fixtures/audit/allowlist-populated.md').read(); assert chr(0x2014) in data, 'em-dash U+2014 missing'"` exits 0 (em-dash byte present)
    - `head -5 scripts/tests/fixtures/audit/sample-project/src/auth.ts | grep -iF 'fixture'` matches (deliberate-fixture comment present in header)
    - `head -5 scripts/tests/fixtures/audit/sample-project/lib/utils.py | grep -iF 'fixture'` matches (deliberate-fixture comment present in header)
  </acceptance_criteria>
  <done>
    All 4 fixture files exist. The populated allowlist contains 1 real entry + the HTML-commented example, both with byte-exact U+2014 em-dashes. The empty allowlist contains only the example block. The two source files have realistic deliberate-vulnerability patterns (SQL-injection in TS, dynamic-exec in Py) at the expected line numbers and explanatory header comments. Markdown fixtures pass markdownlint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/tests/test-audit-pipeline.sh with the schema regression assertions</name>
  <files>scripts/tests/test-audit-pipeline.sh</files>
  <read_first>
    - scripts/tests/test-audit-pipeline.sh (will not exist yet — confirm absence)
    - scripts/tests/test-setup-security-rtk.sh (closest analog per 14-PATTERNS.md — copy header, set, REPO_ROOT, SCRATCH/trap, PASS/FAIL counter, results-and-exit pattern)
    - scripts/tests/test-backup-lib.sh lines 33-42 (assert_contains helper if you choose to use it)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-VALIDATION.md (the 7 Coverage Dimensions every test must cover)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md (`scripts/tests/test-audit-pipeline.sh` analog block — full structure)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md (D-13 frontmatter keys, D-15 Council slot)
    - components/audit-fp-recheck.md (assert presence, no parsing)
    - components/audit-output-format.md (assert presence, grep for byte-exact contracts)
    - commands/audit.md (regression guards on 6-phase headings, slug list, comment-stripping pattern)
  </read_first>
  <action>
Create `scripts/tests/test-audit-pipeline.sh` following the analog from `test-setup-security-rtk.sh`. Make it executable in the verify step. Structure (the literal Bash skeleton below is the implementation contract — fill in the test groups; do not redesign the harness):

**Header (lines 1-7):** shebang + 5-line comment block stating: filename, what it validates, usage, exit codes (0 = all pass, 1 = any fail).

**Setup (lines 8-22):** `set -euo pipefail`; `REPO_ROOT=$(cd "$(dirname "$0")/../.." && pwd)`; `FIXTURE_DIR="$REPO_ROOT/scripts/tests/fixtures/audit"`; `COMPONENTS_DIR="$REPO_ROOT/components"`; `COMMANDS_DIR="$REPO_ROOT/commands"`. Verify `[ -d "$FIXTURE_DIR" ]` or fail-fast with a clear error. Create `SCRATCH=$(mktemp -d ...)` with `trap 'rm -rf "$SCRATCH"' EXIT`. Initialize `PASS=0; FAIL=0` and the helpers `report_pass()` / `report_fail()` exactly as in test-setup-security-rtk.sh lines 20-23.

**Test Group 1 — SOT Component Existence Guards (covers Coverage Dimension 1):**

For each of `$COMPONENTS_DIR/audit-fp-recheck.md` and `$COMPONENTS_DIR/audit-output-format.md`: assert file exists, report_pass on success or report_fail on absence.

**Test Group 2 — components/audit-fp-recheck.md schema (Coverage Dimension 2):**

- Count `^[0-9]+\\. \\*\\*` matches; expect 6 (six numbered ordered-list items)
- Each step label present: grep `**Read context**`, `**Trace data flow**`, `**Check execution context**`, `**Cross-reference exceptions**`, `**Apply platform-constraint rule**`, `**Severity sanity check**` — six separate report_pass/report_fail
- `## Skipped (FP recheck)` heading present
- `dropped_at_step` column present (column header in table)

**Test Group 3 — components/audit-output-format.md schema (Coverage Dimensions 3-7):**

- Path pattern `.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md` present (D-12)
- All 7 YAML frontmatter keys present: loop over the list `audit_type timestamp commit_sha total_findings skipped_allowlist skipped_fp_recheck council_pass`, grep each, report individually
- All 6 H2 sections present in fixed order — extract H2 headings with `grep '^## '` from the SOT skeleton block and verify the sequence Summary -> Findings -> Skipped (allowlist) -> Skipped (FP recheck) -> Council verdict using awk to capture order. Report a single PASS if the order is correct, FAIL otherwise (print the actual sequence on failure).
- All 7 canonical slugs present (loop: security code-review performance deploy-checklist mysql-performance postgres-performance design-review)
- 9-field finding entry numbered list: count `^[0-9]+\\. \\*\\*(ID|Severity|Rule|Location|Claim|Code|Data flow|Why it is real|Suggested fix)\\*\\*` matches, expect exactly 9
- Verbatim code block format header `<!-- File:` present
- Clamp note `Range clamped to file bounds` present
- Council slot byte-exact: `grep -F '_pending — run /council audit-review_'`

**Test Group 4 — commands/audit.md regression guards:**

- 6-phase headings present: loop over `Phase 0 Phase 1 Phase 2 Phase 3 Phase 4 Phase 5`, grep `^### Phase N` for each, report individually
- Allowlist file path present: `.claude/rules/audit-exceptions.md`
- Comment-stripping pattern present: `sed '/^<!--/,/^-->/d'`
- All 7 canonical slugs present (same loop as Group 3)
- Backward-compat aliases present: grep for both `code` and `deploy` in alias context
- All 5 AUDIT-XX traceability comments present (loop: AUDIT-01 AUDIT-02 AUDIT-03 AUDIT-04 AUDIT-05)
- Council handoff present: `/council audit-review`
- Related Commands extended: `/audit-skip` and `/audit-restore`
- No orphan close fence with `text` tag: `! grep -E '^\`\`\`text$' "$COMMANDS_DIR/audit.md"`

**Test Group 5 — Allowlist parser regression (Coverage Dimension 1: AUDIT-01 + Pitfall 3):**

```bash
EXC_FILE="$FIXTURE_DIR/allowlist-populated.md"
STRIPPED="$SCRATCH/stripped.md"

sed '/^<!--/,/^-->/d' "$EXC_FILE" > "$STRIPPED"

# Real entry heading must survive the strip.
if grep -Fxq -- "### lib/utils.py:5 — SEC-DYNAMIC-EXEC" "$STRIPPED"; then
    report_pass "Allowlist parser: real entry heading present after comment strip"
else
    report_fail "Allowlist parser: real entry heading lost during comment strip"
fi

# HTML-commented example heading must NOT survive the strip — Pitfall 3 regression guard.
if ! grep -Fxq -- "### scripts/setup-security.sh:142 — SEC-RAW-EXEC" "$STRIPPED"; then
    report_pass "Allowlist parser: HTML-commented example heading correctly stripped (Pitfall 3 guard)"
else
    report_fail "Allowlist parser: HTML-commented example heading leaked through (Pitfall 3 regression)"
fi

# Extracted triple matches expected.
EXTRACTED="$(grep '^### ' "$STRIPPED" | head -1)"
EXPECTED='### lib/utils.py:5 — SEC-DYNAMIC-EXEC'
if [ "$EXTRACTED" = "$EXPECTED" ]; then
    report_pass "Allowlist parser: extracted heading matches expected triple"
else
    report_fail "Allowlist parser: extracted '$EXTRACTED' != expected '$EXPECTED'"
fi
```

**Test Group 6 — Em-dash byte integrity (Coverage Dimension: D-07):**

Use `python3 -c` to read the populated fixture, find the `### lib/utils.py:5` heading line, and assert the separator is U+2014 (codepoint 8212). On exit code 0, report_pass; otherwise report_fail with the actual codepoint observed.

**Test Group 7 — Report filename regex (Coverage Dimension 5: AUDIT-04):**

Generate `TIMESTAMP="$(date '+%Y-%m-%d-%H%M')"`, then loop over the 7 canonical slugs and for each construct `FILENAME="${SLUG}-${TIMESTAMP}.md"`. Assert each matches `^[a-z-]+-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{4}\\.md$`. Report individually.

**Test Group 8 — Mock report YAML frontmatter (Coverage Dimension 7: AUDIT-04):**

Build a mock report file in `$SCRATCH` with the full skeleton (use a heredoc), then loop over the 7 expected keys and grep each with `^${KEY}:` anchored. Report each individually.

**Test Group 9 — Council slot byte-exact (Coverage Dimension 6: D-15):**

`grep -Fxq '_pending — run /council audit-review_'` against the mock report. The em-dash in this string MUST be U+2014. Report PASS / FAIL accordingly.

**Test Group 10 — dropped_at_step range (Coverage Dimension 2: D-09):**

Mock a Skipped (FP recheck) row inline (any path/rule/reason that pattern-matches a real candidate). Extract the `dropped_at_step` column with awk on `|` separators and verify the value is an integer in the range 1..6. Report PASS / FAIL.

**Final block:** `echo ""`, `echo "Results: $PASS passed, $FAIL failed"`, `if [[ $FAIL -gt 0 ]]; then exit 1; fi`.

**File should land at ~180-260 lines.** After writing, `chmod +x scripts/tests/test-audit-pipeline.sh`.

**Shellcheck:** the script must pass `shellcheck -S warning` (the project's CI level per `Makefile` line 34). Common pitfalls: quote all variable expansions, use `[[ ]]` consistently with sibling tests, use `printf` over `echo -e`, use `IFS= read -r` when reading lines.
  </action>
  <verify>
    <automated>chmod +x scripts/tests/test-audit-pipeline.sh && shellcheck -S warning scripts/tests/test-audit-pipeline.sh && bash scripts/tests/test-audit-pipeline.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/tests/test-audit-pipeline.sh` exits 0 (executable bit set)
    - `shellcheck -S warning scripts/tests/test-audit-pipeline.sh` exits 0
    - `wc -l scripts/tests/test-audit-pipeline.sh` returns N where 120 ≤ N ≤ 320
    - `bash scripts/tests/test-audit-pipeline.sh` exits 0 (all internal assertions pass)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -F 'Results:'` matches (results line printed)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -E 'PASS:.*real entry heading present after comment strip'` matches (Test Group 5 assertion A green)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -E 'PASS:.*HTML-commented example heading correctly stripped'` matches (Pitfall 3 regression guard green)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -iE 'PASS:.*em-dash'` matches (em-dash byte integrity assertion green)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -E 'PASS:.*Council slot string byte-exact'` matches (D-15 contract green)
    - `bash scripts/tests/test-audit-pipeline.sh 2>&1 | grep -cE '^PASS:'` returns ≥ 25 (the test groups together produce at least 25 PASS lines: 2 component-existence + 6 step labels + 7 slug entries + 7 frontmatter keys + others)
    - `grep -F 'set -euo pipefail' scripts/tests/test-audit-pipeline.sh` matches
    - `grep -F 'trap' scripts/tests/test-audit-pipeline.sh` matches (cleanup trap present)
    - `grep -F 'PASS=0' scripts/tests/test-audit-pipeline.sh` matches (counter initialized)
    - `grep -F 'FAIL=0' scripts/tests/test-audit-pipeline.sh` matches
    - `grep -F "sed '/^<!--/,/^-->/d'" scripts/tests/test-audit-pipeline.sh` matches (canonical comment-strip pattern used)
    - `grep -F '_pending — run /council audit-review_' scripts/tests/test-audit-pipeline.sh` matches (em-dash literal in the script)
  </acceptance_criteria>
  <done>
    `scripts/tests/test-audit-pipeline.sh` exists, is executable, passes shellcheck at warning level, and exits 0 when run. The script asserts: (1) both SOT components exist; (2) audit-fp-recheck.md has 6 numbered steps with correct labels; (3) audit-output-format.md has all 7 frontmatter keys, all 6 H2 sections in order, all 7 canonical slugs, the 9-field entry schema, the verbatim code block markers, and the byte-exact Council slot; (4) commands/audit.md has all 6 phase headings, the parser pattern, all 7 slugs, both aliases, all 5 AUDIT-XX traceability comments, and the Related Commands extension; (5) the allowlist parser correctly extracts the real entry while stripping the HTML-commented example (Pitfall 3 regression guard); (6) em-dash is U+2014 byte-exact in the fixture; (7) the report filename regex matches all 7 canonical slugs; (8) a mock report has all 7 YAML frontmatter keys; (9) the Council slot string is byte-exact; (10) dropped_at_step is in range 1-6.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Test 17 to Makefile</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (read lines 95-105 — current Test 16 + 'All tests passed!' tail; need to splice Test 17 before the tail)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md (`Makefile` analog block — Test 16 pattern)
    - scripts/tests/test-audit-pipeline.sh (the script Test 17 invokes — must exist before this edit)
  </read_first>
  <action>
Insert Test 17 into `Makefile`'s `test:` target. The current tail (lines 99-102) is:

```makefile
	@echo "Test 16: full install matrix"
	@bash scripts/tests/test-matrix.sh
	@echo ""
	@echo "All tests passed!"
```

Splice Test 17 between the `@echo ""` blank-separator line (currently line 101) and the `@echo "All tests passed!"` (currently line 102). The new tail must read:

```makefile
	@echo "Test 16: full install matrix"
	@bash scripts/tests/test-matrix.sh
	@echo ""
	@echo "Test 17: audit pipeline fixture — allowlist match + FP schema"
	@bash scripts/tests/test-audit-pipeline.sh
	@echo ""
	@echo "All tests passed!"
```

**Critical formatting:** Each recipe line begins with a literal TAB character, not spaces. Verify with `cat -A Makefile` that the new lines start with `^I` (TAB). The em-dash in the `Test 17:` description is U+2014 (literal `—`).

No `.PHONY` change is needed — Test 17 is inline in the existing `test:` target, not a new standalone target.

**Verification step inside this task:** after the edit, run `make -n test 2>&1 | grep 'Test 17'` first to confirm the target body includes the new lines without executing all 17 tests; then run the full `make test` to confirm green.
  </action>
  <verify>
    <automated>grep -F 'Test 17: audit pipeline fixture' Makefile && grep -F 'bash scripts/tests/test-audit-pipeline.sh' Makefile && make -n test 2>&1 | grep -F 'Test 17:'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Test 17: audit pipeline fixture' Makefile` matches
    - `grep -F 'bash scripts/tests/test-audit-pipeline.sh' Makefile` matches
    - `cat -A Makefile | grep -F 'Test 17'` shows the line starts with `^I` (TAB character) — Makefile recipe rule
    - `grep -B1 -A1 'All tests passed!' Makefile | grep -F 'test-audit-pipeline.sh'` matches (Test 17 inserted immediately before the closing 'All tests passed!')
    - `make -n test 2>&1 | grep -F 'Test 17:'` matches (dry-run target body includes the new lines)
    - `make test 2>&1 | grep -F 'Test 17: audit pipeline'` matches (full test run includes the new test execution; this is the load-bearing assertion)
    - `make test` exits 0 (all 17 tests pass — no regression of the existing 16)
  </acceptance_criteria>
  <done>
    Makefile's `test:` target now invokes `scripts/tests/test-audit-pipeline.sh` as Test 17, slotted between Test 16 and the closing 'All tests passed!' echo. Both lines use literal TAB indentation. `make test` runs all 17 tests and exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture -> test runner | `scripts/tests/fixtures/audit/` files are consumed read-only by the test script. No execution. |
| Test runner -> SOT components | Test reads `components/audit-fp-recheck.md` and `components/audit-output-format.md` to assert byte-exact contracts. Read-only. |
| Test runner -> commands/audit.md | Test reads `commands/audit.md` to assert regression guards. Read-only. |
| Test runner -> mock report in $SCRATCH | Test writes a mock report into a `mktemp -d` SCRATCH dir, asserts schema, and the `trap` cleans it up on exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | Allowlist comment-strip parser | mitigate | Test Group 5 asserts that the canonical `sed '/^<!--/,/^-->/d'` pattern correctly strips the seeded HTML-commented example block while preserving the real entry. This is the explicit Pitfall 3 regression guard from 14-RESEARCH.md. If a future PR drifts the pattern, this test fails before merge. |
| T-14-05 | Tampering | Date / timestamp | accept | Test Group 7 asserts the regex pattern only, not a specific timestamp value. Local clock drift cannot fail the test. The `date '+%Y-%m-%d-%H%M'` invocation is POSIX-portable; format specifiers `%Y`, `%m`, `%d`, `%H`, `%M` are locale-safe (always numeric). |
| T-14-02 | Spoofing | Council verdict slot drift | mitigate | Test Group 9 uses `grep -Fxq` against the byte-exact slot string `_pending — run /council audit-review_`. Any future drift (changing em-dash to hyphen, adding backticks, removing italic) fails the test. |
</threat_model>

<verification>
- `bash scripts/tests/test-audit-pipeline.sh` exits 0
- `shellcheck -S warning scripts/tests/test-audit-pipeline.sh` exits 0
- `make test` exits 0 (all 17 tests pass — Test 17 wired correctly, no regression of existing 16)
- `make check` exits 0 (markdownlint of fixtures passes; no regression)
- All acceptance_criteria across all 3 tasks pass
</verification>

<success_criteria>
- Fixture tree exists: 4 files under `scripts/tests/fixtures/audit/`
- `scripts/tests/test-audit-pipeline.sh` exists, is executable, passes shellcheck, exits 0
- Test asserts every Coverage Dimension from 14-VALIDATION.md: allowlist match, FP recheck drop schema, surviving finding shape, code block fence, report path schema, Council verdict slot, frontmatter YAML
- Test asserts the Pitfall 3 regression guard (HTML-commented example heading does NOT match)
- Test asserts U+2014 em-dash byte integrity
- Test asserts regression guards on `commands/audit.md` (6-phase headings, slug list, parser pattern, traceability comments)
- Test asserts presence of `components/audit-fp-recheck.md` and `components/audit-output-format.md`
- Makefile Test 17 fires inside `make test`
- `make test` exits 0 with all 17 tests reported
- No existing test broken (no regression of Tests 1-16)
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-04-SUMMARY.md`
</output>
