---
phase: 14-audit-pipeline-fp-recheck-structured-reports
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - commands/audit.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
must_haves:
  truths:
    - "commands/audit.md is rewritten with the 6-phase workflow contract: load context -> quick check -> deep analysis -> FP recheck -> structured report -> Council pass"
    - "Phase 0 of the workflow reads .claude/rules/audit-exceptions.md using the post-13-05 comment-safe parser pattern"
    - "Usage section lists the 7 canonical type slugs (security, code-review, performance, deploy-checklist, mysql-performance, postgres-performance, design-review) and documents `code` and `deploy` as backward-compat aliases"
    - "audit.md references components/audit-fp-recheck.md and components/audit-output-format.md by relative path comment so executors / Phase 16 can locate them"
    - "Council handoff note documents `/council audit-review --report <path>` as the Phase 6 step (full wiring lands in Phase 15)"
    - "Existing Framework Detection table preserved verbatim"
    - "audit-skip and audit-restore appended to Related Commands"
    - "File passes markdownlint and shellcheck (markdownlint applies; shellcheck only inspects Bash blocks if a shellcheck-able shebang is on a separate file — N/A here)"
  artifacts:
    - path: "commands/audit.md"
      provides: "/audit dispatcher with 6-phase workflow contract, allowlist read, FP-recheck reference, structured report reference, Council handoff"
      contains: "## 6-Phase Workflow"
      min_lines: 180
  key_links:
    - from: "commands/audit.md"
      to: ".claude/rules/audit-exceptions.md"
      via: "Phase 0 reads the allowlist"
      pattern: ".claude/rules/audit-exceptions.md"
    - from: "commands/audit.md"
      to: "components/audit-fp-recheck.md"
      via: "Phase 4 references the 6-step procedure"
      pattern: "audit-fp-recheck.md"
    - from: "commands/audit.md"
      to: "components/audit-output-format.md"
      via: "Phase 5 references the report schema"
      pattern: "audit-output-format.md"
    - from: "commands/audit.md"
      to: "/council audit-review"
      via: "Phase 6 hands off to Phase 15"
      pattern: "/council audit-review"
---

<objective>
Rewrite `commands/audit.md` to embed the 6-phase workflow contract (load context -> quick check -> deep analysis -> FP recheck -> structured report -> Council pass), the allowlist-aware Phase 0 read using the post-13-05 comment-safe parser, the 7 canonical type slugs (with `code`/`deploy` aliases), and references to the two SOT components delivered by Plans 14-01 and 14-02.

Purpose: This is the audit pipeline's runtime entry point. Claude reads `commands/audit.md` when the user types `/audit <type>`. Every behaviour requirement (AUDIT-01..05) is satisfied by what this file instructs Claude to do. The two SOT components are the SOT for *what* the recheck and report look like; this file is the SOT for *when and how* they fire.

Output: rewritten `commands/audit.md` (was 159 lines; will be ~200-260 lines after the rewrite). File stays in `manifest.json` `files.commands` (already there per D-04 — no manifest change in this phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-VALIDATION.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-01-PLAN.md
@.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-02-PLAN.md
@commands/audit.md
@commands/audit-skip.md
@commands/audit-restore.md
@templates/base/rules/audit-exceptions.md
@CLAUDE.md

<interfaces>
<!-- Existing audit.md anatomy (per 14-RESEARCH.md "Existing commands/audit.md Shape"): -->
<!--   1. ## Purpose ; 2. ## Usage (5 types); 3. ## Quick Checks (bash); 4. ## Audit Workflow (4-step);
       5. ## Output Format ; 6. ## Framework Detection ; 7. ## Actions ; 8. ## Related Commands. -->

<!-- Splice plan: -->
<!--   - Keep ## Purpose (one-liner). -->
<!--   - Rewrite ## Usage with the 7 canonical slugs + aliases (per 14-RESEARCH.md "Type Slug Mapping" + "Pitfall 2"). -->
<!--   - Keep ## Quick Checks (3 bash blocks for security/performance/code-quality grep) — keep verbatim, fix any
        existing ` ```text ` close-fence bug from the original (lines 13/51/64/77/127 close with ` ```text ` instead of ` ``` `). -->
<!--   - REPLACE ## Audit Workflow with ## 6-Phase Workflow (Phase 0 ... Phase 5). The user-facing action steps
        are renumbered/restructured into this section. -->
<!--   - REPLACE ## Output Format with a pointer to components/audit-output-format.md (one paragraph, no
        embedded schema — DRY against Plan 14-02). -->
<!--   - Keep ## Framework Detection verbatim (table). -->
<!--   - REPLACE ## Actions with a Council-handoff sentence (workflow already covers actions). -->
<!--   - Extend ## Related Commands with /audit-skip and /audit-restore. -->

<!-- 6-phase contract (per 14-CONTEXT.md D-01 + ROADMAP success criterion 4): -->
<!--   Phase 0: Load context  — read .claude/rules/audit-exceptions.md (comment-stripped) into a
              path:line:rule allowlist. Build an empty list if absent. -->
<!--   Phase 1: Quick check    — run the existing grep-based heuristics (existing ## Quick Checks block). -->
<!--   Phase 2: Deep analysis  — load the framework prompt; produce candidate findings. -->
<!--   Phase 3: FP recheck     — run the 6-step procedure (components/audit-fp-recheck.md) on every
              candidate. Drop matches against the Phase 0 allowlist into ## Skipped (allowlist).
              Drop step-failures into ## Skipped (FP recheck) per components/audit-fp-recheck.md. -->
<!--   Phase 4: Structured report — write to .claude/audits/<type>-<YYYY-MM-DD-HHMM>.md per
              components/audit-output-format.md. -->
<!--   Phase 5: Council pass   — invoke /council audit-review --report <path>. Audit is incomplete
              until Council returns. Council wiring (Phase 15) — this phase only documents the handoff. -->
<!--   NOTE: ROADMAP success criterion phrases this as 6 phases (load context / quick check / deep analysis /
        FP recheck / structured report / Council pass). The plan numbers them 0-5 internally for clarity but
        the prose in audit.md MUST refer to "the 6-phase workflow" (preserving the count). -->

<!-- Allowlist parser pattern (per 14-RESEARCH.md "Allowlist Read Pattern" + commands/audit-restore.md post-13-05): -->
<!--   sed '/^<!--/,/^-->/d' "$EXC_FILE" > "$STRIPPED_TMP" -->
<!--   grep '^### ' "$STRIPPED_TMP" | while read heading; do ... done -->
<!--   The em-dash separator is U+2014 (literal `—`). Match key is byte-exact. -->

<!-- 7 canonical type slugs (per 14-CONTEXT.md D-12): -->
<!--   security, code-review, performance, deploy-checklist,
       mysql-performance, postgres-performance, design-review -->

<!-- Backward-compat aliases (per 14-RESEARCH.md "Open Question 1" + Pitfall 2 recommendation): -->
<!--   code -> code-review ;  deploy -> deploy-checklist -->
<!--   `full` is RETIRED as a type-producing slug. May survive as a shorthand
        that runs all 7 audits sequentially, each producing its own typed report — never `full-*.md`. -->

<!-- Markdownlint reminder (per 14-RESEARCH.md): -->
<!--   The current commands/audit.md (lines 13, 51, 64, 77, 127) closes fenced blocks with ` ```text `
        instead of plain ` ``` `. This is a markdownlint MD040 latent bug — the close fence does not need
        a language tag. Confirmed by reading commands/audit.md lines 13/51/64/77/127. The rewrite MUST use
        plain ` ``` ` to close every fenced block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite commands/audit.md with the 6-phase workflow contract</name>
  <files>commands/audit.md</files>
  <read_first>
    - commands/audit.md (current 159-line state — capture exact line numbers of sections to splice)
    - components/audit-fp-recheck.md (created by Plan 14-01 — confirm it exists and has the 6-step procedure)
    - components/audit-output-format.md (created by Plan 14-02 — confirm it exists and documents the schema)
    - commands/audit-restore.md Steps 2 and 5 (post-13-05 comment-safe parser pattern — copy verbatim)
    - commands/audit-skip.md Step 4 (HEADING string format and em-dash literal)
    - templates/base/rules/audit-exceptions.md (the file Phase 0 reads — note the `## Entries` H2 anchor and HTML-commented example block)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-RESEARCH.md (Sections "Allowlist Read Pattern", "Type Slug Mapping", "State of the Art", "Common Pitfalls")
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-PATTERNS.md (`commands/audit.md` analog block — keep Framework Detection table verbatim)
    - .planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-CONTEXT.md (D-01, D-05, D-06, D-07, D-12, D-13, D-15)
    - .markdownlint.json
  </read_first>
  <action>
Rewrite `commands/audit.md` end-to-end. The file is currently 159 lines (5 types, 4-step workflow, no allowlist, no FP recheck, no structured report, no Council handoff). The rewrite delivers all of these.

**Section-by-section rewrite plan:**

1. **`# /audit — Run Project Audit`** (H1, line 1) — keep.
2. **`## Purpose`** — keep the one-liner. Update to: "Run a structured project audit (security / performance / code-review / deploy-checklist / DB-performance / design-review) with FP recheck, allowlist suppression, and a mandatory Council pass."
3. **`---` separator** — keep.
4. **`## Usage`** — REWRITE.
   - Code block (text fence): `/audit <type> [scope]`.
   - **Types** subsection lists 7 canonical slugs as a bullet list, each with a one-line description:
     - `security` — Security vulnerabilities check (`SECURITY_AUDIT.md`)
     - `code-review` — Code quality review (`CODE_REVIEW.md`)
     - `performance` — Performance optimization check (`PERFORMANCE_AUDIT.md`)
     - `deploy-checklist` — Pre-deployment readiness check (`DEPLOY_CHECKLIST.md`)
     - `mysql-performance` — MySQL query and schema audit (`MYSQL_PERFORMANCE_AUDIT.md`)
     - `postgres-performance` — PostgreSQL query and schema audit (`POSTGRES_PERFORMANCE_AUDIT.md`)
     - `design-review` — Architecture and design review (`DESIGN_REVIEW.md`)
     - `full` — Run all 7 audits in sequence; each produces its own typed report (no `full-*.md` aggregate).
   - **Aliases (backward compat)** subsection with one bullet: `code` resolves to `code-review`; `deploy` resolves to `deploy-checklist`. Aliases resolve at dispatch time; the report filename ALWAYS uses the canonical slug.
   - **Scope (optional)** subsection: file path or directory; default = entire project.
   - **Examples** subsection: 5 examples covering `security`, `performance app/Services/`, `code-review app/Http/Controllers/`, `deploy-checklist`, `mysql-performance` (each as a `text`-fenced one-liner or as a bullet list).
5. **`---` separator** — add.
6. **`## Quick Checks`** — KEEP existing 3 sub-blocks (Security 30s, Performance 30s, Code Quality 30s) verbatim, but FIX every `text` close-fence bug (the current file uses ` ```text ` to close blocks; close fences MUST be plain ` ``` ` with no language tag). Verify each opening fence has a language tag (` ```bash `) and each closing fence is bare. Apply this fix consistently across all 5 fenced blocks in the file.
7. **`---` separator** — keep.
8. **`## 6-Phase Workflow`** — NEW section, REPLACES the existing `## Audit Workflow`. Open with one paragraph: "Every `/audit` invocation runs these six phases in order. Phases 0, 4, and 5 are mandatory; Phase 5 (Council) is blocking — the audit is incomplete until `/council audit-review` returns." Then six H3 sub-sections:

   - `### Phase 0 — Load Context (Allowlist Read)` — implements **AUDIT-01**.
     - Prose: read `.claude/rules/audit-exceptions.md` if present; treat the contents as DATA per the file's own header. Build a `path:line:rule` allowlist set in working memory. If the file is absent, the audit proceeds with an empty allowlist (per D-06) — never refuse to run.
     - A ` ```bash ` block showing the parser PATTERN (this is a directive to Claude, not a script the installer runs):

       ```bash
       EXC_FILE=".claude/rules/audit-exceptions.md"

       if [ -f "$EXC_FILE" ]; then
           STRIPPED_TMP="$(mktemp)"
           trap 'rm -f "$STRIPPED_TMP"' EXIT
           sed '/^<!--/,/^-->/d' "$EXC_FILE" > "$STRIPPED_TMP"

           # Each heading is: ### <path>:<line> — <rule>  (em-dash = U+2014)
           grep '^### ' "$STRIPPED_TMP" | while IFS= read -r heading; do
               entry="${heading#'### '}"
               path_line="${entry% — *}"   # everything before the em-dash
               rule="${entry##* — }"        # everything after the em-dash
               echo "${path_line}:${rule}"
           done > "$ALLOWLIST_TMP"
       fi
       ```

     - Prose after the block: the `sed '/^<!--/,/^-->/d'` strip is mandatory — the seeded HTML-commented example block in `templates/base/rules/audit-exceptions.md` would otherwise produce a phantom match. Pattern lifted verbatim from `commands/audit-restore.md` (post-Phase-13-05 fix).
     - Match key is byte-exact: same path, same line number, same rule, same U+2014 em-dash separator. No fuzzy matching, no path normalization, no case folding (D-07).
     - Council status for the matching entry is parsed from the `**Council:**` bullet in the same block (used to populate the `council_status` column in `## Skipped (allowlist)`).

   - `### Phase 1 — Quick Check` — Run the heuristics from `## Quick Checks` above. 30 seconds to 2 minutes per type.

   - `### Phase 2 — Deep Analysis` — Load the framework prompt selected by `## Framework Detection` below. Produce CANDIDATE findings. These are not yet reportable — they go through Phase 3 first.

   - `### Phase 3 — FP Recheck (6-Step Procedure)` — implements **AUDIT-02**. Run the 6-step procedure documented in `components/audit-fp-recheck.md` on every candidate finding. For each candidate:
     - If `(path:line, rule)` matches the Phase 0 allowlist, drop it into `## Skipped (allowlist)` with the `council_status` from the matched entry.
     - Otherwise, run the 6 steps. If the candidate fails any step, drop it into `## Skipped (FP recheck)` with `dropped_at_step` (1-6) and a one-line reason ≤ 100 chars grounded in the code.
     - Findings that survive all 6 steps proceed to `## Findings` with the full entry schema.
     - Inline link: see `components/audit-fp-recheck.md` for the canonical 6-step procedure (do NOT redefine the steps in this file — the component is the SOT).

   - `### Phase 4 — Structured Report` — implements **AUDIT-03**, **AUDIT-04**, **AUDIT-05**. Write the report to `.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md`. Auto-create the directory:

     ```bash
     mkdir -p .claude/audits
     TIMESTAMP="$(date '+%Y-%m-%d-%H%M')"
     REPORT_PATH=".claude/audits/${TYPE_SLUG}-${TIMESTAMP}.md"
     ```

     Use the schema documented in `components/audit-output-format.md` — YAML frontmatter (7 keys), fixed section order (Summary -> Findings -> Skipped (allowlist) -> Skipped (FP recheck) -> Council verdict), 9-field finding entries, verbatim 10-lines-each-side code blocks with HTML range comments, byte-exact Council slot string `_pending — run /council audit-review_`. Do NOT redefine the schema here — the component is the SOT.
     - Report filename always uses the canonical slug (D-12): even if the user typed `/audit code`, the report is `.claude/audits/code-review-<timestamp>.md`.
     - Reports are NOT auto-added to `.gitignore`; this repo's blanket `.claude/` exclusion already covers them.

   - `### Phase 5 — Council Pass (Mandatory)` — handoff documented here, implementation lands in Phase 15. After writing the report, invoke `/council audit-review --report <path-to-report>`. The audit is reported as incomplete until the Council returns a per-finding verdict table (REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT) and a "Missed findings" section. Council fills the `## Council verdict` slot in place by replacing the placeholder `_pending — run /council audit-review_`. There is no `--no-council` flag in v4.2 — Council is mandatory.

9. **`---` separator** — add.
10. **`## Output Format`** — REPLACE with a one-paragraph pointer:

    > The structured report schema is defined once in `components/audit-output-format.md`. That component is the source of truth for: report path, YAML frontmatter (7 fields), fixed section order, finding entry schema (9 fields), verbatim code block layout (extension -> language fence map), and the byte-exact Council verdict slot. Do not duplicate the schema here.

11. **`---` separator** — keep.
12. **`## Framework Detection`** — KEEP the existing markdown table verbatim (4 rows: Laravel, Next.js, Node.js, Generic). The table maps file presence to template path.
13. **`---` separator** — keep.
14. **`## Council Handoff (Phase 15)`** — NEW section, REPLACES the existing `## Actions`. One paragraph:

    > Phase 5 of the workflow invokes `/council audit-review --report <path>`. Council is mandatory: the audit run is reported as incomplete until the Council pass returns. Council confirms or rejects each finding using the embedded verbatim code block in the report — see `components/audit-output-format.md` for the report schema Council reads. Council MUST NOT reclassify severity (COUNCIL-02); it confirms REAL vs FALSE_POSITIVE only. When Council marks a finding `FALSE_POSITIVE`, this command prints the verdict and prompts the user to run `/audit-skip` to persist the exception (`/audit` never auto-writes the allowlist).

15. **`---` separator** — keep.
16. **`## Related Commands`** — KEEP existing 4 bullets and APPEND two more, in this order:
    - `/verify` — fast automated checks (build, types, lint, tests)
    - `/deps audit` — focused dependency vulnerability scan
    - `/perf` — detailed performance profiling
    - `/deploy` — pre-deploy safety checks
    - `/audit-skip <file:line> <rule> <reason>` — append a confirmed false positive to `.claude/rules/audit-exceptions.md`
    - `/audit-restore <file:line> <rule>` — remove an allowlist entry that turned out to be a real bug
    - `/council audit-review --report <path>` — Phase 15 mandatory Council pass

**Markdownlint constraints across the whole rewrite:**

- Every fenced block opens with a language tag (` ```bash `, ` ```text `, ` ```yaml `, ` ```markdown `).
- Every fenced block closes with bare ` ``` ` — never ` ```text ` or any other tag on the close line. (The current file violates this on lines 13, 51, 64, 77, 127.)
- Blank line BEFORE and AFTER every list and every fenced block (MD031/MD032).
- No trailing punctuation on any heading (MD026). Subsection titles like `Phase 0 — Load Context (Allowlist Read)` use em-dash and parens — both allowed (parens are not trailing punctuation).
- Sequential numbering on the implicit ordered list of phases. Internal numbering uses Phase 0..Phase 5; the prose still calls it "the 6-phase workflow" because Phase 0 counts as one of six.
- File line count target: 200-260 lines.

**Decision traceability comments (top-of-section bullets):**

- Phase 0: "Implements AUDIT-01 — allowlist read"
- Phase 3: "Implements AUDIT-02 — 6-step FP recheck"
- Phase 4: "Implements AUDIT-03 (verbatim code), AUDIT-04 (report path schema), AUDIT-05 (entry fields)"
- Phase 5: "Implements COUNCIL-01 handoff (full wiring in Phase 15)"

These comments make the requirement-to-prose mapping explicit for the executor and for /gsd-verify-work.
  </action>
  <verify>
    <automated>npx markdownlint-cli commands/audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f commands/audit.md` exits 0
    - `wc -l commands/audit.md` returns N where 180 ≤ N ≤ 280
    - `grep -F '6-Phase Workflow' commands/audit.md` matches (new section heading present)
    - `grep -E '^### Phase 0' commands/audit.md` matches
    - `grep -E '^### Phase 1' commands/audit.md` matches
    - `grep -E '^### Phase 2' commands/audit.md` matches
    - `grep -E '^### Phase 3' commands/audit.md` matches
    - `grep -E '^### Phase 4' commands/audit.md` matches
    - `grep -E '^### Phase 5' commands/audit.md` matches
    - `grep -F '.claude/rules/audit-exceptions.md' commands/audit.md` matches (Phase 0 references the allowlist)
    - `grep -F "sed '/^<!--/,/^-->/d'" commands/audit.md` matches (comment-safe parser pattern lifted verbatim from audit-restore.md)
    - `grep -F 'audit-fp-recheck.md' commands/audit.md` matches (Phase 3 references the FP-recheck SOT component)
    - `grep -F 'audit-output-format.md' commands/audit.md` matches at least 2 times (Phase 4 + Output Format pointer)
    - `grep -F '.claude/audits/<type>-<YYYY-MM-DD-HHMM>.md' commands/audit.md` matches (Phase 4 path verbatim per D-12)
    - `grep -F '/council audit-review' commands/audit.md` matches at least 2 times (Phase 5 + Related Commands)
    - `grep -F 'mkdir -p .claude/audits' commands/audit.md` matches (D-12 mkdir directive)
    - `grep -F 'security' commands/audit.md` matches (slug present)
    - `grep -F 'code-review' commands/audit.md` matches (slug present)
    - `grep -F 'performance' commands/audit.md` matches (slug present)
    - `grep -F 'deploy-checklist' commands/audit.md` matches (slug present)
    - `grep -F 'mysql-performance' commands/audit.md` matches (slug present)
    - `grep -F 'postgres-performance' commands/audit.md` matches (slug present)
    - `grep -F 'design-review' commands/audit.md` matches (slug present)
    - `grep -F 'aliases' commands/audit.md` matches OR `grep -F 'Aliases' commands/audit.md` matches (alias subsection present)
    - `grep -F '`code`' commands/audit.md` matches (alias documented)
    - `grep -F '`deploy`' commands/audit.md` matches (alias documented)
    - `grep -F '/audit-skip' commands/audit.md` matches (Related Commands appended)
    - `grep -F '/audit-restore' commands/audit.md` matches (Related Commands appended)
    - `grep -F 'Framework Detection' commands/audit.md` matches (existing table preserved)
    - `grep -F 'AUDIT-01' commands/audit.md` matches (decision traceability comment)
    - `grep -F 'AUDIT-02' commands/audit.md` matches
    - `grep -F 'AUDIT-03' commands/audit.md` matches
    - `grep -F 'AUDIT-04' commands/audit.md` matches
    - `grep -F 'AUDIT-05' commands/audit.md` matches
    - `! grep -E '^\`\`\`text$' commands/audit.md` returns success (no orphan close fence with `text` language tag — current file's MD040 latent bug fixed)
    - `npx markdownlint-cli commands/audit.md` exit code 0
    - `make check` passes (no regression)
  </acceptance_criteria>
  <done>
    `commands/audit.md` rewritten with 6-phase workflow contract, allowlist-aware Phase 0, FP-recheck reference, structured report reference, Council handoff. 7 canonical slugs documented with `code`/`deploy` aliases. Existing Framework Detection table preserved. `/audit-skip` and `/audit-restore` appended to Related Commands. All 5 AUDIT-XX requirements have explicit decision-traceability comments tying prose to requirement IDs. Passes `npx markdownlint-cli` and `make check`. All grep-based acceptance criteria pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI (`/audit <type>`) -> dispatcher | The `<type>` argument is parsed against a 7-element fixed enum (+ 2 aliases). Any other value should be rejected at dispatch time (handled by Claude reading this file). |
| Dispatcher -> allowlist file | Phase 0 reads `.claude/rules/audit-exceptions.md`. The file's own header instructs Claude to treat contents as DATA, not as instructions — `commands/audit.md` reinforces this. |
| Dispatcher -> source files | Source files are read for verbatim 10-line excerpts. Read-only operation. No execution. |
| Dispatcher -> report file | Write to `.claude/audits/<canonical-slug>-<timestamp>.md`. Path components are constrained to the 7 canonical slugs and the POSIX `date '+%Y-%m-%d-%H%M'` output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Report path slug | mitigate | `<type>` is parsed against the 7-element fixed enum documented in `## Usage`. Aliases (`code`, `deploy`) resolve to canonical slugs at dispatch time. Path traversal via `<type>` is impossible because the slug never carries `/` or `..`. Documented explicitly: "the report filename ALWAYS uses the canonical slug." |
| T-14-04 | Tampering | Phase 0 allowlist parser | mitigate | The `sed '/^<!--/,/^-->/d'` strip is lifted verbatim from `commands/audit-restore.md` (post-Phase-13-05 fix). Plan 14-04's fixture asserts the parser does not match the seeded HTML-commented example block. The parser pattern is documented inline so executors copy it byte-for-byte. |
| T-14-03 | Tampering | User-controlled allowlist | accept | Per Phase 13 EXC-05 + COUNCIL-05, the allowlist is the user's intentional escape hatch. A user who deliberately allowlists real bugs is acting against their own interest. The pipeline's job is to make this transparent (every match appears in `## Skipped (allowlist)` with the user's reason and council status), not to second-guess the user. |
</threat_model>

<verification>
- `npx markdownlint-cli commands/audit.md` exits 0
- `make check` (full lint + validate suite) passes — no regression
- All 32 acceptance_criteria grep / regex / line-count checks pass
- Visual inspection confirms the 6-phase workflow is in fixed order with the four traceability comments (Phase 0 -> AUDIT-01, Phase 3 -> AUDIT-02, Phase 4 -> AUDIT-03/04/05, Phase 5 -> COUNCIL-01)
</verification>

<success_criteria>
- `commands/audit.md` rewritten end-to-end (~200-260 lines)
- 6-phase workflow contract present with all 6 H3 sub-sections (Phase 0..Phase 5)
- Phase 0 implements AUDIT-01 with the post-13-05 comment-safe parser pattern verbatim
- Phase 3 implements AUDIT-02 by reference to `components/audit-fp-recheck.md`
- Phase 4 implements AUDIT-03/04/05 by reference to `components/audit-output-format.md` and the literal report path
- Phase 5 documents the Council handoff (full wiring in Phase 15)
- 7 canonical type slugs in `## Usage`; `code`/`deploy` aliases documented
- Framework Detection table preserved verbatim
- `/audit-skip` and `/audit-restore` appended to Related Commands
- All 5 AUDIT-XX requirement IDs cited inline as traceability anchors
- File passes markdownlint and `make check`
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-pipeline-fp-recheck-structured-reports/14-03-SUMMARY.md`
</output>
