---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - templates/base/CLAUDE.md
  - templates/laravel/CLAUDE.md
  - templates/rails/CLAUDE.md
  - templates/nextjs/CLAUDE.md
  - templates/nodejs/CLAUDE.md
  - templates/python/CLAUDE.md
  - templates/go/CLAUDE.md
  - docs/INSTALL.md
autonomous: true
gap_closure: false
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "README.md positions the toolkit as a complement (not replacement) and shows both standalone and complement install paths with one paragraph of guidance per mode"
    - "CHANGELOG.md contains a [4.0.0] entry in Keep-a-Changelog format with a BREAKING CHANGES block leading the entry"
    - "CHANGELOG.md [4.0.0] version exactly matches manifest.json version field (make validate alignment check passes)"
    - "All 7 templates/*/CLAUDE.md files contain a literal '## Required Base Plugins' section with SP + GSD install commands"
    - "docs/INSTALL.md exists and documents the 12-cell install matrix (4 modes x {fresh, upgrade, re-run}) rendered as markdown pipe-tables"
    - "make check passes green (markdownlint MD040/MD031/MD032/MD026 compliant; shellcheck unaffected; make validate manifest-CHANGELOG alignment passes)"
  artifacts:
    - path: "README.md"
      provides: "Complement-first positioning + dual install paths + upgrade-from-v3.x subsection"
      contains: "Standalone install"
    - path: "CHANGELOG.md"
      provides: "[4.0.0] release entry with BREAKING CHANGES block"
      contains: "## [4.0.0]"
    - path: "templates/base/CLAUDE.md"
      provides: "Required Base Plugins block (canonical, verbatim across all 7)"
      contains: "## Required Base Plugins"
    - path: "templates/laravel/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "templates/rails/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "templates/nextjs/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "templates/nodejs/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "templates/python/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "templates/go/CLAUDE.md"
      provides: "Required Base Plugins block"
      contains: "## Required Base Plugins"
    - path: "docs/INSTALL.md"
      provides: "12-cell install matrix (standalone / complement-sp / complement-gsd / complement-full) x {fresh, upgrade, re-run}"
      contains: "complement-full"
  key_links:
    - from: "CHANGELOG.md"
      to: "manifest.json"
      via: "version alignment (both must read '4.0.0' at end of phase)"
      pattern: "4\\.0\\.0"
    - from: "README.md"
      to: "docs/INSTALL.md"
      via: "relative link in install section"
      pattern: "docs/INSTALL\\.md"
    - from: "templates/*/CLAUDE.md (x7)"
      to: "canonical 15-line Required Base Plugins block"
      via: "verbatim copy of block from 06-RESEARCH.md §5"
      pattern: "## Required Base Plugins"
---

<objective>
Land the four core user-facing text deliverables for v4.0: README positioning, CHANGELOG [4.0.0] entry, per-template Required Base Plugins blocks (x7), and the docs/INSTALL.md 12-cell install matrix.

Purpose: Phase 6 SC-1, SC-2, SC-3, SC-4 (README complement positioning; 7-template disclosure; CHANGELOG BREAKING CHANGES entry; 12-cell matrix) all stem from this plan. Without these the v4.0 release cannot ship.

Output: README.md rewritten install section, CHANGELOG [4.0.0] entry, 7 modified template CLAUDE.md files (identical block), new docs/INSTALL.md file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
@.planning/phases/06-documentation/06-VALIDATION.md
@README.md
@CHANGELOG.md
@manifest.json
@CLAUDE.md

<interfaces>
Key contracts this plan consumes (already exist, no discovery needed):

**From manifest.json** (version alignment target):
- `manifest_version: 2` (schema — do NOT change)
- `version: "3.0.0"` → will be bumped to `"4.0.0"` as part of DOCS-03
- `updated: "2026-04-17"` → will be bumped to release date in Phase 7 (do NOT change in Phase 6)

**From 06-RESEARCH.md §5 — canonical Required Base Plugins block (copy verbatim into all 7 templates):**

```markdown
## Required Base Plugins

This toolkit is designed to **complement** two Claude Code plugins. Install them first for
the full experience; TK will auto-detect them and skip duplicate files.

| Plugin | Purpose | Install |
|--------|---------|---------|
| `superpowers` (obra) | Skills (debugging, plans, TDD, verification, worktrees), `code-reviewer` agent | `claude plugin marketplace add obra/superpowers && claude plugin install superpowers@superpowers` |
| `get-shit-done` (gsd-build) | Phase-based workflow: `/gsd-plan-phase`, `/gsd-execute-phase`, and more | `claude plugin marketplace add gsd-build/get-shit-done && claude plugin install get-shit-done@get-shit-done` |

> **Without these plugins** TK still installs in `standalone` mode — you get every TK file,
> but you'll miss SP's systematic debugging and GSD's phase workflow. See
> [components/optional-plugins.md](.claude/components/optional-plugins.md) for the full rationale.
```

Insertion point (all 7 templates): immediately after `## Project Overview` (or `## 🎯 Project Overview`), before `## 📌 Compact Instructions`.

**From 06-RESEARCH.md §2 — CHANGELOG 4.0.0 content catalog:**
- 8 BREAKING CHANGES (complement-mode default, 7 skipped files, manifest v1→v2, init-local.sh version constant removed, update loop manifest-driven, settings.json merge, settings.json backup, backup dir suffix with pid, post-update summary format)
- Added: detect.sh, lib/install.sh, lib/state.sh, migrate-to-complement.sh, toolkit-install.json, 4 modes, --mode/--dry-run/--force/--force-mode-change flags, 14 Makefile test groups, orchestration-pattern.md, optional-plugins.md, RTK.md, Required Base Plugins section
- Changed: init-claude.sh, init-local.sh, update-claude.sh, setup-security.sh, setup-council.sh, README.md positioning, manifest.json (conflicts_with + sp_equivalent)
- Fixed: BUG-01 through BUG-07 (already present in [Unreleased] — preserve verbatim)

**From 06-RESEARCH.md §3 — 12 matrix cells for docs/INSTALL.md:**
- Rows: 4 modes (standalone, complement-sp, complement-gsd, complement-full)
- Columns: 3 scenarios (fresh install, upgrade from v3.x, re-run / idempotent)
- Each cell: precondition, command, expected stdout headline, expected toolkit-install.json mode field, expected files landed vs skipped, expected duplicates removed vs kept
- Honesty note: `complement-gsd` column is functionally identical to `standalone` in current manifest state (no GSD conflicts); document this explicitly per research §3

**Markdownlint constraints (every file MUST comply):**
- MD040: every fenced code block declares a language (use `bash`, `text`, `json`, `markdown`, `yaml`)
- MD031: blank line BEFORE and AFTER every fenced code block
- MD032: blank line BEFORE and AFTER every list
- MD026: NO trailing punctuation in headings (no `?`, `:`, `.`, `!`)
- MD013 (line length) — disabled per .markdownlint.json
- MD033 (inline HTML) — disabled
- MD060 (table alignment) — disabled (pipe-tables OK)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CHANGELOG [4.0.0] entry (DOCS-03) and bump manifest.json version</name>
  <files>CHANGELOG.md, manifest.json</files>
  <action>
  Implement DOCS-03 per CONTEXT.md locked decision (Keep-a-Changelog layout, BREAKING CHANGES-first block).

  Steps:
  1. Read current CHANGELOG.md (lines 1-19 contain `[Unreleased]` with BUG-01..07 under `### Fixed`).
  2. Rename `## [Unreleased]` heading to `## [4.0.0] - TBD` (date placeholder; Phase 7 VALIDATE-04 fills actual release date — do NOT guess a date in this plan).
  3. Directly under the `## [4.0.0]` heading, insert a `### BREAKING CHANGES` subsection FIRST (before Added/Changed/Fixed). Source content from 06-RESEARCH.md §2 "BREAKING CHANGES" — 8 items, in that order:
     - Default install mode now recommends complement-* when base plugins detected (v3.x users see duplicates until migration)
     - 7 files no longer installed in complement-sp mode (`agents/code-reviewer.md`, `commands/{debug,plan,tdd,verify,worktree}.md`, `skills/debugging/SKILL.md`)
     - manifest.json schema v1→v2 (`manifest_version: 2`; v3.x installers hard-fail against v2 manifest)
     - toolkit-install.json state schema v1→v2 (adds `synthesized_from_filesystem`)
     - scripts/init-local.sh no longer hardcodes VERSION constant (reads manifest.json at runtime)
     - scripts/update-claude.sh no longer hand-iterates files (manifest-driven loop; users who relied on skip-list behavior now see those files installed)
     - ~/.claude/settings.json merged additively (SAFETY-01..04: backup + atomic merge, never clobbers SP hooks)
     - Post-update summary format changed (4-group block: INSTALLED / UPDATED / SKIPPED / REMOVED); backup directories now suffixed with PID to prevent same-second collision
  4. Insert `### Added` with entries from 06-RESEARCH.md §2 Added. Cover: detect.sh, lib/install.sh, lib/state.sh, migrate-to-complement.sh, toolkit-install.json, 4 install modes, --mode/--dry-run flags on init-claude.sh, --offer-mode-switch/--prune/--no-banner on update-claude.sh, conflicts_with / sp_equivalent / requires_base manifest fields, make validate-manifest.py, 14 Makefile test groups, orchestration-pattern.md component, optional-plugins.md component, templates/global/RTK.md, Required Base Plugins template section (x7).
  5. Insert `### Changed` with entries from 06-RESEARCH.md §2 Changed. Cover: init-claude.sh 4-mode dispatch, init-local.sh manifest-read version, update-claude.sh re-detection and manifest-driven iteration, setup-security.sh safe JSON merge, setup-council.sh /dev/tty guards + python3 json.dumps API-key escape, README.md complement repositioning, manifest.json schema v2 entries.
  6. PRESERVE the existing `### Fixed` section with BUG-01..BUG-07 entries VERBATIM — these are Phase 1 outputs already catalogued.
  7. Add a `### Migration from v3.x` subsection at the end of [4.0.0] with one line: "See [docs/INSTALL.md](docs/INSTALL.md) for the install matrix and `scripts/migrate-to-complement.sh` for the automated migration path (per-file confirmation, full backup before any removal)."
  8. Bump `manifest.json` `version` field from `"3.0.0"` to `"4.0.0"` (line 3). Do NOT change `manifest_version` (stays `2`) and do NOT change `updated` (Phase 7 sets release date).

  Lint constraints (MUST pass):
  - MD040: every fenced code block has a language tag. The CHANGELOG has no fences in the existing entries; if you add any, use `bash` or `text`.
  - MD026: no trailing punctuation in headings. Use `### BREAKING CHANGES` not `### BREAKING CHANGES:`.
  - MD031/MD032: blank line before and after lists and fences.

  Per decision D-CHG-01 (CONTEXT.md): Keep-a-Changelog layout with BREAKING CHANGES-first header block above the categories. Rationale: semver 4.0.0 demands immediate visibility.

  Do NOT touch `scripts/init-local.sh` — it already reads version from manifest (BUG-06 Phase 1 fix).
  Do NOT edit `.planning/` files.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && npx markdownlint-cli CHANGELOG.md && make validate</automated>
  </verify>
  <done>
  - CHANGELOG.md contains `## [4.0.0] - TBD` heading
  - `### BREAKING CHANGES` appears BEFORE `### Added` under [4.0.0]
  - All 8 BREAKING CHANGES from 06-RESEARCH.md §2 are present
  - BUG-01..07 still present verbatim under `### Fixed`
  - manifest.json `version` field reads `"4.0.0"`
  - `make validate` passes (CHANGELOG ↔ manifest version alignment)
  - `markdownlint CHANGELOG.md` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md install section for complement-first positioning (DOCS-01)</name>
  <files>README.md</files>
  <action>
  Implement DOCS-01 per CONTEXT.md locked invariants: (1) "complement" not "replacement" language, (2) both standalone and complement install paths shown, (3) one paragraph of guidance per mode.

  Steps:
  1. Read current README.md. Preserve lines 1-25 (header / badges / intro) and lines 78-end (Killer Features table + Structure + rest) UNCHANGED.
  2. Replace the `## Installation (per project)` section (lines 57-75) with three new subsections:

     **a. `## Install Modes`** — one paragraph intro explaining that TK auto-detects `superpowers` and `get-shit-done` and chooses one of 4 modes: `standalone`, `complement-sp`, `complement-gsd`, `complement-full`.

     **b. `### Standalone install`** — one paragraph of guidance: "You don't have superpowers or get-shit-done installed (or you've explicitly opted out). TK installs all 54 files. This is the full-fat default." Then the install command `bash <(curl -sSL https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main/scripts/init-claude.sh)` in a fenced `bash` block.

     **c. `### Complement install`** — one paragraph of guidance: "You have one or both of superpowers (obra) and get-shit-done (gsd-build) installed. TK auto-detects them and skips the 7 files that would duplicate SP functionality, keeping the ~47 unique TK contributions (Council, framework CLAUDE.md templates, components library, cheatsheets, framework-specific skills)." Same install command; TK auto-selects the `complement-*` mode. Include a 2-line note showing the `--mode` override flag.

     **d. `### Upgrading from v3.x`** — one paragraph: "v3.x users who installed SP or GSD after TK should run `scripts/migrate-to-complement.sh` to remove duplicate files with per-file confirmation and a full pre-migration backup. See [docs/INSTALL.md](docs/INSTALL.md) for the full 12-cell matrix and step-by-step guidance."

  3. Insert `## Required Base Plugins` section reference pointer: add a one-line note in the Install Modes section that says "Each framework template documents its required base plugins in `## Required Base Plugins` — see e.g. [templates/base/CLAUDE.md](templates/base/CLAUDE.md)."
  4. Update the existing "Structure After Installation" section (around line 126): add a one-line note "Files marked †conflicts with superpowers — omitted in `complement-sp` and `complement-full` modes."
  5. Do NOT touch the `## Killer Features` table (preserve TK's unique-value pitch).
  6. Do NOT add, move, or delete any heading outside the install section.

  Lint constraints:
  - MD040: every fenced block has a language (`bash` for shell commands).
  - MD031/MD032: blank line around fences and lists.
  - MD026: headings end without punctuation. Use `### Standalone install`, `### Complement install`, `### Upgrading from v3.x`.
  - Preserve pipe-tables as-is (MD060 disabled).

  Tone: one paragraph per mode (not more), declarative sentences. Do NOT use the word "replacement" anywhere near SP/GSD. Use "complement" or "plays nicely with" or "layers on top of".
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && npx markdownlint-cli README.md && grep -q 'complement' README.md && grep -q 'Standalone install' README.md && grep -q 'Upgrading from v3.x' README.md && grep -q 'docs/INSTALL.md' README.md</automated>
  </verify>
  <done>
  - README.md contains the strings "Standalone install", "Complement install", "Upgrading from v3.x"
  - README.md references `docs/INSTALL.md` as a relative link
  - Word "complement" appears in the install section
  - Killer Features table untouched
  - markdownlint README.md exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Add canonical '## Required Base Plugins' block to all 7 templates (DOCS-02)</name>
  <files>templates/base/CLAUDE.md, templates/laravel/CLAUDE.md, templates/rails/CLAUDE.md, templates/nextjs/CLAUDE.md, templates/nodejs/CLAUDE.md, templates/python/CLAUDE.md, templates/go/CLAUDE.md</files>
  <action>
  Implement DOCS-02 per CONTEXT.md discretion: one canonical 15-line block reused VERBATIM across all 7 templates.

  Canonical block (copy this exactly into every template — do NOT paraphrase per template):

  ```markdown
  ## Required Base Plugins

  This toolkit is designed to **complement** two Claude Code plugins. Install them first for
  the full experience; TK will auto-detect them and skip duplicate files.

  | Plugin | Purpose | Install |
  |--------|---------|---------|
  | `superpowers` (obra) | Skills (debugging, plans, TDD, verification, worktrees), `code-reviewer` agent | `claude plugin marketplace add obra/superpowers && claude plugin install superpowers@superpowers` |
  | `get-shit-done` (gsd-build) | Phase-based workflow: `/gsd-plan-phase`, `/gsd-execute-phase`, and more | `claude plugin marketplace add gsd-build/get-shit-done && claude plugin install get-shit-done@get-shit-done` |

  > **Without these plugins** TK still installs in `standalone` mode — you get every TK file,
  > but you'll miss SP's systematic debugging and GSD's phase workflow. See
  > [components/optional-plugins.md](.claude/components/optional-plugins.md) for the full rationale.
  ```

  Steps for each of the 7 templates (`templates/{base,laravel,rails,nextjs,nodejs,python,go}/CLAUDE.md`):

  1. Read the template. Identify the first `## Project Overview` or `## 🎯 Project Overview` heading (typically lines 3-11).
  2. Insert the canonical block IMMEDIATELY AFTER `## Project Overview` and its paragraph(s), BEFORE any `## 📌 Compact Instructions` (or whatever heading follows Project Overview).
  3. Ensure there is ONE blank line before `## Required Base Plugins` and ONE blank line after the closing `>` blockquote line (MD031/MD032).
  4. Do NOT modify any other section in any template. Do NOT rewrite existing content.
  5. The block is byte-identical in all 7 files — the canonical single-source recommendation per 06-RESEARCH.md §5 Option A.

  Drift protection: a future `make validate-base-plugins` target will grep each file for `## Required Base Plugins` — today this plan inserts the section consistently; Pitfall 10 prevention is a Phase 6 continuation item, not this task.

  Lint constraints:
  - MD040: the inner fences `bash` / `markdown` in the canonical block are already tagged — preserve.
  - MD031/MD032: blank line before heading, blank line after blockquote.
  - MD026: "## Required Base Plugins" has no trailing punctuation (OK).

  WARNING: Do NOT use `sed -i` for this — macOS BSD `sed` handles `-i` differently from GNU. Use Edit tool for each file individually.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && for f in templates/base/CLAUDE.md templates/laravel/CLAUDE.md templates/rails/CLAUDE.md templates/nextjs/CLAUDE.md templates/nodejs/CLAUDE.md templates/python/CLAUDE.md templates/go/CLAUDE.md; do grep -q '^## Required Base Plugins' "$f" || { echo "MISSING: $f"; exit 1; }; done && npx markdownlint-cli templates/base/CLAUDE.md templates/laravel/CLAUDE.md templates/rails/CLAUDE.md templates/nextjs/CLAUDE.md templates/nodejs/CLAUDE.md templates/python/CLAUDE.md templates/go/CLAUDE.md</automated>
  </verify>
  <done>
  - All 7 templates/*/CLAUDE.md files contain the literal string `## Required Base Plugins`
  - Each block is byte-identical (verbatim copy)
  - markdownlint passes on all 7 files
  - No other section was modified
  </done>
</task>

<task type="auto">
  <name>Task 4: Create docs/INSTALL.md with 12-cell install matrix (DOCS-04)</name>
  <files>docs/INSTALL.md</files>
  <action>
  Implement DOCS-04 per CONTEXT.md locked decision: docs/INSTALL.md is a STANDALONE file (not an inline README section).

  Steps:
  1. Create `docs/INSTALL.md` (net-new file). If `docs/` directory exists (it does), write directly to `docs/INSTALL.md`.
  2. File structure:
     - Title: `# Install Matrix`
     - Intro paragraph: "This document lists the 12 cells of the v4.0 install matrix. Rows = 4 install modes; columns = 3 scenarios (fresh install, upgrade from v3.x, re-run / idempotent behavior)." Reference README install section as the entry point.
     - One `## Modes Overview` section: one paragraph summarizing the 4 modes (standalone, complement-sp, complement-gsd, complement-full).
     - Four `## Mode: {name}` subsections, one per mode. Each contains a pipe-table with 3 rows (fresh / upgrade / re-run) and a single "Expected Behavior" column. Content from 06-RESEARCH.md §3.
     - Alternative layout (EQUIVALENT, either acceptable): single 12-row table with columns `| Mode | Scenario | Precondition | Command | Expected stdout | toolkit-install.json mode | Files landed vs skipped |`. Pick whichever renders more readable on GitHub. Recommendation: 4 small tables (one per mode) for scannability.
     - Include the honest note from 06-RESEARCH.md §3 that `complement-gsd` is currently functionally identical to `standalone` because no GSD conflicts exist in manifest.json yet (the mode exists for future use and composition into `complement-full`).
     - Include a `## Migration from v3.x` section at the end: one paragraph pointing at `scripts/migrate-to-complement.sh` with a one-line summary of its safety invariants (three-way diff, full pre-migration backup, [y/N/d] per-file prompt, idempotent).
  3. Source content for each cell from 06-RESEARCH.md §3. Do NOT invent new behaviors — every cell must be a faithful restatement of already-validated Phase 3/4/5 SC criteria.
  4. Use pipe-tables (markdownlint MD060 disabled — pipe-tables are fine).
  5. Do NOT touch README.md in this task (Task 2 already added the link reference).

  Lint constraints:
  - MD040: no unlanguaged fences. If showing `bash <(curl ...)` commands, wrap in ```bash blocks.
  - MD031/MD032: blank line around every fence and list.
  - MD026: headings end without punctuation.
  - MD024 (siblings_only): duplicate `### Fresh Install` / `### Upgrade from v3.x` headings are OK because each appears under a distinct `## Mode: ...` parent.

  The matrix content is a presentation of already-validated behavior (Phase 3 SC-1..5, Phase 4 SC-1..5, Phase 5 SC-1..5). Do NOT add new validation — just present.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -f docs/INSTALL.md && npx markdownlint-cli docs/INSTALL.md && grep -qE 'complement-full' docs/INSTALL.md && grep -qE 'standalone' docs/INSTALL.md && grep -qE 'complement-sp' docs/INSTALL.md && grep -qE 'complement-gsd' docs/INSTALL.md</automated>
  </verify>
  <done>
  - docs/INSTALL.md exists
  - File contains all 4 mode names (standalone, complement-sp, complement-gsd, complement-full)
  - File contains all 3 scenario types (fresh, upgrade, re-run)
  - markdownlint docs/INSTALL.md exits 0
  - File contains a reference to migrate-to-complement.sh
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → end-user clone | Markdown docs rendered on GitHub and in downstream tooling; no runtime code path from this plan |
| template file → user `.claude/` copy | Install script copies templates; content of Required Base Plugins block lands verbatim in user projects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure / Integrity | README + CHANGELOG + docs/INSTALL.md (upstream install commands for SP, GSD) | mitigate | Install commands match the project-wide convention documented in CONTEXT.md § SP/GSD install command strings (A1/A2 from research); verified consistent with `scripts/detect.sh:54`, `scripts/verify-install.sh:197-200`, manifest schema references. Phase 7 VALIDATE-04 re-verifies before release tag. |
| T-06-01-02 | Tampering | templates/*/CLAUDE.md (canonical block drift across 7 copies) | mitigate | One block, copied verbatim per research §5 Option A. Pitfall 10 calls for a future `make validate-base-plugins` target — authored as Phase 6 continuation or Phase 7 follow-up, not blocking this plan. |
| T-06-01-03 | Tampering (version drift) | CHANGELOG.md `[4.0.0]` ↔ manifest.json `version` | mitigate | `make validate` enforces manifest ↔ CHANGELOG version alignment (Phase 1 BUG-06 check). Task 1 bumps both fields atomically in a single commit. |
| T-06-01-04 | Information Disclosure | docs/INSTALL.md (12-cell matrix) | accept | No secrets; content is a rewording of Phase 3/4/5 SC criteria. No new threat surface introduced beyond what Phase 3-5 already shipped. |
</threat_model>

<verification>
Run `make check` at the end of the plan. Expect:

- `make shellcheck` — unaffected (no shell files modified in 06-01)
- `make mdlint` — README.md, CHANGELOG.md, docs/INSTALL.md, all 7 template CLAUDE.md all pass
- `make validate` — manifest ↔ CHANGELOG version alignment passes (both `4.0.0`); 7-template `## Required Base Plugins` presence check passes (sentinel grep)

Additional grep sanity checks:
- `grep -c 'complement' README.md` >= 3
- `grep -c 'BREAKING CHANGES' CHANGELOG.md` >= 1
- `grep -rl '## Required Base Plugins' templates/*/CLAUDE.md | wc -l` == 7
- `test -f docs/INSTALL.md`
</verification>

<success_criteria>
- DOCS-01: README positions TK as complement, shows both install paths, one paragraph per mode [VERIFIED by Task 2 done criteria]
- DOCS-02: All 7 templates/*/CLAUDE.md contain `## Required Base Plugins` section [VERIFIED by Task 3 grep loop]
- DOCS-03: CHANGELOG [4.0.0] entry with BREAKING CHANGES block exists; manifest.json version == 4.0.0 [VERIFIED by Task 1 make validate]
- DOCS-04: docs/INSTALL.md exists with 12-cell matrix [VERIFIED by Task 4 file existence + mode-name grep]
- `make check` green at plan completion
- `make validate` green (manifest ↔ CHANGELOG alignment)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md` using the @$HOME/.claude/get-shit-done/templates/summary.md template. Summary must include:
- Requirements satisfied: DOCS-01, DOCS-02, DOCS-03, DOCS-04
- Files modified (10): README.md, CHANGELOG.md, manifest.json, 7 template CLAUDE.md files, docs/INSTALL.md (net-new)
- make check + make validate output (both green)
- Any markdownlint errors encountered + how they were resolved
- Link to the canonical Required Base Plugins block pasted verbatim into all 7 templates
</output>
