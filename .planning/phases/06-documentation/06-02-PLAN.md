---
phase: 06-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/optional-plugins.md
  - templates/global/RTK.md
autonomous: true
gap_closure: false
requirements:
  - DOCS-05-asset
  - DOCS-07-asset
requirements_note: |
  DOCS-05 asset half (components/optional-plugins.md) shipped here; manifest inventory
  registration in 06-03.
  DOCS-07 asset half (templates/global/RTK.md) shipped here; setup-security.sh install
  wiring + ~/.claude/ copy guard in 06-03.

must_haves:
  truths:
    - "components/optional-plugins.md exists and documents rtk + caveman + superpowers + get-shit-done with upstream-verified caveats"
    - "components/optional-plugins.md header declares a verified_upstream date (2026-04-18) for future doc-audit grep"
    - "templates/global/RTK.md exists and contains a Known Issues section pointing at rtk-ai/rtk#1276"
    - "caveman section documents en + wenyan (Classical Chinese) — NOT en + ru (correction applied per CONTEXT.md D-DOCS-05)"
    - "caveman section documents single-generation auto-backup caveat (CLAUDE.original.md overwritten on re-compress)"
    - "rtk section distinguishes upstream's intended fix (LC_ALL=C) from user-side workaround (exclude_commands = [\"ls\"])"
    - "superpowers + get-shit-done install commands match CONTEXT.md `<decisions>` lines 62-66 (SP: claude plugin install superpowers@claude-plugins-official; GSD: bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh))"
    - "markdownlint components/optional-plugins.md templates/global/RTK.md exits 0"
  artifacts:
    - path: "components/optional-plugins.md"
      provides: "Documentation of rtk + caveman + superpowers + get-shit-done as recommended optional plugins with upstream-verified caveats and CONTEXT-locked SP/GSD install strings"
      contains: "verified_upstream: 2026-04-18"
    - path: "templates/global/RTK.md"
      provides: "Fallback RTK.md template with Known Issues section referencing rtk-ai/rtk#1276"
      contains: "rtk-ai/rtk#1276"
  key_links:
    - from: "components/optional-plugins.md (rtk section)"
      to: "templates/global/RTK.md"
      via: "relative link — the optional-plugins component points at the RTK.md file for deeper caveat detail"
      pattern: "RTK\\.md"
    - from: "components/optional-plugins.md (rtk section)"
      to: "rtk-ai/rtk#1276"
      via: "upstream issue URL"
      pattern: "rtk-ai/rtk/issues/1276"
    - from: "06-03 Task 1 (manifest inventory registration)"
      to: "components/optional-plugins.md + components/orchestration-pattern.md"
      via: "06-03 registers both files in NEW top-level manifest.json `inventory.components` section (NOT `files.components` — avoids install-loop side-effect). Wave 2 sequential — manifest edits consolidated there to avoid Wave 1 overlap with 06-01's manifest.json version bump."
      pattern: "inventory\\.components"
---

<objective>
Create the two net-new Phase 6 markdown assets: `components/optional-plugins.md` (DOCS-05 asset half) and `templates/global/RTK.md` (DOCS-07 asset half).

Purpose: Phase 6 SC-5 (optional-plugins documented with caveats; RTK.md template carries Known Issues section pointing to upstream #1276). Without these files, (1) the DOCS-06 script block wired in 06-03 would reference nonexistent documentation, and (2) 06-03 Task 1's manifest inventory registration would fail (can't register a file that doesn't exist).

Output: 2 net-new files. NO manifest edits in this plan (Wave 1 no-overlap invariant with 06-01's manifest.json version bump). NO script edits in this plan (DOCS-07 install wiring is 06-03 Task 4; DOCS-06 shell block is 06-03 Tasks 2-3).

SCOPE BOUNDARY: This plan produces markdown assets ONLY. The three deferred items for 06-03 Wave 2:
- Inventory registration of both new components in NEW `manifest.json.inventory.components` section (06-03 Task 1 — NOT `files.components` per BLOCKING-2 plan-checker feedback: adding to `files` triggers unwanted install-loop iteration in `scripts/lib/install.sh:239`)
- Install wiring for RTK.md (06-03 Task 4 folds into setup-security.sh with `[ ! -f "$HOME/.claude/RTK.md" ]` guard)
- DOCS-06 end-of-run stdout block (06-03 Tasks 2-3 ship `scripts/lib/optional-plugins.sh` + wire into init/update)

Parallelism: This plan runs in Wave 1 alongside 06-01. Wave 1 files_modified have ZERO overlap (06-01 touches README / CHANGELOG / 7 templates / docs/INSTALL.md / manifest.json / Makefile; 06-02 touches only components/optional-plugins.md + templates/global/RTK.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
@.planning/phases/06-documentation/06-VALIDATION.md
@components/orchestration-pattern.md

<interfaces>
**Upstream facts — verified 2026-04-18 (from 06-RESEARCH.md §4). All content in optional-plugins.md MUST match these facts exactly:**

### rtk (rtk-ai/rtk)
- Install (macOS): `brew install rtk`
- Init (global): `rtk init -g` — installs hook + RTK.md
- macOS config path: `~/Library/Application Support/rtk/config.toml`
- Linux config path: `~/.config/rtk/config.toml`
- Known issue: rtk-ai/rtk#1276 — OPEN as of 2026-04-18. Symptom: `rtk ls` returns `(empty)` for non-empty directories on non-English locales.
- Upstream's intended fix: internal `cmd.env("LC_ALL", "C")` in Rust source (NOT user-side config).
- User-side workaround (different from upstream's intended fix): add `exclude_commands = ["ls"]` to config.toml. The component MUST document this distinction honestly.

### caveman (JuliusBrussee/caveman) — CORRECTIONS FROM REQUIREMENTS.md (applied in commit 2444b40)
- Install: `claude plugin marketplace add JuliusBrussee/caveman && claude plugin install caveman@caveman`
- Language modes: `en + wenyan (Classical Chinese)` — NOT "en + ru". The requirement originally said ru; that was wrong. Wenyan = 文言文 = Classical Chinese.
- Intensity levels: Lite / Full (default) / Ultra; wenyan variants Wenyan-Lite / Wenyan-Full / Wenyan-Ultra.
- Compression rate claim: "~46% on average" input token reduction (attribute this to upstream README).
- CLAUDE.md backup: `caveman-compress` AUTO-CREATES `CLAUDE.original.md` as a backup. User does NOT need to back up manually. But the auto-backup is SINGLE-GENERATION — re-running caveman-compress OVERWRITES the prior CLAUDE.original.md. Document this invariant honestly: "commit CLAUDE.md to git BEFORE running compress so you can diff/revert from version control; the auto-backup is single-generation."

### superpowers (obra/superpowers) — CONTEXT.md-LOCKED install string (NOT research §5)
- Install: `claude plugin install superpowers@claude-plugins-official`
- Rationale: matches `enabledPlugins` key verified in `scripts/detect.sh:54` and `scripts/verify-install.sh:197-200`. Any deviation is a BLOCKING lint error.
- Purpose: Skills (systematic-debugging, writing-plans, test-driven-development, verification-before-completion, using-git-worktrees), agents (code-reviewer), commands.
- Relationship to TK: v4.0 toolkit explicitly designed around SP. In complement-sp / complement-full modes, TK skips 7 duplicate files (6 commands/skills + 1 agent).

### get-shit-done (gsd-build/get-shit-done) — CONTEXT.md-LOCKED install string
- Install: `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)`
- Rationale: matches the filesystem detection path `~/.claude/get-shit-done/` in `scripts/detect.sh:29`. Any deviation (e.g., a marketplace-add form) is a BLOCKING lint error.
- Purpose: Phase-based workflow (`/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-discuss-phase`, `/gsd-verify-work`, ...).
- Relationship to TK: Complements TK — phase workflow layers over TK's framework templates.

**From 06-RESEARCH.md §7 — RTK.md content skeleton (proposed; implementer may adjust wording but MUST preserve content invariants):**

- Title: `# RTK — Toolkit Notes (Claude Code)`
- Intro blockquote: Fallback notes when `rtk init -g` has not yet been run; rtk's own ~/.claude/RTK.md is authoritative if present.
- `## What RTK Does` — one paragraph on 60-90% token reduction for dev commands.
- `## Quick Install` — ```bash block with `brew install rtk` + `rtk init -g` (noting that `rtk init -g` overwrites this fallback).
- `## Known Issues` — `### rtk ls returns (empty) on non-English locales — rtk-ai/rtk#1276` subsection documenting symptom + cause + user-side workaround (`exclude_commands = ["ls"]` in config.toml).
- `## Relationship to Claude Code Safety Net` — paragraph on cc-safety-net + combined PreToolUse hook (existing Phase 3 SAFETY work).

**Markdownlint constraints (every file MUST comply):**
- MD040: every fenced code block declares a language (`bash`, `text`, `toml`, `json`, `markdown`).
- MD031/MD032: blank line before/after every fence and list.
- MD026: no trailing punctuation in headings (avoid `?`, `:`, `.`, `!`). Use "What RTK Does" not "What RTK Does?"; "Known Issues" not "Known Issues:".
- MD024 (siblings_only): duplicate `### Fresh Install`-style headings under distinct parents are OK.
- Do NOT nest ```markdown inside ```bash fences (Pitfall 1 — caused all 9 errors in orchestration-pattern.md). Keep fences flat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/optional-plugins.md (DOCS-05 asset) with upstream-verified caveats</name>
  <files>components/optional-plugins.md</files>
  <action>
  Implement DOCS-05 asset half per CONTEXT.md locked corrections (caveman ships en + wenyan NOT en + ru; caveman-compress auto-backs up single-generation; rtk issue #1276 workaround distinct from upstream fix; SP/GSD install strings locked per CONTEXT.md `<decisions>` lines 62-66).

  File structure:

  1. File header with metadata comment:
     - First line: `<!-- verified_upstream: 2026-04-18 -->`
     - Rationale (per 06-CONTEXT.md threat T-06-02-02): a future doc-audit can grep this sentinel to identify stale upstream claims. This is a documentation invariant.
  2. Title: `# Optional Plugins`
  3. Intro paragraph: "The claude-code-toolkit complements four plugins you can install alongside it. TK does NOT auto-install any of them — this document lists them for manual installation. All install commands were verified against upstream on 2026-04-18; re-verify before referencing in new code."
  4. `## rtk (rtk-ai/rtk)` section:
     - Purpose paragraph: Token-optimized CLI proxy, 60-90% savings on common dev operations (`git status`, `cargo test`, etc.).
     - Install code block (```bash): `brew install rtk && rtk init -g`
     - macOS config path: `~/Library/Application Support/rtk/config.toml`
     - Linux config path: `~/.config/rtk/config.toml`
     - Subsection `### Known Issues`:
       - Symptom: `rtk ls` returns `(empty)` on non-English locales
       - Upstream issue: rtk-ai/rtk#1276 (OPEN as of 2026-04-18)
       - Upstream's intended fix: internal `cmd.env("LC_ALL", "C")` in Rust source (NOT user-configurable)
       - User-side workaround (```toml block showing `exclude_commands = ["ls"]`)
       - HONEST NOTE: "The workaround bypasses rtk's optimization for `ls`; the upstream fix preserves optimization. They are NOT the same — the workaround is a stopgap."
     - Subsection `### Relationship to cc-safety-net`: one paragraph noting that `setup-security.sh` installs a combined PreToolUse hook that sequences RTK + cc-safety-net (Phase 3 SAFETY work).
  5. `## caveman (JuliusBrussee/caveman)` section:
     - Purpose paragraph: Claude Code plugin for token compression; ~46% input token reduction on average (attribute to upstream README).
     - Install code block (```bash): `claude plugin marketplace add JuliusBrussee/caveman && claude plugin install caveman@caveman`
     - Language modes: `en + wenyan (Classical Chinese, 文言文)` — explicitly correct the common misconception that it includes Russian; it does NOT.
     - Intensity levels: Lite / Full (default) / Ultra. Wenyan variants: Wenyan-Lite / Wenyan-Full / Wenyan-Ultra.
     - Subsection `### Caveats`:
       - "caveman-compress auto-creates `CLAUDE.original.md` as a backup — no manual backup required."
       - "WARNING: The auto-backup is SINGLE-GENERATION. Re-running caveman-compress OVERWRITES the prior `CLAUDE.original.md`. Commit your CLAUDE.md to git BEFORE running compress so you can diff or revert from version control."
  6. `## superpowers (obra/superpowers)` section:
     - Purpose: Skills (systematic-debugging, writing-plans, test-driven-development, verification-before-completion, using-git-worktrees) + code-reviewer agent + commands.
     - Install code block (```bash): `claude plugin install superpowers@claude-plugins-official`
     - CRITICAL: The install string above is locked in 06-CONTEXT.md `<decisions>` lines 62-64. Do NOT use a marketplace-add form or any other variant — that would contradict the detection path in `scripts/detect.sh:54` + `scripts/verify-install.sh:197-200` and fail CI consistency checks.
     - Note on TK relationship: v4.0 TK is designed around SP. In `complement-sp` / `complement-full` modes, TK skips 7 duplicate files.
  7. `## get-shit-done (gsd-build/get-shit-done)` section:
     - Purpose: Phase-based workflow (`/gsd-plan-phase`, `/gsd-execute-phase`, etc.).
     - Install code block (```bash): `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)`
     - CRITICAL: The install string above is locked in 06-CONTEXT.md `<decisions>` lines 65-66. Do NOT use a `claude plugin marketplace add gsd-build/get-shit-done` form — GSD distributes via curl-install script per the filesystem path `~/.claude/get-shit-done/` detected in `scripts/detect.sh:29`.
     - Note: GSD currently has zero file conflicts with TK (per manifest.json). The `complement-gsd` mode exists for future composability.
  8. `## Source of Truth` section (final):
     - Paragraph: "Upstream facts re-verified 2026-04-18. See header `verified_upstream:` marker. Before authoring new docs that reference these plugins, re-verify via WebFetch or the upstream README." Link to upstream READMEs.

  Lint constraints:
  - MD040: every fence tagged (```bash, ```toml, ```text).
  - MD031/MD032: blank line around fences and lists.
  - MD026: headings without trailing punctuation.
  - MD024: Known Issues / Caveats subsections appear only once per parent — no siblings_only violation.
  - Keep all fences at top-level nesting (Pitfall 1 — no markdown-inside-code-block).

  Content invariants (non-negotiable, per CONTEXT.md):
  - caveman is en + wenyan (NOT en + ru)
  - caveman auto-backs up (NOT manual backup required); backup is single-generation
  - rtk #1276 workaround is NOT upstream's intended fix
  - SP install string: `claude plugin install superpowers@claude-plugins-official` (CONTEXT-locked)
  - GSD install string: `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)` (CONTEXT-locked)

  Do NOT modify any other file in this task. Do NOT touch manifest.json (06-03 Task 1 registers this file in the NEW `inventory.components` top-level section).
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -f components/optional-plugins.md && npx markdownlint-cli components/optional-plugins.md && grep -q 'verified_upstream: 2026-04-18' components/optional-plugins.md && grep -q 'wenyan' components/optional-plugins.md && grep -q 'rtk-ai/rtk#1276' components/optional-plugins.md && grep -q 'single-generation' components/optional-plugins.md && ! grep -q 'en + ru' components/optional-plugins.md && grep -q 'superpowers@claude-plugins-official' components/optional-plugins.md && grep -q 'raw.githubusercontent.com/gsd-build/get-shit-done' components/optional-plugins.md</automated>
  </verify>
  <done>
  - components/optional-plugins.md exists
  - Header contains `verified_upstream: 2026-04-18` marker
  - File documents rtk (with #1276 caveat), caveman (with wenyan + single-generation backup warning), superpowers (CONTEXT-locked install), get-shit-done (CONTEXT-locked install)
  - File does NOT contain the string `en + ru` (correction applied)
  - markdownlint passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create templates/global/RTK.md (DOCS-07 asset — install wiring is 06-03)</name>
  <files>templates/global/RTK.md</files>
  <action>
  Implement DOCS-07 asset half: create the net-new `templates/global/RTK.md` file. Install wiring into `scripts/setup-security.sh` is EXPLICITLY DEFERRED to Plan 06-03 Task 4 per CONTEXT.md D-DOCS-07.

  File structure (per 06-RESEARCH.md §7 content outline):

  1. Title: `# RTK — Toolkit Notes (Claude Code)`
  2. Intro blockquote (first non-title content):
     > Fallback notes when `rtk init -g` has not yet been run. If you installed rtk and ran `rtk init -g`, rtk's own `~/.claude/RTK.md` is authoritative. The Known Issues section below still applies regardless.
  3. `## What RTK Does` section:
     - One paragraph: RTK is a CLI proxy that reduces token consumption by 60-90% on common dev commands (`git status`, `cargo test`, `ls`, `grep`). It installs as a shell hook that transparently rewrites commands before they reach the Claude Code Bash tool.
  4. `## Quick Install` section:
     - Short paragraph pointing at full setup.
     - ```bash fence:
       ```bash
       brew install rtk
       rtk init -g   # installs hook + real RTK.md (overwrites this fallback)
       ```
  5. `## Known Issues` section:
     - Subsection `### rtk ls returns (empty) on non-English locales — rtk-ai/rtk#1276 (open as of 2026-04)`:
       - Symptom paragraph: `rtk ls /tmp` prints `(empty)` even when the directory has files, if your system locale is non-English (e.g., `LANG=es_ES.UTF-8`).
       - Cause paragraph: `rtk ls` parses `ls -la` output with an English-month regex; non-English locales emit localized month names that miss the regex.
       - Workaround subsection (user-side, no code change): ```toml fence showing `exclude_commands = ["ls"]` in `~/Library/Application Support/rtk/config.toml` (macOS) or `~/.config/rtk/config.toml` (Linux).
       - Upstream fix note: one-line patch `cmd.env("LC_ALL", "C")` in rtk Rust source; track status at github.com/rtk-ai/rtk/issues/1276.
       - HONEST NOTE that the user-side workaround is DIFFERENT from upstream's intended fix (bypasses vs preserves rtk's ls optimization).
  6. `## Relationship to Claude Code Safety Net` section:
     - One paragraph: RTK and the `cc-safety-net` hook both register against PreToolUse. The Claude Code Toolkit's `setup-security.sh` installs a combined hook that sequences both. If you see duplicate rewrites or missed blocks, verify your `~/.claude/settings.json` has the combined hook, not two separate entries.

  Lint constraints:
  - MD040: `bash` and `toml` fences tagged.
  - MD031/MD032: blank line before/after fences and the blockquote.
  - MD026: no trailing punctuation in headings. The research §7 skeleton uses `### rtk ls returns (empty) on non-English locales — rtk-ai/rtk#1276 (open as of 2026-04)` — `(open as of 2026-04)` ends with `)` which is fine (not a punctuation violation — MD026 flags `.`, `,`, `;`, `:`, `!`, `?`). Do NOT end a heading with `:` or `.`.
  - Keep all fences flat; no nesting.

  Insert no install-mechanism logic here — this task is the markdown file only. Plan 06-03 Task 4 handles the `scripts/setup-security.sh` fold-in with the `[ ! -f "$HOME/.claude/RTK.md" ]` guard.

  Do NOT modify `scripts/setup-security.sh` in this task. Do NOT modify any other file.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -f templates/global/RTK.md && npx markdownlint-cli templates/global/RTK.md && grep -q 'rtk-ai/rtk#1276' templates/global/RTK.md && grep -q 'exclude_commands' templates/global/RTK.md && grep -q 'Relationship to Claude Code Safety Net' templates/global/RTK.md</automated>
  </verify>
  <done>
  - templates/global/RTK.md exists
  - Contains `## Known Issues` section with rtk-ai/rtk#1276 reference
  - Contains `exclude_commands` workaround in a tagged ```toml fence
  - Contains `## Relationship to Claude Code Safety Net` section
  - markdownlint passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → upstream claims (rtk / caveman / SP / GSD) | Content in optional-plugins.md cites upstream state; downstream renders in user projects |
| repo → user home (~/.claude/RTK.md) | templates/global/RTK.md lands in user home via setup-security.sh (06-03); must never clobber existing RTK.md. This plan creates the source file only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Integrity / Information Disclosure | components/optional-plugins.md (typosquatting or outdated upstream install commands) | mitigate | All install commands verified against upstream 2026-04-18 (06-RESEARCH.md §4). SP/GSD install strings additionally cross-checked against CONTEXT.md `<decisions>` lines 62-66 (which themselves match `scripts/detect.sh:54` / `scripts/verify-install.sh:197-200`). Task 1 carries `verified_upstream: 2026-04-18` header marker. |
| T-06-02-02 | Tampering (stale caveat drift) | components/optional-plugins.md, templates/global/RTK.md | mitigate | `verified_upstream: 2026-04-18` header sentinel on optional-plugins.md enables future doc-audit grep (`grep -r 'verified_upstream:' components/ templates/`). Phase 7 VALIDATE-04 re-checks before release. Research validity expires 2026-05-18 per §16. |
| T-06-02-03 | Information Disclosure | Upstream plugin recommendations (rtk #1276 workaround that differs from upstream fix) | accept | Documented honestly: the workaround bypasses rtk's optimization; upstream's intended fix preserves it. Users are informed of the distinction, not misled. No secrets involved. |
</threat_model>

<verification>
End-of-plan verification:

- `npx markdownlint-cli components/optional-plugins.md templates/global/RTK.md` — 0 errors
- `test -f components/optional-plugins.md && test -f templates/global/RTK.md`
- `grep -q 'verified_upstream: 2026-04-18' components/optional-plugins.md`
- `grep -q 'wenyan' components/optional-plugins.md` (correction applied)
- `! grep -q 'en + ru' components/optional-plugins.md` (incorrect claim absent)
- `grep -q 'rtk-ai/rtk#1276' components/optional-plugins.md templates/global/RTK.md` (both files reference upstream issue)
- `grep -q 'superpowers@claude-plugins-official' components/optional-plugins.md` (CONTEXT-locked SP install)
- `grep -q 'raw.githubusercontent.com/gsd-build/get-shit-done' components/optional-plugins.md` (CONTEXT-locked GSD install)

NOTE on `make check`: This plan does NOT run `make check` / `make validate` as final gate because 06-01 (parallel) is bumping `manifest.json` version to `4.0.0` while 06-02 runs. During Wave 1 execution, either plan's `make validate` could race. Per-file `markdownlint` is the Wave 1 verifier; `make check` runs at wave boundary (after 06-01 AND 06-02 complete, before 06-03 starts).
</verification>

<success_criteria>
- DOCS-05 (asset half): components/optional-plugins.md documents rtk + caveman + SP + GSD with upstream-verified caveats and CONTEXT-locked install strings; carries verified_upstream marker [VERIFIED by Task 1 grep]
- DOCS-07 (asset half): templates/global/RTK.md exists with Known Issues section pointing to rtk-ai/rtk#1276 [VERIFIED by Task 2 grep]
- markdownlint green on both new files
- No manifest.json edit in this plan (explicit exclusion — 06-03 Task 1 registers in NEW `inventory.components` top-level section, NOT `files.components`)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-02-SUMMARY.md` using the @$HOME/.claude/get-shit-done/templates/summary.md template. Summary must include:
- Requirements satisfied: DOCS-05-asset, DOCS-07-asset (install wiring + manifest register deferred to 06-03)
- Files created (2): components/optional-plugins.md, templates/global/RTK.md
- Files modified (0): (manifest.json edit deferred to 06-03; registered in NEW `inventory.components` top-level section to avoid install-loop side-effect — see 06-03 Task 1 for rationale)
- Upstream re-verification date confirmed: 2026-04-18
- Decisions / corrections applied: caveman en+wenyan (NOT ru), caveman auto-backup is single-generation, rtk #1276 workaround ≠ upstream fix, SP/GSD install strings match CONTEXT.md `<decisions>` lines 62-66
- markdownlint output (both files green)
- Hand-off notes to 06-03:
  1. "Register components/optional-plugins.md + components/orchestration-pattern.md in NEW manifest.json `inventory.components` top-level section (NOT `files.components` — the latter would trigger `scripts/lib/install.sh:239` to install them into `.claude/components/`, which is unwanted). See 06-03 Task 1 for schema + validate-manifest.py extension."
  2. "Fold RTK.md install into scripts/setup-security.sh with `[ ! -f \"$HOME/.claude/RTK.md\" ]` guard (06-03 Task 4) — DOCS-07 install wiring half."
  3. "Print end-of-run optional-plugins stdout block via scripts/lib/optional-plugins.sh sourced by init-claude.sh + update-claude.sh (06-03 Tasks 2-3) — DOCS-06."
</output>
</output>
