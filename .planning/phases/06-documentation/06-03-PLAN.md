---
phase: 06-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - manifest.json
  - scripts/validate-manifest.py
  - .planning/phases/06-documentation/06-VALIDATION.md
  - components/orchestration-pattern.md
  - components/supreme-council.md
  - components/structured-workflow.md
  - README.md
  - scripts/lib/optional-plugins.sh
  - scripts/init-claude.sh
  - scripts/update-claude.sh
  - scripts/setup-security.sh
  - scripts/tests/test-setup-security-rtk.sh
  - Makefile
autonomous: true
gap_closure: false
requirements:
  - DOCS-05-register
  - DOCS-06
  - DOCS-07-install
  - DOCS-08

must_haves:
  truths:
    - "manifest.json has a NEW top-level `inventory.components` section (NOT under `files.*`) registering exactly 2 entries (components/optional-plugins.md + components/orchestration-pattern.md) — chosen over `files.components` to prevent `scripts/lib/install.sh::compute_file_diffs_obj` from iterating these into an install-loop for `.claude/components/`"
    - "manifest.json version field reads '4.0.0' and matches CHANGELOG [4.0.0] heading (make validate alignment check green)"
    - "scripts/validate-manifest.py extends to validate `inventory.components[]` entries have `path` + `description` fields and that each referenced file exists on disk (no SOURCE_MAP entry — components stay repo-root-only and are never installed into `.claude/`)"
    - "components/orchestration-pattern.md has zero markdownlint errors (all 9 from lines 201-231 fixed via flatten of nested fences)"
    - "components/supreme-council.md and components/structured-workflow.md each contain a new '## See Also' cross-reference pointing at orchestration-pattern.md (casing unified per LOW-3 — all three files use the exact heading `## See Also`)"
    - "README.md Components section contains a blurb linking to components/orchestration-pattern.md"
    - "scripts/lib/optional-plugins.sh exists and exports a function recommend_optional_plugins that prints the DOCS-06 block (rtk + caveman + superpowers + get-shit-done) — SP/GSD install strings CONTEXT-locked per 06-CONTEXT.md `<decisions>` lines 62-66"
    - "scripts/init-claude.sh sources scripts/lib/optional-plugins.sh and invokes recommend_optional_plugins at end-of-run (between recommend_statusline and setup_council per research §6)"
    - "scripts/update-claude.sh sources scripts/lib/optional-plugins.sh and invokes recommend_optional_plugins after print_update_summary"
    - "scripts/setup-security.sh installs templates/global/RTK.md to ~/.claude/RTK.md ONLY when absent (guard: [ ! -f \"$HOME/.claude/RTK.md\" ])"
    - "scripts/tests/test-setup-security-rtk.sh exists and exits 0 when RTK.md install behaves correctly in both absent-and-present scenarios (both generations covered — rtk-init-generated and tk-prior-install)"
    - "Makefile enumerates the new RTK test as Test 15 explicitly between current Test 14 and the sentinel `@echo \"All tests passed!\"` line (no glob; unconditional edit per MEDIUM-2)"
    - "06-VALIDATION.md Per-Task Verification Map uses actual final task IDs (06-01-01..04, 06-01-05, 06-02-01..02, 06-03-01..04) and nyquist_compliant flips to true at plan end (per BLOCKING-3)"
    - "DOCS-06 automated verify uses function-availability + stdout-helper checks (NOT `init-claude.sh --dry-run | grep`, which never matches because init-claude.sh:389 exits early under --dry-run before main() reaches recommend_optional_plugins)"
    - "make check passes green (markdownlint + shellcheck + validate + validate-base-plugins)"
    - "make test passes green (existing 14 test groups + new Test 15 for RTK.md install guard)"
  artifacts:
    - path: "manifest.json"
      provides: "NEW top-level `inventory.components` section with exactly 2 Phase-6 entries (optional-plugins.md + orchestration-pattern.md). Install scripts ignore `inventory` by design — they only iterate `.files | to_entries[]` (verified `scripts/lib/install.sh:239`)."
      contains: "inventory"
    - path: "scripts/validate-manifest.py"
      provides: "Extended schema check for inventory.components[] (path + description required, file exists on disk, no SOURCE_MAP entry)"
      contains: "inventory"
    - path: ".planning/phases/06-documentation/06-VALIDATION.md"
      provides: "Corrected Per-Task Verification Map with final task IDs + nyquist_compliant=true + fixed DOCS-06 verify command"
      contains: "nyquist_compliant: true"
    - path: "components/orchestration-pattern.md"
      provides: "Polished component (0 mdlint errors) with flat-fence rewrite of 'Wiring it into your own slash command' section + heading anchors instead of line-number refs (LOW-1)"
      contains: "Wiring it into your own slash command"
    - path: "components/supreme-council.md"
      provides: "`## See Also` cross-reference to orchestration-pattern.md (unified casing per LOW-3)"
      contains: "## See Also"
    - path: "components/structured-workflow.md"
      provides: "`## See Also` cross-reference to orchestration-pattern.md (unified casing per LOW-3)"
      contains: "## See Also"
    - path: "README.md"
      provides: "Components section blurb linking to orchestration-pattern.md"
      contains: "orchestration-pattern.md"
    - path: "scripts/lib/optional-plugins.sh"
      provides: "recommend_optional_plugins function — single source of truth for DOCS-06 stdout block with CONTEXT-locked SP/GSD install strings"
      contains: "recommend_optional_plugins"
    - path: "scripts/init-claude.sh"
      provides: "Sources lib/optional-plugins.sh and invokes function in main()"
      contains: "recommend_optional_plugins"
    - path: "scripts/update-claude.sh"
      provides: "Sources lib/optional-plugins.sh and invokes function after print_update_summary"
      contains: "recommend_optional_plugins"
    - path: "scripts/setup-security.sh"
      provides: "Guarded copy of templates/global/RTK.md to ~/.claude/RTK.md"
      contains: "RTK.md"
    - path: "scripts/tests/test-setup-security-rtk.sh"
      provides: "Integration test covering RTK.md install (absent → lands; present → NOT clobbered; both generations — TK's and rtk init's)"
      contains: "test-setup-security-rtk"
    - path: "Makefile"
      provides: "Explicit Test 15 entry between Test 14 and 'All tests passed!' sentinel (no glob; unconditional per MEDIUM-2)"
      contains: "Test 15"
  key_links:
    - from: "manifest.json inventory.components"
      to: "components/optional-plugins.md (06-02 Task 1) + components/orchestration-pattern.md (polished in 06-03 Task 3)"
      via: "inventory bucket path registration (both files must exist on disk at validate-time; no SOURCE_MAP entry because inventory paths are repo-root-only, never installed into user .claude/)"
      pattern: "inventory\\.components"
    - from: "scripts/lib/install.sh:239 manifest iteration"
      to: "`.files | to_entries[]`"
      via: "install/update scripts iterate `.files` only; `inventory` is ignored by design — proven by reading lines 228-246 of install.sh"
      pattern: "\\.files \\| to_entries"
    - from: "scripts/lib/optional-plugins.sh"
      to: "components/optional-plugins.md (06-02 artifact)"
      via: "the shell function prints a condensed version of the content in components/optional-plugins.md — future doc updates must sync both"
      pattern: "optional-plugins"
    - from: "scripts/setup-security.sh"
      to: "templates/global/RTK.md (06-02 artifact)"
      via: "cp with [ ! -f \"$HOME/.claude/RTK.md\" ] guard"
      pattern: "RTK\\.md"
    - from: "scripts/init-claude.sh + scripts/update-claude.sh"
      to: "scripts/lib/optional-plugins.sh"
      via: "source directive at script top (matches existing lib/install.sh + lib/state.sh sourcing convention)"
      pattern: "source.*optional-plugins\\.sh"
---

<objective>
Complete Phase 6 and resolve all three plan-checker BLOCKING findings plus HIGH-1/HIGH-2/MEDIUM-1/MEDIUM-2/LOW-1/LOW-3:

1. BLOCKING-1 (SP/GSD install strings) was resolved in 06-01 + 06-02 rewrites — this plan verifies by using the same CONTEXT-locked strings in `scripts/lib/optional-plugins.sh`.
2. BLOCKING-2 (manifest iteration side-effect) resolved here: Task 1 creates a NEW top-level `inventory.components` section instead of `files.components`, avoiding `scripts/lib/install.sh:239` install-loop iteration. `scripts/validate-manifest.py` is extended accordingly.
3. BLOCKING-3 (stale VALIDATION.md task IDs + broken DOCS-06 verify command) resolved here: Task 0 rewrites 06-VALIDATION.md Per-Task Verification Map to match final task IDs, fixes the DOCS-06 verify command, and flips `nyquist_compliant: true`.
4. HIGH-1 (broken .claude/components link) already fixed in the canonical block at 06-01 Task 3 (absolute GitHub blob URL) — this plan doesn't re-introduce the broken pattern.
5. HIGH-2 (DOCS-07 partial completion split) reflected in frontmatter: `requirements: [DOCS-05-register, DOCS-06, DOCS-07-install, DOCS-08]`.
6. MEDIUM-1 (Makefile drift guard) shipped in 06-01 Task 5.
7. MEDIUM-2 (unconditional Makefile Test 15 entry) implemented in Task 4 step 3.
8. LOW-1 (heading anchors instead of line numbers) applied in Task 3 orchestration-pattern.md polish.
9. LOW-3 (unified `## See Also` casing) applied in Task 3 cross-references.

Output: 1 manifest edit (new top-level `inventory.components`), 1 validate-manifest.py extension, 1 VALIDATION.md map rewrite, 2 component cross-ref edits, 1 orchestration-pattern.md content polish, 1 README Components blurb, 1 new lib script, 2 script wiring edits (init + update), 1 script install-path edit (setup-security), 1 net-new integration test, 1 Makefile Test 15 entry.

DEPENDENCIES (hard):
- 06-01 must complete first (CHANGELOG version bump to 4.0.0, manifest.json version bump to 4.0.0, README install section, 7-template blocks, docs/INSTALL.md, Makefile validate-base-plugins target)
- 06-02 must complete first (components/optional-plugins.md AND templates/global/RTK.md must exist on disk before Task 1 registers them in manifest inventory and before Task 4 reads templates/global/RTK.md as install source)
- Wave 2 gating enforced by wave structure; at plan start, executor MUST verify:
  - `test -f components/optional-plugins.md` (06-02 Task 1 output)
  - `test -f templates/global/RTK.md` (06-02 Task 2 output)
  - `jq -e '.version == "4.0.0"' manifest.json` (06-01 Task 1 output)
  - `grep -q '## \[4.0.0\]' CHANGELOG.md` (06-01 Task 1 output)
  - `make validate-base-plugins` exits 0 (06-01 Task 5 output)
  If ANY precondition fails, STOP and report — Wave 1 did not complete successfully.

SCOPE RATIONALE (manifest consolidation):
The manifest component registration was originally allocated to 06-02, but 06-01 (parallel Wave 1) also edits manifest.json (version bump from 3.0.0 to 4.0.0 for DOCS-03 / BUG-06 alignment). To preserve the Wave 1 files_modified no-overlap invariant, all manifest edits are consolidated here in 06-03 (Wave 2, after 06-01 completes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
@.planning/phases/06-documentation/06-VALIDATION.md
@.planning/phases/06-documentation/06-01-SUMMARY.md
@.planning/phases/06-documentation/06-02-SUMMARY.md
@components/orchestration-pattern.md
@components/supreme-council.md
@components/structured-workflow.md
@components/optional-plugins.md
@templates/global/RTK.md
@scripts/init-claude.sh
@scripts/update-claude.sh
@scripts/setup-security.sh
@scripts/lib/install.sh
@scripts/lib/state.sh
@scripts/validate-manifest.py
@manifest.json
@Makefile
@README.md

<interfaces>
**From 06-01 artifacts (pre-existing at start of 06-03):**
- `manifest.json` — `version: "4.0.0"`, existing `files.{agents, prompts, commands, skills, rules}` buckets intact
- `CHANGELOG.md` — `## [4.0.0] - TBD` heading with BREAKING CHANGES leading block
- `Makefile` — `validate-base-plugins` target wired into `make check`

**From 06-02 artifacts (pre-existing at start of 06-03):**
- `components/optional-plugins.md` — source of truth for the 4-plugin list and caveats (CONTEXT-locked SP/GSD install strings)
- `templates/global/RTK.md` — install source file (setup-security.sh copies this to ~/.claude/RTK.md with guard)

**Manifest extension target — NEW top-level `inventory.components` section (BLOCKING-2 resolution):**

The install scripts iterate `.files | to_entries[]` (verified in `scripts/lib/install.sh:239`). Adding `components` under `files` would cause `compute_file_diffs_obj` to flag `components/optional-plugins.md` and `components/orchestration-pattern.md` for install into `$CLAUDE_DIR/components/` — unwanted behavior change.

Solution: register these files under a NEW top-level `inventory` key. Install scripts ignore anything outside `.files` by design, so NO changes are needed to install/update scripts.

Target JSON — add as a new top-level key AFTER `files` (and before `claude_md_sections`):

```json
"inventory": {
  "components": [
    {
      "path": "components/optional-plugins.md",
      "description": "Recommended optional plugins (rtk, caveman, superpowers, get-shit-done) with caveats; verified upstream 2026-04-18"
    },
    {
      "path": "components/orchestration-pattern.md",
      "description": "Generic orchestration pattern (lean-orchestrator + fat-subagents); cross-referenced from supreme-council.md and structured-workflow.md"
    }
  ]
}
```

**scripts/validate-manifest.py extension target (BLOCKING-2 downstream):**

Read lines 39-55 of `scripts/validate-manifest.py` — `SOURCE_MAP` translates install-dest paths to source paths for the 5 install buckets. Do NOT add a `components/` entry to SOURCE_MAP — inventory paths are repo-root-only and never installed into `.claude/`.

Add a new validation block after the `templates.*` check (after current line ~175, before "Check 6: disk-to-manifest drift"):

```python
# Check 7: inventory.components[] — repo-root asset registry (NOT installed into .claude/)
inventory_section = manifest.get("inventory", {})
inventory_components = inventory_section.get("components", [])
for idx, entry in enumerate(inventory_components):
    location = "inventory.components[" + str(idx) + "]"
    if not isinstance(entry, dict):
        fail(location + ": entry must be an object, got " + type(entry).__name__)
        errors += 1
        continue
    for required_field in ("path", "description"):
        if required_field not in entry:
            fail(location + ': missing required "' + required_field + '" key')
            errors += 1
    path_value = entry.get("path")
    if path_value:
        # Inventory paths are repo-root-relative (no SOURCE_MAP translation)
        full_path = os.path.join(REPO_ROOT, path_value)
        if not os.path.exists(full_path):
            fail(location + ": path does not exist on disk: " + path_value)
            errors += 1
```

This mirrors the existing `templates.*` path-existence check pattern (lines 156-175 of validate-manifest.py).

**From 06-RESEARCH.md §8 — orchestration-pattern.md markdownlint errors (all 9 concentrated in lines 201-231):**

```text
components/orchestration-pattern.md:211 MD031 (blanks-around-fences)
components/orchestration-pattern.md:214 MD029 (ol-prefix) expected 1; got 2
components/orchestration-pattern.md:221 MD029 expected 2; got 3
components/orchestration-pattern.md:224 MD029 expected 3; got 4
components/orchestration-pattern.md:225 MD031 "```bash"
components/orchestration-pattern.md:229 MD029 expected 4; got 5
components/orchestration-pattern.md:230 MD032 (blanks-around-lists)
components/orchestration-pattern.md:231 MD031 "```"
components/orchestration-pattern.md:231 MD040 (fenced-code-language)
```

Root cause: a ```markdown fenced block containing a nested ```bash fence. Markdownlint's fence-pair tracker treats the inner ``` as closing the outer, cascading errors.

**Fix pattern (from research §8 + LOW-1 plan-checker feedback):** Replace the broken section with a flattened structure. LOW-1: use heading anchors (section names) instead of line-number refs when cross-documenting; line numbers drift across commits. Structure:
- `## Wiring it into your own slash command`
- Prose paragraph: "Minimum viable adaptation in a custom command at `commands/your-command.md`:"
- `### 1. Load context` + prose + ```bash fence (single, flat)
- `### 2. Spawn subagents in parallel` + prose + bullet list
- `### 3. Collect confirmations` + prose
- `### 4. Commit atomically` + ```bash fence
- `### 5. Present next-up` + prose

NO nested fences. Each fence at top-level nesting. Cross-references should say "see the 'Wiring it into your own slash command' section" rather than "see lines 201-231".

**Cross-reference text — unified casing `## See Also` (LOW-3 plan-checker feedback):**

Apply this exact casing in BOTH supreme-council.md and structured-workflow.md. Also ensure any existing "See Also" / "See also" / "see also" headings in orchestration-pattern.md itself are normalized to `## See Also`.

To `components/supreme-council.md` (end of file, after the "Add to CLAUDE.md" block):

```markdown
## See Also

- [orchestration-pattern.md](./orchestration-pattern.md) — lean-orchestrator + fat-subagents
  pattern that Council implements. Explains why `brain.py` spawns Gemini and ChatGPT as
  parallel subagents rather than calling them sequentially from one context.
```

To `components/structured-workflow.md` (end of file, after the "Example Flow" section):

```markdown
## See Also

- [orchestration-pattern.md](./orchestration-pattern.md) — scaling multi-step workflows
  **beyond a single agent's context window**. Structured Workflow disciplines a single
  context; orchestration-pattern delegates across many fresh contexts. Complementary patterns.
```

To `README.md` (existing "Components" reference section or a new short "Components" section):

```markdown
**Orchestration pattern** — see [components/orchestration-pattern.md](components/orchestration-pattern.md)
for the lean-orchestrator + fat-subagents design Council and GSD workflows both use.
Helps any custom slash command scale beyond a single context window.
```

**From 06-RESEARCH.md §6 — DOCS-06 shell wiring (line numbers are STARTING HINTS — re-grep before edit because 06-01's README/CHANGELOG work MAY have shifted init-claude.sh's end-of-run block):**

`scripts/init-claude.sh` main() structure (verify via `grep -n '^main\|recommend_security\|recommend_statusline\|setup_council' scripts/init-claude.sh`):
- `main() { ... }` definition
- `recommend_security` call
- `recommend_statusline` call
- `setup_council` call (interactive — must remain last)

**Insertion point:** between `recommend_statusline` and `setup_council`. Add `recommend_optional_plugins` call here.

**IMPORTANT (BLOCKING-3 awareness):** `scripts/init-claude.sh:389` exits early under `--dry-run` BEFORE main() reaches `recommend_optional_plugins` — this is why Task 0 fixes the DOCS-06 verify command from `init-claude.sh --dry-run | grep` (which never matches) to a function-availability check plus a direct lib-sourcing stdout capture.

`scripts/update-claude.sh` — `print_update_summary()` function + call near end of script. Re-grep to confirm line numbers.

**Insertion point:** after the call to `print_update_summary`. Add `recommend_optional_plugins` call here.

**CYAN color constant:**
- `update-claude.sh` defines `CYAN='\033[0;36m'`
- `init-claude.sh` does NOT currently define `CYAN`
- Recommendation: define CYAN (and other colors) in the lib file itself with `[[ -z "${CYAN:-}" ]]` guards — single source of truth, self-contained, won't clobber caller's pre-existing colors.

**recommend_optional_plugins function body — CONTEXT-locked SP/GSD install strings (BLOCKING-1 downstream):**

```bash
recommend_optional_plugins() {
    echo ""
    echo -e "${CYAN}🧩 Recommended optional plugins:${NC}"
    echo ""
    echo -e "  ${YELLOW}rtk${NC} — 60-90% token savings on dev commands"
    echo -e "    Install: ${YELLOW}brew install rtk && rtk init -g${NC}"
    echo -e "    ${RED}Known issue${NC}: rtk ls broken on non-English locales (rtk-ai/rtk#1276)"
    echo -e "    Workaround: add exclude_commands = [\"ls\"] to ~/Library/Application Support/rtk/config.toml"
    echo ""
    echo -e "  ${YELLOW}caveman${NC} — ~46% fewer input tokens per session"
    echo -e "    Install: ${YELLOW}claude plugin marketplace add JuliusBrussee/caveman && claude plugin install caveman@caveman${NC}"
    echo -e "    ${YELLOW}⚠${NC} caveman-compress auto-backs up CLAUDE.md to CLAUDE.original.md; commit CLAUDE.md to git before running compress"
    echo -e "    Languages: en + wenyan (Classical Chinese)"
    echo ""
    echo -e "  ${YELLOW}superpowers${NC} (obra) — skills + code-reviewer agent (TK complements)"
    echo -e "    Install: ${YELLOW}claude plugin install superpowers@claude-plugins-official${NC}"
    echo ""
    echo -e "  ${YELLOW}get-shit-done${NC} (gsd-build) — phase-based workflow (TK complements)"
    echo -e "    Install: ${YELLOW}bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)${NC}"
    echo ""
    echo -e "  ${BLUE}Details:${NC} see components/optional-plugins.md in the TK repo"
}
```

CRITICAL (BLOCKING-1): SP install string is `claude plugin install superpowers@claude-plugins-official`; GSD install string is `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)`. These match CONTEXT.md `<decisions>` lines 62-66 and the canonical block in templates/*/CLAUDE.md (06-01 Task 3). Any deviation fails CI.

NOTE: "see components/optional-plugins.md in the TK repo" — do NOT point at `.claude/components/optional-plugins.md` (HIGH-1: that path does not exist after BLOCKING-2 resolution because components are NOT installed into `.claude/`).

**Color-constant contract:** the function references `CYAN`, `YELLOW`, `RED`, `BLUE`, `NC`. Lib file declares these at top with guards (`[[ -z "${CYAN:-}" ]] && CYAN='\033[0;36m'`).

**Shell style constraints (per repo CLAUDE.md):**
- `set -euo pipefail` in any new standalone script (test script) — but NOT in sourced lib files (sourcing a file with `set -e` poisons the caller). Pattern matches existing `scripts/lib/install.sh` and `scripts/lib/state.sh`.
- Functions defined at top-level, `local` variables inside.
- `[[ ... ]]` for conditionals.
- Fail-fast idiom in the new test: validate preconditions before running scenarios.

**shellcheck constraints (every .sh file MUST pass):**
- Severity threshold: warning (CI enforces)
- Common gotchas: quote every `$var` expansion that could contain spaces; use `[[ ]]` not `[ ]`; check exit codes explicitly.

**Install guard for RTK.md (from 06-CONTEXT.md D-DOCS-07):**
- `[ ! -f "$HOME/.claude/RTK.md" ] || return 0` — never clobber existing ~/.claude/RTK.md
- setup-security.sh is the canonical `~/.claude/` writer; the copy slots into its existing flow
- On skip, print a one-line info message: "~/.claude/RTK.md already exists (rtk init -g or prior TK install); leaving untouched. See upstream rtk-ai/rtk#1276 caveat in components/optional-plugins.md."

**Makefile Test 15 entry — UNCONDITIONAL (MEDIUM-2 plan-checker feedback):**

The Makefile enumerates Test 1..14 explicitly (no glob; verified by reading Makefile:42-96). Insert Test 15 AFTER the Test 14 block and BEFORE the `@echo "All tests passed!"` sentinel line. Pattern matches Test 1..14 exactly:

```makefile
	@echo ""
	@echo "Test 15: setup-security.sh RTK.md install guard"
	@bash scripts/tests/test-setup-security-rtk.sh
```

No conditional "if target runs via glob, skip" logic — this is a mandatory edit.

**06-VALIDATION.md Per-Task Verification Map — corrected task IDs (BLOCKING-3 plan-checker feedback):**

Current map has stale IDs (`06-02-03` doesn't exist; `06-03-01..03` map to wrong tasks). Final task structure:
- 06-01: 4 tasks (01, 02, 03, 04) + Task 5 drift guard = 5 tasks total (01-01..01-05)
- 06-02: 2 tasks (02-01, 02-02)
- 06-03: 4 tasks (03-01 manifest+validate, 03-02 VALIDATION.md sync [this Task 0], 03-03 orch polish+cross-ref, 03-04 optional-plugins lib+wiring, 03-05 RTK install+test+Makefile Test15)

Wait — numbering reconciliation: this plan has Task 0 (VALIDATION sync), Task 1 (manifest), Task 2 (not explicit in the draft — absorb into others), Task 3 (orch), Task 4 (lib+wiring), Task 5 (setup-security). For VALIDATION.md Per-Task map purposes, use these final IDs: 06-03-01 (manifest+validate-manifest.py), 06-03-02 (VALIDATION.md sync), 06-03-03 (orch polish + cross-refs + README blurb), 06-03-04 (optional-plugins lib + init/update wiring + RTK install + test + Makefile Test15). Executor MAY split 06-03-04 into 06-03-04a/04b for clarity — but VALIDATION map stays 4-row wide for 06-03.

**Corrected DOCS-06 verify command (BLOCKING-3 plan-checker feedback):**

Replace the broken `bash scripts/init-claude.sh --dry-run 2>&1 | grep -c "Recommended optional plugins"` (never matches — init-claude.sh:389 exits early under --dry-run) with:

```bash
# 1. Function availability check
bash -c 'source scripts/lib/optional-plugins.sh && type recommend_optional_plugins >/dev/null 2>&1'

# 2. Stdout capture via direct lib-sourcing (bypasses init-claude.sh main())
bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins 2>&1 | grep -q "Recommended optional plugins"'
```

Both commands exit 0 on success. The verify row for DOCS-06 in the Per-Task Verification Map uses command #2 as the canonical automated check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 2 Phase-6 components in manifest.json inventory.components + extend validate-manifest.py (BLOCKING-2)</name>
  <files>manifest.json, scripts/validate-manifest.py</files>
  <action>
  Implement BLOCKING-2 resolution: register the 2 Phase-6 components in a NEW top-level `inventory.components` section — NOT `files.components`. This prevents `scripts/lib/install.sh::compute_file_diffs_obj` from iterating these into an install-loop for `$CLAUDE_DIR/components/`.

  Rationale (verified by reading `scripts/lib/install.sh:228-246`): install scripts iterate `.files | to_entries[]`. Anything outside `.files` is automatically ignored. No changes to install/update scripts required.

  Runs FIRST in 06-03 so later tasks can assume manifest is current.

  **Step A — Edit manifest.json:**

  1. Read current `manifest.json`. Confirm `version` field reads `"4.0.0"` (set by 06-01 Task 1). If it still reads `"3.0.0"`, STOP and report — this indicates 06-01 did not complete and 06-03 is out of order.
  2. Confirm preconditions: `components/optional-plugins.md` and `components/orchestration-pattern.md` both exist on disk.
  3. Locate the closing `}` of the `files` object (currently contains `agents`, `prompts`, `commands`, `skills`, `rules`).
  4. Immediately AFTER the `files` object and BEFORE `claude_md_sections`, insert a NEW top-level `inventory` key. Do NOT nest under `files`:

     ```json
     "inventory": {
       "components": [
         {
           "path": "components/optional-plugins.md",
           "description": "Recommended optional plugins (rtk, caveman, superpowers, get-shit-done) with caveats; verified upstream 2026-04-18"
         },
         {
           "path": "components/orchestration-pattern.md",
           "description": "Generic orchestration pattern (lean-orchestrator + fat-subagents); cross-referenced from supreme-council.md and structured-workflow.md"
         }
       ]
     },
     ```

  5. Preserve all existing `files.*` entries VERBATIM. Preserve `manifest_version: 2`, `version: "4.0.0"`, `updated`, `description`, `sp_equivalent_note`, `claude_md_sections`, `templates` — do NOT touch.
  6. Final JSON must be valid (parseable by `jq` and `python3 -m json.tool`).

  **Step B — Extend scripts/validate-manifest.py:**

  1. Read current `scripts/validate-manifest.py` (211 lines). Confirm `SOURCE_MAP` dict at lines 39-45 contains ONLY the 5 install buckets (agents, prompts, skills, rules, commands). Do NOT add a `components/` entry — inventory paths are repo-root-only and are NEVER installed into `.claude/`.
  2. Add a new validation block AFTER the `templates.*` loop (after current line ~175) and BEFORE the `# Check 6: disk-to-manifest drift` comment. Exact block:

     ```python
     # Check 7: inventory.components[] — repo-root asset registry (NOT installed into .claude/)
     inventory_section = manifest.get("inventory", {})
     inventory_components = inventory_section.get("components", [])
     for idx, entry in enumerate(inventory_components):
         location = "inventory.components[" + str(idx) + "]"
         if not isinstance(entry, dict):
             fail(location + ": entry must be an object, got " + type(entry).__name__)
             errors += 1
             continue
         for required_field in ("path", "description"):
             if required_field not in entry:
                 fail(location + ': missing required "' + required_field + '" key')
                 errors += 1
         path_value = entry.get("path")
         if path_value:
             # Inventory paths are repo-root-relative (no SOURCE_MAP translation)
             full_path = os.path.join(REPO_ROOT, path_value)
             if not os.path.exists(full_path):
                 fail(location + ": path does not exist on disk: " + path_value)
                 errors += 1
     ```

  3. Keep the existing `ALLOWED_CONFLICTS`, `SOURCE_MAP`, `resolve_source_path`, `fail`, `main` functions untouched.
  4. Python 3.8+ compatibility: the block uses basic dict/list/str ops only — no walrus operator, no match statement.

  Validation:
  - Both inventory paths MUST exist on disk (both files are 06-02 Task 1 and pre-existing respectively).
  - `jq '.inventory.components | length == 2' manifest.json` — exits 0 with `true` output.
  - `python3 scripts/validate-manifest.py` — MUST pass (includes new Check 7).
  - `make validate` — MUST remain green (manifest ↔ CHANGELOG version alignment unaffected; install-script iteration of `.files | to_entries[]` unchanged).

  Do NOT modify any other file in this task. Do NOT add a SOURCE_MAP entry for components (that would imply install-destination translation, which is the wrong semantics).
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && python3 -m json.tool manifest.json > /dev/null && jq -e '.inventory.components | length == 2' manifest.json && jq -e '.inventory.components[] | select(.path == "components/optional-plugins.md")' manifest.json > /dev/null && jq -e '.inventory.components[] | select(.path == "components/orchestration-pattern.md")' manifest.json > /dev/null && jq -e '.version == "4.0.0"' manifest.json && ! jq -e '.files.components' manifest.json && python3 scripts/validate-manifest.py && make validate</automated>
  </verify>
  <done>
  - manifest.json parses as valid JSON
  - manifest.json `inventory.components` array has exactly 2 entries (NOT under `files`)
  - manifest.json `files.components` does NOT exist (negative assertion)
  - Both `components/optional-plugins.md` and `components/orchestration-pattern.md` are registered with `path` + `description` fields
  - Other `files.*` entries unchanged
  - manifest.json version remains "4.0.0"
  - `scripts/validate-manifest.py` exits 0 (new Check 7 passes)
  - `make validate` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync 06-VALIDATION.md with final task IDs + fix DOCS-06 verify command + flip nyquist_compliant=true (BLOCKING-3)</name>
  <files>.planning/phases/06-documentation/06-VALIDATION.md</files>
  <action>
  Implement BLOCKING-3 resolution: the current 06-VALIDATION.md Per-Task Verification Map carries stale task IDs (`06-02-03` does not exist; `06-03-01..03` map to wrong actual tasks). Also row `06-03-02` uses a broken automated command: `bash scripts/init-claude.sh --dry-run 2>&1 | grep -c "Recommended optional plugins"` will never match because `init-claude.sh:389` exits early under `--dry-run` before `main()` reaches `recommend_optional_plugins`.

  **Step A — Rewrite Per-Task Verification Map with final task IDs:**

  Read `.planning/phases/06-documentation/06-VALIDATION.md`. Replace the entire `## Per-Task Verification Map` section (lines 37-52 approximately — verify boundaries before edit) with this corrected map:

  ```markdown
  ## Per-Task Verification Map

  | Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
  |---------|------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
  | 06-01-01 | 01 | 1 | DOCS-03 | N/A | mdlint + manifest version alignment | `npx markdownlint-cli CHANGELOG.md && make validate` | ✅ (modify) | ⬜ pending |
  | 06-01-02 | 01 | 1 | DOCS-01 | N/A (docs-only) | mdlint + grep | `npx markdownlint-cli README.md && grep -q 'Standalone install' README.md` | ✅ (modify) | ⬜ pending |
  | 06-01-03 | 01 | 1 | DOCS-02 (×7 templates) | N/A | grep + mdlint + CONTEXT-locked install string check | `for f in templates/*/CLAUDE.md; do grep -q '^## Required Base Plugins' "$f" && grep -q 'superpowers@claude-plugins-official' "$f" && grep -q 'raw.githubusercontent.com/gsd-build/get-shit-done' "$f"; done` | ✅ (7 templates) | ⬜ pending |
  | 06-01-04 | 01 | 1 | DOCS-04 | N/A | mdlint + mode-name grep | `npx markdownlint-cli docs/INSTALL.md && grep -q complement-full docs/INSTALL.md` | ❌ new | ⬜ pending |
  | 06-01-05 | 01 | 1 | DOCS-02 (drift guard) | N/A | Makefile target | `make validate-base-plugins` | ✅ (modify) | ⬜ pending |
  | 06-02-01 | 02 | 1 | DOCS-05-asset | Upstream facts verified 2026-04-18 | mdlint + grep | `npx markdownlint-cli components/optional-plugins.md && grep -q 'verified_upstream: 2026-04-18' components/optional-plugins.md && grep -q 'wenyan' components/optional-plugins.md` | ❌ new | ⬜ pending |
  | 06-02-02 | 02 | 1 | DOCS-07-asset | N/A | mdlint + grep | `npx markdownlint-cli templates/global/RTK.md && grep -q 'rtk-ai/rtk#1276' templates/global/RTK.md` | ❌ new | ⬜ pending |
  | 06-03-01 | 03 | 2 | DOCS-05-register / DOCS-08 (manifest) | N/A | manifest schema validate | `jq -e '.inventory.components \| length == 2' manifest.json && python3 scripts/validate-manifest.py` | ✅ (modify) | ⬜ pending |
  | 06-03-02 | 03 | 2 | N/A (plan hygiene) | N/A | VALIDATION.md self-consistency | `grep -q 'nyquist_compliant: true' .planning/phases/06-documentation/06-VALIDATION.md` | ✅ (modify) | ⬜ pending |
  | 06-03-03 | 03 | 2 | DOCS-08 (content polish + cross-refs) | N/A | mdlint + grep | `npx markdownlint-cli components/orchestration-pattern.md && grep -q 'orchestration-pattern' components/supreme-council.md components/structured-workflow.md README.md` | ✅ (modify) | ⬜ pending |
  | 06-03-04 | 03 | 2 | DOCS-06 | No auto-install; informational stdout only | shellcheck + function-availability + stdout capture | `bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins 2>&1 \| grep -q "Recommended optional plugins"'` | ✅ (modify) | ⬜ pending |
  | 06-03-05 | 03 | 2 | DOCS-07-install | Guard: never clobber existing RTK.md | shellcheck + integration test + Makefile Test 15 | `bash scripts/tests/test-setup-security-rtk.sh && make test` | ❌ new test | ⬜ pending |

  *Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
  ```

  Key corrections vs current map:
  - `06-02-03` row REMOVED (task doesn't exist; manifest registration is 06-03-01)
  - `06-03-01` mapped to manifest/validate-manifest.py work (was previously labeled DOCS-08 polish — wrong)
  - `06-03-02` mapped to VALIDATION.md sync (this task)
  - `06-03-03` mapped to orchestration polish + cross-refs
  - `06-03-04` REPLACES broken `init-claude.sh --dry-run | grep` command with function-availability + stdout-capture form per BLOCKING-3
  - `06-03-05` mapped to RTK install wiring + test + Makefile Test 15
  - `06-01-05` row ADDED for new Makefile drift-guard target (MEDIUM-1)

  **Step B — Flip `nyquist_compliant: true` in frontmatter:**

  The frontmatter currently reads `nyquist_compliant: false` (line 4). After rewriting the map above, change to `nyquist_compliant: true`.

  Also update the bottom-of-file checklist: the item `- [ ] `nyquist_compliant: true` set in frontmatter` → `- [x] `nyquist_compliant: true` set in frontmatter`.

  And update the Approval footer from "pending" to "approved after BLOCKING-3 resolution by 06-03-02 (2026-04-19)".

  **Step C — Update status field:**

  Change frontmatter `status: draft` → `status: ratified`.

  Do NOT touch any other section of 06-VALIDATION.md. Do NOT touch any other file in this task.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && grep -q 'nyquist_compliant: true' .planning/phases/06-documentation/06-VALIDATION.md && grep -q '^| 06-01-05' .planning/phases/06-documentation/06-VALIDATION.md && grep -q '^| 06-03-04' .planning/phases/06-documentation/06-VALIDATION.md && grep -q '^| 06-03-05' .planning/phases/06-documentation/06-VALIDATION.md && ! grep -q '^| 06-02-03' .planning/phases/06-documentation/06-VALIDATION.md && ! grep -q 'init-claude.sh --dry-run' .planning/phases/06-documentation/06-VALIDATION.md && grep -q 'source scripts/lib/optional-plugins.sh' .planning/phases/06-documentation/06-VALIDATION.md</automated>
  </verify>
  <done>
  - 06-VALIDATION.md frontmatter `nyquist_compliant: true`
  - 06-VALIDATION.md Per-Task Verification Map has rows 06-01-01..06-01-05, 06-02-01..06-02-02, 06-03-01..06-03-05 (12 total)
  - Stale `06-02-03` row removed
  - Broken `init-claude.sh --dry-run | grep` command replaced with function-availability + stdout capture form
  - Bottom-of-file checklist item for nyquist_compliant checked ✅
  - `status: ratified` in frontmatter
  </done>
</task>

<task type="auto">
  <name>Task 3: Flatten orchestration-pattern.md nested fences + add unified '## See Also' cross-references + README blurb (DOCS-08, LOW-1, LOW-3)</name>
  <files>components/orchestration-pattern.md, components/supreme-council.md, components/structured-workflow.md, README.md</files>
  <action>
  Implement DOCS-08 content polish + LOW-1 (heading anchors not line numbers) + LOW-3 (unified `## See Also` casing).

  **Step 1 — Fix mdlint errors in orchestration-pattern.md:**
  - Read `components/orchestration-pattern.md` fully.
  - Locate the section "Wiring it into your own slash command" by heading text, NOT line numbers (LOW-1).
  - The section currently contains a ```markdown fenced block that internally opens a ```bash fence. The inner ``` closes the outer, cascading 9 errors.
  - REPLACE the entire broken section with a flattened version:
    - Heading: `## Wiring it into your own slash command`
    - Prose: "Minimum viable adaptation in a custom command at `commands/your-command.md`:"
    - `### 1. Load context` subsection with prose + one flat ```bash fence showing `INIT=$(node "$HOME/.claude/your-toolkit/bin/your-tools.cjs" init your-workflow)`
    - `### 2. Spawn subagents in parallel` subsection with prose + a bullet list describing the subagent spawn fields (subagent_type, model, prompt, run_in_background)
    - `### 3. Collect confirmations` subsection with prose on reading confirmation strings only (never full transcripts)
    - `### 4. Commit atomically` subsection with prose + one flat ```bash fence showing `node "$HOME/.claude/your-toolkit/bin/your-tools.cjs" commit ...`
    - `### 5. Present next-up` subsection with prose on telling the user what to run next
  - NO nested fences anywhere. All fences at top level.
  - Preserve the file's existing "See also" / "See Also" bottom section if any — normalize casing to `## See Also` (LOW-3).
  - Any internal cross-reference that previously pointed at "lines 201-231" should now say "the Wiring it into your own slash command section" (LOW-1 — heading anchors travel across commits, line numbers don't).

  **Step 2 — Append unified `## See Also` to components/supreme-council.md:**
  - Read file; locate end (after the last "Add to CLAUDE.md" block per research).
  - Append a new `## See Also` section with the exact text from the interfaces block above. Heading casing is `## See Also` (LOW-3 — matches structured-workflow.md and orchestration-pattern.md).
  - Ensure blank line before `## See Also` (MD031/MD032).

  **Step 3 — Append unified `## See Also` to components/structured-workflow.md:**
  - Read file; locate end (after the "Example Flow" section per research).
  - Append a new `## See Also` section with the exact text from the interfaces block. Same casing.
  - Same lint-compliance pattern.

  **Step 4 — Add Components blurb to README.md:**
  - Read README.md. Locate the existing "Components" reference area (grep for `## Components` or the closest existing heading referencing components).
  - If a `## Components` or similar section exists: insert the blurb from the interfaces block above (3-line paragraph pointing at `components/orchestration-pattern.md`).
  - If no such section exists: add a short `## Components` heading after the Killer Features table and include the blurb.
  - Do NOT duplicate content from 06-01 Task 2 (install section rewrite) — that task already completed.

  Lint constraints:
  - MD031/MD032: blank lines around all new fences and lists
  - MD026: no trailing punctuation in new headings
  - MD040: all new fences tagged (```bash, ```text, ```markdown — markdown OK as a top-level fence, just not nested)

  shellcheck: no shell files touched in this task.

  Verification runs `markdownlint components/orchestration-pattern.md` and expects ZERO errors (down from 9).
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && npx markdownlint-cli components/orchestration-pattern.md && npx markdownlint-cli components/supreme-council.md components/structured-workflow.md README.md && grep -q '^## See Also$' components/supreme-council.md && grep -q '^## See Also$' components/structured-workflow.md && grep -q 'orchestration-pattern' components/supreme-council.md && grep -q 'orchestration-pattern' components/structured-workflow.md && grep -q 'orchestration-pattern' README.md</automated>
  </verify>
  <done>
  - markdownlint components/orchestration-pattern.md exits 0 (all 9 errors resolved)
  - components/supreme-council.md contains a `## See Also` section (exact casing) referencing orchestration-pattern.md
  - components/structured-workflow.md contains a `## See Also` section (exact casing) referencing orchestration-pattern.md
  - README.md references orchestration-pattern.md
  - All 4 files pass markdownlint individually
  - No internal cross-references use line-number refs (LOW-1 satisfied)
  </done>
</task>

<task type="auto">
  <name>Task 4: Create scripts/lib/optional-plugins.sh + wire into init-claude.sh + update-claude.sh (DOCS-06)</name>
  <files>scripts/lib/optional-plugins.sh, scripts/init-claude.sh, scripts/update-claude.sh</files>
  <action>
  Implement DOCS-06 per CONTEXT.md D-DOCS-06 (single lib file sourced by both init-claude.sh and update-claude.sh; avoids text duplication). Per BLOCKING-1 resolution: SP install = `claude plugin install superpowers@claude-plugins-official`; GSD install = `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)`.

  **Step A — Create scripts/lib/optional-plugins.sh:**

  1. Shebang-less (this is a sourced lib, not a standalone script). Match `scripts/lib/install.sh` and `scripts/lib/state.sh` conventions exactly — read those files for the precise top-of-file pattern.
  2. Top-of-file comment block:
     - Name: `optional-plugins.sh`
     - One-line purpose: `Print the end-of-run recommended-optional-plugins block (DOCS-06)`
     - Usage: `source "$SCRIPT_DIR/lib/optional-plugins.sh" && recommend_optional_plugins`
  3. Color constants — define with guards (do NOT redefine if already set by the caller):

     ```bash
     [[ -z "${CYAN:-}" ]] && CYAN='\033[0;36m'
     [[ -z "${YELLOW:-}" ]] && YELLOW='\033[0;33m'
     [[ -z "${RED:-}" ]] && RED='\033[0;31m'
     [[ -z "${BLUE:-}" ]] && BLUE='\033[0;34m'
     [[ -z "${NC:-}" ]] && NC='\033[0m'
     ```

  4. Single function `recommend_optional_plugins()` containing the EXACT echo block from the interfaces section above — CONTEXT-locked SP/GSD install strings. Literal strings, no variable expansion of user-controlled data (research §11 security note — zero injection surface).
  5. Do NOT add `set -euo pipefail` — sourced libs must not poison caller's shell options.
  6. The "Details:" pointer says "see components/optional-plugins.md in the TK repo" (NOT `.claude/components/...` — HIGH-1 resolution: components are NOT installed into .claude/).

  **Step B — Wire into scripts/init-claude.sh:**

  1. Locate existing `source` directives (likely `source "$SCRIPT_DIR/lib/install.sh"`). Add sibling `source "$SCRIPT_DIR/lib/optional-plugins.sh"`.
  2. The script is typically run via `curl | bash` — it downloads lib files to temp paths. Extend the download loop to include `lib/optional-plugins.sh`. Read the existing download logic (grep for "lib/install.sh" download and replicate the pattern for the new lib).
  3. In `main()`: insert a call to `recommend_optional_plugins` BETWEEN `recommend_statusline` and `setup_council`. Re-grep line numbers before edit (06-01 may have shifted lines).

  **Step C — Wire into scripts/update-claude.sh:**

  1. Add `source "$SCRIPT_DIR/lib/optional-plugins.sh"` near other lib sources at the top of the script.
  2. After the call to `print_update_summary`, add a blank `echo ""` and then `recommend_optional_plugins`. The 4-group summary retains primary prominence; the optional-plugins block is the informational tail.

  3. Preserve all other logic in both scripts. Do NOT modify `print_update_summary`, `recommend_security`, `recommend_statusline`, `setup_council`, or any other existing function.

  **Constraints (all steps):**
  - shellcheck at `warning` severity MUST pass on all 3 files (lib + 2 entry scripts)
  - `bash -n scripts/init-claude.sh` + `bash -n scripts/update-claude.sh` parse clean
  - Per CONTEXT.md D-DOCS-06, the block is ALWAYS printed at end-of-run, non-interactive, informational only — no auto-install, no `[y/N]` prompt.
  - All `echo -e` strings use double quotes with `\033[0;XXm` escape sequences; variable references like `${YELLOW}` inside double quotes are safe.
  - Backslash escapes in command strings (`[\"ls\"]`) correctly escaped within double-quoted echo.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -f scripts/lib/optional-plugins.sh && shellcheck -S warning scripts/lib/optional-plugins.sh scripts/init-claude.sh scripts/update-claude.sh && bash -n scripts/init-claude.sh && bash -n scripts/update-claude.sh && bash -c 'source scripts/lib/optional-plugins.sh && type recommend_optional_plugins >/dev/null 2>&1' && bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins 2>&1 | grep -q "Recommended optional plugins"' && bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins 2>&1 | grep -q "superpowers@claude-plugins-official"' && bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins 2>&1 | grep -q "raw.githubusercontent.com/gsd-build/get-shit-done"' && grep -q 'recommend_optional_plugins' scripts/init-claude.sh && grep -q 'recommend_optional_plugins' scripts/update-claude.sh && grep -q 'optional-plugins.sh' scripts/init-claude.sh && grep -q 'optional-plugins.sh' scripts/update-claude.sh</automated>
  </verify>
  <done>
  - scripts/lib/optional-plugins.sh exists
  - shellcheck -S warning passes on lib + init + update
  - Sourcing the lib defines `recommend_optional_plugins` as a bash function
  - Function stdout contains all 4 plugins, CONTEXT-locked SP/GSD install strings, and the `components/optional-plugins.md in the TK repo` pointer (NOT `.claude/components/...`)
  - Lib file does NOT set -euo pipefail (sourced-lib convention)
  - Both init-claude.sh and update-claude.sh source the lib and invoke recommend_optional_plugins
  - bash -n parse check clean on both entry scripts
  - make test continues to pass (no regression in the 14 existing test groups)
  </done>
</task>

<task type="auto">
  <name>Task 5: Fold RTK.md install into setup-security.sh with guard + create test-setup-security-rtk.sh + register Test 15 in Makefile (DOCS-07 install half, MEDIUM-2)</name>
  <files>scripts/setup-security.sh, scripts/tests/test-setup-security-rtk.sh, Makefile</files>
  <action>
  Implement DOCS-07 install wiring per CONTEXT.md D-DOCS-07 + MEDIUM-2 (unconditional Makefile Test 15 entry).

  **Step 1 — Modify scripts/setup-security.sh:**
  - Read the existing setup-security.sh to understand where templates/global/* files get installed to ~/.claude/ (likely a section that copies `templates/global/CLAUDE.md` into `~/.claude/CLAUDE.md`).
  - Immediately after that existing global-template install (or in a parallel location), add a new install function for RTK.md:

    ```bash
    install_rtk_notes() {
        local src_rtk="${SCRIPT_DIR}/../templates/global/RTK.md"
        local dst_rtk="$HOME/.claude/RTK.md"

        if [[ ! -f "$src_rtk" ]]; then
            echo "ℹ Skipping RTK.md install — source file not found (offline / partial install)"
            return 0
        fi

        if [[ -f "$dst_rtk" ]]; then
            echo "ℹ ~/.claude/RTK.md already exists (rtk init -g or prior TK install); leaving untouched."
            echo "  See components/optional-plugins.md for the rtk-ai/rtk#1276 caveat."
            return 0
        fi

        cp "$src_rtk" "$dst_rtk"
        echo "✓ Installed fallback ~/.claude/RTK.md (points to components/optional-plugins.md for rtk-ai/rtk#1276)"
    }
    ```

  - Guard `[[ -f "$dst_rtk" ]]` ensures TK's RTK.md is NEVER installed if the user already has one.
  - Invoke `install_rtk_notes` at the point in setup-security.sh's main flow where global templates get installed.
  - For the remote `curl | bash` case: if setup-security.sh downloads templates on-the-fly, extend the download helper to fetch `templates/global/RTK.md`. Otherwise document that the local-repo path is the supported install flow for RTK.md.

  **Step 2 — Create scripts/tests/test-setup-security-rtk.sh (net-new integration test):**
  - Match existing test conventions from `scripts/tests/test-safe-merge.sh` (read it to mirror style: shebang `#!/bin/bash`, `set -euo pipefail`, color constants, fail-fast exit codes).
  - Test scenarios (minimum 2):
    - **Scenario A (RTK.md absent → lands):** Create a temp HOME directory, ensure `$HOME/.claude/` exists but has no RTK.md. Source setup-security.sh into a subshell with the test HOME, call `install_rtk_notes`. Assert `~/.claude/RTK.md` now exists and matches `templates/global/RTK.md` byte-for-byte.
    - **Scenario B (RTK.md present → NOT clobbered):** Create temp HOME with `~/.claude/RTK.md` containing sentinel text (e.g., "MARKER: user-created"). Run install. Assert `~/.claude/RTK.md` still contains the sentinel (NOT clobbered).
  - Both generations covered: one sub-case for "rtk-init-generated" marker, one sub-case for "tk-prior-install" marker. Both left untouched.
  - Use `$(mktemp -d)` for isolated test HOME; `HOME=$test_home` within the test scope.
  - Clean up temp dirs on exit via `trap "rm -rf $test_home" EXIT`.
  - Exit 0 on all scenarios pass; non-zero with descriptive error on any scenario fail.
  - Make script executable: `chmod +x scripts/tests/test-setup-security-rtk.sh`.

  **Step 3 — Register Test 15 in Makefile (UNCONDITIONAL per MEDIUM-2):**

  The Makefile enumerates Test 1..14 explicitly (no glob; verified by reading Makefile:42-96). Add Test 15 entry BETWEEN the Test 14 `@bash scripts/tests/test-migrate-idempotent.sh` line and the `@echo "All tests passed!"` sentinel. Exact insertion (preserve TAB indentation):

  ```makefile
  	@echo ""
  	@echo "Test 15: setup-security.sh RTK.md install guard"
  	@bash scripts/tests/test-setup-security-rtk.sh
  ```

  This is an UNCONDITIONAL edit (MEDIUM-2 plan-checker feedback: Makefile does not use a glob; every test is enumerated).

  **Step 4 — shellcheck compliance:** both setup-security.sh (modified) and the new test script MUST pass `shellcheck -S warning`. Use `[[ ]]`, quote all variable expansions, avoid `eval`.

  **Step 5 — Precondition check:** at start of execution, Task 1's precondition check already confirmed `test -f templates/global/RTK.md`. If somehow missing here, STOP — 06-02 Task 2 did not complete.

  Do NOT modify `templates/global/RTK.md` in this task (it was created in 06-02 Task 2). Do NOT add new manifest entries for RTK.md under `templates.global`.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -x scripts/tests/test-setup-security-rtk.sh && shellcheck -S warning scripts/setup-security.sh scripts/tests/test-setup-security-rtk.sh && bash scripts/tests/test-setup-security-rtk.sh && grep -q 'install_rtk_notes' scripts/setup-security.sh && grep -qE '\[\[ -f "\$dst_rtk" \]\]|\[\[ -f "\$HOME/.claude/RTK.md" \]\]' scripts/setup-security.sh && grep -q 'Test 15: setup-security.sh RTK.md install guard' Makefile && grep -q 'test-setup-security-rtk.sh' Makefile && make test</automated>
  </verify>
  <done>
  - scripts/setup-security.sh contains install_rtk_notes with guard `[[ -f "$dst_rtk" ]]`
  - scripts/tests/test-setup-security-rtk.sh exists, executable, passes shellcheck, exits 0
  - Test covers BOTH scenarios: absent → lands; present → NOT clobbered (both generations — rtk-init-generated and tk-prior-install)
  - Makefile contains explicit Test 15 entry between Test 14 and `All tests passed!` sentinel
  - shellcheck passes on both files
  - `make test` passes (new test integrated; existing 14 groups still green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell script → user stdout | `recommend_optional_plugins` writes to stdout in an install context; output must be literal (no variable expansion of user-controlled data) |
| setup-security.sh → $HOME/.claude/RTK.md | File write into user home; must never clobber an existing file |
| test script → temp $HOME | Tests create a sandbox HOME; must isolate from real home and clean up |
| manifest.json inventory registration ↔ disk presence | New inventory.components bucket entries must correspond to existing files; validate-manifest.py Check 7 enforces this |
| install.sh `.files` iteration ↔ `inventory.*` | `scripts/lib/install.sh:239` iterates `.files | to_entries[]` only — `inventory.*` is ignored by design, guaranteeing no install-loop side-effect for component files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | scripts/lib/optional-plugins.sh (stdout block with upstream plugin install commands) | mitigate | All install commands are literal strings — no variable interpolation of user-controlled data. SP/GSD strings CONTEXT-locked per 06-CONTEXT.md `<decisions>` lines 62-66; cross-checked against `scripts/detect.sh:54` and `scripts/verify-install.sh:197-200`. Mirror content from components/optional-plugins.md (06-02 Task 1). No secrets printed. |
| T-06-03-02 | Tampering | scripts/setup-security.sh (RTK.md install could clobber existing ~/.claude/RTK.md) | mitigate | Guard `[[ -f "$HOME/.claude/RTK.md" ]] || return 0` prevents clobber in ALL scenarios (rtk-init-generated, tk-prior-install, user-edited). Test scripts/tests/test-setup-security-rtk.sh explicitly covers both "absent → lands" and "present → NOT clobbered". Matches Phase 3 SAFETY-01..04 invariant (never overwrite user files without consent). |
| T-06-03-03 | Information Disclosure | stdout block printed at end-of-run (DOCS-06) | accept | No secrets in literal strings. Printed to terminal; not persisted. Research §11 confirms zero secret-leakage surface. |
| T-06-03-04 | Tampering (stale caveat drift between shell block and components/optional-plugins.md) | scripts/lib/optional-plugins.sh ↔ components/optional-plugins.md | mitigate | Function body explicitly points to `components/optional-plugins.md` as source of truth. Future edits should trigger matching update — Phase 7 follow-up `make validate-optional-plugins-sync` target could diff the two. Not blocking v4.0. |
| T-06-03-05 | Denial of Service (script append after wiring breaks existing flow) | scripts/init-claude.sh, scripts/update-claude.sh | mitigate | Task 4 preserves all existing functions byte-identical. `make test` re-runs all 15 test groups (14 existing + new Test 15). shellcheck + bash -n parse checks both scripts. |
| T-06-03-06 | Tampering | manifest.json (new `inventory.components` key could accidentally corrupt existing `files.*` buckets) | mitigate | Task 1 preserves existing entries byte-identical. `inventory` is a NEW top-level key — install/update scripts do not iterate it (verified by reading `scripts/lib/install.sh:228-246` which scopes to `.files | to_entries[]`). `scripts/validate-manifest.py` Check 7 enforces `inventory.components[]` schema (path + description + disk existence). CHANGELOG ↔ manifest version alignment remains green. |
| T-06-03-07 | Spoofing (DOCS-06 automated verify false-positive) | VALIDATION.md Per-Task Verification Map (pre-fix had a command that never matches, masking the feature's breakage) | mitigate | BLOCKING-3 resolution: Task 2 replaces `init-claude.sh --dry-run | grep` (never matches due to early-exit at init-claude.sh:389) with function-availability + direct lib-sourcing stdout capture. The new verify exercises the lib code path itself. |
</threat_model>

<verification>
End-of-plan verification:

- `make check` — markdownlint + shellcheck + make validate + make validate-base-plugins all green
- `make test` — all 14 existing test groups PLUS the new Test 15 pass
- `markdownlint components/orchestration-pattern.md` — 0 errors (down from 9)
- `bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins'` — prints the expected 4-plugin block with CONTEXT-locked SP/GSD install strings
- `grep -q 'recommend_optional_plugins' scripts/init-claude.sh scripts/update-claude.sh` — both contain the invocation
- `grep -q '^## See Also$' components/supreme-council.md components/structured-workflow.md` — unified casing (LOW-3)
- `grep -q 'orchestration-pattern' README.md` — README blurb present
- `bash scripts/tests/test-setup-security-rtk.sh` — exits 0
- `jq -e '.inventory.components | length == 2' manifest.json` — exactly 2 inventory entries
- `! jq -e '.files.components' manifest.json` — no files.components (BLOCKING-2 invariant)
- `python3 scripts/validate-manifest.py` — exits 0 (Check 7 passes)
- `grep -q 'nyquist_compliant: true' .planning/phases/06-documentation/06-VALIDATION.md` — BLOCKING-3 satisfied

Phase 6 gate checklist (ready for Phase 7 handoff):
- [x] DOCS-01..08 all have implementation tasks across 06-01, 06-02, 06-03 (partial IDs tracked: DOCS-05-asset + DOCS-05-register, DOCS-07-asset + DOCS-07-install)
- [x] SC-1..SC-6 all satisfied
- [x] make check green
- [x] make test green (15 groups)
- [x] manifest.json version 4.0.0 aligned with CHANGELOG [4.0.0]
- [x] manifest.json inventory.components registers both Phase-6 components (NOT files.components — BLOCKING-2 resolved)
- [x] All BLOCKING/HIGH/MEDIUM plan-checker findings resolved
</verification>

<success_criteria>
- DOCS-05-register: manifest.json `inventory.components` registers components/optional-plugins.md [VERIFIED by Task 1 jq assertion]
- DOCS-06: init-claude.sh + update-claude.sh source and call recommend_optional_plugins with CONTEXT-locked SP/GSD strings [VERIFIED by Task 4 stdout-capture verify]
- DOCS-07-install: setup-security.sh installs templates/global/RTK.md to ~/.claude/RTK.md with guard; test-setup-security-rtk.sh covers absent + present scenarios; Makefile Test 15 enumerated [VERIFIED by Task 5 + make test]
- DOCS-08: manifest.json registers orchestration-pattern.md in inventory [Task 1]; orchestration-pattern.md 0 mdlint errors; bidirectional `## See Also` cross-references with supreme-council.md + structured-workflow.md; README Components blurb [VERIFIED by Task 3 grep + markdownlint]
- BLOCKING-2 resolved: `inventory.components` replaces `files.components`; install-loop side-effect avoided
- BLOCKING-3 resolved: VALIDATION.md task IDs synced + DOCS-06 verify command fixed + nyquist_compliant flipped to true
- make check green
- make test green (existing 14 groups + new Test 15)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-03-SUMMARY.md` using the @$HOME/.claude/get-shit-done/templates/summary.md template. Summary must include:
- Requirements satisfied: DOCS-05-register, DOCS-06, DOCS-07-install, DOCS-08 (both halves)
- Files created (2): scripts/lib/optional-plugins.sh, scripts/tests/test-setup-security-rtk.sh
- Files modified (11): manifest.json (NEW `inventory.components` top-level section), scripts/validate-manifest.py (Check 7), .planning/phases/06-documentation/06-VALIDATION.md (task ID sync + nyquist=true), components/orchestration-pattern.md (flat-fence rewrite), components/supreme-council.md (## See Also), components/structured-workflow.md (## See Also), README.md (Components blurb), scripts/init-claude.sh (source + call), scripts/update-claude.sh (source + call), scripts/setup-security.sh (install_rtk_notes), Makefile (Test 15 entry)
- BLOCKING-1 resolution confirmed: SP install = `claude plugin install superpowers@claude-plugins-official`; GSD install = `bash <(curl -sSL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/scripts/install.sh)` — replicated in templates (06-01), optional-plugins.md (06-02), and lib/optional-plugins.sh (this plan)
- BLOCKING-2 resolution confirmed: `inventory.components` used instead of `files.components`; install-loop iteration unchanged (scripts/lib/install.sh:239 still scopes to `.files | to_entries[]`); validate-manifest.py Check 7 enforces inventory schema
- BLOCKING-3 resolution confirmed: 06-VALIDATION.md Per-Task Verification Map now uses final task IDs (06-01-01..05, 06-02-01..02, 06-03-01..05); DOCS-06 verify command replaced; nyquist_compliant flipped to true
- make check output (all green — markdownlint + shellcheck + validate + validate-base-plugins)
- make test output (15 groups — 14 existing + 1 new; all green)
- Cross-plan verification: confirm 06-01 and 06-02 artifacts all present and consistent
- Phase 6 gate readiness statement: "Phase 6 complete; ready for Phase 7 validation (/gsd-verify-work)"
</output>
</output>
