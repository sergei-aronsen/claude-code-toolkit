---
phase: 06-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - manifest.json
  - components/orchestration-pattern.md
  - components/supreme-council.md
  - components/structured-workflow.md
  - README.md
  - scripts/lib/optional-plugins.sh
  - scripts/init-claude.sh
  - scripts/update-claude.sh
  - scripts/setup-security.sh
  - scripts/tests/test-setup-security-rtk.sh
autonomous: true
gap_closure: false
requirements:
  - DOCS-05
  - DOCS-06
  - DOCS-07
  - DOCS-08

must_haves:
  truths:
    - "manifest.json files.components bucket exists and registers exactly 2 entries (components/optional-plugins.md + components/orchestration-pattern.md) — 27 pre-existing components are out of scope per CONTEXT.md D-MANIFEST-REG"
    - "manifest.json version field reads '4.0.0' and matches CHANGELOG [4.0.0] heading (make validate alignment check green)"
    - "components/orchestration-pattern.md has zero markdownlint errors (all 9 from lines 201-231 fixed via flatten of nested fences)"
    - "components/supreme-council.md and components/structured-workflow.md each contain a new 'See Also' cross-reference pointing at orchestration-pattern.md"
    - "README.md Components section contains a blurb linking to components/orchestration-pattern.md"
    - "scripts/lib/optional-plugins.sh exists and exports a function recommend_optional_plugins that prints the DOCS-06 block (rtk + caveman + superpowers + get-shit-done)"
    - "scripts/init-claude.sh sources scripts/lib/optional-plugins.sh and invokes recommend_optional_plugins at end-of-run (between recommend_statusline and setup_council per research §6)"
    - "scripts/update-claude.sh sources scripts/lib/optional-plugins.sh and invokes recommend_optional_plugins after print_update_summary"
    - "scripts/setup-security.sh installs templates/global/RTK.md to ~/.claude/RTK.md ONLY when absent (guard: [ ! -f \"$HOME/.claude/RTK.md\" ])"
    - "scripts/tests/test-setup-security-rtk.sh exists and exits 0 when RTK.md install behaves correctly in both absent-and-present scenarios (both generations covered — rtk-init-generated and tk-prior-install)"
    - "make check passes green (markdownlint + shellcheck + validate)"
    - "make test passes green (existing 14 test groups + new Test 15 for RTK.md install guard)"
  artifacts:
    - path: "manifest.json"
      provides: "files.components bucket with exactly 2 Phase-6 entries (optional-plugins.md + orchestration-pattern.md)"
      contains: "\"components\":"
    - path: "components/orchestration-pattern.md"
      provides: "Polished component (0 mdlint errors) with flat-fence rewrite of 'Wiring it into your own slash command' section"
      contains: "Wiring it into your own slash command"
    - path: "components/supreme-council.md"
      provides: "'See Also' cross-reference to orchestration-pattern.md"
      contains: "orchestration-pattern.md"
    - path: "components/structured-workflow.md"
      provides: "'See Also' cross-reference to orchestration-pattern.md"
      contains: "orchestration-pattern.md"
    - path: "README.md"
      provides: "Components section blurb linking to orchestration-pattern.md"
      contains: "orchestration-pattern.md"
    - path: "scripts/lib/optional-plugins.sh"
      provides: "recommend_optional_plugins function — single source of truth for DOCS-06 stdout block"
      contains: "recommend_optional_plugins"
    - path: "scripts/init-claude.sh"
      provides: "Sources lib/optional-plugins.sh and invokes function in main()"
      contains: "recommend_optional_plugins"
    - path: "scripts/update-claude.sh"
      provides: "Sources lib/optional-plugins.sh and invokes function after print_update_summary"
      contains: "recommend_optional_plugins"
    - path: "scripts/setup-security.sh"
      provides: "Guarded copy of templates/global/RTK.md to ~/.claude/RTK.md"
      contains: "RTK.md"
    - path: "scripts/tests/test-setup-security-rtk.sh"
      provides: "Integration test covering RTK.md install (absent → lands; present → NOT clobbered; both generations — TK's and rtk init's)"
      contains: "test-setup-security-rtk"
  key_links:
    - from: "manifest.json files.components"
      to: "components/optional-plugins.md (06-02 Task 1) + components/orchestration-pattern.md (polished in 06-03 Task 2)"
      via: "manifest path registration (both files must exist on disk before registration per MANIFEST-04)"
      pattern: "components/optional-plugins\\.md"
    - from: "scripts/lib/optional-plugins.sh"
      to: "components/optional-plugins.md (06-02 artifact)"
      via: "the shell function prints a condensed version of the content in components/optional-plugins.md — future doc updates must sync both"
      pattern: "optional-plugins"
    - from: "scripts/setup-security.sh"
      to: "templates/global/RTK.md (06-02 artifact)"
      via: "cp with [ ! -f \"$HOME/.claude/RTK.md\" ] guard"
      pattern: "RTK\\.md"
    - from: "scripts/init-claude.sh + scripts/update-claude.sh"
      to: "scripts/lib/optional-plugins.sh"
      via: "source directive at script top (matches existing lib/install.sh + lib/state.sh sourcing convention)"
      pattern: "source.*optional-plugins\\.sh"
---

<objective>
Complete Phase 6: consolidate manifest.json component registration (DOCS-05 + DOCS-08 manifest half), polish `orchestration-pattern.md` (DOCS-08 content half), cross-reference it from siblings + README, ship the DOCS-06 end-of-run optional-plugins block via a new lib file wired into both init and update scripts, and fold the DOCS-07 install path for RTK.md into `setup-security.sh` with a guard that never clobbers rtk-init's own RTK.md.

Purpose: Phase 6 SC-5 (RTK.md lands in ~/.claude/ via install wiring) + SC-6 (orchestration-pattern.md polished, registered, cross-referenced, blurbed in README) + manifest-registration half of SC-5 (components/optional-plugins.md registered).

Output: 1 manifest edit (new files.components bucket with 2 entries), 2 component edits (supreme-council, structured-workflow), 1 README edit (Components blurb), 1 content polish (orchestration-pattern 9 mdlint fixes), 1 new lib script, 2 script wiring edits (init + update), 1 script install-path edit (setup-security), 1 net-new integration test.

DEPENDENCIES (hard):
- 06-01 must complete first (CHANGELOG version bump to 4.0.0, manifest.json version bump to 4.0.0, README install section, 7-template blocks, docs/INSTALL.md)
- 06-02 must complete first (components/optional-plugins.md AND templates/global/RTK.md must exist on disk before Task 1 registers them in manifest and before Task 4 reads templates/global/RTK.md as install source)
- Wave 2 gating enforced by wave structure; at plan start, executor MUST verify:
  - `test -f components/optional-plugins.md` (06-02 Task 1 output)
  - `test -f templates/global/RTK.md` (06-02 Task 2 output)
  - `jq -e '.version == "4.0.0"' manifest.json` (06-01 Task 1 output)
  - `grep -q '## \[4.0.0\]' CHANGELOG.md` (06-01 Task 1 output)
  If ANY precondition fails, STOP and report — Wave 1 did not complete successfully.

SCOPE RATIONALE (manifest consolidation):
The manifest `files.components` registration was originally allocated to 06-02, but 06-01 (parallel Wave 1) also edits manifest.json (version bump from 3.0.0 to 4.0.0 for DOCS-03 / BUG-06 alignment). To preserve the Wave 1 files_modified no-overlap invariant, all manifest edits are consolidated here in 06-03 (Wave 2, after 06-01 completes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
@.planning/phases/06-documentation/06-VALIDATION.md
@.planning/phases/06-documentation/06-01-SUMMARY.md
@.planning/phases/06-documentation/06-02-SUMMARY.md
@components/orchestration-pattern.md
@components/supreme-council.md
@components/structured-workflow.md
@components/optional-plugins.md
@templates/global/RTK.md
@scripts/init-claude.sh
@scripts/update-claude.sh
@scripts/setup-security.sh
@scripts/lib/install.sh
@scripts/lib/state.sh
@manifest.json
@README.md

<interfaces>
**From 06-01 artifacts (pre-existing at start of 06-03):**
- `manifest.json` — `version: "4.0.0"`, existing `files.{agents, prompts, commands, skills, rules}` buckets intact
- `CHANGELOG.md` — `## [4.0.0] - TBD` heading with BREAKING CHANGES leading block

**From 06-02 artifacts (pre-existing at start of 06-03):**
- `components/optional-plugins.md` — source of truth for the 4-plugin list and caveats
- `templates/global/RTK.md` — install source file (setup-security.sh copies this to ~/.claude/RTK.md with guard)

**Manifest extension target (from 06-RESEARCH.md §5 + CONTEXT.md D-MANIFEST-REG):**

Current `manifest.json` has buckets `files.{agents, prompts, commands, skills, rules}` + `templates.*`. This plan adds a new `files.components` key IMMEDIATELY AFTER `rules` (preserving the domain-grouped ordering — NOT alphabetical). Per CONTEXT.md, ONLY register the 2 Phase-6 components — the 27 pre-existing components are OUT OF SCOPE (separate refactor).

Target JSON to add under `files`:

```json
"components": [
  { "path": "components/optional-plugins.md" },
  { "path": "components/orchestration-pattern.md" }
]
```

**From 06-RESEARCH.md §8 — orchestration-pattern.md markdownlint errors (all 9 concentrated in lines 201-231):**

```text
components/orchestration-pattern.md:211 MD031 (blanks-around-fences)
components/orchestration-pattern.md:214 MD029 (ol-prefix) expected 1; got 2
components/orchestration-pattern.md:221 MD029 expected 2; got 3
components/orchestration-pattern.md:224 MD029 expected 3; got 4
components/orchestration-pattern.md:225 MD031 "```bash"
components/orchestration-pattern.md:229 MD029 expected 4; got 5
components/orchestration-pattern.md:230 MD032 (blanks-around-lists)
components/orchestration-pattern.md:231 MD031 "```"
components/orchestration-pattern.md:231 MD040 (fenced-code-language)
```

Root cause: a ```markdown fenced block containing a nested ```bash fence. Markdownlint's fence-pair tracker treats the inner ``` as closing the outer, cascading errors.

**Fix pattern (from research §8):** Replace lines 201-231 with a flattened section. Structure:
- `## Wiring it into your own slash command`
- Prose paragraph: "Minimum viable adaptation in a custom command at `commands/your-command.md`:"
- `### 1. Load context` + prose + ```bash fence (single, flat)
- `### 2. Spawn subagents in parallel` + prose + bullet list
- `### 3. Collect confirmations` + prose
- `### 4. Commit atomically` + ```bash fence
- `### 5. Present next-up` + prose

NO nested fences. Each fence at top-level nesting.

**From 06-RESEARCH.md §8 — cross-reference text to append:**

To `components/supreme-council.md` (end of file, after the "Add to CLAUDE.md" block):

```markdown
## See Also

- [orchestration-pattern.md](./orchestration-pattern.md) — lean-orchestrator + fat-subagents
  pattern that Council implements. Explains why `brain.py` spawns Gemini and ChatGPT as
  parallel subagents rather than calling them sequentially from one context.
```

To `components/structured-workflow.md` (end of file, after line 253 "Example Flow" section):

```markdown
## See Also

- [orchestration-pattern.md](./orchestration-pattern.md) — scaling multi-step workflows
  **beyond a single agent's context window**. Structured Workflow disciplines a single
  context; orchestration-pattern delegates across many fresh contexts. Complementary patterns.
```

To `README.md` (existing "Components" reference section or a new short "Components" section after line 90):

```markdown
**Orchestration pattern** — see [components/orchestration-pattern.md](components/orchestration-pattern.md)
for the lean-orchestrator + fat-subagents design Council and GSD workflows both use.
Helps any custom slash command scale beyond a single context window.
```

**From 06-RESEARCH.md §6 — DOCS-06 shell wiring (EXACT line numbers verified):**

`scripts/init-claude.sh` main() structure:
- Line 717: `main() { ... }`
- Line 729: `echo "Next steps:" ...`
- Line 742: `recommend_security`
- Line 743: `recommend_statusline`
- Line 747: `setup_council`
- Line 754: "Restart Claude Code" message
- Line 758: `create_post_install`

**Insertion point:** between line 743 (`recommend_statusline`) and line 747 (`setup_council`). Add `recommend_optional_plugins` call here.

`scripts/update-claude.sh`:
- Line 209: `print_update_summary()` function definition
- Line 763: call to `print_update_summary "$BACKUP_DIR"`

**Insertion point:** after line 763 call to `print_update_summary`. Add `recommend_optional_plugins` call here.

**CYAN color constant:**
- `update-claude.sh` line 31 defines `CYAN='\033[0;36m'`
- `init-claude.sh` does NOT currently define `CYAN` (uses `BLUE` instead for cyan-like emphasis per current code)
- If the lib uses `CYAN`, either define it in the lib file itself (cleanest — shared) OR have init-claude.sh add it near line 14 with other color constants. Recommendation: define in the lib (single source of truth, self-contained).

**From 06-RESEARCH.md §6 — proposed recommend_optional_plugins function:**

```bash
recommend_optional_plugins() {
    echo ""
    echo -e "${CYAN}🧩 Recommended optional plugins:${NC}"
    echo ""
    echo -e "  ${YELLOW}rtk${NC} — 60-90% token savings on dev commands"
    echo -e "    Install: ${YELLOW}brew install rtk && rtk init -g${NC}"
    echo -e "    ${RED}Known issue${NC}: rtk ls broken on non-English locales (rtk-ai/rtk#1276)"
    echo -e "    Workaround: add exclude_commands = [\"ls\"] to ~/Library/Application Support/rtk/config.toml"
    echo ""
    echo -e "  ${YELLOW}caveman${NC} — ~46% fewer input tokens per session"
    echo -e "    Install: ${YELLOW}claude plugin marketplace add JuliusBrussee/caveman && claude plugin install caveman@caveman${NC}"
    echo -e "    ${YELLOW}⚠${NC} caveman-compress auto-backs up CLAUDE.md to CLAUDE.original.md; commit CLAUDE.md to git before running compress"
    echo -e "    Languages: en + wenyan (Classical Chinese)"
    echo ""
    echo -e "  ${YELLOW}superpowers${NC} (obra) — skills + code-reviewer agent (TK complements)"
    echo -e "    Install: ${YELLOW}claude plugin marketplace add obra/superpowers${NC}"
    echo ""
    echo -e "  ${YELLOW}get-shit-done${NC} (gsd-build) — phase-based workflow (TK complements)"
    echo -e "    Install: ${YELLOW}claude plugin marketplace add gsd-build/get-shit-done${NC}"
    echo ""
    echo -e "  ${BLUE}Details:${NC} see ${BLUE}.claude/components/optional-plugins.md${NC}"
}
```

**Color-constant contract:** the function references `CYAN`, `YELLOW`, `RED`, `BLUE`, `NC`. Lib file declares these at top (with `readonly` guard if not already set — `[[ -z "${CYAN:-}" ]] && CYAN='\033[0;36m'`).

**Shell style constraints (per repo CLAUDE.md):**
- `set -euo pipefail` in any new standalone script (test script) — but NOT in sourced lib files (sourcing a file with `set -e` poisons the caller). Pattern matches existing `scripts/lib/install.sh` and `scripts/lib/state.sh`.
- Functions defined at top-level, `local` variables inside.
- `[[ ... ]]` for conditionals.
- Fail-fast idiom in the new test: validate preconditions before running scenarios.

**shellcheck constraints (every .sh file MUST pass):**
- Severity threshold: warning (CI enforces)
- Common gotchas: quote every `$var` expansion that could contain spaces; use `[[ ]]` not `[ ]`; check exit codes explicitly.

**Install guard for RTK.md (from 06-CONTEXT.md D-DOCS-07):**
- `[ ! -f "$HOME/.claude/RTK.md" ] || return 0` — never clobber existing ~/.claude/RTK.md
- setup-security.sh is the canonical `~/.claude/` writer; the copy slots into its existing flow
- On skip, print a one-line info message: "~/.claude/RTK.md already exists (rtk init -g or prior TK install); leaving untouched. See upstream rtk-ai/rtk#1276 caveat in components/optional-plugins.md."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 2 Phase-6 components in manifest.json files.components (DOCS-05 + DOCS-08 manifest half)</name>
  <files>manifest.json</files>
  <action>
  Implement the manifest-registration half of DOCS-08 + DOCS-05 per CONTEXT.md D-MANIFEST-REG: register ONLY the 2 Phase 6 components (optional-plugins.md + orchestration-pattern.md). Do NOT backfill the 27 existing components.

  This task runs FIRST in 06-03 so later tasks can assume manifest is current (Task 2 polishes orchestration-pattern.md and `make validate` must still pass after the polish — manifest path check requires the file exists, which it does).

  Steps:
  1. Read current `manifest.json`. Confirm `version` field reads `"4.0.0"` (set by 06-01 Task 1). If it still reads `"3.0.0"`, STOP and report — this indicates 06-01 did not complete and 06-03 is out of order.
  2. Confirm preconditions: `components/optional-plugins.md` and `components/orchestration-pattern.md` both exist on disk.
  3. Locate the `files` object (currently contains `agents`, `prompts`, `commands`, `skills`, `rules`).
  4. Add a new `components` key IMMEDIATELY AFTER `rules` (preserve the domain-grouped ordering, not alphabetical; current order is NOT alphabetical). Value:

     ```json
     "components": [
       { "path": "components/optional-plugins.md" },
       { "path": "components/orchestration-pattern.md" }
     ]
     ```

  5. Preserve all existing `files.*` entries VERBATIM — every existing agents/prompts/commands/skills/rules entry stays byte-identical.
  6. Preserve the top-level `manifest_version: 2`, `version: "4.0.0"`, `updated`, `description`, `sp_equivalent_note`, `claude_md_sections`, `templates` — do NOT touch any of these.
  7. Final JSON must be valid (parseable by `jq` and `python3 -m json.tool`).

  Validation:
  - Both paths MUST exist on disk (both files are 06-02 Task 1 and pre-existing respectively — Task 1 depends on 06-02 completion).
  - Run `python3 scripts/validate-manifest.py` if present — MUST pass. If absent in this repo, the equivalent check lives in `.github/workflows/quality.yml` validate job or `make validate`.
  - Run `make validate` — MUST remain green (manifest ↔ CHANGELOG version alignment unaffected; new manifest paths exist on disk per MANIFEST-04).

  Do NOT modify any other file.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && python3 -m json.tool manifest.json > /dev/null && jq -e '.files.components | length == 2' manifest.json && jq -e '.files.components[] | select(.path == "components/optional-plugins.md")' manifest.json > /dev/null && jq -e '.files.components[] | select(.path == "components/orchestration-pattern.md")' manifest.json > /dev/null && jq -e '.version == "4.0.0"' manifest.json && make validate</automated>
  </verify>
  <done>
  - manifest.json parses as valid JSON
  - manifest.json files.components array has exactly 2 entries
  - Both `components/optional-plugins.md` and `components/orchestration-pattern.md` are registered
  - Other files.* entries unchanged (diff against pre-edit state shows only the new bucket)
  - manifest.json version remains "4.0.0"
  - make validate passes (manifest paths exist; CHANGELOG ↔ manifest version alignment stays green)
  </done>
</task>

<task type="auto">
  <name>Task 2: Flatten orchestration-pattern.md nested fences + add cross-references + README blurb (DOCS-08 content half)</name>
  <files>components/orchestration-pattern.md, components/supreme-council.md, components/structured-workflow.md, README.md</files>
  <action>
  Implement DOCS-08 content polish. The manifest-registration half was completed in Task 1 of this plan — this task handles the 9-error mdlint fix + bidirectional cross-references + README blurb.

  Steps:

  1. **Fix mdlint errors in orchestration-pattern.md (lines 201-231):**
     - Read `components/orchestration-pattern.md` fully.
     - Locate the section "Wiring it into your own slash command" (around lines 199-231). It currently contains a ```markdown fenced block that internally opens a ```bash fence — the inner ``` closes the outer, cascading 9 errors.
     - REPLACE the entire block (approx lines 199-231, use surrounding context to find exact boundaries) with a flattened version:
       - Heading: `## Wiring it into your own slash command`
       - Prose: "Minimum viable adaptation in a custom command at `commands/your-command.md`:"
       - `### 1. Load context` subsection with prose + one flat ```bash fence showing `INIT=$(node "$HOME/.claude/your-toolkit/bin/your-tools.cjs" init your-workflow)`
       - `### 2. Spawn subagents in parallel` subsection with prose + a bullet list describing the subagent spawn fields (subagent_type, model, prompt, run_in_background)
       - `### 3. Collect confirmations` subsection with prose on reading confirmation strings only (never full transcripts)
       - `### 4. Commit atomically` subsection with prose + one flat ```bash fence showing `node "$HOME/.claude/your-toolkit/bin/your-tools.cjs" commit ...`
       - `### 5. Present next-up` subsection with prose on telling the user what to run next
     - NO nested fences anywhere. All fences at top level.
     - Preserve the file's "See also" bottom section if it exists (lines 237-241 per research) — those cross-references are already there.

  2. **Append 'See Also' cross-reference to components/supreme-council.md:**
     - Read file; locate end (after the last "Add to CLAUDE.md" block per research).
     - Append a new `## See Also` section with the exact text from interfaces block above (pointing at `./orchestration-pattern.md`).
     - Ensure blank line before `## See Also` and between the list item paragraphs (MD031/MD032).

  3. **Append 'See Also' cross-reference to components/structured-workflow.md:**
     - Read file; locate end (after the "Example Flow" section per research).
     - Append a new `## See Also` section with the exact text from interfaces block above.
     - Same lint-compliance pattern.

  4. **Add Components blurb to README.md:**
     - Read README.md. Locate the "Components" area (around line 90 per research; or the closest existing heading referencing components).
     - If a `## Components` or similar section exists: insert the blurb from interfaces block (3-line paragraph pointing at `components/orchestration-pattern.md`).
     - If no such section exists: add a short `## Components` heading after the Killer Features table and include the blurb.
     - Do NOT duplicate content from 06-01 Task 2 (install section rewrite) — that task already completed.

  Lint constraints:
  - MD031/MD032: blank lines around all new fences and lists
  - MD026: no trailing punctuation in new headings
  - MD040: all new fences tagged (```bash, ```text, ```markdown — markdown OK as a top-level fence, just not nested)

  shellcheck: no shell files touched in this task.

  Verification runs `markdownlint components/orchestration-pattern.md` and expects ZERO errors (down from 9).
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && npx markdownlint-cli components/orchestration-pattern.md && npx markdownlint-cli components/supreme-council.md components/structured-workflow.md README.md && grep -q 'orchestration-pattern' components/supreme-council.md && grep -q 'orchestration-pattern' components/structured-workflow.md && grep -q 'orchestration-pattern' README.md</automated>
  </verify>
  <done>
  - markdownlint components/orchestration-pattern.md exits 0 (all 9 errors resolved)
  - components/supreme-council.md contains a `## See Also` section referencing orchestration-pattern.md
  - components/structured-workflow.md contains a `## See Also` section referencing orchestration-pattern.md
  - README.md references orchestration-pattern.md
  - All 4 files pass markdownlint individually
  </done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/lib/optional-plugins.sh + wire into init-claude.sh + update-claude.sh (DOCS-06)</name>
  <files>scripts/lib/optional-plugins.sh, scripts/init-claude.sh, scripts/update-claude.sh</files>
  <action>
  Implement DOCS-06 per CONTEXT.md D-DOCS-06 (single lib file sourced by both init-claude.sh and update-claude.sh; avoids text duplication).

  **Step A — Create scripts/lib/optional-plugins.sh:**

  1. Shebang-less (this is a sourced lib, not a standalone script). Match `scripts/lib/install.sh` and `scripts/lib/state.sh` conventions exactly — read those files for the precise top-of-file pattern (color constants, comment block).
  2. Top-of-file comment block:
     - Name: `optional-plugins.sh`
     - One-line purpose: `Print the end-of-run recommended-optional-plugins block (DOCS-06)`
     - Usage: `source "$SCRIPT_DIR/lib/optional-plugins.sh" && recommend_optional_plugins`
  3. Color constants — define with guards (do NOT redefine if already set by the caller):

     ```bash
     [[ -z "${CYAN:-}" ]] && CYAN='\033[0;36m'
     [[ -z "${YELLOW:-}" ]] && YELLOW='\033[0;33m'
     [[ -z "${RED:-}" ]] && RED='\033[0;31m'
     [[ -z "${BLUE:-}" ]] && BLUE='\033[0;34m'
     [[ -z "${NC:-}" ]] && NC='\033[0m'
     ```

  4. Single function `recommend_optional_plugins()` containing the exact echo block from the interfaces section above. Literal strings, no variable expansion of anything that could contain user-controlled data (research §11 security note — zero injection surface).
  5. Do NOT add `set -euo pipefail` — sourced libs must not poison caller's shell options. Match `scripts/lib/install.sh` / `scripts/lib/state.sh` pattern.

  **Step B — Wire into scripts/init-claude.sh:**

  1. Locate the existing `source` directives for other lib files (likely `source "$SCRIPT_DIR/lib/install.sh"` near the top). Add a sibling `source "$SCRIPT_DIR/lib/optional-plugins.sh"`.
  2. If the remote install path uses `$TMPDIR`-downloaded libs (since init-claude.sh is typically run via `curl | bash`), match the existing pattern: the script already downloads `lib/install.sh` to a temp path and sources it. Extend the download loop to include `lib/optional-plugins.sh`. Read the existing download logic (search for "install.sh" references or the remote-download section).
  3. In `main()` function (line 717): insert a call to `recommend_optional_plugins` BETWEEN line 743 (`recommend_statusline`) and line 747 (`setup_council`). Per research §6, this placement keeps Council setup's interactivity last.

  **Step C — Wire into scripts/update-claude.sh:**

  1. Add `source "$SCRIPT_DIR/lib/optional-plugins.sh"` near other lib sources at the top of the script.
  2. After line 763 (`print_update_summary "$BACKUP_DIR"` call), add a blank `echo ""` and then `recommend_optional_plugins`. The 4-group summary retains primary prominence; the optional-plugins block is the informational tail.

  3. Preserve all other logic in both scripts. Do NOT modify `print_update_summary`, `recommend_security`, `recommend_statusline`, `setup_council`, or any other existing function.

  **Constraints (all steps):**
  - shellcheck at `warning` severity MUST pass on all 3 files (lib + 2 entry scripts)
  - `bash -n scripts/init-claude.sh` + `bash -n scripts/update-claude.sh` parse clean
  - Per CONTEXT.md / D-DOCS-06, the block is ALWAYS printed at end-of-run, non-interactive, informational only — no auto-install, no `[y/N]` prompt. Future opt-out is a Phase 7 consideration.
  - All `echo -e` strings use double quotes with `\033[0;XXm` escape sequences; variable references like `${YELLOW}` inside double quotes are safe.
  - Backslash escapes in command strings (`[\"ls\"]`) correctly escaped within double-quoted echo.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -f scripts/lib/optional-plugins.sh && shellcheck -S warning scripts/lib/optional-plugins.sh scripts/init-claude.sh scripts/update-claude.sh && bash -n scripts/init-claude.sh && bash -n scripts/update-claude.sh && bash -c 'source scripts/lib/optional-plugins.sh && type recommend_optional_plugins | head -1 | grep -q "recommend_optional_plugins is a function"' && grep -q 'recommend_optional_plugins' scripts/init-claude.sh && grep -q 'recommend_optional_plugins' scripts/update-claude.sh && grep -q 'optional-plugins.sh' scripts/init-claude.sh && grep -q 'optional-plugins.sh' scripts/update-claude.sh</automated>
  </verify>
  <done>
  - scripts/lib/optional-plugins.sh exists
  - shellcheck -S warning passes on lib + init + update
  - Sourcing the lib defines `recommend_optional_plugins` as a bash function
  - Function body contains all 4 plugins (rtk, caveman, superpowers, get-shit-done) and the `.claude/components/optional-plugins.md` pointer
  - Lib file does NOT set -euo pipefail (sourced-lib convention)
  - Both init-claude.sh and update-claude.sh source the lib and invoke recommend_optional_plugins
  - bash -n parse check clean on both entry scripts
  - make test continues to pass (no regression in the 14 existing test groups)
  </done>
</task>

<task type="auto">
  <name>Task 4: Fold RTK.md install into setup-security.sh with guard + create test-setup-security-rtk.sh (DOCS-07 install half)</name>
  <files>scripts/setup-security.sh, scripts/tests/test-setup-security-rtk.sh</files>
  <action>
  Implement DOCS-07 install wiring per CONTEXT.md D-DOCS-07: fold into `scripts/setup-security.sh` (already the canonical `~/.claude/` writer) with guard `[ ! -f "$HOME/.claude/RTK.md" ]`. Ship the asset created in 06-02 Task 2 (`templates/global/RTK.md`) to `~/.claude/RTK.md` ONLY when absent.

  Steps:

  1. **Modify scripts/setup-security.sh:**
     - Read the existing setup-security.sh to understand where templates/global/* files get installed to ~/.claude/ (likely a section that copies `templates/global/CLAUDE.md` into `~/.claude/CLAUDE.md`).
     - Immediately after that existing global-template install (or in a parallel location), add a new install block for RTK.md:

       ```bash
       install_rtk_notes() {
           local src_rtk="${SCRIPT_DIR}/../templates/global/RTK.md"
           local dst_rtk="$HOME/.claude/RTK.md"

           if [[ ! -f "$src_rtk" ]]; then
               # Remote install path — source lands in a different location; use download helper
               # Match the pattern used elsewhere in setup-security.sh for templates/global/ files
               # If unavailable, skip with info
               echo "ℹ Skipping RTK.md install — source file not found (offline / partial install)"
               return 0
           fi

           if [[ -f "$dst_rtk" ]]; then
               echo "ℹ ~/.claude/RTK.md already exists (rtk init -g or prior TK install); leaving untouched."
               echo "  See components/optional-plugins.md for the rtk-ai/rtk#1276 caveat."
               return 0
           fi

           cp "$src_rtk" "$dst_rtk"
           echo "✓ Installed fallback ~/.claude/RTK.md (points to components/optional-plugins.md for rtk-ai/rtk#1276)"
       }
       ```

     - The guard `[[ -f "$dst_rtk" ]]` ensures TK's RTK.md is NEVER installed if the user already has one (either from `rtk init -g` or a prior TK install). Per D-DOCS-07 locked in CONTEXT.md.
     - Invoke `install_rtk_notes` at the point in setup-security.sh's main flow where global templates get installed (co-located with the CLAUDE.md global install step).
     - For the remote `curl | bash` case: if setup-security.sh downloads templates on-the-fly (check existing download logic), extend it to download `templates/global/RTK.md` too. If setup-security.sh only operates on a local clone (check via grep for `REPO_URL` or `curl` usage), skip this concern and document that the local-repo path is the supported install flow for RTK.md.

  2. **Create scripts/tests/test-setup-security-rtk.sh (net-new integration test):**
     - Match existing test conventions from `scripts/tests/test-*.sh` (e.g., `test-safe-merge.sh` — read it to mirror its style: shebang `#!/bin/bash`, `set -euo pipefail`, color constants, fail-fast exit codes).
     - Test scenarios (minimum 2):
       - **Scenario A (RTK.md absent → lands):** Create a temp HOME directory, ensure `$HOME/.claude/` exists but has no RTK.md. Run the `install_rtk_notes` function (source setup-security.sh or call the function directly if exported, OR run setup-security.sh in a sandbox). Assert `~/.claude/RTK.md` now exists and matches `templates/global/RTK.md` byte-for-byte.
       - **Scenario B (RTK.md present → NOT clobbered):** Create temp HOME with `~/.claude/RTK.md` containing sentinel text (e.g., "MARKER: user-created"). Run install. Assert `~/.claude/RTK.md` still contains the sentinel (NOT clobbered).
       - **Both generations covered:** One sub-case for "exists because rtk init -g ran" (marker "rtk-init-generated"), one sub-case for "exists because prior TK install ran" (marker "tk-prior-install"). Both must be left untouched.
     - Use `$(mktemp -d)` for isolated test HOME; `HOME=$test_home` within the test scope.
     - Clean up temp dirs on exit via `trap "rm -rf $test_home" EXIT`.
     - Exit 0 on all scenarios pass; non-zero with a descriptive error on any scenario fail.
     - Make the script executable: `chmod +x scripts/tests/test-setup-security-rtk.sh`.

  3. **Register the new test in Makefile (if make test enumerates tests):**
     - Read `Makefile` `test` target (Makefile:42-63 per CLAUDE.md).
     - If the target runs tests via an explicit list: add `scripts/tests/test-setup-security-rtk.sh` to the list.
     - If the target runs tests via a glob `scripts/tests/test-*.sh`: no Makefile edit needed.
     - Per research, Phase 6 adds "Test 15" (the 15th test group) — honor that naming convention if the Makefile labels tests by number.

  4. **shellcheck compliance:** both setup-security.sh (modified) and the new test script MUST pass `shellcheck -S warning`. Use `[[ ]]`, quote all variable expansions, avoid `eval`.

  5. **Precondition check:** at start of plan execution (before this task runs), Task 1's precondition check already confirmed `test -f templates/global/RTK.md`. If somehow missing here, STOP — 06-02 Task 2 did not complete.

  Do NOT modify `templates/global/RTK.md` in this task (it was created in 06-02 Task 2). Do NOT add new manifest entries for RTK.md under `templates.global` — that is a separate task for manifest.templates extension, out of scope for Phase 6 per the CONTEXT.md "register only 2 new components" rule.
  </action>
  <verify>
    <automated>cd /Users/sergeiarutiunian/Projects/claude-code-toolkit && test -x scripts/tests/test-setup-security-rtk.sh && shellcheck -S warning scripts/setup-security.sh scripts/tests/test-setup-security-rtk.sh && bash scripts/tests/test-setup-security-rtk.sh && grep -q 'install_rtk_notes\|RTK.md' scripts/setup-security.sh && grep -qE '\[\[ -f "\$dst_rtk" \]\]|\[\[ -f "\$HOME/.claude/RTK.md" \]\]|! -f.*RTK.md' scripts/setup-security.sh</automated>
  </verify>
  <done>
  - scripts/setup-security.sh contains install_rtk_notes logic with guard `[[ -f "$HOME/.claude/RTK.md" ]]` (or equivalent negation pattern)
  - scripts/tests/test-setup-security-rtk.sh exists, is executable, passes shellcheck, and exits 0
  - Test covers BOTH scenarios: absent → lands; present → NOT clobbered (both generations — rtk-init-generated and tk-prior-install)
  - shellcheck passes on both files
  - make test passes (new test integrated; existing 14 groups still green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell script → user stdout | `recommend_optional_plugins` writes to stdout in an install context; output must be literal (no variable expansion of user-controlled data) |
| setup-security.sh → $HOME/.claude/RTK.md | File write into user home; must never clobber an existing file |
| test script → temp $HOME | Tests create a sandbox HOME; must isolate from real home and clean up |
| manifest.json registration ↔ disk presence | New components bucket entries must correspond to existing files; Phase 2 MANIFEST-04 enforces this via `make validate` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | scripts/lib/optional-plugins.sh (stdout block with upstream plugin install commands) | mitigate | All install commands are literal strings — no variable interpolation of user-controlled data. Commands verified against upstream 2026-04-18 (06-RESEARCH.md §4, pinned in 06-CONTEXT.md). Mirror content from components/optional-plugins.md (06-02 Task 1). No secrets printed. |
| T-06-03-02 | Tampering | scripts/setup-security.sh (RTK.md install could clobber existing ~/.claude/RTK.md) | mitigate | Guard `[[ -f "$HOME/.claude/RTK.md" ]] || return 0` prevents clobber in ALL scenarios (rtk-init-generated, tk-prior-install, user-edited). Test scripts/tests/test-setup-security-rtk.sh explicitly covers both "absent → lands" and "present → NOT clobbered" — Scenario B exercises the guard. Matches Phase 3 SAFETY-01..04 invariant (never overwrite user files without consent). |
| T-06-03-03 | Information Disclosure | stdout block printed at end-of-run (DOCS-06) | accept | No secrets in literal strings. Printed to terminal; not persisted. Research §11 confirms zero secret-leakage surface. |
| T-06-03-04 | Tampering (stale caveat drift between shell block and components/optional-plugins.md) | scripts/lib/optional-plugins.sh ↔ components/optional-plugins.md | mitigate | Function body explicitly points to `.claude/components/optional-plugins.md` as source of truth. Future edits to the component file SHOULD trigger a matching update to the shell block — consider a Phase 7 follow-up `make validate-optional-plugins-sync` target that diffs the two. Not blocking v4.0. |
| T-06-03-05 | Denial of Service (script append after wiring breaks existing flow) | scripts/init-claude.sh, scripts/update-claude.sh | mitigate | Task 3 preserves all existing functions (recommend_security, recommend_statusline, setup_council, print_update_summary, etc.) byte-identical. `make test` re-runs all 14 existing test groups to catch regression. shellcheck + bash -n parse checks both scripts. |
| T-06-03-06 | Tampering | manifest.json (new files.components bucket add could accidentally corrupt existing `files.*` buckets) | mitigate | Task 1 preserves existing entries byte-identical. `make validate` enforces MANIFEST-04 invariants (every manifest path exists). `python3 -m json.tool manifest.json` confirms parse validity in verify step. CHANGELOG ↔ manifest version alignment check (BUG-06 Phase 1) remains green throughout. |
</threat_model>

<verification>
End-of-plan verification:

- `make check` — markdownlint + shellcheck + make validate all green
- `make test` — all 14 existing test groups PLUS the new Test 15 (scripts/tests/test-setup-security-rtk.sh) pass
- `markdownlint components/orchestration-pattern.md` — 0 errors (down from 9)
- `bash -c 'source scripts/lib/optional-plugins.sh && recommend_optional_plugins'` — prints the expected 4-plugin block
- `grep -q 'recommend_optional_plugins' scripts/init-claude.sh scripts/update-claude.sh` — both contain the invocation
- `grep -q 'orchestration-pattern' components/supreme-council.md components/structured-workflow.md README.md` — all 3 cross-references present
- `bash scripts/tests/test-setup-security-rtk.sh` — exits 0
- `jq -e '.files.components | length == 2' manifest.json` — exactly 2 entries registered

Phase 6 gate checklist (ready for Phase 7 handoff):
- [x] DOCS-01..08 all have implementation tasks in 06-01, 06-02, or 06-03
- [x] SC-1..SC-6 all satisfied
- [x] make check green
- [x] make test green
- [x] manifest.json version 4.0.0 aligned with CHANGELOG [4.0.0]
- [x] manifest.json files.components registers both Phase-6 components
</verification>

<success_criteria>
- DOCS-05 (manifest half): manifest.json files.components registers components/optional-plugins.md [VERIFIED by Task 1 jq assertion]
- DOCS-06: init-claude.sh + update-claude.sh print "Recommended optional plugins" block at end-of-run, sourced from scripts/lib/optional-plugins.sh [VERIFIED by Task 3 grep + shellcheck]
- DOCS-07 (install half): setup-security.sh installs templates/global/RTK.md to ~/.claude/RTK.md with guard; test-setup-security-rtk.sh covers absent + present scenarios [VERIFIED by Task 4 test execution]
- DOCS-08 (both halves): manifest.json registers orchestration-pattern.md [Task 1]; orchestration-pattern.md 0 mdlint errors; bidirectional cross-references with supreme-council.md + structured-workflow.md; README Components blurb [VERIFIED by Task 2 grep + markdownlint]
- make check green
- make test green (existing 14 groups + new Test 15)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-03-SUMMARY.md` using the @$HOME/.claude/get-shit-done/templates/summary.md template. Summary must include:
- Requirements satisfied: DOCS-05 (manifest-registration half), DOCS-06 (lib + wiring), DOCS-07 (install wiring half), DOCS-08 (both halves — manifest registration + content polish + cross-refs + README blurb)
- Files created (2): scripts/lib/optional-plugins.sh, scripts/tests/test-setup-security-rtk.sh
- Files modified (8): manifest.json (files.components bucket), components/orchestration-pattern.md, components/supreme-council.md, components/structured-workflow.md, README.md (Components blurb), scripts/init-claude.sh, scripts/update-claude.sh, scripts/setup-security.sh
- make check output (all green — markdownlint + shellcheck + validate)
- make test output (15 groups — 14 existing + 1 new; all green)
- Cross-plan verification: confirm 06-01 and 06-02 artifacts are all present and consistent (components/optional-plugins.md, templates/global/RTK.md, CHANGELOG [4.0.0], manifest.json version 4.0.0, 7 Required Base Plugins blocks)
- Phase 6 gate readiness statement: "Phase 6 complete; ready for Phase 7 validation (/gsd-verify-work)"
</output>
