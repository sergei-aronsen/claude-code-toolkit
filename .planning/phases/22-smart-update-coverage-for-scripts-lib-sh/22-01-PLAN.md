---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - CHANGELOG.md
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "manifest.json exposes all six scripts/lib/*.sh files under files.libs[]"
    - "manifest.json version equals 4.4.0"
    - "CHANGELOG.md top entry is [4.4.0] and consolidates Phase 21 (BOOTSTRAP-01..04) and Phase 22 (LIB-01..02) Added items"
    - "make version-align passes (manifest version, CHANGELOG header, init-local.sh --version all 4.4.0)"
  artifacts:
    - path: "manifest.json"
      provides: "Registration of six lib helper scripts so the update loop discovers them"
      contains: "\"libs\": ["
    - path: "CHANGELOG.md"
      provides: "Consolidated [4.4.0] release entry covering Phase 21 + Phase 22"
      contains: "## [4.4.0]"
  key_links:
    - from: "manifest.json .files.libs[]"
      to: "scripts/update-claude.sh:637 MANIFEST_FILES_JSON jq path"
      via: ".files | to_entries[] | .value[] | .path auto-discovery"
      pattern: "to_entries\\[\\] \\| \\.value\\[\\] \\| \\.path"
    - from: "manifest.json .version"
      to: "CHANGELOG.md ## [4.4.0]"
      via: "make version-align triple-check"
      pattern: "^## \\[4\\.4\\.0\\]"
---

<objective>
Register all six `scripts/lib/*.sh` helpers in `manifest.json` under a new `files.libs[]` array
and bump the toolkit version from 4.3.0 to 4.4.0 with a consolidated `CHANGELOG.md [4.4.0]`
entry that covers both Phase 21 (BOOTSTRAP-01..04) and Phase 22 (LIB-01..02) work.

Purpose: Close the silent gap where lib helpers live outside `manifest.json`, making them
invisible to `update-claude.sh`. Adding the `libs` key under `.files` turns on smart-update
coverage with zero code changes — the existing jq-driven update loop auto-discovers any new
top-level key. Bumping the version + CHANGELOG together honors the version-align gate.

Output: Two edited files (`manifest.json`, `CHANGELOG.md`), `make check` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-CONTEXT.md
@.planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-RESEARCH.md
@.planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From manifest.json (current state, lines 1-6 + 216-221):
```json
{
  "manifest_version": 2,
  "version": "4.3.0",
  "updated": "2026-04-26",
  ...
  "scripts": [
    {
      "path": "scripts/uninstall.sh"
    }
  ]
}
```

From scripts/update-claude.sh:637-638 (auto-discovery query):
```bash
MANIFEST_FILES_JSON=$(jq -c '[.files | to_entries[] | .value[] | .path]' "$MANIFEST_TMP")
```

From CHANGELOG.md (current top, lines 1-10):
```markdown
# Changelog
...
## [4.3.0] - 2026-04-26

### Added
```

From Makefile version-align target (lines 225-247) — three-way enforcement:
1. `jq -r '.version' manifest.json` MUST equal `## [X.Y.Z]` first version header in CHANGELOG.md
2. AND MUST equal output of `bash scripts/init-local.sh --version`
3. `init-local.sh` reads version from `manifest.json` at runtime (Phase 20 D-12) — no edit needed there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register six lib helpers in manifest.json under files.libs[] and bump version to 4.4.0</name>
  <read_first>
    - manifest.json (entire file — must understand existing JSON structure to insert key correctly)
    - scripts/update-claude.sh lines 630-650 (MANIFEST_FILES_JSON extraction confirms .files.* auto-discovery)
    - .planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-PATTERNS.md (manifest.json analog block, lines 22-67)
  </read_first>
  <files>manifest.json</files>
  <action>
    Edit `manifest.json` with two distinct changes:

    (1) Bump root version + updated date. Change line 3 from `"version": "4.3.0",` to `"version": "4.4.0",` and line 4 from `"updated": "2026-04-26",` to `"updated": "2026-04-27",`.

    (2) Add new `"libs"` key inside `.files` immediately after the existing `"scripts"` array (which currently ends at line 220 with `]`). The `"scripts"` block today is:

    ```json
        "scripts": [
          {
            "path": "scripts/uninstall.sh"
          }
        ]
    ```

    Replace with:

    ```json
        "scripts": [
          {
            "path": "scripts/uninstall.sh"
          }
        ],
        "libs": [
          {
            "path": "scripts/lib/backup.sh"
          },
          {
            "path": "scripts/lib/bootstrap.sh"
          },
          {
            "path": "scripts/lib/dry-run-output.sh"
          },
          {
            "path": "scripts/lib/install.sh"
          },
          {
            "path": "scripts/lib/optional-plugins.sh"
          },
          {
            "path": "scripts/lib/state.sh"
          }
        ]
    ```

    Note: comma added after the closing `]` of `scripts` (was previously the last key in `.files`). Six entries, alphabetical by basename per D-01 + Claude's discretion. NO `description:` field — matches existing `files.scripts[]` convention (D-01, CONTEXT.md Discretion section).

    Do NOT modify any other section (`agents`, `prompts`, `skills`, `commands`, `templates`, `components`, `rules`, `inventory`).

    After editing, validate JSON is parseable: `jq empty manifest.json` must exit 0.
  </action>
  <verify>
    <automated>jq empty manifest.json &amp;&amp; test "$(jq -r '.version' manifest.json)" = "4.4.0" &amp;&amp; test "$(jq '.files.libs | length' manifest.json)" = "6" &amp;&amp; jq -r '.files.libs[].path' manifest.json | diff - &lt;(printf 'scripts/lib/backup.sh\nscripts/lib/bootstrap.sh\nscripts/lib/dry-run-output.sh\nscripts/lib/install.sh\nscripts/lib/optional-plugins.sh\nscripts/lib/state.sh\n')</automated>
  </verify>
  <acceptance_criteria>
    - `jq empty manifest.json` exits 0 (valid JSON)
    - `jq -r '.version' manifest.json` outputs exactly `4.4.0`
    - `jq -r '.updated' manifest.json` outputs exactly `2026-04-27`
    - `jq '.files.libs | length' manifest.json` outputs exactly `6`
    - `jq -r '.files.libs[].path' manifest.json` outputs exactly these six lines in this order:
      `scripts/lib/backup.sh`, `scripts/lib/bootstrap.sh`, `scripts/lib/dry-run-output.sh`, `scripts/lib/install.sh`, `scripts/lib/optional-plugins.sh`, `scripts/lib/state.sh`
    - No `description` field in any `.files.libs[]` entry: `jq '.files.libs[] | has("description")' manifest.json` outputs six `false`
    - `.files.scripts` still contains exactly one entry with path `scripts/uninstall.sh`: `jq -r '.files.scripts[0].path' manifest.json` outputs `scripts/uninstall.sh`
    - `python3 scripts/validate-manifest.py` exits 0 (Check 5 path resolution still works — fallback at line 55 covers `scripts/lib/*.sh` per RESEARCH.md verification)
  </acceptance_criteria>
  <done>manifest.json bumped to 4.4.0 with `files.libs[]` containing six alphabetical entries; jq + validate-manifest.py both green.</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG [4.4.0] consolidated entry covering Phase 21 + Phase 22</name>
  <read_first>
    - CHANGELOG.md (top 50 lines — must see the existing `## [4.3.0] - 2026-04-26` block to insert above it correctly)
    - .planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-PATTERNS.md (CHANGELOG.md analog block, lines 71-105)
    - .planning/REQUIREMENTS.md (BOOTSTRAP-01..04 + LIB-01..02 acceptance text — feeds the bullet wording)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Insert a new `## [4.4.0] - 2026-04-27` section between line 6 (the trailing description paragraph ending `...semver.org/spec/v2.0.0.html).`) and line 8 (the existing `## [4.3.0] - 2026-04-26` header).

    The exact block to insert (preserve markdownlint compliance — blank lines before/after each list, no trailing punctuation in headings, MD040 not relevant here as no fenced code blocks):

    ```markdown
    ## [4.4.0] - 2026-04-27

    ### Added

    - **SP/GSD bootstrap installer** (`scripts/lib/bootstrap.sh`, `scripts/lib/optional-plugins.sh`) —
      BOOTSTRAP-01..04: `init-claude.sh` and `init-local.sh` now offer to install `superpowers`
      via `claude plugin install superpowers@claude-plugins-official` and `get-shit-done` via the
      canonical curl install before detection runs. Prompts default to `N`, fail closed when no
      TTY is available, and `--no-bootstrap` (or `TK_NO_BOOTSTRAP=1`) suppresses them entirely
      for CI. After bootstrap, `detect.sh` re-runs so the toolkit installs in the correct mode
      (`complement-sp` / `complement-gsd` / `complement-full`). Hermetic test:
      `scripts/tests/test-bootstrap.sh` (Test 28).

    - **Smart-update coverage for `scripts/lib/*.sh`** — LIB-01: all six sourced helper libraries
      (`backup.sh`, `bootstrap.sh`, `dry-run-output.sh`, `install.sh`, `optional-plugins.sh`,
      `state.sh`) registered in `manifest.json` under a new `files.libs[]` array. LIB-02:
      `update-claude.sh` now refreshes stale lib files using the same diff/backup/safe-write
      contract as top-level scripts — zero code changes to the update loop required (the existing
      `jq -c '[.files | to_entries[] | .value[] | .path]'` query auto-discovers the new key).
      Hermetic test: `scripts/tests/test-update-libs.sh` (Test 29) — five scenarios proving
      stale-refresh, clean-untouched, fresh-install, modified-file fail-closed, and uninstall
      round-trip across all six libs.

    ```

    Markdown formatting requirements (per CLAUDE.md):
    - Blank line BEFORE the new `## [4.4.0]` header
    - Blank line AFTER the new section before the existing `## [4.3.0]` header
    - Blank line before AND after `### Added`
    - Blank line before AND after the unordered list
    - No trailing punctuation in headings (MD026): `### Added` not `### Added.`
    - Each bullet item is one paragraph; line wrapping inside a bullet uses 2-space indent on continuation lines (matches `[4.3.0]` precedent at lines 12-14)

    Do NOT modify any line at or below the existing `## [4.3.0] - 2026-04-26` block. Do NOT delete or reorder any existing release entry.
  </action>
  <verify>
    <automated>test "$(grep -m1 '^## \[' CHANGELOG.md)" = "## [4.4.0] - 2026-04-27" &amp;&amp; grep -q 'BOOTSTRAP-01..04' CHANGELOG.md &amp;&amp; grep -q 'LIB-01' CHANGELOG.md &amp;&amp; grep -q 'LIB-02' CHANGELOG.md &amp;&amp; grep -q 'files.libs\[\]' CHANGELOG.md &amp;&amp; grep -q 'test-update-libs.sh' CHANGELOG.md &amp;&amp; npx markdownlint-cli CHANGELOG.md &amp;&amp; make version-align</automated>
  </verify>
  <acceptance_criteria>
    - First `## [` heading in CHANGELOG.md is exactly `## [4.4.0] - 2026-04-27`: `grep -m1 '^## \[' CHANGELOG.md` returns that string
    - `[4.3.0]` section preserved (exact byte equality with prior content): `git diff CHANGELOG.md` shows ZERO deletions/modifications below the new block — only insertions above the `## [4.3.0]` line
    - All four reference IDs present in the new section: `grep -q 'BOOTSTRAP-01..04' CHANGELOG.md` AND `grep -q 'LIB-01' CHANGELOG.md` AND `grep -q 'LIB-02' CHANGELOG.md` all exit 0
    - Both new test references present: `grep -q 'test-bootstrap.sh' CHANGELOG.md` AND `grep -q 'test-update-libs.sh' CHANGELOG.md` both exit 0
    - `npx markdownlint-cli CHANGELOG.md` exits 0 (MD040, MD031, MD032, MD026 all clean)
    - `make version-align` exits 0 — three-way match holds: `manifest.json .version` (4.4.0) == CHANGELOG top header (4.4.0) == `bash scripts/init-local.sh --version` output (4.4.0)
  </acceptance_criteria>
  <done>CHANGELOG.md leads with `## [4.4.0] - 2026-04-27` consolidating Phase 21 + Phase 22 work; markdownlint passes; version-align gate green.</done>
</task>

</tasks>

<verification>
Run these in order after both tasks complete:

```bash
# Each gate must exit 0
jq empty manifest.json
test "$(jq -r '.version' manifest.json)" = "4.4.0"
test "$(jq '.files.libs | length' manifest.json)" = "6"
python3 scripts/validate-manifest.py
test "$(grep -m1 '^## \[' CHANGELOG.md)" = "## [4.4.0] - 2026-04-27"
npx markdownlint-cli CHANGELOG.md
make version-align
make check
```

`make check` must remain green — `make shellcheck` is not exercised by this plan (no shell edits)
but `make mdlint`, `make validate`, and `make version-align` all exercise the changed files.
</verification>

<success_criteria>
- manifest.json declares six `files.libs[]` entries in the exact alphabetical order defined above
- manifest.json `.version` is `4.4.0`; `.updated` is `2026-04-27`
- CHANGELOG.md leads with `## [4.4.0] - 2026-04-27` and consolidates Phase 21 + Phase 22 Added bullets
- `make check` is green (no markdownlint, validate, or version-align regressions)
- `python3 scripts/validate-manifest.py` exits 0 (path resolution Check 5 confirms `scripts/lib/*.sh` resolves via the REPO_ROOT fallback per RESEARCH.md §1.5)
- LIB-01 acceptance criterion satisfied: registration is in place; behavioral proof comes in Plan 22-02
</success_criteria>

<output>
After completion, create `.planning/phases/22-smart-update-coverage-for-scripts-lib-sh/22-01-SUMMARY.md`
documenting: (1) exact `files.libs[]` order committed, (2) version-align command output as proof
of the three-way match, (3) confirmation that no `update-claude.sh` code edits were necessary
(D-01 / D-07 zero-special-casing invariant), (4) any markdownlint adjustments needed beyond the
template in this plan.
</output>
