---
phase: 31-distribution-tests-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - plugins/tk-skills/.claude-plugin/plugin.json
  - plugins/tk-commands/.claude-plugin/plugin.json
  - plugins/tk-framework-rules/.claude-plugin/plugin.json
  - CHANGELOG.md
autonomous: true
requirements:
  - BRIDGE-DIST-01
  - BRIDGE-DIST-02

must_haves:
  truths:
    - "manifest.json files.libs[] includes scripts/lib/bridges.sh in alphabetized position"
    - "manifest.json::version equals 4.7.0"
    - "All 3 plugin.json files (tk-skills, tk-commands, tk-framework-rules) have version 4.7.0"
    - "CHANGELOG.md has [4.7.0] - 2026-04-29 entry covering all 18 BRIDGE-* REQ-IDs"
    - "make check still passes (markdownlint clean on CHANGELOG.md, JSON valid)"
  artifacts:
    - path: "manifest.json"
      provides: "v4.7.0 manifest with bridges.sh registered in files.libs[]"
      contains: '"path": "scripts/lib/bridges.sh"'
    - path: "plugins/tk-skills/.claude-plugin/plugin.json"
      provides: "Plugin version 4.7.0"
      contains: '"version": "4.7.0"'
    - path: "plugins/tk-commands/.claude-plugin/plugin.json"
      provides: "Plugin version 4.7.0"
      contains: '"version": "4.7.0"'
    - path: "plugins/tk-framework-rules/.claude-plugin/plugin.json"
      provides: "Plugin version 4.7.0"
      contains: '"version": "4.7.0"'
    - path: "CHANGELOG.md"
      provides: "[4.7.0] consolidated entry covering 18 BRIDGE-* REQ-IDs"
      contains: "## [4.7.0] - 2026-04-29"
  key_links:
    - from: "manifest.json"
      to: "scripts/lib/bridges.sh"
      via: "files.libs[] array entry"
      pattern: '"path":\s*"scripts/lib/bridges\.sh"'
    - from: "update-claude.sh"
      to: "scripts/lib/bridges.sh"
      via: "v4.4 LIB-01 D-07 jq path .files | to_entries[] | .value[] | .path (auto-discovery, zero code change)"
      pattern: 'to_entries\[\]'
---

<objective>
Bump manifest + 3 plugin versions to 4.7.0, register `scripts/lib/bridges.sh` in
`manifest.json::files.libs[]` (alphabetized between `bootstrap.sh` and
`cli-recommendations.sh`), and add a single consolidated `[4.7.0]` CHANGELOG entry
covering all 18 BRIDGE-* REQ-IDs.

Purpose: Phase 31 ships v4.7. This plan delivers BRIDGE-DIST-01 (manifest bridges.sh
registration + version 4.7.0) and BRIDGE-DIST-02 (CHANGELOG [4.7.0] consolidated entry).
The v4.4 LIB-01 D-07 invariant (zero code changes — `update-claude.sh` auto-discovers
via existing jq path) means simply listing `bridges.sh` in `files.libs[]` is sufficient
to ship it on next update.

Output: Manifest registered, all 4 version fields bumped in lock-step, CHANGELOG entry
documents Detection / Generation / Sync / UX / Distribution / Docs / Tests sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-distribution-tests-docs/31-CONTEXT.md
@manifest.json
@CHANGELOG.md

<interfaces>
<!-- Verbatim entries — executor copies these into the files. No exploration needed. -->

manifest.json libs[] insertion point (BEFORE this plan, lines 228-268 — alphabetized):
```json
"libs": [
  { "path": "scripts/lib/backup.sh" },
  { "path": "scripts/lib/bootstrap.sh" },
  { "path": "scripts/lib/cli-recommendations.sh" },
  ...
]
```

manifest.json libs[] AFTER this plan (insert bridges.sh between bootstrap.sh and cli-recommendations.sh):
```json
"libs": [
  { "path": "scripts/lib/backup.sh" },
  { "path": "scripts/lib/bootstrap.sh" },
  { "path": "scripts/lib/bridges.sh" },
  { "path": "scripts/lib/cli-recommendations.sh" },
  { "path": "scripts/lib/detect2.sh" },
  ...
]
```

manifest.json version field (line 3):
```json
"version": "4.7.0",
```
(was: "4.6.0")

Each plugin.json file changes only the "version" field:
```json
{
  "name": "tk-skills",
  "version": "4.7.0",
  ...
}
```

CHANGELOG.md template (insert ABOVE existing `## [4.6.0]` block — line 8 region):
```markdown
## [4.7.0] - 2026-04-29

### Added — Multi-CLI Bridge

- **CLI detection** (`scripts/lib/detect2.sh`) — BRIDGE-DET-01..03: Phase 28.
  `is_gemini_installed` and `is_codex_installed` probes added alongside the
  existing 6 binary probes from v4.6 Phase 24. Both return 0/1 binary, fail-soft
  via `command -v <cli>` with `[ -d ~/.gemini/ ]` / `[ -d ~/.codex/ ]` as soft
  cross-check. `detect2_cache` exports `IS_GEM` and `IS_COD`.

- **Bridge generation library** (`scripts/lib/bridges.sh`, 467 lines) —
  BRIDGE-GEN-01..04: Phase 28. `bridge_create_project <target>` writes
  `<project>/GEMINI.md` (gemini) or `<project>/AGENTS.md` (codex) — note this
  is the OpenAI standard, NOT `CODEX.md`. `bridge_create_global <target>` writes
  `~/.gemini/GEMINI.md` / `~/.codex/AGENTS.md` and never touches the canonical
  `CLAUDE.md`. Auto-generated header banner is byte-identical across all
  bridges. Each bridge registers in `~/.claude/toolkit-install.json::bridges[]`
  with `target`, `path`, `scope`, `source_sha256`, `bridge_sha256`,
  `user_owned: false`. Atomic state writes via `tempfile.mkstemp + os.replace`.

- **Sync on update** (`scripts/update-claude.sh`) — BRIDGE-SYNC-01..03: Phase 29.
  `sync_bridges()` iterates `bridges[]` from state file. Source-drift detection
  (recorded `source_sha256` differs from current) triggers re-copy and SHA
  refresh, logging `[~ UPDATE] GEMINI.md`. Bridge-drift detection (user edited
  the bridge file) triggers `[y/N/d]` prompt with default `N`; `d` shows diff
  and re-prompts (mirrors v4.3 UN-03 contract). Orphaned source (CLAUDE.md
  deleted) logs `[? ORPHANED]` and auto-flips `user_owned: true`.

- **Break/restore bridges** (`scripts/update-claude.sh`) — BRIDGE-SYNC-02:
  Phase 29. `--break-bridge <target>` flips `user_owned: true` for the named
  bridge; subsequent updates skip it with `[- SKIP]`. `--restore-bridge <target>`
  reverses the flag and resumes sync on next update.

- **Uninstall integration** (`scripts/uninstall.sh`) — BRIDGE-UN-01..02:
  Phase 29. Bridges from `bridges[]` are classified via `classify_bridge_file`
  helper: clean → REMOVE_LIST; user-modified → MODIFIED_LIST with v4.3 `[y/N/d]`
  prompt. `is_protected_path` correctly bypassed for bridges. `--keep-state`
  (v4.4 KEEP-01) preserves `bridges[]` entries alongside the rest of
  toolkit-install.json — no special-case handling needed.

- **Install-time UX** (`scripts/install.sh`, `scripts/init-claude.sh`,
  `scripts/init-local.sh`) — BRIDGE-UX-01..04: Phase 30. The unified TUI
  (`install.sh`) shows conditional `gemini-bridge` / `codex-bridge` rows in
  the Components page when the corresponding CLI is detected; rows hidden
  otherwise. `init-claude.sh` and `init-local.sh` post-install per-CLI prompt
  defaulting `Y`, fail-closed `N` on no-TTY (CI / piped install). All 3 entry
  points support `--no-bridges` / `TK_NO_BRIDGES=1` (skip) and `--bridges
  gemini,codex` (force-create non-interactively). With `--fail-fast`, absent
  CLI exits 1; without, warns and continues.

- **Multi-CLI bridge documentation** (`docs/BRIDGES.md`, `docs/INSTALL.md`,
  `README.md`) — BRIDGE-DOCS-01..02: Phase 31. New `docs/BRIDGES.md` documents
  supported CLIs (Gemini → `GEMINI.md`, OpenAI Codex → `AGENTS.md`),
  plain-copy semantics, drift handling, opt-out (`--no-bridges`,
  `--break-bridge`, `--restore-bridge`), force-create (`--bridges <list>`),
  symlink-vs-copy rationale, uninstall behaviour, future scope. `INSTALL.md`
  Installer Flags table extended with 4 new flag rows. `README.md` Killer
  Features grid mentions multi-CLI bridges.

- **Manifest registration** (`manifest.json`) — BRIDGE-DIST-01: Phase 31.
  `scripts/lib/bridges.sh` added to `files.libs[]` (alphabetized between
  `bootstrap.sh` and `cli-recommendations.sh`). Auto-discovered by
  `update-claude.sh` via the v4.4 LIB-01 D-07 jq path with zero code changes.

### Changed

- **`write_state` arity extended** (`scripts/lib/state.sh`) — Phase 29 D-29-01
  backward-compatible 10-arg variant accepts `bridges_json` as the 10th
  positional. Existing 9-arg callers (`init-claude.sh`, `update-claude.sh`,
  `install.sh`) work unchanged via Bash positional-default semantics.
  `init-local.sh` and `migrate-to-complement.sh` updated to pass the 10th arg.

- **Manifest version** bumped from 4.6.0 to 4.7.0. All 3 plugin manifests
  (`tk-skills`, `tk-commands`, `tk-framework-rules`) bumped in lock-step.

### Fixed

- **Phase 29 WR-01** — uninstall `[y/N/d]` bypass for user-modified bridges
  fixed by routing through existing v4.3 prompt path instead of skipping.

- **Phase 29 WR-02** — state file path mismatch in test fixtures (was using
  `STATE_FILE_HOME` instead of `TK_BRIDGE_HOME`) corrected; all hermetic tests
  now run in fully isolated sandboxes.

- **Phase 30 WR-01** — silent `--bridges <list>` failure when named CLI absent
  without `--fail-fast` now prints a warning to stderr and continues.

### Tests

- 3 new hermetic suites totalling 50 assertions:
  - `scripts/tests/test-bridges-foundation.sh` (5 assertions, Phase 28)
  - `scripts/tests/test-bridges-sync.sh` (25 assertions, Phase 29)
  - `scripts/tests/test-bridges-install-ux.sh` (20 assertions, Phase 30)
- `scripts/tests/test-bridges.sh` (NEW) — aggregator wrapping the 3 suites
  with a single PASS/FAIL summary; wired into CI (`quality.yml`
  test-init-script job).

### Compatibility

- BACKCOMPAT-01 preserved across all v4.6 baselines:
  - `test-bootstrap.sh` PASS=26 unchanged
  - `test-install-tui.sh` PASS=43 unchanged
  - All 7 v4.3 uninstall-suite tests unchanged
  - All v4.6 MCP / Skills / Marketplace tests unchanged
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register bridges.sh in manifest + bump manifest version</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json (full file — lines 1-15 for version field, lines 228-268 for libs[] array)
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block, lines 26-50)
  </read_first>
  <action>
Edit `manifest.json` with two changes:

(1) Line 3 — bump version field from `"version": "4.6.0",` to `"version": "4.7.0",`. The `manifest_version: 2` (line 2) and `updated: "2026-04-29"` (line 4) fields are unchanged.

(2) Line 228-268 region — insert a new entry in the `files.libs[]` array between the existing `bootstrap.sh` entry (currently lines 232-234) and the existing `cli-recommendations.sh` entry (currently lines 235-237). The new entry must read EXACTLY:

```json
      {
        "path": "scripts/lib/bridges.sh"
      },
```

Resulting libs[] array order (alphabetized, after edit):
1. `scripts/lib/backup.sh`
2. `scripts/lib/bootstrap.sh`
3. `scripts/lib/bridges.sh`  ← NEW
4. `scripts/lib/cli-recommendations.sh`
5. `scripts/lib/detect2.sh`
6. `scripts/lib/dispatch.sh`
7. `scripts/lib/dry-run-output.sh`
8. `scripts/lib/install.sh`
9. `scripts/lib/mcp-catalog.json`
10. `scripts/lib/mcp.sh`
11. `scripts/lib/optional-plugins.sh`
12. `scripts/lib/skills.sh`
13. `scripts/lib/state.sh`
14. `scripts/lib/tui.sh`

NO new keys. NO `manifest_version` change. NO `updated` field change. NO modifications to any other `files.*` array. Do NOT touch `inventory`, `templates`, `mode_notes`, `sp_equivalent_note`.

This implements BRIDGE-DIST-01 per CONTEXT.md decisions block (lines 28-35). The v4.4 LIB-01 D-07 invariant guarantees `update-claude.sh` will auto-ship the new lib via the existing jq path `.files | to_entries[] | .value[] | .path` — zero code changes to `update-claude.sh`.
  </action>
  <verify>
    <automated>jq -e '.files.libs | map(.path) | index("scripts/lib/bridges.sh")' manifest.json &amp;&amp; jq -e '.version == "4.7.0"' manifest.json &amp;&amp; jq -e '.files.libs | map(.path) | . == sort' manifest.json &amp;&amp; jq empty manifest.json</automated>
  </verify>
  <done>
    - `jq -e '.version' manifest.json` returns `"4.7.0"`
    - `jq '.files.libs[].path' manifest.json | grep -c '"scripts/lib/bridges.sh"'` returns 1
    - `jq -r '.files.libs[].path' manifest.json | sort -c` exits 0 (already sorted)
    - `jq empty manifest.json` exits 0 (valid JSON)
    - `jq -r '.manifest_version' manifest.json` still returns `2` (unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version 4.6.0 → 4.7.0 in 3 plugin manifests</name>
  <files>plugins/tk-skills/.claude-plugin/plugin.json, plugins/tk-commands/.claude-plugin/plugin.json, plugins/tk-framework-rules/.claude-plugin/plugin.json</files>
  <read_first>
    - plugins/tk-skills/.claude-plugin/plugin.json (full file)
    - plugins/tk-commands/.claude-plugin/plugin.json (full file)
    - plugins/tk-framework-rules/.claude-plugin/plugin.json (full file)
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block, lines 36-40)
  </read_first>
  <action>
Edit ONLY the `version` field in each of the 3 plugin.json files. Change `"version": "4.6.0"` to `"version": "4.7.0"`. Do NOT modify `name`, `description`, `category`, `tags`, or any other field. Each file remains a single-object JSON with the same key order.

Files to edit (in any order — all 3 are independent):

1. `plugins/tk-skills/.claude-plugin/plugin.json` — `version: 4.6.0 → 4.7.0`
2. `plugins/tk-commands/.claude-plugin/plugin.json` — `version: 4.6.0 → 4.7.0`
3. `plugins/tk-framework-rules/.claude-plugin/plugin.json` — `version: 4.6.0 → 4.7.0`

This mirrors the v4.6 ship pattern (CONTEXT.md decisions line 39): plugin versions match `manifest.json::version` in lock-step. Per Phase 27 D-27 (STATE.md line 121), version is declared once per plugin (in plugin.json, not duplicated in marketplace.json) — so `marketplace.json` is NOT touched here.

Use jq for safe edits (avoids markdown-style sed risks):

```bash
for p in tk-skills tk-commands tk-framework-rules; do
    f="plugins/$p/.claude-plugin/plugin.json"
    tmp="$(mktemp)"
    jq '.version = "4.7.0"' "$f" > "$tmp" && mv "$tmp" "$f"
done
```

(Note: jq emits its own formatting. If the diff shows any structural change beyond the version field, fall back to manual edit with Edit tool to preserve the original 5-line one-key-per-line layout.)
  </action>
  <verify>
    <automated>for p in tk-skills tk-commands tk-framework-rules; do jq -e '.version == "4.7.0"' "plugins/$p/.claude-plugin/plugin.json" || exit 1; done</automated>
  </verify>
  <done>
    - `jq -r .version plugins/tk-skills/.claude-plugin/plugin.json` returns `"4.7.0"`
    - `jq -r .version plugins/tk-commands/.claude-plugin/plugin.json` returns `"4.7.0"`
    - `jq -r .version plugins/tk-framework-rules/.claude-plugin/plugin.json` returns `"4.7.0"`
    - All 3 files still valid JSON (`jq empty` exits 0)
    - `name`, `description`, `category`, `tags` fields preserved in each
  </done>
</task>

<task type="auto">
  <name>Task 3: Add [4.7.0] consolidated CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — focus on lines 1-94 for the existing [4.6.0] block which serves as the formatting template)
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block, lines 104-112; specifics block, line 169)
    - .planning/REQUIREMENTS.md (full 18-REQ-ID list across all 4 phases)
    - .planning/phases/28-bridge-foundation/28-VERIFICATION.md (file/line evidence for Detection + Generation REQ-IDs)
    - .planning/phases/29-sync-uninstall-integration/29-VERIFICATION.md (file/line evidence for Sync + UN REQ-IDs + write_state 10-arg note + WR-01/WR-02 fixes)
    - .planning/phases/30-install-time-ux/30-VERIFICATION.md (file/line evidence for UX REQ-IDs + WR-01 fix)
  </read_first>
  <action>
Insert a new `## [4.7.0] - 2026-04-29` block at the very top of the changelog body — directly above the existing `## [4.6.0] - 2026-04-29` block (currently line 8). The new block goes between line 7 (the trailing blank line after the format reference) and line 8 (the existing [4.6.0] heading). All existing entries from `[4.6.0]` downward are preserved unchanged.

Use the verbatim CHANGELOG template from the `<interfaces>` block in this plan's context section (top of file). The block has 6 sections:

1. `### Added — Multi-CLI Bridge` — 8 bullets covering Detection (BRIDGE-DET-01..03), Bridge generation library (BRIDGE-GEN-01..04), Sync on update (BRIDGE-SYNC-01..03), Break/restore (BRIDGE-SYNC-02 sub-bullet), Uninstall integration (BRIDGE-UN-01..02), Install-time UX (BRIDGE-UX-01..04), Multi-CLI bridge documentation (BRIDGE-DOCS-01..02), Manifest registration (BRIDGE-DIST-01).

2. `### Changed` — `write_state` 10-arg arity extension (Phase 29 D-29-01) and manifest+plugin version bumps.

3. `### Fixed` — Phase 29 WR-01 (uninstall [y/N/d] bypass), WR-02 (state path mismatch), Phase 30 WR-01 (silent --bridges failure).

4. `### Tests` — list 3 hermetic suites (5 + 25 + 20 = 50 assertions) and the new aggregator `test-bridges.sh`.

5. `### Compatibility` — BACKCOMPAT-01 invariant: 26 + 43 + 7 + v4.6 MCP/Skills/Marketplace baselines unchanged.

The block follows the existing v4.6 entry pattern (CHANGELOG.md lines 8-93) for tone, formatting, and structure. All bullets are 2-line wrapped at ~70 cols to match the surrounding markdown style. Every BRIDGE-* REQ-ID is mentioned at least once. Date stamp is `2026-04-29` matching the actual ship date (CONTEXT.md decisions line 112).

Markdown rules to honour (CLAUDE.md):

- MD040: every fenced code block declares a language (none in this CHANGELOG entry — pure prose)
- MD031/MD032: blank line before AND after every fenced block and list (verify around bullets)
- MD026: no trailing punctuation in headings (`### Added — Multi-CLI Bridge` — em-dash counts as separator, not punctuation; matches existing `### Phase 24 Sub-Phase 1 — Globalize Council artifacts` pattern at line 96)

Do NOT modify any of the existing `[4.6.0]`, `[4.5.0]`, `[4.4.0]`, etc. entries.
  </action>
  <verify>
    <automated>grep -c '^## \[4.7.0\] - 2026-04-29' CHANGELOG.md | grep -q '^1$' &amp;&amp; for r in BRIDGE-DET-01 BRIDGE-DET-02 BRIDGE-DET-03 BRIDGE-GEN-01 BRIDGE-GEN-02 BRIDGE-GEN-03 BRIDGE-GEN-04 BRIDGE-SYNC-01 BRIDGE-SYNC-02 BRIDGE-SYNC-03 BRIDGE-UN-01 BRIDGE-UN-02 BRIDGE-UX-01 BRIDGE-UX-02 BRIDGE-UX-03 BRIDGE-UX-04 BRIDGE-DIST-01 BRIDGE-DOCS-01 BRIDGE-DOCS-02; do grep -q "$r" CHANGELOG.md || { echo "missing: $r"; exit 1; }; done &amp;&amp; npx --yes markdownlint-cli CHANGELOG.md</automated>
  </verify>
  <done>
    - `grep -c '^## \[4.7.0\] - 2026-04-29' CHANGELOG.md` returns 1
    - All 18 BRIDGE-* + BRIDGE-DIST-01 REQ-IDs (i.e. all 19 covering DET-01..03, GEN-01..04, SYNC-01..03, UN-01..02, UX-01..04, DIST-01, DOCS-01..02) referenced at least once in the [4.7.0] block
    - Existing `## [4.6.0] - 2026-04-29` block preserved unchanged
    - `npx --yes markdownlint-cli CHANGELOG.md` exits 0 (no markdownlint errors)
    - All 5 sections present: `### Added — Multi-CLI Bridge`, `### Changed`, `### Fixed`, `### Tests`, `### Compatibility`
  </done>
</task>

</tasks>

<verification>
Phase-wide acceptance commands (run after all 3 tasks complete):

```bash
# (1) Manifest version + bridges.sh registration
jq -r .version manifest.json                                   # → "4.7.0"
jq '.files.libs[] | select(.path == "scripts/lib/bridges.sh")' manifest.json  # → entry present
jq -r '.files.libs[].path' manifest.json | sort -c             # exits 0 (sorted)

# (2) Plugin versions in lock-step
jq -r .version plugins/tk-skills/.claude-plugin/plugin.json          # → "4.7.0"
jq -r .version plugins/tk-commands/.claude-plugin/plugin.json        # → "4.7.0"
jq -r .version plugins/tk-framework-rules/.claude-plugin/plugin.json # → "4.7.0"

# (3) CHANGELOG entry presence + REQ-ID coverage
grep -c '^## \[4.7.0\] - 2026-04-29' CHANGELOG.md     # → 1
grep -c 'BRIDGE-' CHANGELOG.md                         # → ≥18 (one per REQ-ID)

# (4) markdownlint on CHANGELOG
npx --yes markdownlint-cli CHANGELOG.md                # exits 0
```
</verification>

<success_criteria>
- `manifest.json::version` is `"4.7.0"`
- `manifest.json::files.libs[]` contains `{ "path": "scripts/lib/bridges.sh" }` in alphabetized position
- All 3 plugin.json files report `version: 4.7.0`
- CHANGELOG.md has new `## [4.7.0] - 2026-04-29` block referencing all 18 BRIDGE-* REQ-IDs across Added / Changed / Fixed / Tests / Compatibility sections
- All previous CHANGELOG entries preserved
- `make check` (run separately by Plan 31-03) passes against the modified CHANGELOG.md
- All JSON files remain valid (`jq empty` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/31-distribution-tests-docs/31-01-SUMMARY.md`
documenting: manifest libs[] line position of bridges.sh, version field byte changes,
the 3 plugin.json edits, and the [4.7.0] CHANGELOG block line range. Confirm BRIDGE-DIST-01
and BRIDGE-DIST-02 covered.
</output>
