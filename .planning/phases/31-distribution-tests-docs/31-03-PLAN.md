---
phase: 31-distribution-tests-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/BRIDGES.md
  - docs/INSTALL.md
  - README.md
autonomous: true
requirements:
  - BRIDGE-DOCS-01
  - BRIDGE-DOCS-02

must_haves:
  truths:
    - "docs/BRIDGES.md exists with 9 sections (Overview, Supported CLIs, How it Works, Drift Handling, Opt-Out Mechanics, Force-Create, Why No Symlink, Uninstall, Future Scope)"
    - "docs/BRIDGES.md explicitly documents AGENTS.md as the OpenAI Codex standard (NOT CODEX.md) at least 2 times"
    - "docs/INSTALL.md Installer Flags table extended with 4 bridge flag rows"
    - "docs/INSTALL.md has new Multi-CLI Bridges (v4.7+) sub-section linking to docs/BRIDGES.md"
    - "README.md Killer Features grid has a row mentioning multi-CLI bridges with link to docs/BRIDGES.md"
    - "make check passes (markdownlint clean on all 3 modified files)"
  artifacts:
    - path: "docs/BRIDGES.md"
      provides: "v4.7 multi-CLI bridge user documentation"
      contains: "AGENTS.md"
      min_lines: 80
    - path: "docs/INSTALL.md"
      provides: "Installer Flags table extended with 4 bridge flag rows + Multi-CLI Bridges sub-section"
      contains: "no-bridges"
    - path: "README.md"
      provides: "Killer Features row referencing multi-CLI bridges"
      contains: "Multi-CLI"
  key_links:
    - from: "README.md::Killer Features grid"
      to: "docs/BRIDGES.md"
      via: "markdown link in row description"
      pattern: 'docs/BRIDGES\.md'
    - from: "docs/INSTALL.md::Multi-CLI Bridges sub-section"
      to: "docs/BRIDGES.md"
      via: "markdown link"
      pattern: 'BRIDGES\.md'
    - from: "docs/INSTALL.md::Installer Flags table"
      to: "scripts/{init-claude,init-local,install,update-claude}.sh"
      via: "Applies To column listing each flag's entry points"
      pattern: 'no-bridges'
---

<objective>
Create `docs/BRIDGES.md` (NEW) as the canonical user documentation for v4.7's
multi-CLI bridge feature. Extend `docs/INSTALL.md` Installer Flags table with
four new bridge flag rows + a `### Multi-CLI Bridges (v4.7+)` sub-section
mirroring the v4.4 `--no-bootstrap` / `--no-banner` / `--keep-state` block
pattern. Add a single Killer Features row to `README.md` referencing the
bridge feature with a link to docs/BRIDGES.md.

Purpose: Delivers BRIDGE-DOCS-01 (BRIDGES.md content with locked 9-section
structure per CONTEXT.md decisions lines 76-87) and BRIDGE-DOCS-02
(INSTALL.md flag table + sub-section + README mention per CONTEXT.md
decisions lines 89-102). The README mention is the single user-discovery
touchpoint that brings traffic to the new doc (CONTEXT.md specifics line 170).

Output: 1 NEW + 2 MODIFIED markdown files; all 3 markdownlint-clean so `make
check` passes green; AGENTS.md (not CODEX.md) emphasized as the OpenAI
standard in BRIDGES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-distribution-tests-docs/31-CONTEXT.md
@docs/INSTALL.md
@README.md
@.markdownlint.json

<interfaces>
<!-- Locked section structure for BRIDGES.md per CONTEXT.md decisions lines 76-87. -->
<!-- Wording is at picker's discretion (CONTEXT.md lines 130-133); structure is NOT. -->

docs/BRIDGES.md required sections (in this order):

1. Title — `# Multi-CLI Bridges`
2. `## Overview` — what bridges do, why they exist (single-source-of-truth invariant)
3. `## Supported CLIs` — markdown table mapping CLI to bridge file:
   - Gemini CLI -> GEMINI.md (detection: command -v gemini)
   - OpenAI Codex CLI -> AGENTS.md (detection: command -v codex)
   Followed by an explicit note: "OpenAI Codex CLI reads `AGENTS.md` (the OpenAI
   standard for agent context files), NOT `CODEX.md`. The toolkit follows the
   upstream convention."
4. `## How it Works` — plain-copy semantics + verbatim 4-line banner heredoc:

   ```text
   <!--
     Auto-generated from CLAUDE.md by claude-code-toolkit (v4.7+).
     Edit CLAUDE.md (canonical source). This file regenerates on update-claude.sh.
     To stop sync: run `update-claude.sh --break-bridge <name>`.
   -->
   ```

5. `## Drift Handling` — 3 log states: `[~ UPDATE]` (source changed, bridge clean),
   `[y/N/d]` prompt (bridge user-edited; default N, y overwrites, d shows diff),
   `[? ORPHANED]` (CLAUDE.md missing; bridge preserved, auto-flips user_owned)
6. `## Opt-Out Mechanics` — three escape hatches with bash examples:
   - `--no-bridges` flag / `TK_NO_BRIDGES=1` env on init-claude.sh, init-local.sh, install.sh
   - `update-claude.sh --break-bridge <target>` flips user_owned: true
   - `update-claude.sh --restore-bridge <target>` reverses the flag
7. `## Force-Create` (or `## Force-Create (Non-Interactive)`) —
   `--bridges <list>` flag on the 3 install entry points; behaviour with/without
   `--fail-fast`; bash examples
8. `## Why No Symlink` (NO question mark — MD026) — three rationale bullets:
   per-CLI customization, future tone overlays, transparent SHA256 drift
9. `## Uninstall` — classify_bridge_file routing (REMOVE vs MODIFIED), `--keep-state` semantics
10. `## Future Scope` — BRIDGE-FUT-01 deferred to v4.8; BRIDGE-FUT-03/04 (Cursor/Aider) out of scope

(Sections 2-10 = 9 sections + title.)

---

docs/INSTALL.md edit — Installer Flags table currently lines 63-72:

CURRENT table layout (with `--keep-state` row at line 71 and `--no-council` at line 72).

APPEND 4 new rows BETWEEN `--keep-state` (line 71) and `--no-council` (line 72):

```markdown
| `--no-bridges` | `init-claude.sh`, `init-local.sh`, `install.sh` | Skip all bridge prompts. Equivalent env: `TK_NO_BRIDGES=1`. Mirrors `--no-bootstrap` symmetry. |
| `--bridges <list>` | `init-claude.sh`, `init-local.sh`, `install.sh` | Force-create bridges for named CLIs (comma-separated, e.g. `gemini,codex`). Skips per-CLI prompt. With `--fail-fast`: absent CLI exits 1; otherwise warns and continues. |
| `--break-bridge <target>` | `update-claude.sh` | Flip `user_owned: true` for the named bridge target. Subsequent `update-claude.sh` runs skip that bridge silently. |
| `--restore-bridge <target>` | `update-claude.sh` | Reverse `--break-bridge`. Next `update-claude.sh` re-syncs the named bridge. |
```

ALSO append a new sub-section AFTER the existing `### --keep-state for uninstall.sh (v4.4+)`
block (which currently ends at line 107) and BEFORE the `---` separator at line 109.

Verbatim sub-section content:

```markdown
### Multi-CLI Bridges (v4.7+)

`init-claude.sh` and `init-local.sh` post-install (and `install.sh` via the unified
TUI) detect installed Gemini CLI and OpenAI Codex CLI binaries. For each detected CLI,
the installer offers to create a bridge file: `GEMINI.md` for Gemini, `AGENTS.md` for
OpenAI Codex (the OpenAI agent-context standard). Bridges are plain copies of
`CLAUDE.md` with an auto-generated header banner; `update-claude.sh` keeps them in
sync via SHA256 drift detection.

Pass `--no-bridges` (or set `TK_NO_BRIDGES=1`) to skip every bridge prompt; pass
`--bridges gemini,codex` (or any comma-separated subset) to force-create
non-interactively for CI / scripted installs. Use `update-claude.sh --break-bridge
<target>` to mark a bridge `user_owned: true` (skipped on subsequent updates) and
`--restore-bridge <target>` to undo.

See [docs/BRIDGES.md](BRIDGES.md) for the full multi-CLI bridge specification including
drift handling, the `[y/N/d]` prompt contract for user-edited bridges, and the
symlink-vs-copy rationale.
```

---

README.md edit — Killer Features grid currently lines 135-143.

APPEND 1 row at the END of the grid table (after the `Structured Workflow` row at line 143):

```markdown
| **Multi-CLI Bridges** | Auto-sync `CLAUDE.md` to Gemini CLI's `GEMINI.md` and OpenAI Codex's `AGENTS.md`. Drift-detected, opt-out via `--no-bridges`. See [docs/BRIDGES.md](docs/BRIDGES.md) |
```

Single row per CONTEXT.md "Claude's Discretion" line 133 ("recommend single row for compactness").
</interfaces>

<markdownlint_rules>
Every file MUST pass `markdownlint` per `.markdownlint.json`:

- MD040: every fenced code block declares a language. Use `text` for plain prose
  snippets, `bash` for shell, `markdown` for markdown samples.
- MD031/MD032: blank line BEFORE and AFTER every fenced code block AND every list.
- MD026: NO trailing punctuation in headings (`?`, `:`, `.`, `!` forbidden).
  Section 8 is `## Why No Symlink` — NOT `## Why No Symlink?`.
- MD024 (siblings_only): duplicate headings allowed across different parents — irrelevant for new BRIDGES.md if section names are unique within the file.
- MD029 (style: ordered): if using ordered lists, items use sequential numbers `1.`, `2.`, `3.` — not `1.`, `1.`, `1.`.

Em-dashes inside headings are permitted (`### Phase 24 Sub-Phase 1 — Globalize`
exists in CHANGELOG and lints clean). Parens in headings are permitted
(`### Multi-CLI Bridges (v4.7+)` is fine).
</markdownlint_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/BRIDGES.md with locked 9-section structure</name>
  <files>docs/BRIDGES.md</files>
  <read_first>
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block lines 76-87 — locked section structure)
    - .planning/REQUIREMENTS.md (lines 13-78 REQ-ID feature spec; banner verbatim from BRIDGE-GEN-03 lines 23-29; symlink-rejection rationale at line 102; AGENTS.md note at line 73)
    - .markdownlint.json (rules to obey)
    - docs/INSTALL.md (existing format reference for table styling and cross-doc tone)
  </read_first>
  <action>
Create `docs/BRIDGES.md` with the 9-section structure from `<interfaces>`.

Specific content requirements:

(1) Title `# Multi-CLI Bridges` (no version stamp).

(2) `## Overview` (5-8 lines): explain what bridges are, why they exist (so users
running multiple agentic CLIs do not maintain duplicate context files manually
— phrasing inspired by REQUIREMENTS.md line 5). State the single-source-of-truth
invariant: CLAUDE.md is canonical; bridges are regenerable.

(3) `## Supported CLIs` MUST include a markdown table with EXACTLY 2 data rows
(Gemini -> GEMINI.md, OpenAI Codex -> AGENTS.md) AND the explicit note about
AGENTS.md being the OpenAI standard (not CODEX.md). Include the words
"OpenAI", "AGENTS.md", "NOT", and "CODEX.md" so the verification grep finds
them. The string "AGENTS.md" must appear at least 2 times in the entire
file (table + note row).

(4) `## How it Works` MUST include the 4-line banner heredoc verbatim from
REQUIREMENTS.md BRIDGE-GEN-03 (lines 23-29). Wrap in a fenced ```text block
(MD040 — `text` language tag mandatory for HTML-comment prose).

(5) `## Drift Handling`: name and explain the 3 log states verbatim
(`[~ UPDATE]`, `[y/N/d]`, `[? ORPHANED]`). Mention default `N` matches the
v4.3 UN-03 contract.

(6) `## Opt-Out Mechanics`: three escape hatches in `bash`-fenced code examples
showing the flags + envs (`--no-bridges`, `TK_NO_BRIDGES=1`,
`update-claude.sh --break-bridge gemini`, `update-claude.sh --restore-bridge gemini`).

(7) `## Force-Create` (or `## Force-Create (Non-Interactive)`): `bash`-fenced
examples for `init-claude.sh --bridges gemini,codex` and `install.sh --bridges
gemini --fail-fast`.

(8) `## Why No Symlink` (NO question mark — MD026): three rationale bullets
ending with a cross-link to REQUIREMENTS.md "Out of Scope" or PROJECT.md
"Key context".

(9) `## Uninstall`: 2-3 sentences naming `classify_bridge_file`, REMOVE_LIST,
MODIFIED_LIST + `[y/N/d]` prompt, and `--keep-state` semantics.

(10) `## Future Scope`: 1 paragraph naming BRIDGE-FUT-01 (branding substitution,
deferred to v4.8 if friction) and BRIDGE-FUT-03/04 (Cursor `.cursorrules` and
Aider `CONVENTIONS.md` use different conventions; out of v4.7 scope).

Total file length: ~80-150 lines. Prose-driven, not code-heavy. Every fenced
block has a language tag. Every heading is verb-or-noun phrase, no trailing
punctuation. Em-dashes and parens in headings OK.

After writing, run:

```bash
npx --yes markdownlint-cli docs/BRIDGES.md
```

Fix any reported violations.
  </action>
  <verify>
    <automated>test -f docs/BRIDGES.md &amp;&amp; lc=$(wc -l &lt; docs/BRIDGES.md) &amp;&amp; [ "$lc" -ge 80 ] &amp;&amp; [ "$(grep -c '^# Multi-CLI Bridges' docs/BRIDGES.md)" = "1" ] &amp;&amp; [ "$(grep -c 'AGENTS.md' docs/BRIDGES.md)" -ge 2 ] &amp;&amp; grep -q 'OpenAI' docs/BRIDGES.md &amp;&amp; grep -q 'NOT' docs/BRIDGES.md &amp;&amp; grep -q 'CODEX.md' docs/BRIDGES.md &amp;&amp; for s in 'Overview' 'Supported CLIs' 'How it Works' 'Drift Handling' 'Opt-Out' 'Force-Create' 'Why No Symlink' 'Uninstall' 'Future Scope'; do grep -qE "^## .*$s" docs/BRIDGES.md || { echo "missing: $s"; exit 1; }; done &amp;&amp; ! grep -E '^##.*[?:!.]$' docs/BRIDGES.md &amp;&amp; npx --yes markdownlint-cli docs/BRIDGES.md</automated>
  </verify>
  <done>
    - `docs/BRIDGES.md` exists, ≥80 lines
    - Title `# Multi-CLI Bridges` present (1 occurrence)
    - All 9 required `## ` headings present: Overview, Supported CLIs, How it Works, Drift Handling, Opt-Out (Mechanics), Force-Create, Why No Symlink, Uninstall, Future Scope
    - `AGENTS.md` mentioned ≥2 times
    - The OpenAI/NOT/CODEX.md callout present
    - No `## ` heading ends with `?`, `:`, `!`, or `.` (MD026 enforced)
    - The 4-line auto-generated banner heredoc present verbatim in a fenced ```text block
    - `npx --yes markdownlint-cli docs/BRIDGES.md` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend docs/INSTALL.md flag table + add Multi-CLI Bridges sub-section</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md (lines 58-109 — Installer Flags table region)
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block lines 89-96)
  </read_first>
  <action>
Edit `docs/INSTALL.md` with two additions:

(1) Append 4 new rows to the existing Installer Flags table (currently lines
63-72). Insert the rows BETWEEN the `--keep-state` row (line 71) and the
`--no-council` row (line 72). The 4 verbatim rows are listed in the
`<interfaces>` block above (no-bridges, bridges, break-bridge, restore-bridge).

(2) Append a new `### Multi-CLI Bridges (v4.7+)` sub-section AFTER the existing
`### --keep-state for uninstall.sh (v4.4+)` block (which ends at ~line 107) and
BEFORE the `---` separator at line 109. Use the verbatim sub-section content
from `<interfaces>` (3 paragraphs ending with a `[docs/BRIDGES.md](BRIDGES.md)`
link).

Heading is exactly `### Multi-CLI Bridges (v4.7+)` (no trailing punctuation).
The link to docs/BRIDGES.md uses the relative form `(BRIDGES.md)` because both
files live under `docs/`.

Markdownlint rules:
- MD031/MD032: blank line before AND after the new heading and any list.
- MD040: any fenced block declares a language (the verbatim sub-section content
  has none — pure prose with inline code spans, which is valid).

Do NOT modify ANY other section: the `## Modes Overview`, `## Install via marketplace`, the existing v4.4 sub-sections (`### --no-bootstrap`, `### --no-banner`, `### --keep-state for uninstall.sh`), the `## install.sh (unified entry, v4.5+)` block, or any other content.

After editing, run:

```bash
npx --yes markdownlint-cli docs/INSTALL.md
```
  </action>
  <verify>
    <automated>[ "$(grep -c 'no-bridges' docs/INSTALL.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'break-bridge' docs/INSTALL.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'restore-bridge' docs/INSTALL.md)" -ge 1 ] &amp;&amp; [ "$(grep -c 'bridges &lt;list&gt;' docs/INSTALL.md)" -ge 1 ] &amp;&amp; [ "$(grep -c '### Multi-CLI Bridges' docs/INSTALL.md)" = "1" ] &amp;&amp; grep -q 'BRIDGES.md' docs/INSTALL.md &amp;&amp; grep -q 'TK_NO_BRIDGES' docs/INSTALL.md &amp;&amp; npx --yes markdownlint-cli docs/INSTALL.md</automated>
  </verify>
  <done>
    - 4 new flag rows present in the Installer Flags table (`--no-bridges`, `--bridges <list>`, `--break-bridge <target>`, `--restore-bridge <target>`)
    - `### Multi-CLI Bridges (v4.7+)` heading present exactly once
    - Sub-section links to `BRIDGES.md` (relative path)
    - `TK_NO_BRIDGES` environment variable referenced
    - `npx --yes markdownlint-cli docs/INSTALL.md` exits 0
    - Existing v4.4 sub-sections (`--no-bootstrap`, `--no-banner`, `--keep-state for uninstall.sh`) preserved verbatim
    - `## install.sh (unified entry, v4.5+)` block at line 111 unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Multi-CLI Bridges row to README.md Killer Features grid + run make check</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 133-145 — Killer Features grid section)
    - .planning/phases/31-distribution-tests-docs/31-CONTEXT.md (decisions block lines 99-102)
  </read_first>
  <action>
Edit `README.md` with one addition:

Append 1 new row at the END of the Killer Features table (currently the last
row at line 143 is `**Structured Workflow**`). The new row goes immediately
after that row, before the next non-table line (line 144 is the closing context
of the table — verify by grepping the actual line range).

Verbatim row to append:

```markdown
| **Multi-CLI Bridges** | Auto-sync `CLAUDE.md` to Gemini CLI's `GEMINI.md` and OpenAI Codex's `AGENTS.md`. Drift-detected, opt-out via `--no-bridges`. See [docs/BRIDGES.md](docs/BRIDGES.md) |
```

This single grouped row covers both project-scope and global-scope bridges
(per CONTEXT.md "Claude's Discretion" line 133: "recommend single row for compactness").

Do NOT modify any other Killer Features row, any other README section, or
any other file in this task.

After editing this file AND tasks 1+2 are complete (BRIDGES.md and INSTALL.md
already written), run the full project check to verify all 3 markdown files
are clean and the project's full lint pipeline passes:

```bash
make check
```

`make check` runs `make shellcheck` + `make mdlint` + `make validate`. The
mdlint step covers ALL markdown files in the repo, so if any of the 3 files
this plan modified has a markdownlint violation, the check fails. Fix any
violations before finishing the task.

Note: `make check` also runs `validate-skills-desktop` and `validate-marketplace`
(when `TK_HAS_CLAUDE_CLI=1`); those steps are unrelated to this plan's edits
but must continue to pass — confirm they still pass at v4.6 baseline.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Multi-CLI Bridges' README.md)" -ge 1 ] &amp;&amp; grep -q 'docs/BRIDGES.md' README.md &amp;&amp; grep -q 'GEMINI.md' README.md &amp;&amp; grep -q 'AGENTS.md' README.md &amp;&amp; grep -q 'no-bridges' README.md &amp;&amp; npx --yes markdownlint-cli README.md &amp;&amp; make check</automated>
  </verify>
  <done>
    - `grep -c '\*\*Multi-CLI Bridges\*\*' README.md` returns 1
    - Row references `docs/BRIDGES.md`, `GEMINI.md`, `AGENTS.md`, `--no-bridges`
    - `npx --yes markdownlint-cli README.md` exits 0
    - `make check` exits 0 (full project lint pipeline green: shellcheck + mdlint + validate)
    - Existing Killer Features rows (Self-Learning, Auto-Activation Hooks, Knowledge Persistence, Systematic Debugging, Production Safety, Supreme Council, Structured Workflow) preserved
  </done>
</task>

</tasks>

<verification>
Phase-wide acceptance commands (run after all 3 tasks complete):

```bash
# (1) docs/BRIDGES.md exists with required structure
test -f docs/BRIDGES.md
wc -l docs/BRIDGES.md                                              # ≥80
grep -c 'AGENTS.md' docs/BRIDGES.md                                # ≥2
grep -c '^## ' docs/BRIDGES.md                                     # ≥9 sections
! grep -E '^##.*[?:!.]$' docs/BRIDGES.md                           # MD026 (no trailing punct)

# (2) docs/INSTALL.md table + sub-section
grep -c 'no-bridges\|bridges <list>\|break-bridge\|restore-bridge' docs/INSTALL.md  # ≥4
grep -c '### Multi-CLI Bridges (v4.7+)' docs/INSTALL.md            # 1

# (3) README.md Killer Features row
grep -c '\*\*Multi-CLI Bridges\*\*' README.md                      # 1
grep 'Multi-CLI Bridges' README.md | grep -q 'docs/BRIDGES.md'

# (4) Full project lint pipeline
make check                                                          # exits 0
```
</verification>

<success_criteria>
- `docs/BRIDGES.md` exists with all 9 required sections (Overview, Supported CLIs, How it Works, Drift Handling, Opt-Out Mechanics, Force-Create, Why No Symlink, Uninstall, Future Scope) plus title
- AGENTS.md (NOT CODEX.md) emphasized as the OpenAI standard ≥2 times in BRIDGES.md
- 4-line auto-generated banner heredoc reproduced verbatim in BRIDGES.md
- `docs/INSTALL.md` Installer Flags table contains 4 new rows (`--no-bridges`, `--bridges <list>`, `--break-bridge <target>`, `--restore-bridge <target>`)
- `### Multi-CLI Bridges (v4.7+)` sub-section in INSTALL.md links to docs/BRIDGES.md
- `README.md` Killer Features grid has new row mentioning multi-CLI bridges with link to docs/BRIDGES.md
- `make check` passes green (markdownlint + shellcheck + validate)
- BRIDGE-DOCS-01 + BRIDGE-DOCS-02 fully covered
</success_criteria>

<output>
After completion, create `.planning/phases/31-distribution-tests-docs/31-03-SUMMARY.md`
documenting: BRIDGES.md final line count + section list, the byte-range of the 4
new INSTALL.md rows + the Multi-CLI Bridges (v4.7+) sub-section, the 1-row
Killer Features addition to README, and the `make check` exit-code confirmation.
Confirm BRIDGE-DOCS-01 and BRIDGE-DOCS-02 covered.
</output>
