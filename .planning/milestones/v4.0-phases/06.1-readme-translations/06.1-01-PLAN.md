---
phase: 06.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/readme/es.md
  - docs/readme/pt.md
  - docs/readme/fr.md
  - docs/readme/de.md
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
must_haves:
  truths:
    - "TRANS-01..04 exist in .planning/REQUIREMENTS.md with exact wording from 06.1-RESEARCH.md"
    - "docs/readme/es.md is a full v4.0 rewrite mirroring README.md (12 sections, complement-first positioning)"
    - "docs/readme/pt.md is a full v4.0 rewrite mirroring README.md (12 sections, complement-first positioning)"
    - "docs/readme/fr.md is a full v4.0 rewrite mirroring README.md with correct French typographic spacing"
    - "docs/readme/de.md is a full v4.0 rewrite mirroring README.md in formal Sie register, within 242-line ceiling"
    - "All 4 translations land in the 161-242 line band"
    - "make translation-drift exits 0 after Plan 01 completes"
    - "make mdlint exits 0 after Plan 01 completes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRANS-01..04 requirement IDs claimable by phase plans"
      contains: "TRANS-01"
    - path: "docs/readme/es.md"
      provides: "Spanish README mirror of v4.0 complement-first English"
      min_lines: 161
    - path: "docs/readme/pt.md"
      provides: "Brazilian Portuguese README mirror of v4.0 complement-first English"
      min_lines: 161
    - path: "docs/readme/fr.md"
      provides: "French README mirror of v4.0 complement-first English"
      min_lines: 161
    - path: "docs/readme/de.md"
      provides: "German README mirror of v4.0 complement-first English (formal Sie register)"
      min_lines: 161
  key_links:
    - from: "docs/readme/es.md"
      to: "docs/howto/es.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/es\\.md"
    - from: "docs/readme/pt.md"
      to: "docs/howto/pt.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/pt\\.md"
    - from: "docs/readme/fr.md"
      to: "docs/howto/fr.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/fr\\.md"
    - from: "docs/readme/de.md"
      to: "docs/howto/de.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/de\\.md"
    - from: "all 4 translations"
      to: "../../README.md"
      via: "language switcher row 8"
      pattern: "\\.\\./\\.\\./README\\.md"
---

# Plan 06.1-01: Sync Romance + Germanic README translations (es / pt / fr / de)

## Objective

Append TRANS-01..04 to `.planning/REQUIREMENTS.md` (prerequisite shared by all three plans
in Phase 06.1), then rewrite four README translations — Spanish, Brazilian Portuguese,
French, and German — from the current English `README.md` (202 lines, v4.0 complement-first).
Each translation mirrors README.md's 12 canonical sections section-by-section with identical
code blocks, identical proper nouns, and a localized language switcher. Delivers the entire
Romance + Germanic family in Wave 1. German carries the highest length risk (+15–25%
expansion) and includes an explicit rebalance instruction if output approaches the 242-line
ceiling.

**Purpose:** Unblock the Phase 7 release gate (`make check` dependency on
`make translation-drift`) for 4 of 8 languages.

**Output:** 4 rewritten translation files + 1 updated REQUIREMENTS.md, all passing
`make translation-drift` and `make mdlint`, committed per-language (5 commits total).

## Tasks

<task id="06.1-01-01" type="auto">
  <name>Task 1: Append TRANS-01..04 to .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (see current Docs section structure before appending; new items go immediately after DOCS-08 and before the Validation section, in a new subsection titled "### Translations")
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md (copy the exact TRANS-01..04 wording verbatim from § "TRANS-01..04 Proposed Wording", lines 829–864; strip the leading `>` blockquote markers since REQUIREMENTS.md uses plain checkbox items)
    - .planning/ROADMAP.md (confirm phase 06.1 requirements line reads `TRANS-01, TRANS-02, TRANS-03, TRANS-04`)
  </read_first>

  <action>
    Append TRANS-01..04 to REQUIREMENTS.md using the exact wording proposed in
    06.1-RESEARCH.md § "TRANS-01..04 Proposed Wording". Do NOT paraphrase — copy the
    proposed sentences verbatim (strip the leading `> ` markdown blockquote markers used
    in the research doc to render them as plain description text in REQUIREMENTS.md).

    Location: Insert a new `### Translations` subsection in REQUIREMENTS.md after the
    existing `### Documentation` block (after DOCS-08, before the `### Validation` block).
    Format each requirement as a standard checkbox item matching the existing DOCS-* style:

    ```markdown
    ### Translations

    README translation sync for v4.0 complement-first positioning.

    - [ ] **TRANS-01**: All 8 README translations (`docs/readme/{de,es,fr,ja,ko,pt,ru,zh}.md`) are within ±20% of `README.md`'s line count: each file has between 161 and 242 lines (inclusive), where the bounds derive from `floor(202 × 0.8) = 161` and `floor(202 × 1.2) = 242`.
    - [ ] **TRANS-02**: `make translation-drift` exits 0 when run from the repository root after all 8 translation files have been written. This is the mechanical gate that unblocks Phase 7 Plan 07-04 (`make check` dependency chain).
    - [ ] **TRANS-03**: Each of the 8 translations contains an equivalent of the English `## Install Modes` section with three subheadings equivalent to "Standalone install", "Complement install", and "Upgrading from v3.x" in its target language. The section conveys the complement-first positioning ("TK is a complement to `superpowers` + `get-shit-done`") at least once in localized prose. All install commands within the section are verbatim-identical to those in `README.md`.
    - [ ] **TRANS-04**: `make mdlint` exits 0 when run from the repository root after all 8 translation files have been written, with zero regressions versus the pre-phase baseline.
    ```

    Also update the Traceability table at the bottom of REQUIREMENTS.md to add 4 new rows
    mapping each TRANS-* to `Phase 06.1 | Pending`, inserted immediately before the
    `VALIDATE-01` row (keep rows in declaration order).

    Update the Coverage footer numbers accordingly (`v1 requirements: 55 total` → `59 total`;
    `Mapped to phases: 55` → `59`).

    Do NOT touch any other content in REQUIREMENTS.md (locked by decision fidelity per D-01..D-15).

    Commit with message: `docs(06.1): add TRANS-01..04 requirements to REQUIREMENTS.md`

    Use a HEREDOC for the commit body so formatting is preserved. Add a `Co-Authored-By` line
    per repo convention.
  </action>

  <verify>
    <automated>grep -c '^- \[ \] \*\*TRANS-0[1-4]\*\*:' .planning/REQUIREMENTS.md</automated>
    Expected output: exactly `4`. If not 4, the TRANS items were either not appended,
    duplicated, or formatted incorrectly.

    Also run: `grep -c '| TRANS-0[1-4] | Phase 06.1 | Pending |' .planning/REQUIREMENTS.md`
    Expected output: exactly `4`.

    Also run: `grep -c 'v1 requirements: 59 total' .planning/REQUIREMENTS.md`
    Expected output: exactly `1`.
  </verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/REQUIREMENTS.md`
    - All 4 items present: `grep -c '^- \[ \] \*\*TRANS-0[1-4]\*\*:' .planning/REQUIREMENTS.md` equals `4`
    - Traceability rows present: `grep -c '| TRANS-0[1-4] | Phase 06.1 | Pending |' .planning/REQUIREMENTS.md` equals `4`
    - Coverage footer updated: `grep -F 'v1 requirements: 59 total' .planning/REQUIREMENTS.md` returns a match
    - Mapped-to-phases updated: `grep -F 'Mapped to phases: 59' .planning/REQUIREMENTS.md` returns a match
    - No existing DOCS/BUG/MODE/etc. items were modified: `git diff --stat .planning/REQUIREMENTS.md` shows ONLY additions (no deletions except possibly trailing whitespace)
    - `make mdlint` still passes after the edit
    - Commit landed: `git log -1 --oneline -- .planning/REQUIREMENTS.md` returns a commit whose message matches `docs\(06\.1\): add TRANS-01\.\.04 requirements`
  </acceptance_criteria>
</task>

<task id="06.1-01-02" type="auto">
  <name>Task 2: Rewrite docs/readme/es.md (Spanish, formal usted register)</name>
  <files>docs/readme/es.md</files>

  <read_first>
    - README.md (202 lines — SOURCE OF TRUTH; translation mirrors this file section-by-section)
    - docs/readme/es.md (current v3.x state — 148 lines; DO NOT reuse body content, but observe the language-switcher-row shape and howto-link pattern from existing line 8 and line 10)
    - docs/readme/ru.md (existing translation that established the language switcher / howto link patterns; reference line 8 and line 10 shape)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions (D-01..D-15 locked — full rewrite, code English, prose localized, formal register, howto → ../howto/es.md, English link → ../../README.md)
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Spanish (es) conventions (lines 246–278), § Length Expansion Profile (es: +10–15%, projected 222–232 lines, safe midband), § Localized Heading Glossary (all 12 Spanish headings), § Language-Switcher Pattern, § Markdownlint Pitfalls
    - .markdownlint.json (MD040, MD031, MD032, MD026 active; MD013/MD033/MD041/MD060 disabled; MD024 siblings_only)
    - docs/howto/es.md (confirm it exists as link target — already verified in RESEARCH.md)
  </read_first>

  <action>
    Overwrite `docs/readme/es.md` with a complete Spanish translation of `README.md` (202
    lines). This is a full rewrite per D-01 — do NOT patch-translate the existing v3.x
    content.

    **Source structure to mirror (exactly 12 blocks, in order):**

    1. H1 title + one-line tagline (README.md lines 1–3)
    2. Badges (lines 4–6) — keep alt text English (badge alt text is D-04 verbatim)
    3. Language switcher row (line 8) — see exact recipe below
    4. Howto callout blockquote (line 10)
    5. `## Who Is This For` → `## Para quién es esto`
    6. `## Quick Start` → `## Inicio rápido` with `### 1. Global Setup (once)` → `### 1. Configuración global (una vez)` and subsections `#### a) Security Pack` (keep "Security Pack" English per glossary), `#### b) RTK — Optimizador de tokens (recomendado)`, `#### c) Rate Limit Statusline (Claude Max / Pro, opcional)`
    7. `## Install Modes` → `## Modos de instalación` with `### Instalación independiente`, `### Instalación complementaria`, `### Actualización desde v3.x`
    8. `## Killer Features` → `## Killer Features` (keep English per glossary) with table columns `Función | Descripción`
    9. `## MCP Servers (recommended!)` → `## Servidores MCP (¡recomendado!)` with `### Global (todos los proyectos)` and `### Por proyecto (credenciales)`
    10. `## Structure After Installation` → `## Estructura después de la instalación`
    11. `## Supported Frameworks` → `## Frameworks compatibles` with table columns `Framework | Plantilla | Skills | Detección automática`
    12. `## Components` → `## Componentes`

    **Exact line 8 (language switcher) — Spanish unlinked, 8 siblings linked, English → ../../README.md:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **Español** | **[Deutsch](de.md)** | **[Français](fr.md)** | **[中文](zh.md)** | **[日本語](ja.md)** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10 (howto callout):**

    ```markdown
    > Primero lea la [guía de instalación paso a paso](../howto/es.md) completa.
    ```

    **Code blocks (carry VERBATIM — byte-identical to README.md lines 32–34, 40–43, 53–55, 71–73, 85–87, 129–134, 142–144):**

    Security Pack:

    ```bash
    bash <(curl -sSL https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main/scripts/setup-security.sh)
    ```

    RTK:

    ```bash
    brew install rtk
    rtk init -g
    ```

    Statusline:

    ```bash
    bash <(curl -sSL https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main/scripts/install-statusline.sh)
    ```

    Standalone install:

    ```bash
    bash <(curl -sSL https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main/scripts/init-claude.sh)
    ```

    Complement install override:

    ```bash
    bash <(curl -sSL https://raw.githubusercontent.com/sergei-aronsen/claude-code-toolkit/main/scripts/init-claude.sh) --mode complement-full
    ```

    Global MCP servers (4 lines, all verbatim):

    ```bash
    claude mcp add -s user context7 -- npx -y @upstash/context7-mcp
    claude mcp add -s user playwright -- npx @playwright/mcp@latest --browser chromium
    claude mcp add -s user sequential-thinking -- npx -y @modelcontextprotocol/server-sequential-thinking
    claude mcp add --transport http sentry https://mcp.sentry.dev/mcp
    ```

    Per-project dbhub:

    ```bash
    claude mcp add dbhub -- npx -y @bytebase/dbhub --dsn "postgresql://user:pass@localhost:5432/dbname"
    ```

    Structure tree block (```text fence from README.md lines 154–178) — keep ASCII art and
    comments; translate the inline comments from English to Spanish:

    - `# Main instructions (adapt for your project)` → `# Instrucciones principales (adapte para su proyecto)`
    - `# Hooks, permissions` → `# Hooks, permisos`
    - `# Slash commands` → `# Comandos slash`
    - `# † omitted in complement-sp/full` → `# † omitido en complement-sp/full`
    - `# Audits` → `# Auditorías`
    - `# Subagents` → `# Subagentes`
    - `# Framework expertise` → `# Experiencia de frameworks`
    - `# Auto-loaded project facts` → `# Hechos del proyecto cargados automáticamente`
    - `# Working notes` → `# Notas de trabajo`

    **Proper nouns preserved EXACTLY (per D-06 locked):** `Claude Code`, `superpowers`,
    `get-shit-done`, `RTK`, `MCP`, `Supreme Council`, `Gemini`, `ChatGPT`, `Laravel`,
    `Rails`, `Next.js`, `Node.js`, `Python`, `Go`, `Ruby on Rails`, `context7`, `playwright`,
    `sequential-thinking`, `sentry`, `dbhub`. Also preserve: `Security Pack` (Spanish
    glossary retains English), `Rate Limit Statusline` (English), `Skills` as a column
    header (English), `Token` in "Optimizador de tokens" is fine in lowercase.

    **Register:** formal `usted` throughout prose. Verbs conjugate third-person singular
    (`instale`, `ejecute`, `configure`).

    **Admonition labels (translate):** `> **Note:**` → `> **Nota:**`, `> **Important:**` →
    `> **Importante:**`, `> **Security:**` → `> **Seguridad:**`.

    **Punctuation:** use ` — ` (spaced em-dash), ASCII `"..."` quotes, Spanish accents
    (á é í ó ú ñ ü) as natural.

    **What NOT to translate (per D-04):** code blocks, CLI commands, CLI flags, file paths,
    package names (`@upstash/context7-mcp`, `@playwright/mcp@latest`, etc.), env var names,
    URL anchors, badge alt text, GitHub shield URLs.

    **Line count target:** aim for 202 lines (mirror source); expected expansion +10–15%
    lands at 222–232 — well inside the 161–242 band. If the file ends up outside the band,
    rebalance adjacent short paragraphs per D-10 — NEVER pad with filler or delete content.

    **Trailing newline:** file MUST end with a single `\n` (verified in RESEARCH.md).

    Commit message: `docs(06.1): sync es README to v4.0 complement-first`

    Use a HEREDOC for the commit message, add `Co-Authored-By` per repo convention.
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/es.md && [ "$(wc -l < docs/readme/es.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/es.md | tr -d ' ')" -le 242 ]</automated>
    Must exit 0. Line count within 161–242 inclusive.
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/es.md`
    - Line count in band: `LINES=$(wc -l < docs/readme/es.md | tr -d ' '); [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ]`
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/es.md` exits 0
    - H2 parity with README.md: `[ "$(grep -c '^## ' docs/readme/es.md)" = "$(grep -c '^## ' README.md)" ]`
    - H3 parity with README.md: `[ "$(grep -c '^### ' docs/readme/es.md)" = "$(grep -c '^### ' README.md)" ]`
    - Language switcher has English link pointing up two levels: `grep -F '[English](../../README.md)' docs/readme/es.md` returns ≥ 1 line
    - Current language unlinked: `grep -F '**Español**' docs/readme/es.md` returns ≥ 1 line AND `grep -F '[Español]' docs/readme/es.md` returns 0 lines
    - Howto link present and points to Spanish howto: `grep -F '../howto/es.md' docs/readme/es.md` returns ≥ 1 line
    - Howto target exists: `test -f docs/howto/es.md`
    - Complement positioning mentioned: `grep -F 'superpowers' docs/readme/es.md` returns ≥ 2 lines
    - GSD mentioned: `grep -F 'get-shit-done' docs/readme/es.md` returns ≥ 1 line
    - Complement word (Spanish or English) present: `grep -E -i 'complement' docs/readme/es.md` returns ≥ 1 line
    - At least 3 `bash <(curl` commands present (security, statusline, install): `grep -c 'bash <(curl' docs/readme/es.md` returns integer ≥ 3
    - Install Modes section present: `grep -F '## Modos de instalación' docs/readme/es.md` returns exactly 1 line
    - Install Modes subheadings present: `grep -E '^### Instalación (independiente|complementaria)' docs/readme/es.md` returns ≥ 2 lines AND `grep -F '### Actualización desde v3.x' docs/readme/es.md` returns 1 line
    - File ends with newline: `tail -c 1 docs/readme/es.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/es.md` matches `docs\(06\.1\): sync es README`
  </acceptance_criteria>
</task>

<task id="06.1-01-03" type="auto">
  <name>Task 3: Rewrite docs/readme/pt.md (Brazilian Portuguese, formal você register)</name>
  <files>docs/readme/pt.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/pt.md (current v3.x state — do NOT reuse body content)
    - docs/readme/ru.md (structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Portuguese (pt) conventions (lines 281–308), § Length Expansion Profile (pt: +10–15%, 222–232 lines), § Localized Heading Glossary, § Language-Switcher Pattern, § Markdownlint Pitfalls
    - .markdownlint.json
    - docs/howto/pt.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/pt.md` with a complete Brazilian Portuguese translation of
    `README.md`. Full rewrite per D-01.

    **Structure:** mirror README.md's 12 sections exactly. Use the localized headings from
    06.1-RESEARCH.md § Localized Heading Glossary pt column:

    - `## Para quem é isto`
    - `## Início rápido` with `### 1. Configuração global (uma vez)`, `#### a) Security Pack`, `#### b) RTK — Otimizador de tokens (recomendado)`, `#### c) Rate Limit Statusline (Claude Max / Pro, opcional)`
    - `## Modos de instalação` with `### Instalação independente`, `### Instalação complementar`, `### Atualização do v3.x`
    - `## Killer Features` (English) with columns `Função | Descrição`
    - `## Servidores MCP (recomendado!)` with `### Global (todos os projetos)` and `### Por projeto (credenciais)`
    - `## Estrutura após a instalação`
    - `## Frameworks compatíveis` with columns `Framework | Modelo | Skills | Detecção automática`
    - `## Componentes`

    **Exact line 8 — Portuguese unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **[Deutsch](de.md)** | **[Français](fr.md)** | **[中文](zh.md)** | **[日本語](ja.md)** | **Português** | **[한국어](ko.md)**
    ```

    **Exact line 10:**

    ```markdown
    > Primeiro leia o [guia de instalação passo a passo](../howto/pt.md) completo.
    ```

    **Code blocks:** carry VERBATIM from README.md. Same list as Task 2 (Security Pack
    curl, RTK brew/init, statusline curl, init-claude standalone, init-claude --mode
    complement-full, 4-line global MCP block, dbhub one-liner).

    **Structure tree comments translated to pt:**
    - `# Instruções principais (adapte para seu projeto)`
    - `# Hooks, permissões`
    - `# Comandos slash`
    - `# † omitido em complement-sp/full`
    - `# Auditorias`
    - `# Subagentes`
    - `# Experiência de frameworks`
    - `# Fatos do projeto carregados automaticamente`
    - `# Notas de trabalho`

    **Proper nouns preserved EXACTLY (D-06 list, same as Task 2).** Also keep `Security Pack`
    and `Rate Limit Statusline` in English per glossary.

    **Register:** formal `você`, verbs third-person singular (`instale`, `execute`).

    **Admonition labels:** `> **Nota:**`, `> **Importante:**`, `> **Segurança:**`.

    **Punctuation:** ` — ` (spaced em-dash) or colon/parenthesis alternatives per pt tech-doc
    convention; ASCII `"..."` quotes; preserve ç, ã, õ, á, é, ê, í, ó, ô, ú accents.

    **What NOT to translate (D-04):** code, CLI flags, file paths, package names, URLs,
    badge alt text.

    **Line count target:** aim for 202 lines; projected 222–232 (safe midband).

    **Trailing newline required.**

    Commit message: `docs(06.1): sync pt README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/pt.md && [ "$(wc -l < docs/readme/pt.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/pt.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/pt.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/pt.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/pt.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/pt.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/pt.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/pt.md` returns ≥ 1
    - Current language unlinked: `grep -F '**Português**' docs/readme/pt.md` returns ≥ 1 AND `grep -F '[Português]' docs/readme/pt.md` returns 0
    - Howto link: `grep -F '../howto/pt.md' docs/readme/pt.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/pt.md`
    - `grep -F 'superpowers' docs/readme/pt.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/pt.md` ≥ 1
    - `grep -E -i 'complement' docs/readme/pt.md` ≥ 1
    - `grep -c 'bash <(curl' docs/readme/pt.md` ≥ 3
    - Install Modes section: `grep -F '## Modos de instalação' docs/readme/pt.md` returns 1
    - Install Modes subheadings: `grep -c '^### Instalação' docs/readme/pt.md` ≥ 2 AND `grep -F '### Atualização do v3.x' docs/readme/pt.md` returns 1
    - File ends with newline: `tail -c 1 docs/readme/pt.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/pt.md` matches `docs\(06\.1\): sync pt README`
  </acceptance_criteria>
</task>

<task id="06.1-01-04" type="auto">
  <name>Task 4: Rewrite docs/readme/fr.md (French, formal vous register, typographic spacing)</name>
  <files>docs/readme/fr.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/fr.md (current v3.x state)
    - docs/readme/ru.md (structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § French (fr) conventions (lines 311–351) — CRITICAL for typographic spacing rules, § Length Expansion Profile (fr: +10–15%, 222–232 lines), § Localized Heading Glossary, § Markdownlint Pitfalls § French space-before-colon note
    - .markdownlint.json
    - docs/howto/fr.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/fr.md` with a complete French translation of `README.md`. Full
    rewrite per D-01.

    **CRITICAL typographic rule:** French prose uses a space before `:`, `!`, `?` (French
    typography). USE THIS in prose (e.g., "Configuration :", "Important :"). DO NOT use
    it in headings (MD026 blocks trailing punctuation in headings anyway — keep headings
    as plain noun phrases without trailing `:`). DO NOT use it inside code blocks or code
    spans (D-04 — code stays English/verbatim).

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH fr column:

    - `## Pour qui est-ce`
    - `## Démarrage rapide` with `### 1. Configuration globale (une fois)`, `#### a) Security Pack` (keep English per glossary), `#### b) RTK — Optimiseur de tokens (recommandé)`, `#### c) Rate Limit Statusline (Claude Max / Pro, optionnel)`
    - `## Modes d'installation` with `### Installation autonome`, `### Installation complémentaire`, `### Mise à jour depuis v3.x`
    - `## Fonctionnalités clés` with columns `Fonctionnalité | Description`
    - `## Serveurs MCP (recommandé !)` with `### Global (tous les projets)` and `### Par projet (identifiants)`
    - `## Structure après l'installation`
    - `## Frameworks pris en charge` with columns `Framework | Modèle | Skills | Détection automatique`
    - `## Composants`

    **Exact line 8 — French unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **[Deutsch](de.md)** | **Français** | **[中文](zh.md)** | **[日本語](ja.md)** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10:**

    ```markdown
    > Lisez d'abord le [guide d'installation pas à pas](../howto/fr.md) complet.
    ```

    **Code blocks VERBATIM** — same list as Task 2.

    **Structure tree inline comments translated to fr:**
    - `# Instructions principales (adaptez pour votre projet)`
    - `# Hooks, permissions`
    - `# Commandes slash`
    - `# † omis dans complement-sp/full`
    - `# Audits`
    - `# Sous-agents`
    - `# Expertise frameworks`
    - `# Faits du projet chargés automatiquement`
    - `# Notes de travail`

    **Proper nouns preserved EXACTLY (D-06 list).**

    **Register:** formal `vous`.

    **Admonition labels:** `> **Remarque :**`, `> **Important :**`, `> **Sécurité :**`
    (note: French typographic space before colon on each).

    **Punctuation:** ` — ` (spaced em-dash), French typographic space before `:` / `!` / `?`
    in prose ONLY (never in headings or code), ASCII `"..."` quotes. French accents (é è
    ê ë à â ù û î ï ç œ) natural.

    **What NOT to translate (D-04).**

    **Line count target:** 202 lines source → 222–232 projected.

    **Trailing newline required.**

    Commit message: `docs(06.1): sync fr README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/fr.md && [ "$(wc -l < docs/readme/fr.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/fr.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/fr.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/fr.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/fr.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/fr.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/fr.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/fr.md` returns ≥ 1
    - Current language unlinked: `grep -F '**Français**' docs/readme/fr.md` returns ≥ 1 AND `grep -F '[Français]' docs/readme/fr.md` returns 0
    - Howto link: `grep -F '../howto/fr.md' docs/readme/fr.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/fr.md`
    - `grep -F 'superpowers' docs/readme/fr.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/fr.md` ≥ 1
    - `grep -E -i 'complément|complement' docs/readme/fr.md` ≥ 1 (French or English complement word)
    - `grep -c 'bash <(curl' docs/readme/fr.md` ≥ 3
    - Install Modes section: `grep -F "## Modes d'installation" docs/readme/fr.md` returns 1
    - Install Modes subheadings: `grep -E '^### Installation (autonome|complémentaire)' docs/readme/fr.md` ≥ 2 AND `grep -F '### Mise à jour depuis v3.x' docs/readme/fr.md` returns 1
    - No heading ends with `:` (MD026 guard): `grep -E '^#+.*:$' docs/readme/fr.md` returns 0
    - File ends with newline: `tail -c 1 docs/readme/fr.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/fr.md` matches `docs\(06\.1\): sync fr README`
  </acceptance_criteria>
</task>

<task id="06.1-01-05" type="auto">
  <name>Task 5: Rewrite docs/readme/de.md (German, formal Sie register — WATCH 242-line ceiling)</name>
  <files>docs/readme/de.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/de.md (current v3.x state — uses informal du/dein and ae/oe/ue fallbacks; v4.0 rewrite MUST use formal Sie and proper Unicode umlauts ä/ö/ü)
    - docs/readme/ru.md (structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § German (de) conventions (lines 203–243) — CRITICAL: length risk warning, § Length Expansion Profile (de: +15–25%, **projected 232–252 — risks exceeding 242**), § Localized Heading Glossary de column, § Line-Count Edge Cases § Edge: German near 242 (mitigation strategy), § Open Questions Q2 (file tree convention — use `dein-projekt/` as code label, formal Sie in prose)
    - .markdownlint.json
    - docs/howto/de.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/de.md` with a complete German translation of `README.md`. Full
    rewrite per D-01. **HIGHEST LENGTH RISK of all 8 languages — German expansion is
    +15–25% which risks exceeding 242 lines.**

    **Mitigation strategy (from RESEARCH.md § Edge: German near 242):** Write tight prose,
    avoid German "padded formality" constructions. If after first draft the file exceeds
    242 lines, identify 2 adjacent short paragraphs in the Install Modes section and merge
    them into one paragraph. DO NOT delete the `> **Wichtig:**` admonition block. DO NOT
    delete any content section. DO NOT pad if below 161.

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH de column:

    - `## Für wen ist das`
    - `## Schnellstart` with `### 1. Globale Einrichtung (einmalig)`, `#### a) Security Pack`, `#### b) RTK — Token-Optimierer (empfohlen)`, `#### c) Rate Limit Statusline (Claude Max / Pro, optional)` (kept English per glossary)
    - `## Installationsmodi` with `### Eigenständige Installation`, `### Komplement-Installation`, `### Upgrade von v3.x`
    - `## Killer-Features` (keep "Killer Features" token per glossary; hyphenate per German compound convention) with columns `Feature | Beschreibung`
    - `## MCP-Server (empfohlen!)` with `### Global (alle Projekte)` and `### Pro Projekt (Zugangsdaten)`
    - `## Struktur nach der Installation`
    - `## Unterstützte Frameworks` with columns `Framework | Template | Skills | Auto-Erkennung`
    - `## Komponenten`

    **Exact line 8 — German unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **Deutsch** | **[Français](fr.md)** | **[中文](zh.md)** | **[日本語](ja.md)** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10:**

    ```markdown
    > Lesen Sie zuerst die vollständige [Schritt-für-Schritt-Installationsanleitung](../howto/de.md).
    ```

    **Code blocks VERBATIM.** Same list as Task 2.

    **Structure tree inline comments (German, formal Sie context, keep file/dir labels):**

    Use proper Unicode umlauts (ä ö ü ß), NOT ae/oe/ue fallbacks. The file tree path
    `your-project/` translates to `dein-projekt/` (code label — natural German directory
    convention, treated as code not prose per Open Questions Q2). Prose comments:

    - `# Hauptanweisungen (passen Sie sie für Ihr Projekt an)`
    - `# Hooks, Berechtigungen`
    - `# Slash-Befehle`
    - `# † ausgelassen in complement-sp/full`
    - `# Audits`
    - `# Subagenten`
    - `# Framework-Expertise`
    - `# Automatisch geladene Projektfakten`
    - `# Arbeitsnotizen`

    **Proper nouns preserved EXACTLY (D-06 list).**

    **Register:** formal `Sie`/`Ihr` throughout prose. Capitalize all nouns per German
    orthography (including tech loanwords: `der Hook`, `das Plugin`, `der Skill`, `das
    Template`, `der Agent`, `das Framework`, `der Workflow`, `das Toolkit`).

    **Admonition labels:** `> **Hinweis:**`, `> **Wichtig:**`, `> **Sicherheit:**`.

    **Punctuation:** ` — ` spaced em-dash or ` – ` en-dash (both acceptable German
    typography; prefer em-dash to match README.md style). ASCII `"..."` quotes. No space
    before `:` (unlike French).

    **What NOT to translate (D-04).**

    **Line count target:** 202 lines source; projected 232–252. **Target 232–240 range**
    to leave margin under 242 ceiling. If first draft exceeds 242, apply rebalance
    mitigation. If first draft is still outside band after rebalancing, ask the user
    before truncating content (content fidelity beats line-gaming per D-10).

    **Trailing newline required.**

    Commit message: `docs(06.1): sync de README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/de.md && [ "$(wc -l < docs/readme/de.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/de.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/de.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/de.md` ≤ 242 (tight — monitor actively)
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/de.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/de.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/de.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/de.md` returns ≥ 1
    - Current language unlinked: `grep -F '**Deutsch**' docs/readme/de.md` returns ≥ 1 AND `grep -F '[Deutsch]' docs/readme/de.md` returns 0
    - Howto link: `grep -F '../howto/de.md' docs/readme/de.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/de.md`
    - `grep -F 'superpowers' docs/readme/de.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/de.md` ≥ 1
    - `grep -E -i 'komplement|complement' docs/readme/de.md` ≥ 1
    - `grep -c 'bash <(curl' docs/readme/de.md` ≥ 3
    - Install Modes section: `grep -F '## Installationsmodi' docs/readme/de.md` returns 1
    - Install Modes subheadings: `grep -F '### Eigenständige Installation' docs/readme/de.md` returns 1 AND `grep -F '### Komplement-Installation' docs/readme/de.md` returns 1 AND `grep -F '### Upgrade von v3.x' docs/readme/de.md` returns 1
    - Uses Unicode umlauts (not ae/oe/ue): `grep -E 'ü|ö|ä' docs/readme/de.md` returns ≥ 5 lines (headings like Für/Unterstützte/Größe/etc. should all render as proper Unicode)
    - Formal Sie register check: `grep -c '\bSie\b' docs/readme/de.md` ≥ 1 (at least one formal-you in prose)
    - File ends with newline: `tail -c 1 docs/readme/de.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/de.md` matches `docs\(06\.1\): sync de README`
  </acceptance_criteria>
</task>

<task id="06.1-01-06" type="auto">
  <name>Task 6: Plan 01 full machine verification — make translation-drift + make mdlint</name>
  <files>(no files modified — verification-only task)</files>

  <read_first>
    - Makefile (§ translation-drift target at line 178, § mdlint target at line 37)
    - .planning/phases/06.1-readme-translations/06.1-VALIDATION.md § Sampling Rate § Per-plan merge
    - docs/readme/es.md, pt.md, fr.md, de.md (the 4 files just written — sanity-check that all 4 exist on disk)
  </read_first>

  <action>
    Run the full per-plan machine-verify gate for Plan 01. This is the handoff check before
    Plan 01 hands control back to the phase orchestrator.

    Execute in order:

    1. `make translation-drift`
    2. `make mdlint`

    Both must exit 0. Record the output in the plan's summary commit message body
    (inline, not as a new file — no new files are created by this task).

    If `make translation-drift` fails (any of the 4 files out of band), report which file
    and by how much, then halt Plan 01 for fix iteration — the user's executor must go
    back to the offending language task and apply D-10 rebalance, then re-run the gate.

    If `make mdlint` fails on one of the 4 files, report the exact rule violations per
    line, then halt Plan 01 for fix iteration.

    If both pass, Plan 01 is complete. No commit is required for this task — it is a
    pure verification step. However, if the executor wishes to record the green result
    in the phase summary, it may create a short note in the next `/gsd-verify-work` output
    rather than producing a commit here.

    Note: the other 4 translations (zh, ja, ko, ru) may still be stale at this point if
    Plans 02 and 03 haven't run yet — that is expected. `make translation-drift` will
    still pass as long as the 4 touched by Plan 01 are in band AND the other 4 are ALSO
    in band (which they are NOT pre-Phase-06.1, since all 8 start at 148 lines = below 161).

    **EXPECTED BEHAVIOR:** `make translation-drift` will FAIL at this point because zh/ja/
    ko/ru are still at 148 lines (below 161 floor). This is expected — the gate is a
    phase-level gate, not a plan-level gate. Plan 01's local gate is: the 4 files it
    touched pass a per-file check.

    **CORRECTED verify for this task:** run per-file checks for the 4 Plan-01 files, then
    run `make mdlint` (which should pass because mdlint is per-file and the other 4 files
    were already lint-clean). Do NOT run `make translation-drift` at plan-01 close because
    it will fail on the untouched languages. Let the phase-level wrap-up (gsd-verify-work)
    run `make translation-drift` after all 3 plans complete.

    Replace step 1 above with this script instead:

    ```bash
    set -e
    for lang in es pt fr de; do
      LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' ')
      if [ "$LINES" -lt 161 ] || [ "$LINES" -gt 242 ]; then
        echo "FAIL: docs/readme/${lang}.md has $LINES lines (outside 161-242)"
        exit 1
      fi
      echo "PASS: docs/readme/${lang}.md has $LINES lines"
    done
    echo "All 4 Plan 01 files within 161-242 band"
    ```

    Then `make mdlint` (which runs on all `**/*.md` and should be clean).
  </action>

  <verify>
    <automated>for lang in es pt fr de; do LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' '); [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] || exit 1; done && make mdlint</automated>
    Must exit 0 — all 4 Plan 01 files within band AND repo-wide markdownlint passes.
  </verify>

  <acceptance_criteria>
    - All 4 Plan 01 files within 161–242 line band (per-file script above exits 0)
    - `make mdlint` exits 0
    - No commit required for this task (verification-only)
  </acceptance_criteria>
</task>

## Verification

Run at the end of Plan 01 (executor-side):

```bash
# 1. All 4 files exist with correct line counts
for lang in es pt fr de; do
  LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' ')
  echo "docs/readme/${lang}.md: ${LINES} lines"
  [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] || { echo "OUT OF BAND"; exit 1; }
done

# 2. Structural parity with README.md
for lang in es pt fr de; do
  H2_TR=$(grep -c '^## ' "docs/readme/${lang}.md")
  H2_EN=$(grep -c '^## ' README.md)
  [ "$H2_TR" = "$H2_EN" ] || { echo "H2 mismatch on ${lang}"; exit 1; }
  H3_TR=$(grep -c '^### ' "docs/readme/${lang}.md")
  H3_EN=$(grep -c '^### ' README.md)
  [ "$H3_TR" = "$H3_EN" ] || { echo "H3 mismatch on ${lang}"; exit 1; }
done

# 3. Per-file lint
for lang in es pt fr de; do
  npx markdownlint-cli2 "docs/readme/${lang}.md" || exit 1
done

# 4. Repo-wide mdlint (no regressions anywhere)
make mdlint

# 5. TRANS-01..04 present in REQUIREMENTS.md
grep -c '^- \[ \] \*\*TRANS-0[1-4]\*\*:' .planning/REQUIREMENTS.md
# Expected: 4
```

## Must-Haves (plan-level goal-backward checklist)

1. **TRUTH:** `.planning/REQUIREMENTS.md` contains TRANS-01..04 with exact proposed wording.
2. **TRUTH:** `docs/readme/es.md` mirrors README.md's 12 sections in Spanish, 161–242 lines.
3. **TRUTH:** `docs/readme/pt.md` mirrors README.md's 12 sections in Portuguese, 161–242 lines.
4. **TRUTH:** `docs/readme/fr.md` mirrors README.md's 12 sections in French, 161–242 lines.
5. **TRUTH:** `docs/readme/de.md` mirrors README.md's 12 sections in German (formal Sie), 161–242 lines.
6. **TRUTH:** All 4 files pass `npx markdownlint-cli2` individually.
7. **TRUTH:** `make mdlint` passes repo-wide after Plan 01 commits.
8. **TRUTH:** Each of the 4 files has all code fences byte-identical to README.md.
9. **TRUTH:** Each of the 4 files has the language switcher with current language unlinked and English pointing to `../../README.md`.
10. **TRUTH:** Each of the 4 files has its howto link resolving to `../howto/{lang}.md`.

## Output

After Plan 01 completes, the phase orchestrator's `/gsd-verify-work` pass will run the
full phase gate (`make check` including `make translation-drift`) which at that point
will still fail until Plans 02 and 03 ship their 4 remaining languages. Plan 01 is
atomic on its own 4 files and 1 REQUIREMENTS.md append.
