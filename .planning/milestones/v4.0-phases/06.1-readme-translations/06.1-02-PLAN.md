---
phase: 06.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/readme/zh.md
  - docs/readme/ja.md
  - docs/readme/ko.md
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
must_haves:
  truths:
    - "docs/readme/zh.md is a full v4.0 rewrite mirroring README.md in Simplified Chinese formal written register"
    - "docs/readme/ja.md is a full v4.0 rewrite mirroring README.md in Japanese です/ます form"
    - "docs/readme/ko.md is a full v4.0 rewrite mirroring README.md in Korean -습니다 form"
    - "All 3 CJK translations land in the 161-242 line band (expected close to 202)"
    - "make mdlint passes after Plan 02 completes (CJK does not introduce lint regressions)"
  artifacts:
    - path: "docs/readme/zh.md"
      provides: "Simplified Chinese README mirror of v4.0 complement-first English"
      min_lines: 161
    - path: "docs/readme/ja.md"
      provides: "Japanese README mirror of v4.0 complement-first English"
      min_lines: 161
    - path: "docs/readme/ko.md"
      provides: "Korean README mirror of v4.0 complement-first English"
      min_lines: 161
  key_links:
    - from: "docs/readme/zh.md"
      to: "docs/howto/zh.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/zh\\.md"
    - from: "docs/readme/ja.md"
      to: "docs/howto/ja.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/ja\\.md"
    - from: "docs/readme/ko.md"
      to: "docs/howto/ko.md"
      via: "blockquote link on line 10"
      pattern: "\\.\\./howto/ko\\.md"
    - from: "all 3 translations"
      to: "../../README.md"
      via: "language switcher row 8"
      pattern: "\\.\\./\\.\\./README\\.md"
---

# Plan 06.1-02: Sync East Asian CJK README translations (zh / ja / ko)

## Objective

Rewrite three East Asian CJK README translations — Simplified Chinese, Japanese, and
Korean — from the current English `README.md` (202 lines, v4.0 complement-first). Each
translation mirrors README.md's 12 canonical sections with identical code blocks,
identical proper nouns (English acronyms preserved: `MCP`, `RTK`, `Claude Code`,
`superpowers`, `get-shit-done`), and a localized language switcher.

**Purpose:** Unblock the Phase 7 release gate for the 3 CJK languages.

**Output:** 3 rewritten translation files, all passing per-file `markdownlint-cli2` and
contributing to `make mdlint` green. Committed per-language (3 commits total).

## Tasks

<task id="06.1-02-01" type="auto">
  <name>Task 1: Rewrite docs/readme/zh.md (Simplified Chinese, formal written register)</name>
  <files>docs/readme/zh.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/zh.md (current v3.x state — do NOT reuse body content; observe existing language switcher + howto link pattern)
    - docs/readme/ru.md (canonical structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Chinese Simplified (zh) conventions (lines 354–398), § Length Expansion Profile (zh: −5% to +5%, 192–212 lines — safe), § Localized Heading Glossary zh column, § Markdownlint Pitfalls § CJK-Specific
    - .markdownlint.json
    - docs/howto/zh.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/zh.md` with a complete Simplified Chinese (zh-Hans) translation
    of `README.md`. Full rewrite per D-01. Formal written register; use `您` if direct
    address is needed (typically implicit).

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH zh column:

    - `## 适用人群`
    - `## 快速开始` with `### 1. 全局设置（仅需一次）`, `#### a) Security Pack` (keep English per glossary), `#### b) RTK — Token 优化器（推荐）`, `#### c) Rate Limit Statusline（Claude Max / Pro，可选）` (kept English per glossary, note fullwidth parens)
    - `## 安装模式` with `### 独立安装`, `### 补充安装`, `### 从 v3.x 升级`
    - `## 核心亮点` with columns `功能 | 描述`
    - `## MCP 服务器（推荐！）` with `### 全局（所有项目）` and `### 每个项目（凭据）`
    - `## 安装后的结构`
    - `## 支持的框架` with columns `框架 | 模板 | 技能 | 自动检测`
    - `## 组件`

    **Exact line 8 — Chinese unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **[Deutsch](de.md)** | **[Français](fr.md)** | **中文** | **[日本語](ja.md)** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10:**

    ```markdown
    > 请先阅读完整的[分步安装指南](../howto/zh.md)。
    ```

    **Code blocks VERBATIM from README.md.** Same list as Plan 01 Task 2 (Security Pack
    curl, RTK brew/init, statusline curl, init-claude standalone, init-claude --mode
    complement-full, 4-line global MCP block, dbhub one-liner).

    **Structure tree inline comments translated to zh (use Simplified characters):**

    - `# 主要指令（根据项目调整）`
    - `# 钩子、权限`
    - `# 斜杠命令`
    - `# † 在 complement-sp/full 中省略`
    - `# 审计`
    - `# 子代理`
    - `# 框架专业知识`
    - `# 自动加载的项目信息`
    - `# 工作笔记`

    **Proper nouns preserved EXACTLY (D-06 list):** `Claude Code`, `superpowers`,
    `get-shit-done`, `RTK`, `MCP`, `Supreme Council`, `Gemini`, `ChatGPT`, `Laravel`,
    `Rails`, `Next.js`, `Node.js`, `Python`, `Go`, `Ruby on Rails`, `context7`,
    `playwright`, `sequential-thinking`, `sentry`, `dbhub`. Also: `Security Pack` (English),
    `Rate Limit Statusline` (English), `Skills` allowed in Frameworks table or localized
    to `技能` — glossary allows either; prefer `技能` for consistency with column headers.

    **Punctuation:**
    - In prose: fullwidth CJK punctuation `，` `。` `：` `！` `？` `（` `）` is natural
      in running Chinese text.
    - In headings: Chinese headings typically end without punctuation. Safe for MD026.
    - In code blocks / inline code: ASCII only (D-04).
    - Space boundary: insert a regular space between CJK characters and Latin segments
      (e.g., `MCP 服务器`, `Claude Code 工具包`) — standard CJK-Latin boundary spacing.

    **Admonition labels:** `> **注意：**`, `> **重要：**`, `> **安全：**` (fullwidth colons
    after Chinese labels are natural; this is prose, not headings, so MD026 does not apply).

    **What NOT to translate (D-04):** code blocks, CLI commands, CLI flags, file paths,
    package names, URL anchors, badge alt text, all proper nouns above.

    **Line count target:** 192–212 projected. Stay within 161–242 band.

    **Trailing newline required.**

    Commit message: `docs(06.1): sync zh README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/zh.md && [ "$(wc -l < docs/readme/zh.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/zh.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/zh.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/zh.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/zh.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/zh.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/zh.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/zh.md` returns ≥ 1
    - Current language unlinked: `grep -F '**中文**' docs/readme/zh.md` returns ≥ 1 AND `grep -F '[中文]' docs/readme/zh.md` returns 0
    - Howto link: `grep -F '../howto/zh.md' docs/readme/zh.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/zh.md`
    - `grep -F 'superpowers' docs/readme/zh.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/zh.md` ≥ 1
    - `grep -E '补充|complement' docs/readme/zh.md` ≥ 1 (Chinese or English complement word)
    - `grep -c 'bash <(curl' docs/readme/zh.md` ≥ 3
    - Install Modes section: `grep -F '## 安装模式' docs/readme/zh.md` returns 1
    - Install Modes subheadings: `grep -F '### 独立安装' docs/readme/zh.md` returns 1 AND `grep -F '### 补充安装' docs/readme/zh.md` returns 1 AND `grep -F '### 从 v3.x 升级' docs/readme/zh.md` returns 1
    - No heading ends with fullwidth period or ASCII period: `grep -E '^#+.*[。.]$' docs/readme/zh.md` returns 0
    - File ends with newline: `tail -c 1 docs/readme/zh.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/zh.md` matches `docs\(06\.1\): sync zh README`
  </acceptance_criteria>
</task>

<task id="06.1-02-02" type="auto">
  <name>Task 2: Rewrite docs/readme/ja.md (Japanese, です/ます form)</name>
  <files>docs/readme/ja.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/ja.md (current v3.x state — do NOT reuse body content)
    - docs/readme/ru.md (structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Japanese (ja) conventions (lines 401–440), § Length Expansion Profile (ja: −5% to +5%, 192–212 lines — safe), § Localized Heading Glossary ja column, § Markdownlint Pitfalls § CJK-Specific
    - .markdownlint.json
    - docs/howto/ja.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/ja.md` with a complete Japanese translation of `README.md`.
    Full rewrite per D-01. Use です/ます form (polite register, standard for developer
    documentation).

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH ja column:

    - `## 対象者`
    - `## クイックスタート` with `### 1. グローバルセットアップ（一度だけ）`, `#### a) セキュリティパック` (Japanese rendering per glossary), `#### b) RTK — トークンオプティマイザー（推奨）`, `#### c) Rate Limit Statusline（Claude Max / Pro、オプション）` (kept English + katakana per glossary)
    - `## インストールモード` with `### スタンドアロンインストール`, `### 補完インストール`, `### v3.xからのアップグレード`
    - `## 主な機能` with columns `機能 | 説明`
    - `## MCPサーバー（推奨！）` with `### グローバル（全プロジェクト）` and `### プロジェクトごと（認証情報）`
    - `## インストール後の構造`
    - `## サポートされているフレームワーク` with columns `フレームワーク | テンプレート | スキル | 自動検出`
    - `## コンポーネント`

    **Exact line 8 — Japanese unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **[Deutsch](de.md)** | **[Français](fr.md)** | **[中文](zh.md)** | **日本語** | **[Português](pt.md)** | **[한국어](ko.md)**
    ```

    **Exact line 10:**

    ```markdown
    > 最初に完全な[ステップバイステップのインストールガイド](../howto/ja.md)をお読みください。
    ```

    **Code blocks VERBATIM from README.md.**

    **Structure tree inline comments translated to ja (katakana for tech terms):**

    - `# 主な指示（プロジェクトに合わせて調整）`
    - `# フック、権限`
    - `# スラッシュコマンド`
    - `# † complement-sp/full では除外`
    - `# 監査`
    - `# サブエージェント`
    - `# フレームワーク専門知識`
    - `# 自動読み込みされるプロジェクト情報`
    - `# 作業メモ`

    **Proper nouns preserved EXACTLY (D-06 list).** Keep `MCP` and `RTK` as acronyms (not
    katakana-ized) per glossary.

    **Punctuation:**
    - In prose: fullwidth CJK punctuation `、` `。` `：` `！` `？` `（` `）` is natural.
    - In headings: no trailing punctuation.
    - In code: ASCII only (D-04).
    - Space boundary: regular space between katakana/kanji and Latin segments
      (e.g., `MCP サーバー`, `Claude Code ツールキット`).

    **Admonition labels:** `> **注意：**`, `> **重要：**`, `> **セキュリティ：**`.

    **What NOT to translate (D-04).**

    **Line count target:** 192–212 projected. Stay within 161–242 band.

    **Trailing newline required.**

    Commit message: `docs(06.1): sync ja README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/ja.md && [ "$(wc -l < docs/readme/ja.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/ja.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/ja.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/ja.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/ja.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/ja.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/ja.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/ja.md` returns ≥ 1
    - Current language unlinked: `grep -F '**日本語**' docs/readme/ja.md` returns ≥ 1 AND `grep -F '[日本語]' docs/readme/ja.md` returns 0
    - Howto link: `grep -F '../howto/ja.md' docs/readme/ja.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/ja.md`
    - `grep -F 'superpowers' docs/readme/ja.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/ja.md` ≥ 1
    - `grep -E '補完|complement' docs/readme/ja.md` ≥ 1
    - `grep -c 'bash <(curl' docs/readme/ja.md` ≥ 3
    - Install Modes section: `grep -F '## インストールモード' docs/readme/ja.md` returns 1
    - Install Modes subheadings: `grep -F '### スタンドアロンインストール' docs/readme/ja.md` returns 1 AND `grep -F '### 補完インストール' docs/readme/ja.md` returns 1 AND `grep -F '### v3.xからのアップグレード' docs/readme/ja.md` returns 1
    - No heading ends with fullwidth period or ASCII period: `grep -E '^#+.*[。.]$' docs/readme/ja.md` returns 0
    - File ends with newline: `tail -c 1 docs/readme/ja.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/ja.md` matches `docs\(06\.1\): sync ja README`
  </acceptance_criteria>
</task>

<task id="06.1-02-03" type="auto">
  <name>Task 3: Rewrite docs/readme/ko.md (Korean, -습니다 form)</name>
  <files>docs/readme/ko.md</files>

  <read_first>
    - README.md (SOURCE OF TRUTH)
    - docs/readme/ko.md (current v3.x state — do NOT reuse body content)
    - docs/readme/ru.md (structural pattern reference)
    - .planning/phases/06.1-readme-translations/06.1-CONTEXT.md § Decisions
    - .planning/phases/06.1-readme-translations/06.1-RESEARCH.md § Korean (ko) conventions (lines 443–477), § Length Expansion Profile (ko: −5% to +5%, 192–212 lines — safe), § Localized Heading Glossary ko column, § Markdownlint Pitfalls § CJK-Specific
    - .markdownlint.json
    - docs/howto/ko.md (confirm exists)
  </read_first>

  <action>
    Overwrite `docs/readme/ko.md` with a complete Korean translation of `README.md`.
    Full rewrite per D-01. Use -습니다/-ㅂ니다 form (합쇼체, formal polite — standard for
    professional developer documentation).

    **Structure:** mirror README.md's 12 sections. Localized headings from RESEARCH ko column:

    - `## 대상`
    - `## 빠른 시작` with `### 1. 전역 설정 (한 번만)`, `#### a) Security Pack` (keep English per glossary), `#### b) RTK — 토큰 최적화 도구 (권장)`, `#### c) Rate Limit Statusline (Claude Max / Pro, 선택 사항)` (English label per glossary)
    - `## 설치 모드` with `### 독립 설치`, `### 보완 설치`, `### v3.x에서 업그레이드`
    - `## 핵심 기능` with columns `기능 | 설명`
    - `## MCP 서버 (권장!)` with `### 전역 (모든 프로젝트)` and `### 프로젝트별 (자격 증명)`
    - `## 설치 후 구조`
    - `## 지원되는 프레임워크` with columns `프레임워크 | 템플릿 | 스킬 | 자동 감지`
    - `## 컴포넌트`

    **Exact line 8 — Korean unlinked:**

    ```markdown
    **[English](../../README.md)** | **[Русский](ru.md)** | **[Español](es.md)** | **[Deutsch](de.md)** | **[Français](fr.md)** | **[中文](zh.md)** | **[日本語](ja.md)** | **[Português](pt.md)** | **한국어**
    ```

    **Exact line 10:**

    ```markdown
    > 먼저 전체 [단계별 설치 가이드](../howto/ko.md)를 읽어주세요.
    ```

    **Code blocks VERBATIM from README.md.**

    **Structure tree inline comments translated to ko:**

    - `# 주요 지침 (프로젝트에 맞게 조정)`
    - `# 후크, 권한`
    - `# 슬래시 명령어`
    - `# † complement-sp/full에서 생략됨`
    - `# 감사`
    - `# 서브에이전트`
    - `# 프레임워크 전문 지식`
    - `# 자동 로드되는 프로젝트 정보`
    - `# 작업 메모`

    **Proper nouns preserved EXACTLY (D-06 list).** Keep `MCP` and `RTK` as acronyms.

    **Punctuation:** ASCII punctuation is standard in Korean developer docs (not fullwidth).
    Use ASCII `,` `.` `:` `!` `?` `(` `)` in prose and tables. Headings no trailing
    punctuation. Code blocks ASCII only.

    **Admonition labels:** `> **참고:**`, `> **중요:**`, `> **보안:**`.

    **Space rules:** standard Korean spacing (space between words, space around Latin
    segments like `MCP 서버`, `Claude Code 툴킷`).

    **What NOT to translate (D-04).**

    **Line count target:** 192–212 projected. Stay within 161–242 band.

    **Trailing newline required.**

    Commit message: `docs(06.1): sync ko README to v4.0 complement-first`
  </action>

  <verify>
    <automated>npx markdownlint-cli2 docs/readme/ko.md && [ "$(wc -l < docs/readme/ko.md | tr -d ' ')" -ge 161 ] && [ "$(wc -l < docs/readme/ko.md | tr -d ' ')" -le 242 ]</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f docs/readme/ko.md`
    - Line count in band: 161 ≤ `wc -l < docs/readme/ko.md` ≤ 242
    - Markdownlint clean: `npx markdownlint-cli2 docs/readme/ko.md` exits 0
    - H2 parity: `grep -c '^## ' docs/readme/ko.md` equals `grep -c '^## ' README.md`
    - H3 parity: `grep -c '^### ' docs/readme/ko.md` equals `grep -c '^### ' README.md`
    - English link: `grep -F '[English](../../README.md)' docs/readme/ko.md` returns ≥ 1
    - Current language unlinked: `grep -F '**한국어**' docs/readme/ko.md` returns ≥ 1 AND `grep -F '[한국어]' docs/readme/ko.md` returns 0
    - Howto link: `grep -F '../howto/ko.md' docs/readme/ko.md` returns ≥ 1
    - Howto target exists: `test -f docs/howto/ko.md`
    - `grep -F 'superpowers' docs/readme/ko.md` ≥ 2
    - `grep -F 'get-shit-done' docs/readme/ko.md` ≥ 1
    - `grep -E '보완|complement' docs/readme/ko.md` ≥ 1
    - `grep -c 'bash <(curl' docs/readme/ko.md` ≥ 3
    - Install Modes section: `grep -F '## 설치 모드' docs/readme/ko.md` returns 1
    - Install Modes subheadings: `grep -F '### 독립 설치' docs/readme/ko.md` returns 1 AND `grep -F '### 보완 설치' docs/readme/ko.md` returns 1 AND `grep -F '### v3.x에서 업그레이드' docs/readme/ko.md` returns 1
    - No heading ends with period: `grep -E '^#+.*\.$' docs/readme/ko.md` returns 0
    - File ends with newline: `tail -c 1 docs/readme/ko.md | xxd | grep -q '0a'`
    - Commit landed: `git log -1 --oneline -- docs/readme/ko.md` matches `docs\(06\.1\): sync ko README`
  </acceptance_criteria>
</task>

<task id="06.1-02-04" type="auto">
  <name>Task 4: Plan 02 machine verification — per-file lint + band + make mdlint</name>
  <files>(no files modified — verification-only task)</files>

  <read_first>
    - Makefile (§ mdlint target at line 37)
    - .planning/phases/06.1-readme-translations/06.1-VALIDATION.md § Sampling Rate § Per-plan merge
    - docs/readme/zh.md, ja.md, ko.md (the 3 files just written — sanity-check that all 3 exist on disk)
  </read_first>

  <action>
    Run the per-plan machine-verify gate for Plan 02. Like Plan 01, avoid running
    `make translation-drift` at this stage because the 5 non-Plan-02 translations may
    still be out-of-band (4 of them might also be stale if Plans 01 and 03 haven't
    finished; all 3 plans run in parallel Wave 1). The phase-level wrap in
    `/gsd-verify-work` runs `make translation-drift` after all 3 plans complete.

    Execute:

    ```bash
    set -e
    for lang in zh ja ko; do
      LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' ')
      if [ "$LINES" -lt 161 ] || [ "$LINES" -gt 242 ]; then
        echo "FAIL: docs/readme/${lang}.md has $LINES lines (outside 161-242)"
        exit 1
      fi
      echo "PASS: docs/readme/${lang}.md has $LINES lines"
    done
    echo "All 3 Plan 02 files within 161-242 band"
    ```

    Then `make mdlint` (runs on all `**/*.md` — must still pass repo-wide).

    If any CJK file fails lint, the most likely causes are:
    - Missing language on a code fence (MD040) — grep `^```$` in the file to find bare
      fences.
    - Missing blank line around a fence (MD031/MD032) — check each fence has a blank
      line before and after.
    - Trailing punctuation in heading (MD026) — check no heading ends with `。`, `：`,
      `?`, `!`, `.`.

    Do not create a commit for this task; verification-only.
  </action>

  <verify>
    <automated>for lang in zh ja ko; do LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' '); [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] || exit 1; done && make mdlint</automated>
  </verify>

  <acceptance_criteria>
    - All 3 Plan 02 files within 161–242 line band
    - `make mdlint` exits 0
    - No commit required (verification-only)
  </acceptance_criteria>
</task>

## Verification

Run at end of Plan 02 (executor-side):

```bash
# 1. All 3 CJK files exist with correct line counts
for lang in zh ja ko; do
  LINES=$(wc -l < "docs/readme/${lang}.md" | tr -d ' ')
  echo "docs/readme/${lang}.md: ${LINES} lines"
  [ "$LINES" -ge 161 ] && [ "$LINES" -le 242 ] || { echo "OUT OF BAND"; exit 1; }
done

# 2. Structural parity
for lang in zh ja ko; do
  H2_TR=$(grep -c '^## ' "docs/readme/${lang}.md")
  H2_EN=$(grep -c '^## ' README.md)
  [ "$H2_TR" = "$H2_EN" ] || { echo "H2 mismatch on ${lang}"; exit 1; }
  H3_TR=$(grep -c '^### ' "docs/readme/${lang}.md")
  H3_EN=$(grep -c '^### ' README.md)
  [ "$H3_TR" = "$H3_EN" ] || { echo "H3 mismatch on ${lang}"; exit 1; }
done

# 3. Per-file lint
for lang in zh ja ko; do
  npx markdownlint-cli2 "docs/readme/${lang}.md" || exit 1
done

# 4. Repo-wide mdlint
make mdlint
```

## Must-Haves (plan-level goal-backward checklist)

1. **TRUTH:** `docs/readme/zh.md` mirrors README.md's 12 sections in Simplified Chinese, 161–242 lines.
2. **TRUTH:** `docs/readme/ja.md` mirrors README.md's 12 sections in Japanese (です/ます), 161–242 lines.
3. **TRUTH:** `docs/readme/ko.md` mirrors README.md's 12 sections in Korean (-습니다), 161–242 lines.
4. **TRUTH:** All 3 files pass `npx markdownlint-cli2` individually.
5. **TRUTH:** `make mdlint` passes repo-wide after Plan 02 commits.
6. **TRUTH:** Each file has all code fences byte-identical to README.md.
7. **TRUTH:** Each file has the language switcher with current language unlinked and English → `../../README.md`.
8. **TRUTH:** Each file has its howto link resolving to `../howto/{lang}.md`.

## Output

After Plan 02 completes, 3 of 8 non-English READMEs are in v4.0 shape. The phase is
complete only after all 3 plans land and `make translation-drift` passes across all 8
files, which is verified during `/gsd-verify-work` at the phase wrap-up.
