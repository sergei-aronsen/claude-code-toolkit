---
phase: 39-tui-per-row-scope-toggle
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - scripts/lib/mcp.sh
  - scripts/install.sh
autonomous: true
requirements:
  - TUI-SCOPE-02
  - TUI-SCOPE-03
  - TUI-SCOPE-05
tags:
  - tui
  - bash
  - mcp
  - scope
  - dispatch

must_haves:
  truths:
    - "Pressing the global header `s` keypress writes the next scope value into EVERY MCP_SELECTED_SCOPE slot in one stroke"
    - "The header banner reads 'Set all to: <U|P|L> · press s to cycle' (D-11) instead of the v4.9 user/local toggle copy"
    - "The global cycle is user → project → local → user (D-10), matching the per-row Tab cycle"
    - "install.sh integrations dispatcher exports TK_MCP_SCOPE per iteration BEFORE mcp_wizard_run"
    - "The pre-loop TK_MCP_SCOPE export is preserved for CLI --mcp-scope but overwritten per-iteration in the TUI dispatcher path"
    - "CLI-only rows do NOT trigger TK_MCP_SCOPE export; index parity from Plan 01 ensures dispatcher never reads off the end of MCP_SELECTED_SCOPE"
  artifacts:
    - path: "scripts/lib/mcp.sh"
      provides: "mcp_toggle_scope repurposed as 3-state set-all + new mcp_render_scope_header banner copy per D-11"
      contains: "_MCP_SETALL_SCOPE"
    - path: "scripts/install.sh"
      provides: "Per-row TK_MCP_SCOPE export inside the dispatch loop, sourced from MCP_SELECTED_SCOPE"
      contains: "MCP_SELECTED_SCOPE"
    - path: "scripts/install.sh"
      provides: "TUI_ROW_KEY/TUI_ROW_FN wiring before tui_checklist invocation"
      contains: "TUI_ROW_KEY"
  key_links:
    - from: "tui.sh case-match catch-all (TUI_HEADER_FN dispatch)"
      to: "mcp_toggle_scope (repurposed)"
      via: "TUI_HEADER_KEY=s wired by install.sh"
      pattern: "TUI_HEADER_FN"
    - from: "mcp_toggle_scope (repurposed)"
      to: "for-loop write to every MCP_SELECTED_SCOPE slot"
      via: "module-local _MCP_SETALL_SCOPE cycles user/project/local"
      pattern: "for.*MCP_SELECTED_SCOPE"
    - from: "install.sh dispatch loop"
      to: "mcp_wizard_run via per-iteration TK_MCP_SCOPE export"
      via: "TK_MCP_SCOPE assigned from MCP_SELECTED_SCOPE[tui_i] before wizard call"
      pattern: "MCP_SELECTED_SCOPE"
---

<objective>
Wire the consumer half of Phase 39:

1. **Set-all (TUI-SCOPE-03):** repurpose the `s` keypress (header function bound at install.sh:465 to `mcp_toggle_scope`) so it cycles a module-local "global pending" scope through user → project → local → user (D-10) and writes the new value into EVERY `MCP_SELECTED_SCOPE` slot. Banner copy changes from `Scope: ◉ user (global) ...` (Phase 37) to `Set all to: <bracket> · press s to cycle` (D-11). Stop exporting `TK_MCP_SCOPE` from this function — env-var is now strictly per-call (D-18).

2. **Per-row dispatcher export (TUI-SCOPE-05):** in `install.sh`'s integrations dispatcher loop (lines 522-614), set `TK_MCP_SCOPE` from `MCP_SELECTED_SCOPE` IMMEDIATELY before each `mcp_wizard_run` invocation. This is the core D-17 contract: per-iteration injection, never a stale shell-global. The reinstall path (lines 591-607) reads `TK_MCP_SCOPE` so it picks up the row-correct scope automatically once the export is in place.

3. **TUI launch wiring:** in `install.sh`'s pre-tui_checklist block (lines 455-471), bind `TUI_ROW_KEY` to the Tab byte and `TUI_ROW_FN="mcp_cycle_row_scope"` alongside the existing `TUI_HEADER_KEY="s"` / `TUI_HEADER_FN="mcp_toggle_scope"`. Add to the unset list on cancel + post-success.

Purpose: closes the dispatch contract end-to-end. After this plan, a user can launch the TUI, press Tab to flip individual rows or `s` to flip them all, and each `mcp_wizard_run` receives the row-correct `TK_MCP_SCOPE`.

Output: complete per-MCP scope routing through the integrations TUI; Phase 38's `mcp_wizard_run` reads `TK_MCP_SCOPE` per call (already verified at .planning/phases/38-wizard-dispatch-integration/38-VERIFICATION.md), so no wizard work this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-tui-per-row-scope-toggle/39-CONTEXT.md
@.planning/phases/39-tui-per-row-scope-toggle/39-PATTERNS.md
@.planning/phases/39-tui-per-row-scope-toggle/39-01-PLAN.md
@.planning/phases/38-wizard-dispatch-integration/38-VERIFICATION.md
@scripts/lib/mcp.sh
@scripts/install.sh

<interfaces>
<!-- Contracts produced by Plan 01. Must already be landed before this plan runs. -->

From scripts/lib/mcp.sh after Plan 01 lands:

- MCP_SELECTED_SCOPE — parallel array to TUI_LABELS, value in {user, project, local}.
  Initialized from MCP_DEFAULT_SCOPE in mcp_status_array.
- mcp_cycle_row_scope — per-row Tab handler; mutates MCP_SELECTED_SCOPE[FOCUS_IDX].
- _mcp_render_scope_glyph — helper; emits "[U] [P] [L]" with active wrapped green.

From scripts/lib/mcp.sh:973-1003 (Phase 37 implementation, to repurpose):
The current mcp_render_scope_header reads TK_MCP_SCOPE and renders
"Scope: user|local · press s to toggle". Phase 39 banner switches to
"Set all to: <bracket> · press s to cycle" reading _MCP_SETALL_SCOPE.
The current mcp_toggle_scope flips TK_MCP_SCOPE between user and local
and exports it. Phase 39 stops exporting and writes every row's
MCP_SELECTED_SCOPE slot in a 3-state cycle.

From scripts/install.sh:455-471 (TUI launch wiring — modification target):
Currently sets TK_MCP_SCOPE default, calls mcp_render_scope_header,
binds TUI_HEADER_KEY="s" and TUI_HEADER_FN="mcp_toggle_scope", then
calls tui_checklist. Phase 39 adds TUI_ROW_KEY (Tab byte) and
TUI_ROW_FN="mcp_cycle_row_scope" to that block, and extends the
unset list on cancel + success.

From scripts/install.sh:522-614 (dispatch loop — TK_MCP_SCOPE export site):
Loop iterates tui_i over TUI_LABELS. The wizard call site at line 611
is mcp_wizard_run inside a subshell. Phase 39 inserts the per-row
TK_MCP_SCOPE export AFTER local_flags is built (line 583) and BEFORE
the reinstall block (line 591) so the reinstall step also picks up
the row-correct scope via _scope_for_rm.

From Phase 38 verification:
mcp_wizard_run reads TK_MCP_SCOPE fresh on every invocation.
- TK_MCP_SCOPE=project → writes to project .env, calls claude mcp add
  with --scope project and ${VAR} substitution form.
- TK_MCP_SCOPE=user (or unset) → writes to mcp-config.env, calls
  claude mcp add with --scope user and literal env values.
- TK_MCP_SCOPE=local → user-shape behavior with --scope local.
Per-row export from THIS plan flows into that contract — zero wizard
changes needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repurpose mcp_toggle_scope as global set-all + update banner copy</name>
  <files>scripts/lib/mcp.sh</files>
  <read_first>
    - scripts/lib/mcp.sh:953-1003 (current mcp_render_scope_header + mcp_toggle_scope — full repurpose target)
    - scripts/lib/mcp.sh::mcp_status_array (Plan 01 — produces MCP_SELECTED_SCOPE)
    - scripts/lib/mcp.sh::mcp_cycle_row_scope (Plan 01 — companion per-row handler; share _mcp_render_scope_glyph for label rebuild)
    - scripts/lib/tui.sh:152-200 (TUI_HEADER_TEXT consumed by _tui_render — verify color-aware rendering still works after copy update)
    - .planning/phases/39-tui-per-row-scope-toggle/39-CONTEXT.md (D-09..D-12, D-16, D-18)
    - .planning/phases/39-tui-per-row-scope-toggle/39-PATTERNS.md "TUI_HEADER_FN repurpose"
  </read_first>
  <behavior>
    - Test 1 (TUI-SCOPE-03 set-all writes every slot): given `MCP_SELECTED_SCOPE=(user user project)`, calling `mcp_toggle_scope` produces all three slots equal to the new global cycled value.
    - Test 2 (TUI-SCOPE-03 cycle order): repeated calls cycle the module-local through user → project → local → user (D-10).
    - Test 3 (banner update): after a call, `TUI_HEADER_TEXT` contains the substring `Set all to:` and one of `[U]`, `[P]`, `[L]` matching the new value (per D-11).
    - Test 4 (D-18 — env-var no longer exported): `mcp_toggle_scope` does NOT call `export TK_MCP_SCOPE`; the dispatcher in Task 2 owns `TK_MCP_SCOPE` exclusively.
    - Test 5 (D-12 — overwrite by design): if user previously cycled row 1 to local via Tab, then presses `s`, row 1's value gets overwritten by the new global value.
    - Test 6 (label re-render across all rows): after set-all, every row's `TUI_LABELS` reflects the new active scope (green bracket moved). Labels rebuilt for every TUI index, not just one.
    - Test 7 (banner color discipline): under TTY+color, banner contains ANSI escapes for bold/dim; under NO_COLOR or non-TTY, banner is plain ASCII. Width under 80 columns in both modes.
  </behavior>
  <action>
    Edit `scripts/lib/mcp.sh`. Three surgical changes around lines 953-1003.

    **Step 1 — module-local global pending state**

    Place at file-scope just after the existing color guards (~line 53). This is a top-level (module-scope) declaration, NOT inside any function — module state lives at file-scope where readers expect it. Insert:

        # Phase 39 (TUI-SCOPE-03): module-local "global pending" scope used by
        # the repurposed `s` keypress. NOT exported — TK_MCP_SCOPE is now
        # strictly the per-call wizard env-var (Phase 38 contract +
        # Phase 39 D-18). Initialized to "user" so the first banner read
        # shows a sane default before any cycle.
        : "${_MCP_SETALL_SCOPE:=user}"

    (Both the rationale and location are unified above — single file-scope insertion near color guards.)

    **Step 2 — rewrite mcp_render_scope_header (lines 973-993)**

    Replace the entire function body. New copy reads `Set all to: <bracket> · press s to cycle`, where `<bracket>` is the active scope's bracketed letter (D-11):

        mcp_render_scope_header() {
            # Phase 39 (TUI-SCOPE-03 D-11): banner repurposed from
            # "Scope: user (global)  local (this project)" toggle copy to
            # "Set all to: [U] · press s to cycle". Reads module-local
            # _MCP_SETALL_SCOPE.
            local _cur="${_MCP_SETALL_SCOPE:-user}"
            local _glyph
            case "$_cur" in
                user)    _glyph="[U]" ;;
                project) _glyph="[P]" ;;
                local)   _glyph="[L]" ;;
                *)       _glyph="[U]" ;;
            esac
            if [[ "${_TUI_COLOR:-0}" -eq 1 ]] && [[ -z "${NO_COLOR+x}" ]]; then
                # Bold "Set all to:" + bright green active bracket + dim hint.
                TUI_HEADER_TEXT=$'\e[1mSet all to:\e[0m '$'\e[1;32m'"${_glyph}"$'\e[0m  \e[2m· press s to cycle\e[0m'
            else
                TUI_HEADER_TEXT="Set all to: ${_glyph}  · press s to cycle"
            fi
        }

    Width budget: `Set all to: [U]  · press s to cycle` is 36 chars, well under 80-col after the 2-char indent at tui.sh:153.

    **Step 3 — rewrite mcp_toggle_scope (lines 995-1003)**

    Replace the entire function body. Behavior: cycle module-local `_MCP_SETALL_SCOPE` through user → project → local → user, write the new value into EVERY `MCP_SELECTED_SCOPE` slot, rebuild every row's `TUI_LABELS` (so the active green bracket moves), refresh banner. STOP exporting `TK_MCP_SCOPE` (D-18):

        mcp_toggle_scope() {
            # Phase 39 (TUI-SCOPE-03): repurposed from "flip TK_MCP_SCOPE
            # between user/local" (Phase 37) to "set ALL visible MCP rows to
            # the next scope value" (D-09/D-10). 3-state cycle: user →
            # project → local → user (matches per-row Tab cycle in
            # mcp_cycle_row_scope). Module-local _MCP_SETALL_SCOPE is the
            # source of truth for the banner; per-row state is mirrored into
            # MCP_SELECTED_SCOPE[]. TK_MCP_SCOPE is NOT exported here —
            # env-var is now strictly per-call (D-18, Phase 38 contract).
            case "${_MCP_SETALL_SCOPE:-user}" in
                user)    _MCP_SETALL_SCOPE="project" ;;
                project) _MCP_SETALL_SCOPE="local" ;;
                local)   _MCP_SETALL_SCOPE="user" ;;
                *)       _MCP_SETALL_SCOPE="user" ;;
            esac

            # Write the new value into every per-row slot (D-12: explicit
            # overwrite of any per-row Tab tweaks — by user request).
            local _len="${#MCP_SELECTED_SCOPE[@]}"
            local _j
            for ((_j=0; _j<_len; _j++)); do
                MCP_SELECTED_SCOPE[$_j]="$_MCP_SETALL_SCOPE"
            done

            # Rebuild every row's label so the active green bracket follows
            # the new scope. Mirrors the per-row label rebuild in
            # mcp_cycle_row_scope (Plan 01) but applies to all rows, not
            # just FOCUS_IDX.
            local _c_scope_active="" _c_nc="" _c_y=""
            if [ -t 1 ] && [ -z "${NO_COLOR+x}" ]; then
                _c_scope_active=$'\033[0;32m'
                _c_nc=$'\033[0m'
                _c_y=$'\033[1;33m'
            fi
            local _g_bang="!"
            for ((_j=0; _j<_len; _j++)); do
                local _mcp_idx="${TUI_TO_MCP_IDX[$_j]:-0}"
                local _scope_glyph
                _scope_glyph=$(_mcp_render_scope_glyph "$_MCP_SETALL_SCOPE")
                local _name_part
                if [[ "${MCP_UNOFFICIAL[$_mcp_idx]:-0}" == "1" ]]; then
                    if [[ -n "$_c_y" ]]; then
                        _name_part="${_c_y}${_g_bang}${_c_nc} ${MCP_DISPLAY[$_mcp_idx]}"
                    else
                        _name_part="[!] ${MCP_DISPLAY[$_mcp_idx]}"
                    fi
                else
                    _name_part="${MCP_DISPLAY[$_mcp_idx]}"
                fi
                TUI_LABELS[$_j]="${_scope_glyph} ${_name_part}"
            done

            mcp_render_scope_header
        }

    **Bash 3.2 invariants:**
    - C-style `for ((...))` loop is Bash 3.0+ (works on macOS BSD bash 3.2).
    - Plain indexed array indexing is fine.
    - `${#MCP_SELECTED_SCOPE[@]}` is fine on 3.2.
    - No associative arrays, no mapfile, no `${var,,}`.

    **Threat mitigation:**
    - T1 (wrong-scope dispatch from stale TK_MCP_SCOPE): the function STOPS exporting `TK_MCP_SCOPE` (D-18 enforced). The dispatcher in Task 2 becomes the single writer of `TK_MCP_SCOPE`, eliminating the stale-global threat at the source.
    - T2 (CLI-only rows): the for-loop iterates 0 to `${#MCP_SELECTED_SCOPE[@]}` exclusive. CLI-only entries have no slot in `MCP_SELECTED_SCOPE` (Plan 01 index parity), so they are NOT overwritten. Index parity is the contract; verified by acceptance criteria.
    - T5 (glyph drift): `_mcp_render_scope_glyph` (Plan 01) is the single source of truth for the bracket render — `mcp_toggle_scope` calls the same helper that `mcp_status_array` calls, so set-all and initial render agree byte-for-byte.
  </action>
  <verify>
    <automated>bash scripts/tests/test-mcp-selector.sh; bash scripts/tests/test-mcp-wizard.sh; make shellcheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_MCP_SETALL_SCOPE" scripts/lib/mcp.sh` returns at least 4 matches (init line, banner read, toggle write loop, comments).
    - `grep -nE "Set all to:" scripts/lib/mcp.sh` returns at least 2 matches (color and plain branches of mcp_render_scope_header).
    - `grep -cE "press s to cycle" scripts/lib/mcp.sh` is at least 2.
    - `grep -nE "for \(\(_j=0" scripts/lib/mcp.sh` returns at least 2 matches inside `mcp_toggle_scope` (one per write loop: array values + label rebuild).
    - The function `mcp_toggle_scope` no longer contains the literal string `export TK_MCP_SCOPE` (D-18) — verified by reading the function body.
    - The Phase 37 banner copy `Scope: ◉` no longer appears anywhere in `scripts/lib/mcp.sh`.
    - `bash -n scripts/lib/mcp.sh` exits 0.
    - `make shellcheck` exits 0.
    - Smoke test: 3 calls to `mcp_toggle_scope` returns `_MCP_SETALL_SCOPE` to its starting value.
    - Smoke test: after `mcp_toggle_scope`, every `MCP_SELECTED_SCOPE` slot equals `_MCP_SETALL_SCOPE` (uniformity invariant — D-09/D-12).
  </acceptance_criteria>
  <done>
    `mcp_toggle_scope` is a 3-state set-all cycle that writes every `MCP_SELECTED_SCOPE` slot, rebuilds every row's label, refreshes the banner copy to `Set all to: <bracket> · press s to cycle`, and does NOT export `TK_MCP_SCOPE`. Module-local `_MCP_SETALL_SCOPE` tracks the pending global value. Banner is NO_COLOR-aware. All baselines stay green; shellcheck clean; Bash 3.2 compatible.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire TUI_ROW_KEY/FN at TUI launch and per-row TK_MCP_SCOPE export in dispatch loop</name>
  <files>scripts/install.sh</files>
  <read_first>
    - scripts/install.sh:382-479 (TUI launch block + scope wiring)
    - scripts/install.sh:455-471 (current Phase 37 wiring — modification target for TUI_ROW_KEY/FN add)
    - scripts/install.sh:517-614 (dispatch loop — per-row export site at line 611)
    - scripts/install.sh:591-607 (reinstall path — auto-picks per-row scope, no edit needed)
    - scripts/install.sh:801-820 (post-install summary 4-field tuple reader — Phase 38 contract preserved)
    - scripts/lib/mcp.sh::mcp_cycle_row_scope (Plan 01 — wired here)
    - scripts/lib/mcp.sh::mcp_toggle_scope (Task 1 — repurposed; called via TUI_HEADER_FN)
    - .planning/phases/39-tui-per-row-scope-toggle/39-CONTEXT.md (D-17, D-18, D-19)
    - .planning/phases/39-tui-per-row-scope-toggle/39-PATTERNS.md "Per-row export site"
  </read_first>
  <behavior>
    - Test 1 (TUI launch wiring): after install.sh sets `TUI_ROW_KEY` to the Tab byte and `TUI_ROW_FN="mcp_cycle_row_scope"`, the call to `tui_checklist` has both globals visible — pressing Tab on a focused row mutates `MCP_SELECTED_SCOPE`.
    - Test 2 (TUI launch unset): on cancel path AND on success path, `TUI_ROW_KEY` and `TUI_ROW_FN` are unset alongside `TUI_HEADER_*` so the next `tui_checklist` invocation (e.g., for the skills picker) does not inherit MCP-specific bindings.
    - Test 3 (TUI-SCOPE-05 happy path — user scope): with `MCP_SELECTED_SCOPE[0]="user"`, the dispatcher's `mcp_wizard_run` invocation receives `TK_MCP_SCOPE=user`. Verified by mock claude or fake `mcp_wizard_run` capturing the env at call time.
    - Test 4 (TUI-SCOPE-05 happy path — project scope): with `MCP_SELECTED_SCOPE[0]="project"`, the wizard receives `TK_MCP_SCOPE=project`. Phase 38's wizard then routes through the project-scope branch.
    - Test 5 (TUI-SCOPE-05 mixed-scope dispatch): `MCP_SELECTED_SCOPE=(user project user)` for three selected rows produces three distinct `TK_MCP_SCOPE` values across three wizard invocations — no cross-iteration contamination.
    - Test 6 (CLI flag preserved): `--mcp-scope user` exported pre-loop (line 459) is still honored when no TUI runs (e.g., headless `--yes` path bypasses TUI but still iterates dispatch). The per-row export at the new insertion point picks up `MCP_SELECTED_SCOPE` populated by `mcp_status_array` even in headless mode.
    - Test 7 (Phase 38 reinstall path auto-picks scope): in reinstall scenarios, `_scope_for_rm` reads the row-correct scope because the per-row export was already set immediately above the reinstall check. No second edit needed in the reinstall block.
    - Test 8 (subshell isolation): the wizard runs in a subshell at line 611. The pre-export must be set in the parent shell BEFORE the subshell starts so the child inherits it.
  </behavior>
  <action>
    Edit `scripts/install.sh`. Two surgical changes.

    **Step 1 — TUI launch wiring (lines 455-471)**

    Locate the existing block at lines 455-471. Modify it to add `TUI_ROW_KEY`/`TUI_ROW_FN` alongside the existing `TUI_HEADER_KEY`/`TUI_HEADER_FN` bindings.

    Before:

        # Phase 37: wire up scope toggle banner. `s` cycles user ↔ local;
        # ...
        TK_MCP_SCOPE="${TK_MCP_SCOPE:-user}"
        export TK_MCP_SCOPE
        mcp_render_scope_header
        # shellcheck disable=SC2034  # consumed by tui.sh _tui_render via TUI_HEADER_KEY/FN
        TUI_HEADER_KEY="s"
        # shellcheck disable=SC2034
        TUI_HEADER_FN="mcp_toggle_scope"
        if ! tui_checklist; then
            unset TUI_HEADER_TEXT TUI_HEADER_KEY TUI_HEADER_FN
            echo "MCP install cancelled."
            exit 0
        fi
        unset TUI_HEADER_TEXT TUI_HEADER_KEY TUI_HEADER_FN

    After:

        # Phase 37 (preserved for CLI --mcp-scope) + Phase 39 (TUI-SCOPE-03/05):
        # The pre-loop TK_MCP_SCOPE export is kept ONLY so the v4.9 CLI
        # flag --mcp-scope still wins for non-interactive scripted use.
        # The TUI dispatcher loop overwrites TK_MCP_SCOPE per-iteration
        # from MCP_SELECTED_SCOPE[$tui_i] (around line 587+) so this
        # initial export is effectively replaced once any TUI wizard runs.
        TK_MCP_SCOPE="${TK_MCP_SCOPE:-user}"
        export TK_MCP_SCOPE
        # Phase 39 (banner cosmetics): seed the set-all banner from CLI
        # --mcp-scope so the initial render reflects the user's CLI choice.
        # Per-row MCP_SELECTED_SCOPE[] still wins for actual dispatch
        # (D-13/D-15) — this assignment is purely banner display state.
        _MCP_SETALL_SCOPE="${TK_MCP_SCOPE:-user}"
        mcp_render_scope_header
        # shellcheck disable=SC2034  # consumed by tui.sh _tui_render via TUI_HEADER_KEY/FN
        TUI_HEADER_KEY="s"
        # shellcheck disable=SC2034
        TUI_HEADER_FN="mcp_toggle_scope"
        # Phase 39 (TUI-SCOPE-02): per-row scope hotkey wiring. Tab byte
        # ($'\t') dispatches mcp_cycle_row_scope to mutate the focused
        # row's MCP_SELECTED_SCOPE slot. Mirrors TUI_HEADER_KEY/FN shape.
        # shellcheck disable=SC2034  # consumed by tui.sh _tui_render via TUI_ROW_KEY/FN
        TUI_ROW_KEY=$'\t'
        # shellcheck disable=SC2034
        TUI_ROW_FN="mcp_cycle_row_scope"
        if ! tui_checklist; then
            unset TUI_HEADER_TEXT TUI_HEADER_KEY TUI_HEADER_FN \
                  TUI_ROW_KEY TUI_ROW_FN
            echo "MCP install cancelled."
            exit 0
        fi
        unset TUI_HEADER_TEXT TUI_HEADER_KEY TUI_HEADER_FN \
              TUI_ROW_KEY TUI_ROW_FN

    **Step 2 — per-row TK_MCP_SCOPE export in dispatch loop**

    The reinstall block (lines 591-607) reads `_scope_for_rm="${TK_MCP_SCOPE:-user}"`, so the export must come BEFORE the reinstall block, NOT between reinstall and wizard.

    Locate inside the `for ((tui_i=0; tui_i<local_mcp_count; tui_i++))` loop (lines 522-614), specifically the segment AFTER the unofficial confirm gate (line 564) and AFTER the `if [[ "$CLI_ONLY" -eq 1 ]] then ... else` opening (line 569 / 574), at the point where `local_flags` is built. Insert IMMEDIATELY after `local_flags=()` and the dry-run flag append (lines 581-583), and BEFORE the `# Phase 36-A: reinstall path` comment (line 584):

    Before (illustrative — actual lines 581-591):

        local_flags=()
        [[ "$DRY_RUN" -eq 1 ]] && local_flags+=("--dry-run")

        # Phase 36-A: reinstall path — TUI_INSTALLED[tui_i]=1 AND
        # TUI_RESULTS[tui_i]=1 means the user toggled the row from
        # `[installed ✓]` to `[reinstall ↻]`. ...
        local_reinstall=0
        if [[ "${TUI_INSTALLED[$tui_i]:-0}" -eq 1 ]]; then

    After:

        local_flags=()
        [[ "$DRY_RUN" -eq 1 ]] && local_flags+=("--dry-run")

        # Phase 39 (TUI-SCOPE-05 D-17): export per-row scope for THIS
        # iteration's mcp_wizard_run invocation. The pre-loop export
        # at line 459 set the CLI --mcp-scope value or "user" default;
        # we overwrite per-iteration so each MCP gets its own scope
        # (TUI Tab/`s` mutations land in MCP_SELECTED_SCOPE — read
        # here as the source of truth). Index basis is $tui_i (TUI
        # render order — parallel to MCP_SELECTED_SCOPE), NOT $i (the
        # MCP_NAMES alpha index — wrong frame). Fallback to "user"
        # when MCP_SELECTED_SCOPE is unset/empty (defensive: pre-v5.0
        # callers, headless scripts before mcp_status_array runs).
        # Reinstall block immediately below reads _scope_for_rm via
        # ${TK_MCP_SCOPE:-user} so this export drives BOTH the
        # `claude mcp remove --scope X` step and the wizard call.
        TK_MCP_SCOPE="${MCP_SELECTED_SCOPE[$tui_i]:-user}"
        export TK_MCP_SCOPE

        # Phase 36-A: reinstall path — TUI_INSTALLED[tui_i]=1 AND
        # TUI_RESULTS[tui_i]=1 means the user toggled the row from
        # `[installed ✓]` to `[reinstall ↻]`. ...
        local_reinstall=0
        if [[ "${TUI_INSTALLED[$tui_i]:-0}" -eq 1 ]]; then

    **Step 3 — defensive: ensure dispatcher does not break when MCP_SELECTED_SCOPE is empty**

    The headless `--yes` path bypasses the TUI but still iterates dispatch (the loop at line 522 runs regardless). In that path, `mcp_status_array` is still called earlier (around line 405), so `MCP_SELECTED_SCOPE` is populated. If for any reason it is not (pre-v5.0 caller, test harness, edge case), the `${MCP_SELECTED_SCOPE[$tui_i]:-user}` fallback covers it. Defensive only — should not be a hot path.

    **Bash 3.2 invariants:**
    - `$'\t'` literal Tab byte — works on macOS BSD bash 3.2 (Bash 2+).
    - Array indexing with default fallback `${MCP_SELECTED_SCOPE[$tui_i]:-user}` is fine.
    - `unset VAR1 VAR2 VAR3` multi-arg form is POSIX, works on bash 3.2.
    - The export is in the parent shell; the subshell that runs `mcp_wizard_run` inherits the env.

    **Threat mitigation:**
    - T1 (wrong-scope dispatch — wizard receives stale value): the per-iteration export at the new insertion point is the AUTHORITATIVE writer of `TK_MCP_SCOPE` for the TUI dispatcher. Combined with Task 1's removal of the export inside `mcp_toggle_scope`, exactly ONE place writes `TK_MCP_SCOPE` in the install.sh→wizard hot path: this line. Eliminates the stale-value class of bug.
    - T2 (CLI-only rows getting scope dispatch): the for-loop iterates `tui_i` over `${#TUI_LABELS[@]}` which equals `${#MCP_SELECTED_SCOPE[@]}` per Plan 01's parity invariant. Every iteration corresponds to an MCP row — CLI-only entries are not in `TUI_LABELS` at all (they are tracked separately via `RESULT_*` arrays through a different code path). The export only fires for MCP rows.
    - T5 (drift between TUI render and dispatch): the dispatcher reads `MCP_SELECTED_SCOPE` — the SAME array the TUI render wrote and the user mutated via Tab/`s`. Single source of truth; no parallel state to drift.
  </action>
  <verify>
    <automated>bash -n scripts/install.sh; bash scripts/tests/test-mcp-selector.sh; bash scripts/tests/test-mcp-wizard.sh; make shellcheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "TUI_ROW_KEY" scripts/install.sh` is at least 4 (declaration, fn-name comment, two unset lists).
    - `grep -cE "TUI_ROW_FN" scripts/install.sh` is at least 4.
    - `grep -nE "MCP_SELECTED_SCOPE\[\\\$tui_i\]" scripts/install.sh` returns exactly 1 match (the per-row export site inside the dispatch loop).
    - `grep -nE 'mcp_cycle_row_scope' scripts/install.sh` returns at least 1 match (TUI_ROW_FN binding).
    - The new per-row export comes BEFORE `# Phase 36-A: reinstall path` in the source file (verified by reading line numbers; required so reinstall step picks up the row-correct scope).
    - The Phase 37 pre-loop `TK_MCP_SCOPE="${TK_MCP_SCOPE:-user}"` export at line 459 is preserved (CLI `--mcp-scope` honor — D-18).
    - `bash -n scripts/install.sh` exits 0.
    - `make shellcheck` exits 0.
    - End-to-end smoke (manual or test harness): with mock `claude` capturing argv+env, set `MCP_SELECTED_SCOPE[0]="project"` before launching `bash scripts/install.sh --mcps --yes` and assert the captured `TK_MCP_SCOPE=project` for that row's invocation. (Plan 03 codifies this as a hermetic test.)
    - Phase 38 baselines remain green: test-mcp-wizard.sh PASS=53, test-mcp-secrets.sh PASS=11, test-project-secrets.sh PASS=42.
    - Phase 39 selector test still PASS=21 floor (Plan 03 will extend in wave 3).
  </acceptance_criteria>
  <done>
    `install.sh` binds `TUI_ROW_KEY` to Tab and `TUI_ROW_FN` to `mcp_cycle_row_scope` at TUI launch (alongside the Phase 37 `TUI_HEADER_*` wiring); the dispatch loop exports `TK_MCP_SCOPE` per iteration from `MCP_SELECTED_SCOPE[$tui_i]` BEFORE the reinstall block so both reinstall and wizard call see the row-correct scope; the unset list on cancel/success covers the new globals; CLI `--mcp-scope` still honored for non-interactive scripted use; all Phase 38 baselines green; shellcheck clean; Bash 3.2 compatible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI keypress (`s`) → `mcp_toggle_scope` (Task 1) | untrusted byte routed via tui.sh case-match catch-all; only the configured `TUI_HEADER_KEY` byte reaches the function |
| `MCP_SELECTED_SCOPE[$tui_i]` → `TK_MCP_SCOPE` env-var (Task 2) | shell-local read; values are constrained to {user, project, local} by the only writers (Plan 01 init + Plan 02 mutators) |
| install.sh parent shell → `mcp_wizard_run` subshell | env-var inheritance across `( ... )` boundary; values exported BEFORE subshell starts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering (T1 from spec) | stale `TK_MCP_SCOPE` shell-global contaminates next wizard call | mitigate | Per-iteration export at the insertion point in install.sh dispatch loop overwrites the variable on every loop turn. Task 1 also removes the `export TK_MCP_SCOPE` from `mcp_toggle_scope` so the dispatcher is the single writer. Combined: exactly one writer of `TK_MCP_SCOPE` in the TUI hot path. |
| T-39-07 | Spoofing (T2 from spec) | CLI-only row receives dispatch (no MCP installed but wizard called) | mitigate | The dispatch for-loop iterates `tui_i` over `${#TUI_LABELS[@]}`. CLI-only rows (no `components.mcp.<name>` block) never enter `TUI_LABELS` (Plan 01 invariant). Index parity ensures `MCP_SELECTED_SCOPE[$tui_i]` exists for every loop iteration; the `:-user` fallback covers any unexpected gap defensively. |
| T-39-08 | Tampering | `_MCP_SETALL_SCOPE` value escaping {user, project, local} via untrusted input | mitigate | The variable is only written by `mcp_toggle_scope` (Task 1) using a fixed `case` block with explicit values. No external input reaches it. Default branch falls through to "user" — fail-safe. |
| T-39-09 | Information Disclosure | banner/footer copy reveals scope value | accept | Scope letter is not secret — it advertises which `claude mcp add --scope <s>` will run. Visible to anyone shoulder-surfing the install. |
| T-39-10 | Denial of Service | `s` mash spam on a populated catalog (20 rows) | accept | One `mcp_toggle_scope` call rebuilds 20 labels in pure shell — O(n) with small n, no I/O, no fork. User can mash `s` without resource impact. |
</threat_model>

<verification>
- `bash -n scripts/lib/mcp.sh` and `bash -n scripts/install.sh` both exit 0.
- `make shellcheck` exits 0 (no new warnings on either file).
- `mcp_toggle_scope` smoke (manual or bash one-liner): three calls return `_MCP_SETALL_SCOPE` to its starting value; after each call every `MCP_SELECTED_SCOPE` slot equals `_MCP_SETALL_SCOPE`.
- `TUI_HEADER_TEXT` smoke: after a `mcp_toggle_scope` call, the value contains `Set all to:` and one of `[U]`, `[P]`, `[L]`.
- The literal `export TK_MCP_SCOPE` no longer appears inside the `mcp_toggle_scope` function body (D-18 enforced — the only `export TK_MCP_SCOPE` lines should be at install.sh:460 (CLI default preserve) and the new per-row export inside the dispatch loop).
- All Phase 38 baselines green:
  - `bash scripts/tests/test-mcp-wizard.sh` PASS=53.
  - `bash scripts/tests/test-mcp-secrets.sh` PASS=11.
  - `bash scripts/tests/test-project-secrets.sh` PASS=42.
- Phase 39 selector test still PASS=21 floor (Plan 03 extends in wave 3).
</verification>

<success_criteria>
- TUI-SCOPE-03 set-all wired: `mcp_toggle_scope` cycles `_MCP_SETALL_SCOPE` user → project → local → user, writes the new value to every `MCP_SELECTED_SCOPE` slot, rebuilds every row's `TUI_LABELS`, and refreshes the banner via `mcp_render_scope_header`.
- TUI-SCOPE-03 banner copy: rendered banner reads `Set all to: <bracket> · press s to cycle` (D-11), color-aware, ≤80-col.
- TUI-SCOPE-05 dispatcher wired: install.sh exports `TK_MCP_SCOPE` per iteration from `MCP_SELECTED_SCOPE[$tui_i]` BEFORE the reinstall + wizard blocks; CLI `--mcp-scope` flag preserved for non-interactive scripted use (D-18).
- TUI launch wiring: `TUI_ROW_KEY=$'\t'` and `TUI_ROW_FN="mcp_cycle_row_scope"` set alongside `TUI_HEADER_*`; both unset on cancel + success paths.
- D-18 enforced: `mcp_toggle_scope` no longer exports `TK_MCP_SCOPE`. The dispatcher in install.sh is the single writer of `TK_MCP_SCOPE` for the TUI hot path.
- All Phase 38 baselines green; Phase 39 selector test still PASS=21 floor; shellcheck clean; Bash 3.2 compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tui-per-row-scope-toggle/39-02-SUMMARY.md` documenting:
- Files modified with line ranges (`scripts/lib/mcp.sh`, `scripts/install.sh`).
- Functions repurposed: `mcp_render_scope_header`, `mcp_toggle_scope`.
- New module-local var: `_MCP_SETALL_SCOPE`.
- New install.sh wiring: `TUI_ROW_KEY=$'\t'` + `TUI_ROW_FN="mcp_cycle_row_scope"` + per-row TK_MCP_SCOPE export.
- D-18 enforcement: `export TK_MCP_SCOPE` removed from `mcp_toggle_scope`; install.sh dispatcher is sole writer.
- Verified: shellcheck clean, all Phase 38 baselines green, Phase 39 selector PASS=21 floor preserved.
- Any deviations from the plan (e.g., if the Phase 37 banner string had to be hunted in additional files).
</output>
