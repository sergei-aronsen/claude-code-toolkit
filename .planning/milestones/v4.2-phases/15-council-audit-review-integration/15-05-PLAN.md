---
phase: 15-council-audit-review-integration
plan: 05
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-04"
files_modified:
  - commands/council.md
autonomous: true
requirements:
  - COUNCIL-01
must_haves:
  truths:
    - "commands/council.md contains a new '## Modes' H2 section documenting both validate-plan (existing default) and audit-review (new) modes (D-14)"
    - "The audit-review mode subsection states the invocation syntax `/council audit-review --report <path-to-audit-report>` (D-02 / D-14)"
    - "The audit-review mode subsection documents the produced output: per-finding verdict table (REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT), missed-findings section, in-place Council verdict slot rewrite (COUNCIL-03 / COUNCIL-04 / D-14)"
    - "The audit-review mode subsection states when to use it: after every /audit run (Phase 5 of the audit workflow — mandatory) (COUNCIL-01)"
    - "The audit-review mode subsection links to the prompt SOT at `scripts/council/prompts/audit-review.md` (D-14)"
    - "The validate-plan mode subsection preserves the existing behavior description: PROCEED / SIMPLIFY / RETHINK / SKIP verdict scheme (no regression)"
    - "Net line addition is ≤ 60 lines (D-14 — file is currently 144 lines, target ≤ 210)"
    - "File passes markdownlint after the edit"
    - "Existing sections (Purpose, Usage, When to Use, Prerequisites, Process, Iron Rules, Output Format, Integration) remain unchanged — the edit is purely additive"
  artifacts:
    - path: "commands/council.md"
      provides: "User-facing /council command with documented Modes section (validate-plan + audit-review)"
      contains: "## Modes"
      min_lines: 170
  key_links:
    - from: "commands/council.md"
      to: "scripts/council/prompts/audit-review.md"
      via: "audit-review mode references the prompt template by path"
      pattern: "scripts/council/prompts/audit-review\\.md"
    - from: "commands/council.md"
      to: "scripts/council/brain.py"
      via: "audit-review mode invokes brain.py --mode audit-review --report <path>"
      pattern: "/council audit-review"
---

<objective>
Add a new `## Modes` H2 section to `commands/council.md` documenting the two Council modes: `validate-plan` (existing default) and `audit-review` (new in Phase 15). Net addition ≤ 60 lines (D-14). The file is currently 144 lines; target ≤ 210 lines post-edit.

Purpose: Surface the new `/council audit-review --report <path>` invocation as a user-facing command. The Modes section is the single place that documents both modes side-by-side — invocation syntax, what each mode produces, when to use it, and where to find the prompt.

Output: One file modified — `commands/council.md`. The edit is purely additive: a new `## Modes` H2 section inserted after the existing `## Usage` section. No other sections touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-council-audit-review-integration/15-CONTEXT.md
@.planning/phases/15-council-audit-review-integration/15-RESEARCH.md
@.planning/phases/15-council-audit-review-integration/15-PATTERNS.md
@.planning/phases/15-council-audit-review-integration/15-01-PLAN.md
@.planning/phases/15-council-audit-review-integration/15-04-PLAN.md
@commands/council.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing structure of commands/council.md (read-confirmed, 144 lines):
     Lines 1-7:    # /council header + ## Purpose
     Lines 9-21:   ## Usage (with /council <feature description> codeblock + 3 examples)
     Lines 23-36:  ## When to Use (table)
     Lines 38-50:  ## Prerequisites
     Lines 52-95:  ## Process (Step 1 Create Plan / Step 2 Run Council / Step 3 Read Report / Step 4 Report to User)
     Lines 97-110: ## Iron Rules
     Lines 112-133:## Output Format
     Lines 135-145:## Integration

     Insertion target: insert ## Modes section between ## Usage (line 21) and ## When to Use (line 23).
     Use a horizontal rule (`---`) above the new section to match the existing style.
-->

<!-- Pattern from 15-PATTERNS.md `commands/council.md` self-extension block:
     ## Modes

     ### validate-plan (default)

     **Invocation:** `/council <feature description>`
     **Produces:** Per-reviewer assessment + final consolidated verdict (PROCEED / SIMPLIFY / RETHINK / SKIP).
     **When to use:** Before implementing any non-trivial feature or architectural change.
     **Prompt:** Built into `brain.py` (`GEMINI_SYSTEM` / `GPT_SYSTEM` constants).

     ---

     ### audit-review

     **Invocation:** `/council audit-review --report <path-to-audit-report>`
     **Produces:** Per-finding verdict table (REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT),
       missed-findings section, in-place Council verdict slot rewrite in the report.
     **When to use:** After every `/audit` run (Phase 5 — mandatory). The audit run is incomplete until Council returns.
     **Prompt:** `scripts/council/prompts/audit-review.md`
-->

<!-- markdownlint rules to honor (.markdownlint.json):
     MD040 — every fenced code block declares a language (use `text` for plain text)
     MD031 — blank line before/after every fenced code block
     MD032 — blank line before/after every list
     MD026 — no trailing punctuation in headings (no trailing colons in H2/H3)
     MD024 — siblings_only=true (duplicate H3 names allowed under different H2s)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert ## Modes section into commands/council.md</name>
  <files>commands/council.md</files>
  <read_first>
    - commands/council.md (read full file — confirm Usage section ends at line 21, When to Use H2 starts at line 23; insertion site is between them)
    - .planning/phases/15-council-audit-review-integration/15-PATTERNS.md (`commands/council.md` self-extension block — verbatim section pattern)
    - .planning/phases/15-council-audit-review-integration/15-CONTEXT.md D-14 (≤ 60 net lines, target ≤ 210 total)
    - .markdownlint.json (lint rules)
  </read_first>
  <action>
Modify ONLY the area between the existing `## Usage` section (lines 9-21) and `## When to Use` H2 (line 23). Insert a new `## Modes` H2 section. Do NOT modify any other section in the file.

**Before edit (lines 21-23 of current file):**

```markdown
- `/council implement role-based permissions`

---

## When to Use
```

**After edit (insert ## Modes between the existing `---` and `## When to Use`):**

```markdown
- `/council implement role-based permissions`

---

## Modes

The Council orchestrator (`scripts/council/brain.py`) supports two modes. Pick the one that matches your task.

### validate-plan (default)

**Invocation:**

```text
/council <feature description>
```

**Produces:** Per-reviewer assessment (Problem Assessment, Simplicity Check, Concerns) plus a final consolidated verdict — `PROCEED / SIMPLIFY / RETHINK / SKIP`.

**When to use:** Before implementing any non-trivial feature or architectural change. Run this BEFORE writing code so the Council's verdict influences scope.

**Prompt:** Built into `brain.py` (`GEMINI_SYSTEM` and `GPT_SYSTEM` constants in `scripts/council/brain.py`).

**Output:** `.claude/scratchpad/council-report.md`

---

### audit-review

**Invocation:**

```text
/council audit-review --report <path-to-audit-report>
```

**Produces:** Per-finding verdict table with columns `| ID | verdict | confidence | justification |`. Each row is one of `REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT`. Plus a `## Missed findings` section listing real issues visible in the embedded code blocks that the auditor did not report. Plus in-place rewrite of the report's `## Council verdict` slot and the YAML `council_pass:` frontmatter key (mutates `pending` to `passed`, `failed`, or `disputed`).

**When to use:** After every `/audit` run (Phase 5 of the audit workflow — mandatory). The audit run is incomplete until Council returns. There is no `--no-council` flag in v4.2.

**Constraints:** The Council MUST NOT reclassify severity (COUNCIL-02). Severity stays with the auditor. Disagreements between Gemini and ChatGPT are flagged `disputed` with confidence `min(g_conf, c_conf)` and surfaced to the user without auto-resolution (COUNCIL-06).

**Prompt:** `scripts/council/prompts/audit-review.md`

**Output:** Mutates the input report file in place; prints a collated stdout summary.

---

## When to Use
```

**Editing constraints:**

- Insert exactly between the closing `---` of the Usage block and the existing `## When to Use` H2. Do NOT delete or modify the existing `---` separator that already exists between Usage and When to Use; insert your new content BEFORE that separator (so the Modes section ends with its own `---` and the original separator continues to introduce When to Use).
- Each fenced code block uses `text` language tag (MD040).
- Blank line before and after every fenced code block (MD031).
- Blank line before and after every list (MD032).
- No trailing punctuation on H2/H3 headings (MD026 — no trailing colons or periods on heading lines).
- The H3 names `validate-plan (default)` and `audit-review` are unique siblings within `## Modes` (MD024 sibling check passes).

**Existing sections to preserve unchanged:**

- `# /council — Multi-AI Plan Validation (Supreme Council)` (H1)
- `## Purpose`
- `## Usage` and its 3 examples
- `## When to Use` (table)
- `## Prerequisites` (install command + check command)
- `## Process` (Step 1-4)
- `## Iron Rules`
- `## Output Format`
- `## Integration`

After editing:

1. Run `npx markdownlint-cli commands/council.md` — must exit 0
2. Confirm line count: `wc -l commands/council.md` should return 170-210 (was 144; +26 to +66 lines is the acceptable band; D-14 cap is +60 net so target ~+50)
3. Confirm the existing `## When to Use` heading is still present and immediately follows the new Modes section's closing `---`
4. Confirm all existing examples in Usage are preserved (`grep -qF '/council add OAuth login with Google' commands/council.md` etc.)
  </action>
  <verify>
    <automated>npx markdownlint-cli commands/council.md && grep -qF '## Modes' commands/council.md && grep -qF '### validate-plan (default)' commands/council.md && grep -qF '### audit-review' commands/council.md && grep -qF '/council audit-review --report' commands/council.md && grep -qF 'scripts/council/prompts/audit-review.md' commands/council.md && grep -qF 'PROCEED / SIMPLIFY / RETHINK / SKIP' commands/council.md && grep -qF '| ID | verdict | confidence | justification |' commands/council.md && grep -qF 'REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT' commands/council.md && grep -qF 'Phase 5 of the audit workflow' commands/council.md && wc -l commands/council.md | awk '{ if ($1 < 170 || $1 > 210) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `npx markdownlint-cli commands/council.md` exits 0 (lint clean)
    - `grep -qF '## Modes' commands/council.md` matches (new H2 present)
    - `grep -qF '### validate-plan (default)' commands/council.md` matches (existing-mode subsection)
    - `grep -qF '### audit-review' commands/council.md` matches (new-mode subsection)
    - `grep -qF '/council audit-review --report' commands/council.md` matches (invocation syntax for new mode)
    - `grep -qF 'scripts/council/prompts/audit-review.md' commands/council.md` matches (prompt SOT linked per D-14)
    - `grep -qF 'PROCEED / SIMPLIFY / RETHINK / SKIP' commands/council.md` matches (validate-plan verdict scheme preserved)
    - `grep -qF '| ID | verdict | confidence | justification |' commands/council.md` matches (audit-review verdict table contract documented per COUNCIL-03)
    - `grep -qF 'REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT' commands/council.md` matches (audit-review verdict values documented)
    - `grep -qF 'Missed findings' commands/council.md` matches (COUNCIL-04 reference)
    - `grep -qiF 'mandatory' commands/council.md && grep -qF 'Phase 5' commands/council.md` matches (COUNCIL-01 mandatory framing)
    - `grep -qiF 'no --no-council' commands/council.md || grep -qF 'no `--no-council`' commands/council.md` matches (explicit no-flag rule)
    - `grep -qF 'COUNCIL-02' commands/council.md && grep -qF 'COUNCIL-06' commands/council.md` matches (constraint references)
    - `grep -qF 'severity' commands/council.md && grep -qiF 'disputed' commands/council.md` matches (severity-stays-with-auditor + disputed semantics covered)
    - All existing sections preserved: `for h in '## Purpose' '## Usage' '## When to Use' '## Prerequisites' '## Process' '## Iron Rules' '## Output Format' '## Integration'; do grep -qF "$h" commands/council.md || exit 1; done`
    - `wc -l commands/council.md | awk '{print $1}'` returns N where 170 ≤ N ≤ 210 (D-14 — was 144; net addition ≤ 60)
    - `## When to Use` H2 still appears AFTER `## Modes`: `awk '/^## Modes/{m=1} /^## When to Use/{ if (m) print "ok" }' commands/council.md | head -1` prints "ok"
    - Original Usage examples preserved: `grep -qF '/council add OAuth login with Google' commands/council.md && grep -qF '/council refactor payment service' commands/council.md && grep -qF '/council implement role-based permissions' commands/council.md`
  </acceptance_criteria>
  <done>
    `commands/council.md` now contains a `## Modes` H2 section between `## Usage` and `## When to Use`. The section has two H3 subsections: `### validate-plan (default)` (preserves existing behavior with PROCEED/SIMPLIFY/RETHINK/SKIP scheme) and `### audit-review` (new mode with `/council audit-review --report <path>` invocation, REAL/FALSE_POSITIVE/NEEDS_MORE_CONTEXT verdicts, byte-exact column header `| ID | verdict | confidence | justification |`, COUNCIL-01/02/06 constraints documented, link to `scripts/council/prompts/audit-review.md` SOT). All existing sections (Purpose, Usage, When to Use, Prerequisites, Process, Iron Rules, Output Format, Integration) are preserved unchanged. File grows from 144 to 170-210 lines (≤ 60 net addition per D-14). Passes markdownlint.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commands/council.md -> markdownlint | Read-only by lint. No code execution. |
| commands/council.md -> /council invocation | Documentation file consumed by Claude at runtime when the user types `/council ...`. No untrusted input flows through the documentation itself; the documentation describes behavior `brain.py` implements (Plan 15-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering | Documentation drift weakens the documented audit-review contract (e.g., a future PR removes the `--report` flag mention or the prompt-file link) | mitigate | Plan 15-06's regression test asserts the byte-exact presence of: `## Modes` H2, `### audit-review` H3, `/council audit-review --report` invocation, `scripts/council/prompts/audit-review.md` prompt link, `| ID | verdict | confidence | justification |` column header, and the `REAL / FALSE_POSITIVE / NEEDS_MORE_CONTEXT` verdict scheme. Drift fails CI before merge. |
| T-15-17 | Information disclosure | Documentation accidentally reveals secret-handling details (API key formats, etc.) | accept | The new section documents only the user-facing CLI surface and the prompt path. No API keys, tokens, or secret material appears in `commands/council.md`. The existing `## Prerequisites` section (preserved) already references the install script via curl URL — that pattern is unchanged. |
</threat_model>

<verification>
- `npx markdownlint-cli commands/council.md` exits 0
- All acceptance_criteria pass
- The `## Modes` section is positioned between `## Usage` and `## When to Use`
- File line count grows from 144 to 170-210 (≤ 60 net addition per D-14)
- All byte-exact contract strings (column header, verdict values, invocation syntax, prompt path) are present
- Existing 8 H2 sections (Purpose, Usage, When to Use, Prerequisites, Process, Iron Rules, Output Format, Integration) remain intact
</verification>

<success_criteria>
- COUNCIL-01 surfaced via documentation: users see "Phase 5 of the audit workflow — mandatory" and the explicit no-`--no-council`-flag rule
- D-14 satisfied: `## Modes` section added, ≤ 60 lines net (target ~50), file ≤ 210 lines total
- D-02 documented: invocation syntax `/council audit-review --report <path-to-audit-report>` is verbatim in the new H3
- COUNCIL-03 surfaced: byte-exact column header `| ID | verdict | confidence | justification |` appears in the docs
- COUNCIL-04 surfaced: `## Missed findings` section behavior documented
- COUNCIL-06 surfaced: `disputed` semantics + `min(g_conf, c_conf)` documented
- D-17 honored: the audit-review prompt is referenced by path (`scripts/council/prompts/audit-review.md`); commands/council.md does NOT redefine the prompt's content
- File passes markdownlint
- No regression of existing sections
</success_criteria>

<output>
After completion, create `.planning/phases/15-council-audit-review-integration/15-05-SUMMARY.md`
</output>
