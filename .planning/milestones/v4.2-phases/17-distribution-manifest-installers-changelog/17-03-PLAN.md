---
phase: 17-distribution-manifest-installers-changelog
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - manifest.json
  - CHANGELOG.md
autonomous: false
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
threat_refs:
  - T-17-01
  - T-17-03
user_setup: []

must_haves:
  truths:
    - "commands/audit.md still carries `## 6-Phase Workflow` and `## Council Handoff` H2 headings (DIST-02 verify-only)"
    - "commands/council.md still carries `## Modes` H2 with an `### audit-review` subsection (DIST-02 verify-only)"
    - "CHANGELOG.md `[4.2.0]` heading no longer contains the literal `YYYY-MM-DD` placeholder — actual ship date is in place"
    - "manifest.json `updated:` field carries the same actual ship date"
    - "`make version-align` exits 0 (manifest version == CHANGELOG top-entry version == `init-local.sh --version`)"
    - "`make check && make test` exits 0 end-to-end (Test 16 matrix verifies fresh-install path per T-17-03)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[4.2.0] entry with real ship date"
      contains: "## [4.2.0] - 2026-"
    - path: "manifest.json"
      provides: "Real ship date in updated: field"
      contains: '"updated": "2026-'
  key_links:
    - from: "manifest.json `version`"
      to: "CHANGELOG.md `## [4.2.0]` heading + scripts/init-local.sh --version"
      via: "make version-align (D-06 gate)"
      pattern: "version-align"
    - from: "Phase 17 install changes"
      to: "Test 16 matrix"
      via: "make test (D-07 mitigation for T-17-03)"
      pattern: "test-matrix.sh"
---

<objective>
Close the v4.2.0 release: verify DIST-02 markers are still intact in `commands/audit.md` and `commands/council.md` (no edits — Phase 14/15 already delivered them), replace the `YYYY-MM-DD` placeholder in CHANGELOG.md and manifest.json with the real ship date, and run the full quality gate (`make check && make test`) end-to-end. This is the milestone-close commit.

Purpose: D-08 — ship date is set on the final commit so a slipped milestone never embeds a wrong date. D-06 — `make version-align` is the three-file alignment gate that exits 0 only when manifest.json + CHANGELOG.md + `init-local.sh --version` agree. D-03 — DIST-02 is verify-only; the planner does not re-write content that already shipped.

Output: a single milestone-close commit with the real ship date in two files plus a passing quality gate.

This plan is `autonomous: false` because the executor MUST pause at the date-stamping step to confirm the actual ship date with the user before writing it (the date depends on when the user runs the close commit; cannot be guessed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-distribution-manifest-installers-changelog/17-CONTEXT.md
@.planning/phases/17-distribution-manifest-installers-changelog/17-01-PLAN.md
@.planning/phases/17-distribution-manifest-installers-changelog/17-02-PLAN.md

# Files we'll mutate (date placeholder swap only)
@manifest.json
@CHANGELOG.md

# Files we'll verify (no edits)
@commands/audit.md
@commands/council.md
@scripts/init-local.sh
@Makefile

<interfaces>
DIST-02 markers already in place. Confirmed at Phase 17 plan time:
- commands/audit.md line 86: "## 6-Phase Workflow"
- commands/audit.md line 192: "## Council Handoff (Phase 15)"
- commands/council.md line 23: "## Modes"
- commands/council.md line 48: "### audit-review"

Plan 17-03 verifies these headings still exist; do NOT edit either file.

`make version-align` (Makefile:193-215) compares:
- `jq -r '.version' manifest.json`
- `grep -m1 '^## \[[0-9]' CHANGELOG.md | sed 's/.*\[\([^]]*\)\].*/\1/'`
- `bash scripts/init-local.sh --version 2>&1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1`

All three must equal "4.2.0" after this plan.

`scripts/init-local.sh` reads version from manifest.json at runtime (line 18: `VERSION=$(jq -r '.version' "$MANIFEST_FILE")`). No manual init-local.sh edit required: 17-01's manifest bump cascades automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DIST-02 markers intact in commands/audit.md (no edits)</name>
  <files>commands/audit.md</files>
  <action>VERIFY-ONLY task per D-03. `commands/audit.md` already documents the 6-phase `/audit` workflow (Phase 14 ship) and the Council Handoff UX (Phase 15 ship). The planner does NOT re-write or amend this file.

Run these checks:

1. `grep -n '^## 6-Phase Workflow' commands/audit.md` — must match.
2. `grep -n '^## Council Handoff' commands/audit.md` — must match.
3. `grep -cE '^### Phase [0-5]' commands/audit.md` — must return at least 6 (Phase 0 through Phase 5).
4. `grep -nF 'audit-skip' commands/audit.md` — at least one match (FALSE_POSITIVE nudge UX text).

If ANY check fails, the markers have drifted since Phase 14/15 — STOP and surface the regression to the user. Do NOT attempt to fix in this plan; that signals a Phase 14 or 15 regression that needs separate gap-closure.

Do NOT modify `commands/audit.md`. Expected `git diff commands/audit.md` is empty.</action>
  <verify>
    <automated>grep -q '^## 6-Phase Workflow' commands/audit.md &amp;&amp; grep -q '^## Council Handoff' commands/audit.md &amp;&amp; [ "$(grep -cE '^### Phase [0-5]' commands/audit.md)" -ge 6 ] &amp;&amp; grep -qF 'audit-skip' commands/audit.md &amp;&amp; [ -z "$(git diff --name-only commands/audit.md 2>/dev/null)" ]</automated>
  </verify>
  <done>All four marker checks pass; `git diff commands/audit.md` is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DIST-02 markers intact in commands/council.md (no edits)</name>
  <files>commands/council.md</files>
  <action>Same verify-only contract as Task 1, applied to `commands/council.md` per D-03. Phase 15 (plan 15-05) already added the `## Modes` H2 + `### audit-review` H3 subsection.

Run these checks:

1. `grep -n '^## Modes' commands/council.md` — must match.
2. `grep -n '^### audit-review' commands/council.md` — must match.
3. `grep -nF 'audit-review --report' commands/council.md` — at least one match (input format documentation per DIST-02).
4. `grep -qE 'REAL.*FALSE_POSITIVE|FALSE_POSITIVE.*REAL' commands/council.md` — verdict-table schema documented.
5. `grep -qF 'MUST NOT reclassify severity' commands/council.md` — COUNCIL-02 no-reclassify rule documented.

If any check fails, surface the regression and STOP — do NOT attempt to re-add the section here.

Do NOT modify `commands/council.md`. Expected `git diff commands/council.md` is empty.</action>
  <verify>
    <automated>grep -q '^## Modes' commands/council.md &amp;&amp; grep -q '^### audit-review' commands/council.md &amp;&amp; grep -qF 'audit-review --report' commands/council.md &amp;&amp; grep -qE 'REAL.*FALSE_POSITIVE|FALSE_POSITIVE.*REAL' commands/council.md &amp;&amp; [ -z "$(git diff --name-only commands/council.md 2>/dev/null)" ]</automated>
  </verify>
  <done>All five marker checks pass; `git diff commands/council.md` is empty.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 17-01 + 17-02 deliverables in place before date stamping</name>
  <files>manifest.json, CHANGELOG.md, scripts/setup-council.sh, scripts/init-claude.sh</files>
  <action>Confirm the prerequisite Wave 1 plans landed correctly. This is the safety check before the irreversible date stamp in Task 4.

1. `jq -e '.version == "4.2.0"' manifest.json` — must exit 0 (17-01 Task 1).
2. `jq -e '.updated == "YYYY-MM-DD"' manifest.json` — must exit 0 (placeholder still in place, ready for swap).
3. `jq -e '.files.rules | map(.path) | contains(["rules/audit-exceptions.md"])' manifest.json` — must exit 0.
4. `python3 scripts/validate-manifest.py` — must exit 0.
5. `grep -qF '## [4.2.0] - YYYY-MM-DD' CHANGELOG.md` — must match.
6. All 9 mandatory CHANGELOG coverage terms appear inside the [4.2.0] section: audit-exceptions.md, /audit-skip, /audit-restore, 6-phase, structured, Council, audit-review, 49, 4.2.0.
7. `grep -qF 'council/prompts/audit-review.md' scripts/setup-council.sh` — must match (17-02 Task 1).
8. `grep -qF 'council/prompts/audit-review.md' scripts/init-claude.sh` — must match (17-02 Task 2).
9. `make shellcheck` — must exit 0.

If ANY check fails, return to the prerequisite plan and fix before proceeding. Do NOT advance to Task 4 with a failing precondition.</action>
  <verify>
    <automated>jq -e '.version == "4.2.0" and .updated == "YYYY-MM-DD" and (.files.rules | map(.path) | contains(["rules/audit-exceptions.md"]))' manifest.json &amp;&amp; python3 scripts/validate-manifest.py &amp;&amp; grep -qF '## [4.2.0] - YYYY-MM-DD' CHANGELOG.md &amp;&amp; for term in 'audit-exceptions.md' '/audit-skip' '/audit-restore' '6-phase' 'structured' 'Council' 'audit-review' '49' '4.2.0'; do awk '/^## \[4.2.0\]/,/^## \[4.1.1\]/' CHANGELOG.md | grep -qF "$term" || { echo "MISSING: $term"; exit 1; }; done &amp;&amp; grep -qF 'council/prompts/audit-review.md' scripts/setup-council.sh &amp;&amp; grep -qF 'council/prompts/audit-review.md' scripts/init-claude.sh &amp;&amp; make shellcheck</automated>
  </verify>
  <done>All preconditions verified; Wave 1 deliverables intact; ready to stamp the date.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Confirm ship date with user (milestone-close decision)</name>
  <decision>What is today's actual ship date for v4.2.0?</decision>
  <context>D-08 in 17-CONTEXT.md mandates that the literal `YYYY-MM-DD` placeholder is replaced with the real ship date in the milestone-close commit. The date is whatever today's date is at the moment of running this plan — the executor cannot guess this in advance because Phase 17 may span multiple days. The user confirms the date so a wrong date never lands.

Note: the project's `currentDate` system marker shows `2026-04-26`. The user may confirm that or specify a different date if the milestone closes later.</context>
  <options>
    <option id="today">
      <name>Use today's date (typically 2026-04-26)</name>
      <pros>Matches typical Phase 17 same-day close; aligns with the "milestone-close commit sets the date" convention from v4.1.1.</pros>
      <cons>None if the close happens today.</cons>
    </option>
    <option id="custom">
      <name>User specifies a different YYYY-MM-DD</name>
      <pros>Handles slipped milestones, intentional future-dating, or backdating to match an already-tagged release.</pros>
      <cons>Requires explicit user input.</cons>
    </option>
  </options>
  <resume-signal>Reply with the ship date in `YYYY-MM-DD` format (e.g. `2026-04-26`).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Stamp the confirmed ship date into manifest.json and CHANGELOG.md</name>
  <files>manifest.json, CHANGELOG.md</files>
  <action>Using the `<SHIP_DATE>` confirmed in Task 4 (format `YYYY-MM-DD`):

1. `manifest.json`: change `"updated": "YYYY-MM-DD"` → `"updated": "<SHIP_DATE>"`. Use a portable in-place edit. Recommended:
   ```bash
   tmp=$(mktemp) &amp;&amp; jq --arg d "<SHIP_DATE>" '.updated = $d' manifest.json > "$tmp" &amp;&amp; mv "$tmp" manifest.json
   ```
   This preserves JSON formatting; `jq` is already a project dependency (used elsewhere in Makefile).

2. `CHANGELOG.md`: change the line `## [4.2.0] - YYYY-MM-DD` → `## [4.2.0] - <SHIP_DATE>`. Use a portable sed:
   ```bash
   sed -i.bak 's/^## \[4.2.0\] - YYYY-MM-DD$/## [4.2.0] - <SHIP_DATE>/' CHANGELOG.md &amp;&amp; rm -f CHANGELOG.md.bak
   ```
   The `-i.bak` form is portable across macOS BSD and GNU Linux per the project compatibility constraint (PROJECT.md: "macOS BSD ... no GNU-only flags").

3. Confirm the literal `YYYY-MM-DD` is now absent from BOTH files: `grep -F 'YYYY-MM-DD' manifest.json CHANGELOG.md` must exit 1 (no matches).

Do NOT touch any other line in either file. The diff scope is exactly two character-level edits across two files.</action>
  <verify>
    <automated>! grep -qF 'YYYY-MM-DD' manifest.json &amp;&amp; ! grep -qF 'YYYY-MM-DD' CHANGELOG.md &amp;&amp; jq -e '.updated | test("^[0-9]{4}-[0-9]{2}-[0-9]{2}$")' manifest.json &amp;&amp; grep -qE '^## \[4\.2\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md</automated>
  </verify>
  <done>Both `YYYY-MM-DD` placeholders are gone; both files carry the same `YYYY-MM-DD`-format real date; no collateral edits.</done>
</task>

<task type="auto">
  <name>Task 6: Run make version-align (D-06 — three-file alignment gate)</name>
  <files>(reads only) manifest.json, CHANGELOG.md, scripts/init-local.sh</files>
  <action>Execute `make version-align`. Per D-06 this gate exits 0 only when:
- `jq -r '.version' manifest.json` == `4.2.0`
- top `## [X.Y.Z]` heading in CHANGELOG.md == `4.2.0`
- `bash scripts/init-local.sh --version` echoes a version string ending in `4.2.0`

`scripts/init-local.sh` reads `VERSION` from `manifest.json` at runtime via jq, so 17-01's manifest bump should already cascade to all three surfaces. If the gate fails, inspect each surface independently:

1. `jq -r '.version' manifest.json` — should print `4.2.0`.
2. `grep -m1 '^## \[[0-9]' CHANGELOG.md | sed 's/.*\[\([^]]*\)\].*/\1/'` — should print `4.2.0`.
3. `bash scripts/init-local.sh --version 2>&1` — should print a line containing `v4.2.0`.

If surface 3 prints something else (e.g. `unknown` or an old version), check that `scripts/init-local.sh:18` still reads `.version` from `$MANIFEST_FILE` and that `$MANIFEST_FILE` resolves correctly when invoked from the repo root.

This task does NOT modify any file. It just runs the gate.</action>
  <verify>
    <automated>make version-align</automated>
  </verify>
  <done>`make version-align` exits 0; T-17-01 mitigation closed.</done>
</task>

<task type="auto">
  <name>Task 7: Run full quality gate — make check && make test</name>
  <files>(reads only) entire repo</files>
  <action>Execute the full project quality gate end-to-end. Per D-07 this is the T-17-03 mitigation: Test 16 (`scripts/tests/test-matrix.sh`) runs `init-local.sh` against synthetic Laravel + Next.js + generic projects and asserts `.claude/prompts/SECURITY_AUDIT.md` lands. After Phase 17 changes, that test must still pass — and the generic case must additionally show the seeded `rules/audit-exceptions.md` because Phase 13 wired the seed step into `init-local.sh`.

1. Run `make check`:
   - `lint` (shellcheck + markdownlint)
   - `validate` (template structure + manifest schema + version-align embedded check)
   - `validate-base-plugins`
   - `version-align`
   - `translation-drift`
   - `agent-collision-static`
   - `validate-commands`
   - `cell-parity`

   Must exit 0. If any sub-target fails, surface the failure to the user and STOP — do NOT mask or skip a CI gate.

2. Run `make test` (Tests 1 through 20). Must exit 0.

   - Test 1–3: init scripts on Laravel / Next.js / generic projects (T-17-03 fresh-install proof).
   - Test 16: full install matrix.
   - Test 18: audit pipeline fixture (Phase 14 — should still pass).
   - Test 19: council audit-review (Phase 15 — should still pass).
   - Test 20: template propagation idempotency (Phase 16 — should still pass).

3. After both gates pass, capture the exact `make` invocation and exit codes in the SUMMARY.

If `make test` fails on Test 16 specifically, that is a T-17-03 hit — the Phase 17 changes broke a fresh install. Diagnose by re-running `bash scripts/tests/test-matrix.sh` standalone with `bash -x` to surface the failing assertion.</action>
  <verify>
    <automated>make check &amp;&amp; make test</automated>
  </verify>
  <done>Both gates exit 0; T-17-03 mitigation closed; release is ready for the milestone-close commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → end users | Final release artifacts: manifest.json (version + ship date), CHANGELOG.md (ship date), and the three-file version-align contract. Drift here is a release-day bug. |
| Phase 17 changes → fresh-install path | Test 16 (full install matrix) is the proof that Phase 17 changes did not break the fresh-install flow for new users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | manifest.json `version` + `updated`, CHANGELOG.md `[4.2.0]` heading, init-local.sh `--version` | mitigate | `make version-align` (Task 6). After Task 5 stamps the real ship date, all three surfaces report `4.2.0` and the gate exits 0. CI also runs `make check` which embeds the same comparison. |
| T-17-03 | Denial of service / regression | Fresh-install path on Laravel + Next.js + generic projects | mitigate | `make test` (Task 7) runs Test 16 (`scripts/tests/test-matrix.sh`) which executes `init-local.sh` against the three synthetic projects and asserts the expected `.claude/` tree lands. Reuses existing harness per D-07 — no new test surface required. |
</threat_model>

<verification>
- DIST-02 markers in `commands/audit.md` and `commands/council.md` confirmed intact via grep; both files have empty `git diff`.
- `manifest.json` and `CHANGELOG.md` no longer contain the literal `YYYY-MM-DD` placeholder.
- `manifest.json` `updated:` is a `YYYY-MM-DD`-format real date matching the CHANGELOG `[4.2.0]` heading date.
- `make version-align` exits 0 (three-file alignment closed).
- `make check` exits 0 (full lint + validate stack including embedded version-align inside `validate`).
- `make test` exits 0 (all 20 tests; Test 16 specifically proves T-17-03 mitigation).
- `git diff` scope across the entire phase is bounded to: `manifest.json` (version + updated + new rules entry), `CHANGELOG.md` (new [4.2.0] section + date stamp), `scripts/setup-council.sh` (audit-review.md install block), `scripts/init-claude.sh` (audit-review.md install block inside `setup_council()`).
</verification>

<success_criteria>
- DIST-01 closed: manifest.json registers `rules/audit-exceptions.md`, version `4.2.0`, real ship date.
- DIST-02 closed: verify-only confirmation that `commands/audit.md` (6-phase workflow + Council Handoff) and `commands/council.md` (## Modes / ### audit-review) carry the required documentation.
- DIST-03 closed: CHANGELOG.md `[4.2.0]` entry covers all Phase 13–16 user-visible features and carries a real ship date.
- T-17-01 mitigated by `make version-align` exit 0.
- T-17-03 mitigated by `make test` (Test 16 matrix) exit 0.
- v4.2.0 ready for git tag + GitHub Release per the standard manual-tag policy (PROJECT.md: "release date flip manual; git tag manual" — agent does NOT push tags).
</success_criteria>

<output>
After completion, create `.planning/phases/17-distribution-manifest-installers-changelog/17-03-SUMMARY.md` per the standard summary template. Include:
- The confirmed ship date (from Task 4 user reply).
- `git diff --stat` for the entire phase across all four files (manifest.json, CHANGELOG.md, scripts/setup-council.sh, scripts/init-claude.sh).
- `make version-align`, `make check`, and `make test` exit codes.
- A note that v4.2.0 is ready for manual git tag + GitHub Release (matches v4.0.0 / v4.1.0 / v4.1.1 release flow per PROJECT.md D-08 Key Decisions).
</output>
