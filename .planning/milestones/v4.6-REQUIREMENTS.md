# v4.6 Requirements — Install Flow UX & Desktop Reach (Archived)

**Defined:** 2026-04-29
**Shipped:** 2026-04-29
**Status:** ✅ All 36/36 satisfied
**Audit:** [v4.6-MILESTONE-AUDIT.md](../v4.6-MILESTONE-AUDIT.md) — passed

---

**Core Value:** Install only what adds value over `superpowers` + `get-shit-done`. No duplicates, no name collisions.

**Milestone goal:** Replace the multi-command first-run flow with one TUI installer (Components / MCPs / Skills) and publish the toolkit as a Claude Code plugin marketplace so Claude Desktop users get the value surface architecturally available to them.

## v4.6 Requirements (36/36 complete)

### Phase 24 — Unified TUI Installer + Centralized Detection (16/16 ✅)

#### TUI library (foundation reused by Phases 25–26)

- [x] **TUI-01**: `scripts/lib/tui.sh` exposes `tui_checklist <items_var> <results_var>` that renders a Bash 3.2 compatible checkbox menu (`read -rsn1` + `read -rsn2` for arrow tail; no `read -N`, no float `-t`, no `declare -n` namerefs). Live tested on macOS Bash 3.2.57.
- [x] **TUI-02**: TUI reads exclusively from `< /dev/tty` with `TK_TUI_TTY_SRC` test seam (mirrors `TK_BOOTSTRAP_TTY_SRC` from v4.4 BOOTSTRAP-01). Fail-closed default-set when `/dev/tty` unavailable (CI, piped install).
- [x] **TUI-03**: TUI registers `trap '<restore-stty>' EXIT INT TERM` BEFORE entering raw mode. Ctrl-C mid-render restores terminal cleanly. Restore uses `|| true` so trap failure doesn't compound.
- [x] **TUI-04**: Each item displays label + status (`[ ]`, `[x]`, `[installed ✓]`) + optional description on focused row. Selection visually distinguished (reverse video or arrow indicator).
- [x] **TUI-05**: Confirmation step before any installer runs (`Install N component(s)? [y/N]`). Default N. Cancel returns to menu without changes.
- [x] **TUI-06**: `--no-color` and `${NO_COLOR+x}` honored ([no-color.org](https://no-color.org)). `[ -t 1 ]` gates color output.
- [x] **TUI-07**: `scripts/tests/test-install-tui.sh` hermetic test (≥15 assertions): keystroke injection via fixture file, no-TTY fallback path, `--yes` non-interactive default-set, `--dry-run` zero-mutation, `--force` re-runs detected components.

**Outcome:** Validated. PASS=43 (38 base + 5 from S10 added by Phase 27 routing). Live PTY render + Ctrl-C restore deferred as HUMAN-UAT.

#### Centralized detection v2

- [x] **DET-01**: `scripts/lib/detect2.sh` sources existing `scripts/detect.sh` (does not duplicate SP/GSD logic). Adds `is_<name>_installed` functions returning 0/1 for: `toolkit`, `superpowers`, `gsd`, `security`, `rtk`, `statusline`.
- [x] **DET-02**: `is_security_installed` uses `command -v cc-safety-net` (covers brew **and** npm install paths) plus `grep cc-safety-net ~/.claude/hooks/pre-bash.sh` (or equivalent settings.json scan). Fixes v4.4 regression where brew installs were missed.
- [x] **DET-03**: `is_statusline_installed` checks `~/.claude/statusline.sh` exists AND `grep statusLine ~/.claude/settings.json` returns 0.
- [x] **DET-04**: `is_rtk_installed` uses `command -v rtk` (filesystem and PATH agnostic).
- [x] **DET-05**: `is_toolkit_installed` checks `~/.claude/toolkit-install.json` exists (existing v4.0 STATE-01 contract).

**Outcome:** Validated. 6 binary `is_*_installed` probes + 3-state extension for MCPs (Phase 25).

#### Dispatch + install.sh entry

- [x] **DISPATCH-01**: `scripts/lib/dispatch.sh` exposes per-component dispatchers (`dispatch_toolkit`, `dispatch_security`, `dispatch_rtk`, `dispatch_statusline`) that invoke existing per-component scripts as `bash -c` subprocesses with appropriate flags. Order-of-operations contract: SP/GSD → toolkit → security → RTK → statusline.
- [x] **DISPATCH-02**: `setup-security.sh` learns `--yes` flag that gates every interactive `read -r -p` block (use safe defaults). `install-statusline.sh` learns `--yes` as accepted-but-no-op (semantic symmetry). `init-claude.sh` already non-interactive — no flag added.
- [x] **DISPATCH-03**: `scripts/install.sh` is a new top-level orchestrator (NOT a trampoline). Sources `lib/{tui,detect2,dispatch}.sh`, runs detection → TUI → confirmation → dispatch → post-install summary. Failure of step N reports per-component status; remaining components continue (configurable via `--fail-fast`).

**Outcome:** Validated. `TK_DISPATCH_ORDER` honored; per-component `bash -c` isolation.

#### Backwards compatibility

- [x] **BACKCOMPAT-01**: Existing `init-claude.sh` URL stays valid and unchanged. v4.4 BOOTSTRAP-01..04 contract preserved (26-assertion `test-bootstrap.sh` stays green throughout Phase 24). `bootstrap.sh` becomes the no-TTY fallback for SP/GSD prompts only — TUI replaces the interactive layer above it. `--no-bootstrap`, `--no-banner`, `TK_NO_BOOTSTRAP`, `NO_BANNER` env-vars/flags preserved on all paths.

**Outcome:** Validated. `test-bootstrap.sh` PASS=26 stayed green end-to-end.

### Phase 25 — MCP Selector (7/7 ✅)

#### MCP catalog + per-MCP wizard

- [x] **MCP-01**: 9-entry curated catalog at `scripts/lib/mcp-catalog.json`. **Adjusted scope:** simpler JSON catalog adopted instead of `templates/mcps/<name>/{mcp.json,setup.sh,config-prompt.txt}` per-MCP layout — single catalog file proved cleaner and avoided duplication for zero-config MCPs. Curated MCPs: `context7`, `firecrawl`, `magic`, `notion`, `openrouter`, `playwright`, `resend`, `sentry`, `sequential-thinking`.
- [x] **MCP-02**: `is_mcp_installed <name>` parses `claude mcp list` output (one MCP per row) and returns 0/1/2 (3-state — added "unknown" return for CLI-absent fail-soft scenarios).
- [x] **MCP-03**: `scripts/install.sh --mcps` (or second TUI page) renders the catalog with detected status per MCP. Selected MCPs trigger per-MCP wizard.
- [x] **MCP-04**: Per-MCP wizard reads catalog entries, prompts inline for required values (`read -rsp` for sensitive fields like API keys, `read -r` for URLs/usernames), invokes `claude mcp add <name> <flags-from-catalog>` with collected values plumbed in via env vars.
- [x] **MCP-05**: `scripts/tests/test-mcp-selector.sh` (PASS=21) + `test-mcp-secrets.sh` (PASS=11) + `test-mcp-wizard.sh` (PASS=14) hermetic tests with mock `claude mcp list`/`claude mcp add` via PATH override.

**Outcome:** Validated. Catalog adjustment recorded as a key decision (single JSON file vs. per-MCP directory tree). Live PTY interactive run deferred as HUMAN-UAT.

#### MCP secrets handling

- [x] **MCP-SEC-01**: `~/.claude/mcp-config.env` created with mode `0600` (owner-only readable, `chmod 600` after write). File is gitignored at user level (TUI prints warning if user is inside a git worktree without `.gitignore`).
- [x] **MCP-SEC-02**: `mcp-config.env` schema is `KEY=value` lines (no quotes, trailing newline per entry). TUI appends; existing keys are overwritten with `[y/N]` confirmation. `docs/MCP-SETUP.md` documents the file location, plaintext-on-disk caveat, and a "rotate to secret manager" recipe.

**Outcome:** Validated. `mcp_secrets_set` enforces `chmod 600`; collision flow tested.

### Phase 26 — Skills Selector (5/5 ✅)

#### Skills marketplace mirror

- [x] **SKILL-01**: 22 skills mirrored under `templates/skills-marketplace/<name>/SKILL.md` from skills.sh upstream: `ai-models`, `analytics-tracking`, `chrome-extension-development`, `copywriting`, `docx`, `find-skills`, `firecrawl`, `i18n-localization`, `memo-skill`, `next-best-practices`, `notebooklm`, `pdf`, `resend`, `seo-audit`, `shadcn`, `stripe-best-practices`, `tailwind-design-system`, `typescript-advanced-types`, `ui-ux-pro-max`, `vercel-composition-patterns`, `vercel-react-best-practices`, `webapp-testing`. Each ships with companion files where they exist.
- [x] **SKILL-02**: License audit complete. 2 skills carry upstream `LICENSE` artifacts; remaining 20 received an explicit `SKILL-LICENSE.md` provenance fallback file. `docs/SKILLS-MIRROR.md` records skills.sh upstream URLs, mirror-date, and the `scripts/sync-skills-mirror.sh` re-sync procedure.
- [x] **SKILL-03**: `is_skill_installed <name>` checks `[ -d ~/.claude/skills/<name>/ ]`. `scripts/install.sh --skills` renders catalog with detected status, copies selected from `templates/skills-marketplace/<name>/` to `~/.claude/skills/<name>/` via `cp -R`. Idempotent: re-install with `--force` overwrites; without `--force` returns 2.
- [x] **SKILL-04**: `scripts/tests/test-install-skills.sh` (PASS=15) covers `cp -R` correctness, idempotency on re-run, `--force` overwrite, refusal-to-overwrite without `--force`.
- [x] **SKILL-05**: `manifest.json` registers `templates/skills-marketplace/` content under new `files.skills_marketplace[]` (22 entries) so `update-claude.sh` ships skill updates to existing TK installs via the LIB-01 D-07 invariant (`.files | to_entries[] | .value[] | .path`).

**Outcome:** Validated. Mirror snapshot is 70.5k+ insertions across 347 files. License fallback (`SKILL-LICENSE.md`) recorded as a key decision.

### Phase 27 — Marketplace Publishing + Claude Desktop Reach (8/8 ✅)

#### Marketplace surface

- [x] **MKT-01**: `.claude-plugin/marketplace.json` at repo root with schema `{name, owner.name, plugins[]}` validated against current Anthropic spec at `code.claude.com/docs/en/plugin-marketplaces`. `name` = `claude-code-toolkit`, `owner.name` = `sergei-aronsen`. Three sub-plugins listed.
- [x] **MKT-02**: `plugins/<name>/.claude-plugin/plugin.json` for three sub-plugins: `tk-skills` (skills surface — Desktop-compatible), `tk-commands` (29 slash commands — Code only), `tk-framework-rules` (7 framework CLAUDE.md template fragments — Code only). Each `plugin.json` declares `version`, `description`, `category`, `tags`. **Decision:** version is single source of truth in `plugin.json`; marketplace.json entries omit version (silently overridden by plugin.json).
- [x] **MKT-03**: Live `claude plugin marketplace add ./` smoke test from a hermetic clone validates the marketplace structure end-to-end. `make validate-marketplace` target runs the smoke (gated behind opt-in `TK_HAS_CLAUDE_CLI=1` env-var; CI runner does not have `claude` by default). **Status:** structure verified statically; live runtime smoke deferred as HUMAN-UAT.
- [x] **MKT-04**: README + `docs/INSTALL.md` gain a "Install via marketplace" section alongside the curl-bash install. Both channels documented as equivalent for Code users; marketplace is the only path for Desktop users.

**Outcome:** Validated. 3 sub-plugins symlinked (zero duplication). MKT-03 partial: structurally verified, runtime requires external `claude` CLI.

#### Claude Desktop reach

- [x] **DESK-01**: `docs/CLAUDE_DESKTOP.md` documents the capability matrix: Claude Desktop Code tab has full plugin runtime parity with terminal Claude Code; Desktop Chat tab has no plugin system; remote (cloud-hosted) Code sessions block plugins per Anthropic docs. Marketplace is the only Desktop install channel (`/plugin marketplace add ./local-dir` is blocked).
- [x] **DESK-02**: `scripts/validate-skills-desktop.sh` scans `templates/skills-marketplace/*/SKILL.md` for Code-only assumptions (Bash code blocks that *require* execution by the agent, references to `Read`/`Bash`/`Write` tools as required dependencies). Output: per-skill PASS/FLAG verdict. Wired into `make check`.
- [x] **DESK-03**: `scripts/install.sh` detects Desktop-only users (no `claude` CLI on PATH) and routes to `--skills-only` branch that places skills under `~/.claude/plugins/tk-skills/` (instead of project `.claude/`). Surfaces a one-liner explaining the limitation.
- [x] **DESK-04**: Skill audit gate fails Phase 27 if fewer than 4 skills pass `validate-skills-desktop.sh`. Below that threshold, `tk-skills` sub-plugin scope rebalances toward instruction-only (zero-tool) skills. **Result:** 20 of 22 skills PASS; 2 (firecrawl, shadcn) FLAG informational — well above the 4-skill threshold.

**Outcome:** Validated. Capability matrix shipped; auto-routing tested. Live Desktop end-to-end install deferred as HUMAN-UAT.

## Adjustments / Dropped Requirements

| REQ-ID | Outcome | Note |
|--------|---------|------|
| MCP-01 | Adjusted | Replaced `templates/mcps/<name>/{mcp.json,setup.sh,config-prompt.txt}` per-MCP tree with single `scripts/lib/mcp-catalog.json` — simpler and avoids duplication for zero-config MCPs. Functional contract preserved. |
| MCP-01 | Adjusted | Curated set rebalanced from `notebooklm/toolbox/youtrack` to `firecrawl/notion/resend` based on user-feedback / install-frequency signal. |
| SKILL-02 | Adjusted | License preservation strategy: 2 skills with upstream `LICENSE` files mirrored as-is; 20 skills without explicit upstream license received `SKILL-LICENSE.md` provenance fallback (mirror date + upstream URL + license-status disclosure). |
| MKT-03 | Partial | Static structure validation complete; live runtime smoke (`claude plugin marketplace add ./`) deferred as HUMAN-UAT — requires `claude` CLI on the runner. |

No requirements dropped. All 36 satisfied (1 with partial caveat above).

## Tech Debt Carried Forward

8 HUMAN-UAT items deferred (live PTY / external CLI dependent — see [v4.6-MILESTONE-AUDIT.md](../v4.6-MILESTONE-AUDIT.md) for the inventory).

5 advisory code-review WR-class findings in Phase 24 (`tui.sh` seam-bypass + EXIT-trap clobber + `dispatch.sh` `eval` env-var injection — low-risk; tracked in `.planning/phases/24-unified-tui-installer-centralized-detection/24-REVIEW.md`).

## Traceability

| Phase | Requirements | Status |
|-------|--------------|--------|
| 24 — Unified TUI Installer + Centralized Detection | TUI-01..07, DET-01..05, DISPATCH-01..03, BACKCOMPAT-01 (16) | ✅ Shipped |
| 25 — MCP Selector | MCP-01..05, MCP-SEC-01/02 (7) | ✅ Shipped |
| 26 — Skills Selector | SKILL-01..05 (5) | ✅ Shipped |
| 27 — Marketplace + Desktop Reach | MKT-01..04, DESK-01..04 (8) | ✅ Shipped (MKT-03 partial) |
| **Totals** | **36** | **36/36 ✅** |
